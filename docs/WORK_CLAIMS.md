# Work Claims

> **2026-08-14 — post-B master reconciliation.** Any claim row below referencing pre-B-completion state is
> closed. The reconciliation touched planning and documentation only, on
> `claude/master-reconciliation-post-audit-ewgl00`. No C implementation is authorized.


**What you are about to work on, recorded before you start.**

One row per piece of work in flight. Add yours in your first commit; set it
`done` in your last. Check it before you begin:

```bash
python scripts/check_work_claims.py --files <paths you will change> --defect <ids>
```

That script also scans remote branches, because claims are voluntary and
branches are evidence.

---

## What an active claim blocks — and what it does not (2026-08-20)

Added when the owner authorized the POST-V1 C-Series mass-build campaign
(`docs/EXECUTION_PLAN.md` §0), which puts five lanes through this list at once.

A claim blocks **concurrent edits to the files and defect scope it names.** It does **not**
block an entire lane.

When a lane reaches a unit whose files are actively claimed:

1. mark that **unit** `BLOCKED_BY_ACTIVE_CLAIM`, naming the claim it collided with;
2. **move to the next dependency-ready unit** — do not stop the session;
3. do **not** fork the file, and do **not** build a parallel implementation to route around the
   claim. That turns a scheduling conflict into a permanent second owner, which costs far more
   than waiting.

**A claim's scope is what it wrote down, not the area it sits in.** A repair-only claim naming
specific frontend defects does not reserve all of C8/C9/C10; a claim on one trade file does not
reserve the trade substrate. Read the row's `Paths` and `Defect ids` — those are the boundary.

**Stale rows are a real cost now.** A claim left `open` after its work merged reports a live
editor who is not there, and under a five-lane campaign that manufactures false blocks. Set your
row `done` in your last commit.

## Open claims

| Claim | Paths | Defect ids | Branch | Status |
|---|---|---|---|---|
| **Claude 3 / V1-49 return-TD scoring keys — reconcile #1020 vs #1027 and repair the real gap.** V1 numerator sprint task. #1020 and #1027 do not disagree — they audit disjoint V1-49 residuals; both hold on current `main`. Investigated #1020's four disputed Sleeper keys and resolved each: `punt_ret_td` is a real weekly-feed column (`pt_return_tds`, confirmed against the live nflverse header) added to `realized_points._SIMPLE_KEYS`; `kick_ret_td` has no weekly column and is derived from real PBP (`play_type=="kickoff"` + `return_touchdown`, excluding `own_kickoff_recovery_td`), added to `PBP_SUPPLEMENT_KEYS` mirroring the existing `pass_int_td` predicate exactly; `idp_def_pr_td`/`idp_def_kr_td` are CLOSED as proven-unsupported (no Sleeper scoring-UI category, zero nonzero instances across three sampled 2025 host dumps) rather than fabricated as scorable. Host-truth validated against real 2025 events, not merely unit-tested: Charlie Jones (Sleeper 10228, GSIS 00-0038576) week 9 and Rashid Shaheed (8676/00-0037545) week 14 both reconcile exactly to `kick_ret_td=1`; week 14's other two `st_td` scorers (Marvin Mims, Isaiah Williams) independently confirmed as punt returns on the real PBP release, proving the identity `kick_ret_td + punt_ret_td == st_td`. Fixtures `tests/nfl_data/fixtures/pbp_2025_wk{1,5,11,14}_slice.csv` regenerated from the live 2025 PBP release with two new required columns, byte-verified identical to the prior committed fixtures on every pre-existing column (0 mismatches, 2738/2454/2575/2422 rows) before the new columns were appended. Two mutations, both RED-before/GREEN-after. L2: current-league point delta is provably 0 on both live leagues (neither configures any of the four keys); synthetic fully-configured-fixture delta is exact (+6.0/event). Deliberately NOT claiming: the `host_native_scoring` promotion path (unchanged, still `PRODUCTION_ONLY`, 4 of 10 gate items open — this repair does not touch or depend on it), any position-normalization table (separate, prior-session finding, proven unreachable), any further V1-49 code change. | `src/nfl_data/realized_points.py`, `src/nfl_data/pbp_weekly.py`, `src/nfl_data/scoring_coverage.py`, `tests/nfl_data/fixtures/pbp_2025_wk{1,5,11,14}_slice.csv`, `tests/nfl_data/test_individual_special_teams.py`, `tests/nfl_data/test_pbp_weekly.py`, `tests/nfl_data/test_scoring_coverage.py`, `tests/nfl_data/test_pbp_supplement_join.py`, `docs/scoring/V1_49_RETURN_TD_SPLIT_KEYS.md` (new), `docs/WORK_CLAIMS.md` | V1-49 | `claude/v1-49-return-td-scoring-keys` | done — `FEATURE_GREEN`, `READY_FOR_INTEGRATION` |
| **Claude 9 / V1-97 / C3-REPLAY-01 — public activity feed hindsight-leak fix.** Resolved a prior-session ambiguity between two candidate surfaces (recorded in `docs/cseries-delivery/CLAUDE_9.md`): the PUBLIC `/league` Activity tab's trade-grade badge is the confirmed live leak — `build_valuation_from_contract` built one valuation callable from TODAY's `latest_contract_data` with no date parameter, and `activity._apply_trade_grades` applied it to every trade regardless of the trade's own `createdAt` (used for sorting, never for valuation). The private `/trades` "How It Aged" surface was re-verified and is NOT the leak — its four anti-hindsight rules genuinely hold; its remaining defect is sourcing from legacy `rank_history.jsonl` instead of the canonical `src.history` ledger, a separate architecture-debt item, explicitly out of scope here. Repair: `asof.batch_known_before` (new, on the canonical C1-U4 owner — groups requests by distinct asset key, one SQL round trip per key, never per request) plus `public_activity_valuation.build_asof_valuation`/`asset_history_key`/`trade_instant_from_created_at`, wired through `activity.build_section`'s new `valuation_factory` param and `_apply_trade_grades`, which now resolves every asset AS OF the trade's own instant and stamps an explicit `_unavailable_grade` sentinel (never a partial or substituted total) when any asset on a side has no admissible historical observation — one side's missing evidence never poisons a sibling side in the same trade. `server.py::_build_public_activity_valuation` now delegates to the ledger-backed factory instead of the contract parser; every existing call site passes the value through opaquely so no other server.py line changed. Frontend `TradeCard` renders the honest unavailable state instead of a blank letter-grade badge. **Known, flagged consequence, not a defect**: the ledger's permanent pre-2026-07-14 floor means most trades that predate it will lose their grade badge entirely on this fix — correct per V1-97, but a visible behavior change worth a human look before merge. RED-before/GREEN-after verified against pre-fix code checked out from `origin/main`; two hand-applied mutations (fallback-to-now on a bad timestamp; coerce-missing-to-0.0) confirmed caught. Deliberately NOT claiming: the private `/trades` page's legacy-history-source migration, any trade-fairness threshold/VA coefficient/package-weight change, PR #962 (frozen, untouched — zero file overlap confirmed). | `src/history/asof.py` (add-only), `src/api/public_activity_valuation.py` (add-only), `src/public_league/activity.py`, `src/public_league/public_contract.py`, `server.py` (one function body), `frontend/app/league/sections/activity.jsx`, `tests/history/test_batch_known_before.py`, `tests/api/test_public_activity_asof_valuation.py`, `tests/public_league/test_activity_asof_grading.py`, `tests/public_league/test_public_contract.py`, `tests/public_league/test_trade_grade_parity.py`, `frontend/__tests__/components/TradeCard.test.jsx`, `docs/cseries-delivery/CLAUDE_9.md`, `docs/WORK_CLAIMS.md` | V1-97, C3-REPLAY-01 | `claude/v1-97-activity-hindsight-fix` | open — `FEATURE_GREEN`, not yet merged; Claude 5 owns integration and promotion to `VERIFIED` |
| **Lane 8 PR B — the multi-bridge cross-position translation owner (production valuation change).** Owner-authorized in `docs/EXECUTION_PLAN.md` (LANE 8 CHARTERED). Rewires `_compute_unified_rankings`'s Phase 0 backbone selection off the `is_backbone` label onto the bridge owner's measured capability (`src/bridges/*`, PR A); adds the unconditional vote-withholding repair (an IDP-only source with no usable bridge casts no vote, replacing the untranslated-rank defect); adds the `multi_bridge_ladder` flag (OFF by default) gating whether more than one qualified bridge combines; stamps `crossPositionBridges` diagnostics (every declared bridge's state, ladder contributors, per-source withheld count) onto the contract, which `_compute_unified_rankings` computed but never surfaced. Measured: healthy board 0 votes withheld, single-bridge ladder byte-identical to the incumbent; excluding `idpTradeCalc` alone reproduces none of #950's four defect numbers (661→0 untranslated votes, 310→138 flagged rows, 9,999→7,032 top IDP, 29→12 IDP-in-top-100) and needs 0 withheld votes because Draft Sharks (family, not key — PR A's repair) takes over; excluding every cardinal bridge withholds 702 votes and refuses the ladder rather than fabricating one. All 5 required mutations (PENDING eligible, family dedup removed, missing/zero value coerced, ordinal rank reaching the shared pool directly, fail-closed-with-no-bridge removed) confirmed RED then reverted. No new bridge-weighting/precedence/confidence/disagreement/tie-break methodology invented — combination reuses the existing `weighted_count_aware_mean_median_blend`; monotonicity is a structural constraint, not a policy choice. Full record: `docs/sources/LANE8_PR_B_MULTI_BRIDGE_TRANSLATION.md`. **Flagged for Integration, not resolved here** (`src/api/data_contract.py` is SERIAL, one writer only, per the Lane 8 charter): `scripts/check_work_claims.py` reports three OTHER branches also touching `data_contract.py` (`claude/2029-pick-completeness-7akfy8`, `claude/v1-trade-roster-rehearsal`, `claude/v1-trade-roster-rehearsal-2` — none currently an open PR, unverified whether stale) and one open PR, **#983** ("PENDING does not vote — consumer-side"), which is test-only and touches no production file, but whose `tests/api/test_bridge_consumer_boundary.py` census asserts `src/bridges` has ZERO production consumers today — that assertion is EXPECTED to flip once this PR lands (its own description says so), and Integration should sequence the two merges with that in mind rather than reading the resulting test failure as a regression. Deliberately NOT claiming: any IDP value ceiling, market corridor, or post-consensus clamp (none exists in this diff); flipping `multi_bridge_ladder` on (owner decision); Dynasty Dealer qualification (PR D); any Hill constant, `_ALPHA_SHRINKAGE`, `TAIL_SATURATION_RANK`, or source weight; the V1 denominator itself. | `src/api/data_contract.py`, `src/api/feature_flags.py`, `src/bridges/ladder.py`, `src/bridges/registry.py`, `config/bridges/bridges_v1.json`, `tests/bridges/test_bridge_capability.py`, `tests/api/test_curve_routing_coordinate_pool.py`, `tests/api/test_name_join_hygiene.py`, `tests/consensus_edge/test_fair_value.py`, `docs/sources/LANE8_PR_B_MULTI_BRIDGE_TRANSLATION.md` | PR #950 findings; Lane 8 charter (`docs/EXECUTION_PLAN.md`); PR A (#954, merged) | `claude/lane8-multi-bridge-translation` | open |
| **Claude 13 — PR A: PSI reference-route migration (foundation + shell + Rankings).** Owner-authorized real production migration of Dynasty Rankings onto the Premium Sports Intelligence editorial direction (see `docs/PREMIUM_SPORTS_INTELLIGENCE_DESIGN_NORTH_STAR.md`), superseding the prior "prep only" posture — recorded honestly as not yet reconciled into `docs/VERSION_1_COMPLETION_CONTRACT.md` (`C8-PSI-02` still shows `NOT STARTED`) or `docs/EXECUTION_PLAN.md` §6 (still lists "Premium route migration" as not-yet-begun); owner instruction is to proceed and let Claude 5 reconcile the record, not to self-edit it. Full authorization record and plan: `docs/cseries-delivery/CLAUDE_13.md`. Batch A1 (editorial token layer): additive `.psi-editorial` CSS scope in `tokens.css` re-mapping the SAME semantic aliases every `ds/` primitive already consumes (cream surfaces, near-black text, burnt-red accent, re-derived CVD-safe market-direction marks — the terminal pair fails the 3:1 mark floor on this scope's nested surface — one new `--font-display` token as a system serif stack, deliberately not a new font asset since the app's only font mechanism is checked-in `next/font/local` files for a network-restricted build). Every color WCAG-computed, not eyeballed (22 tests in `tokens-contract.test.js`, 8 new). Does not touch `globals.css` or any existing dark/light token — additive only. Batch A2 (shell restyle): `frontend/app/shell.css` (the app-chrome stylesheet — NOT the Lane-6-claimed `globals.css`) already consumed only semantic token aliases with zero raw hex, so applying `.psi-editorial` to `TopBar`/`MobileChrome`'s root elements re-skins the whole header/nav/drawer/dropdowns automatically — no CSS rewrite needed. One additive rule for a "CU" monogram-block brand mark. This is a global, immediate change (one persistent shell across every route) — deliberate, per the north star's own route-by-route-but-temporarily-split migration method. Batch A3 (Rankings migration): `board.module.css` was also already 100% token-driven, so `.psi-editorial` on the page root re-skins the whole table automatically; closed 3 legacy-globals.css-class leaks (`button-reset`, `rankings-player-name`, `muted` — all referenced pre-R0 tokens like `--cyan` invisible to the new scope) with new local classes. Hero eyebrow reuses the page's existing freshness derivation; `<h1>` stays the canon "Rankings" (a first attempt at "The Dynasty Board" broke `page-title-canon.test.jsx`'s nav-label-equals-h1 invariant — reverted rather than widen scope into a nav-copy change; "The Dynasty Board" moved into the description instead). Legacy badge system (`display-helpers.js`'s `posBadgeClass`/`confBadgeClass`/`marketEdge`) and the mobile-source-row/audit-row background tint deliberately NOT touched — both already named in `board.module.css`'s own header comment as deferred to a future "R5 CSS purge," not this batch's scope. Verified: full suite 142/2243 green, `next build` clean (both Turbopack and `--webpack`), bundle budget `/rankings/page` 66.6/75 KB. **Batch A4 (real browser + axe verification, unlike the earlier ticker batch — a real stack was booted in-sandbox this time):** found and fixed two real rendering defects invisible from source reading alone. (1) Full-bleed background: `.psi-editorial` scoping alone doesn't repaint `.page` itself, and the claimed `globals.css`'s `.main-shell` padding left the old dark surface visible as a frame around the whole section on every side — fixed with matching negative margins at every one of its breakpoints, no edit to that file. (2) A real axe-core (WCAG 2.0/2.1 A+AA) scan against authenticated `/rankings` with real canonical data found 187 failing nodes, all tracing to legacy `globals.css` badge/tier-badge/league-switcher classes reading legacy tokens (`--cyan`, `--subtext`, `--text`, `--border`, …) that A3's "deferred, visually inconsistent" note above did not anticipate would actually fail contrast once the canvas changed. Fixed across four re-verified rounds: `.psi-editorial` now remaps every legacy COLOR alias (never structural ones) to its already-validated semantic equivalent; three of four `badge-*` classes' own hardcoded background/border literals needed separate `.page :global(.badge-*)` overrides (higher specificity, no `globals.css` edit) — including two cases where naively reusing an existing `--*-muted` token at its existing alpha individually measured 4.49:1 (same-hue tinting a light backdrop toward a dark foreground erodes contrast) and needed a lower measured-safe alpha instead; `--text-tertiary` itself was 4.44:1 (fixed to 4.60:1, "still AA at body sizes" was not a real exception) with a new pinning test added since none existed. Re-verified 0 axe violations after the last fix; full suite now 142/2245 green; bundle budget unchanged. One pre-existing, out-of-scope finding confirmed unrelated to this branch (`.scouting-insight-badge--down/--warn` on unmigrated `/` content). Full record: `docs/cseries-delivery/CLAUDE_13.md` Batch A4. Commits `11c69e7d` (background), `3acd6347` (axe fixes), `9489fe56` (docs). **PR A is FEATURE_GREEN / READY_FOR_INTEGRATION as of this update — handed to Claude 5, not self-merged.** **PR B (Universal Player Profile / "Player File", C8-PSI-02): while Claude 5 was reconciling PR A (#984) with `main` on this same branch, PR B's commit landed directly on this branch rather than a separate stacked PR as originally planned — recorded as a deviation, not silently done; PR #984's title/body updated to cover both.** New route `/players/[playerId]` reuses `PlayerPopup.jsx`'s real data plumbing (three previously-inline `useMemo` bodies extracted to exported `computeOwnership`/`computeSiteDetails`/`computeConsensusText`; four section components exported; `PlayerPopup` itself unchanged otherwise, gained one "Full profile" launcher link). Five tabs map to real data only — Trades tab is an honest "not available" state (no canonical per-player trade-history source exists, and a standalone "Trade Desk" button is separately not-yet-authorized per `EXECUTION_PLAN.md` §6). Actions verified against real routes before wiring: `/trade` and `/arbitrage` (the real KTC finder — `/finder` is a legacy redirect) accept no player-seeding param, so "Open in Trade Calculator" is an honest plain link, not a pre-fill claim. Verified: full suite 143/2255 green (post-merge with `main`), `next build` clean, real axe-core scan 0 violations on first pass (desktop/mobile/not-found) — found and fixed one real bug (loading/error/not-found states rendered outside the `.psi-editorial` scope, caught by an actual screenshot). Full record: `docs/cseries-delivery/CLAUDE_13.md` Batches B1-B4. Commit `db84a6df`. **PR B is FEATURE_GREEN / READY_FOR_INTEGRATION — same PR #984, not self-merged.** | `frontend/app/tokens.css`, `frontend/components/ds/token-contract.js`, `frontend/__tests__/components/ds/tokens-contract.test.js`, `frontend/components/shell/TopBar.jsx`, `frontend/components/shell/MobileChrome.jsx`, `frontend/app/shell.css`, `frontend/app/rankings/page.jsx`, `frontend/app/rankings/board.module.css`, `frontend/app/rankings/board-sections.jsx`, `frontend/components/PlayerPopup.jsx`, `frontend/app/players/[playerId]/page.jsx`, `frontend/app/players/[playerId]/player-file.module.css`, `docs/cseries-delivery/CLAUDE_13.md`, `docs/WORK_CLAIMS.md` | C8-PSI-02 | `claude/psi-reference-routes` | done — merged `5c43da9e8` (#984); both gates green at exact head `e7ca29ab8` (Validate PR run 32389263637, Boot stack + run journeys run 32389263761). Integration produced the browser evidence (`docs/psi/`), measured contrast back to 0 failing groups, and found one real E2E regression the unit suites could not see (the class swap dropped the `rankings-player-name` hook four specs select on), which the lane fixed. `V1-111` / `C8-PSI-02` is NOT verified by this merge — its target is L4 |
| **Lane 8 PR C — Dynasty Nerds IDP Top-275, acquisition and preservation only.** Public, no auth: `dynastynerds.com/idp/dynasty-idp-rankings-tiers/` returns 275 rows across 10 tier tables inline in page HTML, joining the `dynastyNerds` provider family the existing `fetch_dynasty_nerds.py` (SFLEXTEP offense) already registers. Ordinal/tier only — `ArchivedRow.value` is `None` on every one of 275 rows; no cardinal value is derived from rank. Schema pin verified live (Rank/Player/Position/Age/Team/`<year> IDP Rank` headers, JSON-LD `dateModified` used as `source_as_of` rather than "now"); a violation reports `SCHEMA_CHANGED`/`PARSE_FAILED` and does not archive. Writes only to `src/source_archive/` via `archive_board`; does not touch `src/api/data_contract.py`, `_RANKING_SOURCES`, any weight, or any production CSV. Deliberately NOT claiming: production wiring of this source into the ranking pipeline (a separate, later-authorized step); any bridge-combination policy; Dynasty Dealer or IDP Show/Footballguys acquisition (tracked separately). | `scripts/fetch_dynasty_nerds_idp.py`, `tests/sources/test_dynasty_nerds_idp_acquisition.py`, `tests/fixtures/dynasty_nerds/*`, `docs/WORK_CLAIMS.md` | Lane 8 program (owner-designated top-priority V1, 2026-08-20); adjacency to PR A (`src/sources/acquisition_state.py`, `src/source_archive/`) | `claude/lane8-dynasty-nerds-acquisition` | done — merged `8386e93b5` (#971) at exact head `fedb3570d`; ordinal-vs-cardinal prohibition mutation-verified at Integration (`value=float(rank)` ⇒ 7 failed / 4 passed) and the acquisition boundary re-checked ON the merged tree. The section 9 preconditions — weighting, cardinal precedence, disagreement, confidence, tie-breaker — remain owner decisions and are NOT authorised by this merge |
| **V1-53 / C5-ROS-01 — canonical-alias write guard widened to cover the seasonal lane.** `tests/api/test_canonical_ownership_protections.py`'s repo-wide `_CANONICAL_WRITE` scan — the sole guard against a non-canonical module writing dynasty value — matched only `rankDerivedValue` by name, not the three aliases (`overall`/`finalAdjusted`/`displayValue`) `test_the_aliases_are_intentional_and_pinned` already proves equal it on every row. A module writing an alias directly (exactly the shape a redraft/ROS seasonal-lane leak would take under the dynasty/seasonal source-domain boundary) passed the gate silently. Widened the regex to the three aliases (dict-key and attribute forms), added `test_no_seasonal_lane_module_assigns_a_canonical_alias` scoped to `src/ros/**/*.py` and naming the invariant directly. False-positive check: a repo-wide scan for alias assignment outside `tests/`/`docs/`/`scripts/`/`frontend/__tests__/` found exactly one writer per alias — the already-approved `src/api/data_contract.py` — so widening introduces zero false positives. Mutation-proved: a synthetic `row["displayValue"] = ...` write appended to a scratch copy of `src/ros/team_strength.py` sends both the generic and the new dedicated test RED, each naming the file; restoring returns both to green (mutation not committed). `tests/api/test_canonical_ownership_protections.py` 20/20, `tests/ros/` 202/1-skipped, `tests/api/` (excl. livedata) 2088 passed/0 failed. Deliberately NOT claiming: `V1-52` (power-rankings consolidation — blocked on `C2-U4`, documented not implemented, see `docs/cseries-delivery/CLAUDE_11.md` §2), the full projection ensemble / Game Day / WAR/VORP / player-fit-college-translation (all POST-V1 DEFERRED per `VERSION_1_COMPLETION_CONTRACT.md` §4.1/§4.2), `V1-49`/`V1-51` (already delivered by other work, verified only, untouched), any `rankDerivedValue` methodology or `src/ros/team_strength.py` formula change. | `tests/api/test_canonical_ownership_protections.py`, `docs/cseries-delivery/CLAUDE_11.md` (new), `docs/WORK_CLAIMS.md` | V1-53, C5-ROS-01 | `claude/cseries-seasonal-intelligence-efz837` | done — merged as **#964** (`a13615bad`); **V1-53 `VERIFIED` at L1** 2026-08-20 (#972). Same stale-claim correction: the branch is on `main`, so leaving it `open` reports a live editor who is not there |
| **Claude 13 — C8/C9/C10 mass-build campaign, Batch 1: Chase Upside Market Ticker.** Elevates the existing `MarketTicker.jsx` (live on `/`) to Premium Sports Intelligence quality rather than forking a second ticker. Revised from the original plan once the code was actually read: `_compute_movers()` on `/api/terminal` turned out unnecessary to touch, because the ticker already reads canonical `rows` (the full materialized contract) and `row.confidence` — the same `marketBreadthAgreementIndex ?? marketConfidence` pass-through `ds/Badge`'s `Movement`/`Confidence` are built for — was already sitting unused on every row `computeMovers()` iterates. So the whole change stayed frontend-only: `computeMovers()` gained a `confidence` pass-through (guarded against `Number(null) === 0` — caught by a new RED test before the fix, `market-movers.test.js`), `MarketTicker.jsx` swapped its hand-rolled `role="tablist"` scope switch for `ds/SegmentedControl` and its raw colored delta for `ds/Badge`'s `<Movement>` (which reads `--data-up`/`--data-down` off `ds.css`, not the legacy `--green`/`--red`/`--cyan` in `globals.css`), and added a freshness affordance from the contract's own `generatedAt` via the existing `timeAgo()` helper (`lib/news-service.js`) — no new formatter invented. `market-ticker.module.css` replaces the migrated globals.css rules structurally (delta color + tab styling now live in `ds.css` via the primitives; `globals.css` itself is untouched — it's Lane 6-claimed). De-listed `MarketTicker.jsx` from `a11y-tab-roles.test.js`'s baseline (2 → 0 `role="tab"` sites) per that test's own designed ratchet. No new frontend value/rank computation anywhere — every rendered number is a pass-through of an existing canonical field. Verified: 5 new + 143 existing frontend test files green (2,245 tests), `next build` clean. Deliberately NOT claiming: `src/api/terminal.py` or `/api/terminal`'s `movers.*` block (not needed for this batch — flagged as a possible future dedup of `computeMovers()` vs. `_compute_movers()`, not done here to keep the diff minimal); any change to `globals.css` (Lane 6-claimed); a visual/manual browser check — attempted, but this sandbox has no working Python backend environment (`pip install -r requirements.txt` fails on an unrelated `http-ece` native-build error, and no `fastapi`/`uvicorn`/`pytest` are installed at all), so it's an honest gap rather than a silent claim, covered instead by 5 new + 2,240 existing automated tests and a clean `next build`. Full campaign authorization and Lane-6 file-exclusion list: `docs/cseries-delivery/CLAUDE_13.md`. | `frontend/components/terminal/MarketTicker.jsx`, `frontend/lib/market-movers.js`, `frontend/components/terminal/market-ticker.module.css` (new), `frontend/__tests__/market-movers.test.js` (new), `frontend/__tests__/a11y-tab-roles.test.js` (baseline de-list only), `docs/cseries-delivery/CLAUDE_13.md`, `docs/WORK_CLAIMS.md` | C8-PSI-01, C8-A11Y-01 | `claude/cseries-premium-public-closure` | open — `FEATURE_GREEN` / `READY_FOR_INTEGRATION`, Claude 5 owns the merge |
| **Claude 13 — C8/C9/C10 mass-build campaign, Batch 2: C9-HIST-01 franchise continuity.** `src/public_league/identity.py`'s `_RETIRED_OWNER_IDS` filter was a previous deliberate design (own dedicated test suite) that erased a retired owner's participation from EVERY season, including past seasons where they were a legitimate active roster — which is what made a season's declared `numTeams` disagree with its `season_standings()` row count (the manifest's stated symptom). Reversing an intentional, tested privacy decision isn't a unilateral bug fix, so the owner was asked which way to resolve it before any code was written; decision: restore historical rows, keep retired owners out of current-facing directories only. Shipped: `Manager.is_retired` flag; `build_manager_registry()` no longer drops a retired owner's aliases/`roster_to_owner` entries, it flags them instead; `ManagerRegistry.ordered_managers(include_retired=False)` is the new (and only) exclusion point, so `to_public_list()`'s directory behavior is unchanged. Audited every `by_owner_id`/`ordered_managers` call site rather than assuming correctness: `archives.py`, `franchise.py`, `overview.py` needed no change (their views are inherently historical/all-time, so restoring retired owners is the correct outcome there); `src/ros/power_v2.py::_enumerate_owner_ids` DID need a fix — its docstring explicitly relied on `by_owner_id` excluding retired owners for a CURRENT power-rankings table, so it now reads `{m.owner_id for m in ordered_managers()}` instead of the raw dict, preserving the original exclusion. `matchup_recap.py`'s existing orphan-fallback (`retired:{league}:{rid}` synthetic id) is unaffected in mechanism, fires less often (only genuine orphans now, not also retired owners) — stale inline comment and a stale test docstring corrected. Test file rewritten (not patched) since the old version pinned the reversed contract as correct: `tests/public_league/test_identity_retirement.py`, 8 tests incl. a full `metrics.season_standings()` end-to-end reproduction of the manifest's exact symptom. Deliberately NOT claiming: any frontend surface (no change needed); Public League Experience v3 (`C9-V3-01`) — this is its stated precondition, not the feature; whether a retired owner's franchise page should be linked from anywhere now that it resolves. | `src/public_league/identity.py`, `src/public_league/matchup_recap.py` (comment only), `src/ros/power_v2.py`, `tests/public_league/test_identity_retirement.py` (rewritten), `tests/public_league/test_matchup_unresolved_owner.py` (docstring only), `docs/cseries-delivery/CLAUDE_13.md`, `docs/WORK_CLAIMS.md` | C9-HIST-01 | `claude/cseries-premium-public-closure` | open — `FEATURE_GREEN` / `READY_FOR_INTEGRATION`, Claude 5 owns the merge |
| **Claude 13 — C8/C9/C10 mass-build campaign, Batch 3: C10-CLOSE-02 dead-code/duplicate-owner census re-verification.** Documentation-only batch that changed direction on real evidence. Corrected `C3-VA-01` (manifest + `dead-code-map.csv` `D-032`): direct code read + a green `test_trade_grade_parity.py` (8 tests/50 subtests) confirm the monkeypatch, rounding-divergence and dead V12/V13 exports the row described are ALL fixed — `market_value_adjustment.py` is now a 0-computation re-export of `ktc_va.py`; 2 Python ports remain (`ktc_va.py` + `src/public_league/trade_grading.py`, parity-tested), not 5; left the question of whether the second port should still collapse to Lane 2 (Claude 9, C3), since that's methodology not documentation drift. Before deleting anything from the census's "deprecate"-flagged dead modules, checked each candidate's own test suite for a reason it might be intentionally kept — and found real ones on the FIRST TWO checked: `D-120` (`src/api/chat.py`) has a live guard (`tests/api/test_chat_layers_are_consistently_wired.py`) whose docstring says "recorded rather than deleted"; `D-112` (`/draft-capital` nav mapping) is a legacy routing-layer 308 redirect in `next.config.mjs`, pinned public by `public-routes.test.js` — "no page directory" was never evidence of dead code. Both corrected in `dead-code-map.csv` with the real evidence; given 2-for-2 false positives on the census's own dispositions, stopped attempting deletions this session rather than push the same risk through the remaining rows, and flagged that every `D-1xx` "deprecate" row needs the same per-row check before anyone acts on it. Also ran (then reverted) `tools/verify_closure.py` on the separate older W##-F### tracker: confirmed by direct code read that `W11-F006` is genuinely fixed on HEAD despite dropping out of the regenerated "claimed closed" list — a shallow-clone git-history artifact in this sandbox, not a regression — but did not commit the regenerated `CLOSURE_STATUS.md`/`closure.json` (would have been a *less* accurate snapshot) or hand-edit the frozen claims ledger with an unreliable commit sha; flagged for Claude 5 to fix with full git history. Deliberately NOT claiming: any code deletion; the other `C10-CLOSE-02` DUPLICATED rows (`C2-REPL-01`, `C2-STR-01`, `C2-WEAK-01`, `C3-PKG-01`, `C3-EQ-01`, `C5-POW-01`, `C5-PLAY-01`, `C6-SIG-01`) — live engines owned by other lanes' methodology, not documentation drift; `W11-F006`'s frozen-ledger fix; the un-rechecked `D-121`–`D-129` rows. | `docs/C_SERIES_SCOPE_MANIFEST.md` (C3-VA-01 rows only), `docs/master-site-audit/evidence/W30/dead-code-map.csv` (D-032, D-112, D-120 only), `docs/cseries-delivery/CLAUDE_13.md`, `docs/WORK_CLAIMS.md` | C10-CLOSE-02 | `claude/cseries-premium-public-closure` | open — `FEATURE_GREEN` / `READY_FOR_INTEGRATION`, Claude 5 owns the merge |
| **V1-51 blocker (#943) — `rosChampionship` claimed 10,000 simulations while returning no odds.** Measured live on `e37d2786e`, preseason (`weeksPlayed: 0`): three public probability surfaces described one state three ways — `playoffOdds` `numSims: 0` + `unsimulable`, `rosPlayoffOdds` `n_simulations: 0`, and `rosChampionship` `championshipOdds: []` beside **`n_simulations: 10000`** with `rosStrengthAvailable: true` and a fully resolved 7-team bracket. The count was the caller's ARGUMENT echoed from a branch that never entered the simulation loop, so absence of evidence published as a completed measurement. Repair is ONE branch — `championship.py`'s `if not distributions:` now reports `n_simulations: 0` plus an `unsimulable` block reusing the sibling engine's existing `no_scored_weeks_in_league` vocabulary (deliberately distinct from the bracket-unknown reason: different operator response). RED-first cross-surface agreement test drives all three engines through one shared state and normalises `numSims`/`n_simulations`; it failed on `rosChampionship` only, which is the isolation the issue claims. Mutation-proved three ways. Success path unchanged (8 rows, real count, odds sum 1.0); frontend provably unaffected — it early-returns on the empty list before reading the count. Deliberately NOT claiming: championship methodology, any probability value, `playoff_sim`'s own no-distributions branch (truthful but SILENT — no `unsimulable`; a different and smaller defect in a different file, reported not fixed), a `simulated` flag for the ROS engines (the public engine's field; adding it to one refusal branch only would be a new asymmetry), or the L2 post-deploy remeasurement V1-51 needs for promotion. | `src/ros/championship.py` (one branch), `tests/ros/test_probability_surface_agreement.py` (new), `docs/WORK_CLAIMS.md` | V1-51, #943 | `claude/v1-51-championship-truthful-sims` | done — merged #956 (`4157adfe4`); **V1-51 `VERIFIED` at L2 2026-08-20** on deployed `cca488cd7`: `rosChampionship` `n_simulations 10000` → `0` with an explicit `unsimulable no_scored_weeks_in_league`, all three engines agreeing on one number and one word. Closed by Integration; leaving it `open` would report a live editor on the ROS/championship files and falsely block Claude 11 (C5) |
| **Claude 10 — C-Series Market/FAAB/Sharp/Waiver/Draft lane (post-V1-sprint continuation work, per `docs/EXECUTION_PLAN.md` §0 "lane assignments deliberately extend past V1").** Owns remaining C4 (Market/FAAB/Analyst) work not owned by Claude 8's cross-position source-acquisition project, plus dependency-ready C7 waiver/draft consumers. Unit 1 (**C4-WAIV-01 — Waiver ledger**): new `src/trade/waiver_ledger.py`, a pure read-projection over the canonical acquisition ledger (C1-ACQ-01/C1-U8) that groups recorded WAIVER/FREE_AGENT transactions into one claim per transaction (added + dropped assets, FAAB bid, timestamp), with an aggregate `waiver_ledger_summary`. Deliberately built as a projection rather than a second Sleeper-fetch collector — see the module docstring for why this does not duplicate `sleeper_overlay._build_waivers_block` (ephemeral 365-day display window) or `src.trade.faab_history` (bid-distribution-only market calibration). 10 new tests, all invariant-based (zero-bid ≠ missing bid, undated ≠ dated-zero, multi-add claims stay one row, drop-only transactions excluded, league isolation). Unit 2 (**C4-MTL-01 — Market Trade Ledger, own-league lane**): new `src/trade/market_trade_ledger.py`, same projection pattern over TRADE/TRADE_AWAY acquisition events, grouped per transaction into a multi-team-safe `teams: {rid: {received, sent}}` shape (never flattened into a fictional two-party pair) plus structured format metadata via the existing `TargetFormat.from_registry` (no second scoring-card parser). Explicitly scoped to the own-league lane only — the broader cross-market population (`C4-MTL-02`, KTC trade-database ingestion) stays gated on the uncaptured `F-EXT-01` permission grant and is NOT claimed here; cross-source dedup is deferred to when a second source lane actually exists. 10 new tests. Both units required extending the structural guard `tests/acquisition/test_board_inertness.py::test_the_whole_src_tree_is_free_of_valuation_side_consumers` with two named, justified exceptions (pure historical projections, never reaching `rankDerivedValue` or any canonical value — the same legitimate direction `src/history` already reads value history) rather than a blanket prefix allowance, so a third consumer still has to earn its own line. Deliberately NOT claiming: `C4-MTL-02` (external KTC ingestion, permission-gated), `C4-MTL-03` (comparable-trade matching, deps on this unit), `C4-FAAB-01` (FAAB Market Heat, deps on `C4-MTL-01` — next in this lane), any frontend/API route wiring for either ledger (kept to the canonical owner module + tests this pass; consumers are a separate bounded unit), `C4-SHARP-*` production hardening, `C4-SRC-*` (source-health, lane-5 territory per the V1 sprint's lane table — will revisit only if explicitly requested), any Claude-8-owned source-acquisition/cross-position-bridge work. Unit 3 (**C4-MTL-03 — comparable-trade matching**): new `src/trade/comparable_trades.py`, `comparable_trades_for_asset(asset_id, target_league_key)` searches every other active league's `market_trades()` for trades touching the asset and classifies each against the target league's format via `TargetFormat`, into 4 of the spec's 5 match tiers (the 5th, `NORMALIZED COMPARABLE`, is deliberately never emitted — it requires a cross-format value-conversion model that does not exist in this repo, and fabricating the tier without it would manufacture false confidence); an unproven format dimension fails closed to `UNSUPPORTED_UNVERIFIED` rather than a weaker assigned tier. 10 new tests (6 direct against the pure `_classify` function covering every tier plus "unknown ≠ mismatch", 3 against wiring — self-exclusion, asset filtering, newest-first sort — 1 empty-registry check). Mid-session correction recorded in the delivery doc: before starting Sharp-hardening work, found `docs/lane4/LANE4_V1_RECONCILIATION.md` (prior lane-4 session, already merged to `main`) already diagnosing `C4-SHARP-01`/`C4-SHARP-02` as **BLOCKED on production admin access** (owned by Claude 5/prod, not re-openable from a sandbox) and `C4-SHARP-03` as already closed via #911 — so no Sharp-hardening gap was left to claim. Unit 4 (**Batch 3 — read endpoints**): `GET /api/market/waivers` + `GET /api/market/trades` in `server.py`, immediately after `/api/waiver/faab-recommend`, following `/api/draft/roster-context`'s established shape (`_resolve_league_for_request` for scoping; no per-route auth check — `/api/*` auth is enforced once, globally, by the `_private_api_gate` middleware, discovered when the first test came back 401). Each returns `{leagueKey, summary, recent<Claims|Trades>, recent<...>Truncated}` capped at 100 most-recent rows. 8 new tests in `tests/api/test_market_ledger_endpoints.py` exercising the real HTTP layer (league resolution, seeded round-trip, unknown-league 400s, truncation flag, cross-league isolation). PR #961 (batch 1) hit one unrelated CI failure fixed here (a `ruff format` miss on `market_trade_ledger.py`, pushed) and one CI failure NOT fixed here and explained on the PR thread: `check_source_health.py`'s content-staleness check (`idpTradeCalc` CSV unchanged 36 days) is base-wide vendor-data drift, confirmed via PR #921 passing the identical check a day earlier — a CI-wiring question (an "advisory, loud always" script apparently hard-failing the job) for Integration/CI-reliability, not this lane's diff. **Correction (Batch 4), same finding restated properly:** that CI diagnosis was itself wrong — Claude 5 (Integration) caught it by checking per-step `conclusion` values instead of a log tail. `check_source_health.py` genuinely is advisory (`continue-on-error: true`); the real hard failure was the **Decision-path coercion gate** (`scripts/check_decision_coercions.py`), which correctly flagged three real `occurredAtMs or 0` sort-key fabrications introduced by units 1-3 (`waiver_ledger.py:96`, `market_trade_ledger.py:146`, `comparable_trades.py:151`). Fixed by extracting `src/trade/ledger_sort.py` — two pure key functions that partition rows into known/unknown-timestamp groups so a missing timestamp is never compared against or replaced by a fabricated number (tuple-comparison short-circuits via `==` before ever reaching cross-group magnitude comparison) — wired into all three call sites, with 8 new tests pinning both that the key never coerces `None` to `0` and that resulting sort order is unchanged from the old code. `check_decision_coercions.py` now reports clean. PR #961's description and the earlier misattributing comment were both corrected, crediting the catch rather than silently editing history. | `src/trade/waiver_ledger.py`, `src/trade/market_trade_ledger.py`, `src/trade/comparable_trades.py`, `src/trade/ledger_sort.py`, `server.py` (two new endpoints only), `tests/trade/test_waiver_ledger.py`, `tests/trade/test_market_trade_ledger.py`, `tests/trade/test_comparable_trades.py`, `tests/trade/test_ledger_sort.py`, `tests/api/test_market_ledger_endpoints.py`, `tests/acquisition/test_board_inertness.py`, `docs/cseries-delivery/CLAUDE_10.md`, `docs/WORK_CLAIMS.md` | C4-WAIV-01, C4-MTL-01, C4-MTL-03 | `claude/cseries-market-waiver-draft-2njtem` | open — batches 1-4 `READY_FOR_INTEGRATION` (PR #961) |
| **Claude 8 / cross-position bridge V1 — eliminate the single-source IDP backbone dependency.** Owner-designated top-priority V1, 2026-08-20. `idpTradeCalc` is the ONLY source whose value column spans both pools, so it is the only thing that can seed the shared-market ladder; measured with it excluded, **661 votes cast on untranslated ranks, 310 flagged rows, top IDP published at 9,999, IDP top-100 count 29 against a healthy 8** (evidence: PR #950 / `docs/sources/CROSS_POSITION_SOURCE_AND_IDP_CEILING_AUDIT_2026-08-20.md`). **PR A (this claim's first unit)**: acquisition-state and bridge-state vocabularies (none exist today — `grep AUTH_REQUIRED|PARSE_FAILED|SCHEMA_CHANGED|AcquisitionState` returns zero hits across `src/ scripts/ config/`, and `scheduled-refresh.yml::run_fetcher` collapses fetcher exit 1 and 2 into "don't stamp", losing the schema-regression signal before any observer sees it); a per-row source archive record so rank + positional rank + tier + cardinal value + native unit + ids + provenance survive preservation (`ArchivedBoard.rows` is `dict[str, float]` and can express none of them); and a bridge descriptor interface. **No production valuation change in PR A.** Later units: PR B multi-bridge translation owner (SERIAL on `data_contract.py` pipeline core — schedule with Claude 5), PR C Dynasty Nerds public ordinal/tier IDP Top-275, PR D Dynasty Dealer acquisition + same-basis qualification. Deliberately NOT claiming: any IDP value ceiling or corridor; `TAIL_SATURATION_RANK`; `_ALPHA_SHRINKAGE`; any Hill constant or source weight; the V1 denominator itself (proposed rows go to Claude 5 per `VERSION_1_COMPLETION_CONTRACT.md` §10); source-health FRONTEND surfaces (Lane 6) or global source-health semantics (Lane 5); `src/source_archive/`'s existing C1-U9 contract beyond the additive schema extension its own claim row leaves unowned. | `src/sources/acquisition_state.py`, `src/bridges/*`, `src/source_archive/store.py`, `src/data_models/contracts.py`, `docs/sources/*`, `tests/sources/*`, `tests/bridges/*` | PR #950 findings; `V1-23` (proposed); C1-U9 adjacency | `claude/cross-position-bridge-v1` | open |
| **Lane 6 / premium-UI V1 repair queue — frontend defects only, no route reskin.** Owner-scoped 2026-08-18 to the feature freeze's permitted repair set (performance / accessibility / data-integrity defects in currently implemented functionality); explicitly NOT a Premium Sports Intelligence route migration, which north star §5.3/§6 still gates. Batch 1 (**F-13 + mobile parity**): `compact_contract` carried BOTH player encodings while `array` dropped the legacy dict, so the "compact" mobile view was the LARGER of the two (735.0 vs 631.8 KB gz on a 1,109-row contract) — and it pruned 14 of the 17 per-player fields `_materializePlayerArrayRow` READS, so mobile and desktop rendered different numbers for the same player (Flagged count 0 on /edge; `blendedSourceRank` is a SORT KEY so the Consensus ORDER could differ; confidence string; PlayerPopup's value chain; the listed-vs-no-contribution honesty branch) and /rankings' methodology section was absent on mobile only. `isMobileProfile()` also fires on `deviceMemory <= 4`, so this was never confined to phones. Repaired: dict dropped when `playersArray` is present (kept when it is the only encoding — the runtime view), prune list reduced to the three fields nothing reads, `methodology` un-pruned. Measured **735.0 → 491.0 KB gz, -22.3% against array**, carrying MORE fields than before. Deliberately NOT claiming: any valuation, ranking, trade or FAAB methodology; any backend business logic; `_SLIM_SOURCE_RANK_META_FIELDS` (verified `percentile`/`isAnchor`/`ladderDepth` have no frontend reader); the compact-view cache key's resize behaviour (recorded, not fixed). Batch 2 (**#779 admin crash**): `fmtPassExpiry` was called twice in `components/admin/GuestPassPanel.jsx` and defined nowhere it could see — the definition stayed behind in `app/settings/page.jsx` as module-private dead code when the panel was extracted, so `/admin` reached the global client-side error boundary on every load that had a pass to list. Reachable only WITH data (the expiry cell renders inside `passes.map`), which is why an empty-list smoke test never saw it. Moved to `frontend/lib/guest-pass-format.js`, imported at the call site, orphan deleted; RED-verified (the test reproduces `ReferenceError: fmtPassExpiry is not defined` before the fix). Census: an ESLint `no-undef` probe over `app/`+`components/`+`lib/` reports **0** other undefined free variables, and reintroducing the missing import turns it red on both call sites — so this was the only instance, and the gate is worth keeping (queued for the accessibility-instrumentation batch). Deliberately NOT claiming: the temp-password system end to end (create → persist → sign → display → auth → expiry → revocation is Claude 5's system-level scope). Batch 3 (**#760 rankings windowing**): the board mounted every row after "Show all" — 982 rows / 31,172 DOM nodes — and scrolled at **6.8 FPS** against a 60.0 FPS trivial-page control on the same hardware. #760's stated prerequisite (pinned column widths) had already landed as `freezeColumnWidths`, so the blocker was gone and nobody had noticed. New `components/ds/useRowWindow.js` + a `virtualize` prop on `DataTable`, which REFUSES to window without the width freeze (ignored with one console warning, not thrown). Heights are derived from consecutive mounted-row offset deltas within a single layout burst — never a constant — so caller-injected tier separators and the expansion drawer are reserved for rather than modelled. **6.8 → 35.6 FPS, 5.2x, DOM nodes -95%.** Does NOT reach the >=50 target, and the reason is measured: a 200-row windowed board scores 35.9 against the 982-row board's 35.6, so **row count is no longer the driver**; sticky, box-shadow and the edge rail were each ablated on the live board and all three sit inside the ~6% baseline drift. Naming the residual is open and separate. Restored #760's corrected FPS harness (no timer in the loop, control printed per throttle rate) and fixed two of its own guards — it read `aria-rowcount` off `<tbody>` where ARIA puts it on `<table>`, and it refused any run mounting <50 rows, which is every successful windowed run. E2E assertions that counted `<tr>` as a proxy for "the board has data" now read `aria-rowcount` via a new `journey.js::boardRowCount`, with mounted-count asserted separately so both failures stay distinguishable. Deliberately NOT claiming: the residual per-frame cost, any change to the row cap or the `useTransition` growth wrapper (different controls, both kept), or virtualizing any other table. Batch 4 (**failure semantics**): the contract fetch threw `new Error("Failed to load dynasty data: ${status} ${text}")` and `useDynastyData` recovered the status back out with `/\b401\b/.test(message)` — so a 503 (the scoring-identity fail-closed gate, a working guard), a 403 (unhandled anywhere), a 500 and a network timeout were one indistinguishable string across eight high-use routes, and a player named "401" in the body would have triggered the sign-out redirect. Status + parsed body now travel ON the error; new `lib/contract-failure.js` classifies them into nine kinds modelled exactly on the existing `classifyBdvmFailure`/`classifyEdgeFailure` (three classifiers, one shape); new `ds/FailureState` renders the kind, choosing tone so `Banner`'s role=status/role=alert split matches whether anything is actually wrong, and withholding a retry button where retrying cannot help. `useDynastyData` publishes `failure` and a `retry` alongside the unchanged `error` string, so no existing consumer loses anything. Routes repaired: /rankings (one negative banner for every state), /settings (a bare red `<p>`, no role, no primitive), /admin (asserted an allowlist cause for EVERY failure incl. 500s and dropped connections), /login (a 429 or 503 read as "Invalid username or password", and the message was never announced). `ui/ErrorState` gains the `role="alert"` it never had. Also fixed FOUR stale e2e assertions that counted mounted `<tr>` as a proxy for board size — one of them (`journey-settings-overrides:144`) was broken by batch 3's windowing and is mine; the `journey-tools-health` one was stale against **F-7, repaired on main earlier today**, which changed `SourceHealthStrip` to render the 21 registered sources rather than the 2 runtime-enabled ones while the test kept asserting the old branch. Corrected my own earlier read of that failure: the strip is right, the test was wrong. Deliberately NOT claiming: /league, /rosters and the player pages (same class, not yet converted); the settings-overrides "custom-mix badge" failure (fails identically on main's nightly, not mine). Batch 5 (**#759 non-data routes**): six private routes mounted `PrivateAppShell`, which mounts `useDynastyData`, which calls `prefetchBaseContract()` in a mount effect — so `/login` downloaded the multi-MB player contract and materialized ~1,100 rows to render a password box. Rebased #759's branch (unmerged since 2026-08-05) and re-verified rather than trusted: a transitive import scan over 73 modules reachable from the six gated routes finds NO route-owned consumer of `useApp()`/`useDynastyData()` — only the shell itself, which is what the gate switches off — and a real browser probe measures **0 contract requests on all six** while all six still render, against **1 each** on the `/rankings` + `/settings` + `/tools/trade-coverage` control group. Preserved #759's design decisions verbatim, including the reasons: a SECOND predicate list rather than more entries in `PUBLIC_ONLY_ROUTE_PREFIXES` (that constant is a privacy boundary and every gated route is private, so folding them would make a future "what is public" audit read /admin off the public list), and `/settings` + `/tools/trade-coverage` pinned as deliberate EXCLUSIONS. Also carries #759's live sub-defect: `searchEnabled` is `privateDataEnabled && authenticated` and `openSearch()` returns early without it, but both nav bars rendered the button on `authenticated` alone, so a signed-in user on /league saw a search control that did nothing. Deliberately NOT claiming: gating on `authenticated` instead of a route list (rejected in #759 — `useAuth` is tri-state, so it would serialize the contract fetch behind an auth round-trip on every page). Batch 6 (**accessibility, with an instrument**): the repo had NO ESLint config anywhere, no `jsx-a11y`, no axe-core and no Playwright a11y scan — the only a11y test was a structural guard on one rule shipping a 7-violation baseline (`C8-A11Y-01`, recorded PARTIAL for exactly this). Added `@axe-core/playwright` + `tests/e2e/specs/a11y-axe.spec.js`, a RATCHET over 10 routes x 2 viewports that fails on anything new AND on a baseline allowance that has gone stale. Its first run found 5 classes, all now fixed and the baseline is **empty by measurement**: `/settings` `label` x4 + `select-name` x3 (every numeric valuation control on the page was an unnamed input — labels were SIBLINGS, not wrappers) and `aria-prohibited-attr` x21 (status dots carrying `aria-label` on a bare `<span>`, which has no role to name); `/rosters` `color-contrast` **x57** (white assumed readable on saturated chips and bar segments, down to 2.19:1 on PICKS) and `scrollable-region-focusable` (a sideways-scrolling table with no keyboard way in); `/rankings` a 4.494:1 near-miss from a 0.85 opacity compositing the badge blue toward the surface. Contrast is fixed by COMPUTATION, not by repainting the palette: new `lib/contrast.js` picks black-or-white by WCAG luminance for text ON a mark, and derives the missing text-safe variant for a mark used AS text — six of the eight position colours are returned byte-identical, so it is a floor rather than a restyle, and it survives the pending `OD-05` palette decision. One subtlety caught by re-measuring: the variant must be derived against the PANEL surface (#131519), not the page (#0b0d10) — deriving against the page cleared the floor on paper and left LB at 4.36:1 on screen. Result: **20/20 axe checks green, zero WCAG A/AA violations** across /rankings, /trade, /waivers, /rosters, /trades, /settings, /admin, /login, /league and / on both viewports. Deliberately NOT claiming: an ESLint `no-undef` gate (the #779 defect class — measured 0 instances, still worth a permanent gate, deferred); best-practice axe rules (opinions, not conformance); the keyboard-inaccessible `<span onClick>` draft actions and the 58 hand-rolled tables that bypass `DataTable` — real, out of this batch, recorded. Batch 7 (**OD-05 — the token direction**, owner-approved 2026-08-18 as a standalone revertible commit): the manifest recorded the live tokens as in CONFLICT with the Premium Sports Intelligence north star. Audited, the conflict is NARROW — near-zero radii (2/2/3px), thin rules, overlay-only shadows, elevation by surface lightness, tabular numerals, the type scale and the CVD-validated chart order are all things the north star asks for and all already shipped, so they are untouched rather than re-derived. The real divergence is the ACCENT: the terminal pass deleted the franchise colours for a structural signal-blue, and the north star asks for a premium sports identity with front-office credibility. Franchise gold restored as the one accent in both themes (#ffc704 dark, #7d6200 light — the latter already present as the light theme's focus ring beside a blue accent, which is the drift showing through). Every ratio re-measured, not carried forward: dark accent 12.44/11.68/10.92/10.06 on surfaces 0-3, text-on-accent 12.82:1; light accent 4.97:1 worst surface, white-on-accent 5.80:1. `--cyan` in globals.css follows (it IS the accent for ~200 call sites; leaving it blue would have rendered the app in two accents at once) while `--amber` deliberately does not, because gold's discipline depends on warning staying a separate ROLE. Verified with batch 6's own instrument: **axe 20/20 still green after the palette change**, so the change introduced no contrast regression anywhere. Stale copy corrected in the same commit: `/design`'s gallery labelled its own accent "signal blue", and `docs/DESIGN-SYSTEM.md` had never stopped documenting gold while the code shipped blue. Deliberately NOT claiming: any route migration or component restyle (north star §5.3 still gates that, and nothing here changes a layout); the visual JUDGEMENT, which is the owner's — this is one commit and reverts alone. Batch 7 (**F-16 — the budget gate could not fail**): `frontend/scripts/check-bundle-sizes.mjs` is the BLOCKING per-page budget check in PR Validation, and a budgeted page whose chunks did not resolve was logged "(no chunks — skipped)" while the run printed "all pages under budget ✓" and exited **0**. Reproduced on a synthetic build: deleting `/rankings/page` outright — the densest route in the app, and the one #760's windowing numbers are measured against — left the gate green. `assertGateIsMeasuring` only refused at **zero** of 14, so 13 of 14 vanishing passed. The header advertised a `--strict` flag to change this and the script **never read `process.argv`**, so a hatch that did not exist was the only thing making the default look deliberate. Repaired: unresolved budgets are a hard exit 2 naming each page and both remedies (delete the entry with the route, or build `--webpack`); the zero case keeps its separate Turbopack diagnosis; every run now prints `measured N of M` because "✓" alone cannot distinguish 14 of 14 from 13. Real build: **14 of 14, exit 0**. Guarded by `frontend/__tests__/bundle-budget-gate.test.js`, which drives the shipped script via `NEXT_DIST_DIR` (not a reimplementation), reads the budget table out of the script so the test cannot police a different set, and pins the over-budget and zero-resolve paths so the new refusal cannot shadow them. **Correction to this row's own earlier handoff note:** `/tools/source-health` rendering 21 rows is CORRECT (F-7 deliberately enumerates `registered_sources`); the real defect is the inverse and is measured — `SourceHealthStrip.jsx:51-52` tests `failed_sources`/`partial_sources` membership using **registry keys** (`ktcSfTep`, 21 of them) against lists built from the scraper's **display names** (`KTC`, 12 of them, `Dynasty Scraper.py:3065`). The intersection is EMPTY, both branches are structurally dead, every source falls through to age-only, and a failed fetch usually leaves the previous CSV in place — so a hard-failed source renders green. Fix needs one canonical mapping at the seam that already holds both vocabularies (`server.py::_source_health`); flagged on PR #912 before writing, because that is the function #909 touched. Batch 8 (**F-12, frontend half**): measured against the live `/api/status` and the scraper's own `source_enabled_map` — `registered_sources` is 21 registry keys, the run vocabulary is 12 display names, and the **intersection is exactly 0**, so all three per-row lookups (`failed_sources`, `partial_sources`, and the `source_failures` reason join at line 146) were structurally dead and every row fell through to age. They are also NOT a rename of one another: one scraper step feeds several registry keys (KTC → `ktc` + `ktcSfTep`) and most registry sources are fetched by their own `scripts/` timers and appear in no scrape run at all, so a real mapping has to be DECLARED by the owner of source identity rather than inferred. Done here, needing no mapping: run-reported failures that match no row are no longer discarded — they render under the run's own names with the run's own reason, in a distinct `source-health-unattributed` block labelled as unmatched, so "three rows fine" and "the run failed somewhere we cannot place" stop reading the same. The membership tests are KEPT (pinned by a test) so the per-row path lights up on its own if run state is ever published in registry keys; and a second test pins that unattributed failures are NOT smeared across the rows, which on a partial run would paint the whole board red. Corrected copy that asserted what the code cannot know: the component docstring and `/tools/source-health`'s legend both said green = "last run OK"; the dot is FRESHNESS, and a failed fetch leaves the previous CSV in place. Deliberately NOT claiming: the backend mapping (still the identity owner's), any change to `overall_status`, or any source-health methodology. Batch 9 (**keyboard reachability, WCAG 2.1.1**): censused the whole class rather than the three sites this PR had named — **30** clickable non-interactive elements across 15 files, and `app/draft/page.jsx` had **6**, not 3. Five were the player NAME on the next-best-target boards, so *drafting a player* was mouse-only from five separate panels; the sixth was the main board row. axe reports **zero** WCAG A/AA violations across ten routes and misses every one — a clickable span is not machine-distinguishable from a decorative one, so it is a manual check that had never happened. Fixed: the five names are now real `<button>`s (focus, Enter/Space and role for free), with CSS resets so the element changed and the pixels did not, plus an explicit `:focus-visible` ring because `globals.css` strips outlines without substitutes; the unpriced case is now `disabled` rather than `cursor: default`, which said it only to a pointer. The `<tr>` takes the documented fallback (`tabIndex` + Enter/Space, Space preventDefault'd) because a row cannot become a button without destroying the table semantics a screen reader navigates by. Verified in a real browser on a clean build: 20 name buttons, 0 spans, focusable, ring present, **Enter opens the draft modal**; 72 rows focusable with a ring. Instrumented by `frontend/__tests__/a11y-clickable-keyboard.test.js` — a ratchet in the existing vitest gate (no ESLint exists in this repo), failing on new violations AND stale allowances, with a self-guard that pins the matcher against synthetic positives and negatives. The remaining **24** are baselined, mostly `/league`, and are a separate larger unit. Deliberately NOT claiming: any `/league` migration, or the 24 baselined sites. Batch 10 (**a failure is not an absence**): the enabling defect was mine and upstream of the routes — batch 4 added `failure`/`retry` to `useDynastyData`, and `AppShell`'s context forwarded only the `error` STRING, so no `useApp()` consumer could tell a 503 from a 403 however well the hook classified it. Both are now on the context (default `failure: null`, because "no classified failure" is not "a failure of unknown kind"). `/rosters` rendered `EmptyState title="Error" message={error}` — wrong three ways at once: `EmptyState` is a non-interrupting `role="status"` surface so a screen reader was told *nothing is here*; it offers no retry; and since contract failures now carry their response body it printed a raw JSON 503 into the page (observed verbatim in a control run). Now renders `ds/FailureState`, which picks role from the KIND and offers retry only when retryable; an unclassified error is coerced to an explicit generic failure rather than rendering blank, because `FailureState` returns null on a falsy failure — caught in my own first draft, which passed a `message` prop that does not exist. Guarded by `frontend/__tests__/a11y-failure-not-empty.test.js`, a ratchet baselining the remaining **16** sites. Two matcher defects found by its own assertions and fixed rather than baselined around: it matched failure vocabulary anywhere in the props (flagging `/league/activity`'s *correct* empty-view-with-degraded-input note), so it now reads the TITLE, which is what names the state; and its title capture truncated at an apostrophe, so `title="Couldn't load data"` read as clean — the stale-allowance assertion is what surfaced that. Verified in a browser: `/rosters` renders h1 + table with no raw JSON in the DOM. Deliberately NOT claiming: the 16 baselined sites (mostly `/league`, a public route of ~10.8k lines, each section owning its own fetch). | `src/api/compact_view.py`, `server.py` (one comment), `frontend/lib/device-profile.js` + `frontend/lib/dynasty-data.js` (comments), `frontend/scripts/check-bundle-sizes.mjs`, `frontend/__tests__/bundle-budget-gate.test.js`, `frontend/components/SourceHealthStrip.jsx`, `frontend/app/tools/source-health/page.jsx`, `frontend/app/globals.css`, `frontend/__tests__/components/source-health-strip.test.jsx`, `frontend/app/draft/page.jsx`, `frontend/app/draft/draft.css`, `frontend/__tests__/a11y-clickable-keyboard.test.js`, `frontend/__tests__/a11y-failure-not-empty.test.js`, `frontend/components/AppShell.jsx`, `frontend/app/rosters/page.jsx`, `frontend/scripts/measure-route-baselines.mjs`, `tests/api/test_compact_view*.py`, `tests/e2e/specs/api-view-parity.spec.js`, `docs/WORK_CLAIMS.md` | F-13, F-16, F-12, C0-PERF-01, WCAG 2.1.1, mobile/desktop parity | `claude/premium-ui-migration-ltldy0` (mirrored to `v1/premium-ui`) | done — merged as **#912** (`b3ea4cf5a`). Closed by Integration 2026-08-20 as a **stale-claim correction**, not a reassignment: the PR was repair-only and is on `main`, so the row was reserving `frontend/components/AppShell.jsx`, `frontend/app/globals.css`, `frontend/app/draft/page.jsx` and `frontend/app/rosters/page.jsx` against a session that had finished. That was already costing real work — #965 records avoiding `globals.css` because this row still read `open`. The repairs themselves are **protected, not released**: compact/mobile parity, virtualization, aria row semantics, failure-state semantics, the WCAG baseline and the bundle budgets are parity gates on the PSI migration (`EXECUTION_PLAN.md` §0) |
| **P1 — an interactive request may not start a heavyweight nflverse ingest.** PR #938's E2E repair proved the defect underneath it: `/api/bdvm/*` filled the nflverse caches as a side effect of the FIRST interactive values request, and `/api/data` then starved past the Next bridge's 4s inter-chunk timeout — measured here at **49.9s and 295,111 rows fetched and parsed inside the serving process**. `run_in_threadpool` is not the protection and cannot be (GIL, not the event loop). Two stages: `_cached_or_fetch` gains a cache-only request mode (stale readable, never relabelled — a TTL governs the REFRESH owner, not readability), and the BUILT player context is materialised out of band, because cache-only alone still left 9.0s of GIL-bound parsing off a 369 MB weekly-stats entry. `src/bdvm/schedule.py`'s SECOND nflverse downloader (its own `urllib.request.urlopen`, no cache, no single-flight, no feature gate) is retired into `ingest.fetch_schedules` and structurally guarded. Warm wired into the EXISTING `dynasty-bdvm-refresh` timer — no new scheduler. Measured **49.9s → 0.48s, 0 remote fetches**, and `run_valuation` proven to receive byte-identical arguments in both trees on the real 23,939-player context and 2026 schedule (sha256 `5482d2f7…`). Deliberately NOT claiming: any BDVM methodology, `blend_ros_mu`, replacement math, strategy currencies, the feature-flag default, `rankDerivedValue`, #938's E2E pre-warm (assessed redundant, removal left to the E2E lane), or materialising the in-season actuals read (measured residual: 1.65s once per season per UTC day, named at the call site). | `src/nfl_data/{ingest,cache,nflverse_direct}.py`, `src/bdvm/{context,context_store,actuals,schedule}.py`, `src/api/bdvm_api.py`, `scripts/refresh_bdvm_inputs.py` (new), `deploy/systemd/dynasty-bdvm-refresh.service.template`, `deploy/systemd/README.md`, `tests/bdvm/test_request_path_is_local.py`, `docs/WORK_CLAIMS.md` | P1-BDVM-INGEST (from #938) | `claude/bdvm-nonblocking-ingestion-v2` | done — merged `015570741` (#946); CI green on the merged head `647019be8` (Validate PR 16m21s, E2E Safety Net 6m42s); Deploy Production SUCCESS and the production state closed at Integration 2026-08-20 (`docs/ops/P1_BDVM_INGEST_PRODUCTION_CLOSURE_2026-08-20.md`). Closed by Integration: the row was still reporting a live editor on `src/nfl_data/`, `src/bdvm/` and `src/api/bdvm_api.py` weeks after the work shipped, which under the five-lane campaign is a false block on real files. Salvaged from #949, which was closed as superseded — its OTHER edit (the V1-51 row) would have overwritten `main`'s richer text with an older draft |
| **CI reliability lane — CI's own side effects, not the seven red ticks.** Audited the seven named failure families against the live Actions API. Four are correctly red or already resolved and this lane changes none of them: Bootstrap Sharp Records (0/19, runaway prod collector — owner), Retention health (0/8 ever, `C1-RET-07` dead since 2026-04-20 — owner), Rank-Form Drift (real drift from #787; constants are an ADR-008 human act — owner), E2E (another lane landed `1af1d2b3`; PR #762 open). Scheduled Data Refresh is 23 consecutive greens and PR Validation's 2-in-60 failures were both real. What IS repaired is a class the 2026-08-18 triage did not look at: **a verification workflow writing to `main` 42 times a day** (42 of 66 bot commits in 24h, all recording an unchanged status — class-C drift under every open PR), four superseded workflows still holding production SSH credentials (one with a confirmed `exit 0`-on-missing-secrets path), three gates that render green while asserting nothing (job-level `if` skip = neutral = success; a gate with no credential warning green forever; trackers that cannot find their own issue because the `calibration` label does not exist — #777/#895 are the same request seven days apart). Deliberately NOT claiming: `deploy.yml`, `release-candidate.yml`, `config/coercion_baseline.json`, `CLAUDE.md`, `frontend/**`, `e2e.yml`, `requirements*.txt`, `package.json`, the rank-form constants, the Bootstrap trigger cadence, a shared concurrency group (measured to add a failure mode and remove none), SHA-bound production verification (needs `deploy.yml`), or the `mergeable_state: dirty` → zero-checks-scheduled hole (a repository-settings act no PR can land). **Path overlap, declared:** `tests/deploy/` is named by the audit-batch row above. Four files there are NEW; `test_sharp_smoke_commit_order.py` is EXTENDED because it is the canonical guard for the workflow this lane changes, and putting the extension elsewhere would create a second owner for one invariant. No false-green mechanism introduced; zero retries added. | `.github/workflows/{verify-sharp-production,health-check,smoke-test,audit-rank-form-drift,refit-hill-curves}.yml`, deletion of `.github/workflows/{check-sharp-production-now,force-sharp-production-now,trigger-sharp-now-via-merge,trigger-sharp-no-environment}.yml`, `scripts/sharp_smoke_record.py` (new), `tests/deploy/{test_no_workflow_exits_green_on_missing_secrets,test_production_checks_do_not_skip_green,test_bootstrap_reports_the_units_real_outcome,test_calibration_issue_tracking_is_self_healing}.py` (new), `tests/deploy/test_sharp_smoke_commit_order.py` (extended), `tests/ops/test_sharp_smoke_record.py` (new), `docs/ops/CI_FAILURE_MATRIX_2026-08-20.md` (new), `docs/CI_RELIABILITY_LANE.md`, `docs/WORK_CLAIMS.md` | main-movement churn from `verify-sharp-production.yml`; unconditional-success path in `trigger-sharp-no-environment.yml`; job-level skip-as-green in `health-check.yml` / `smoke-test.yml`; missing `calibration` label + mis-keyed tracker dedup | `claude/ci-reliability-hardening` | done — merged #948 (`9e83509607736f0419dddf3ac00da4110b569f6b`), deployed and verified. Its smoke-commit repair works (0 commits in 78 min against a 48/24 h baseline, with the workflow still running green); the tracker-duplication regression it introduced is tracked as #958 and repaired in #960, NOT by reopening this row |
| **POST-MERGE C-SERIES AUDIT AND STABILITY GATE — feature freeze** (owner directive 2026-08-17, later the same day as the campaign directive). Judges the actual repository and production behaviour at audit base `96ecc22a9` — not PR text, not prior green CI, and explicitly not this session's own earlier claims. Ends in a binary **PASS / FAIL** plus one recommendation, and stops there: **`C2-U2` does not begin even on `PASS`.** Repairs land as repair-only PRs in labelled batches — **A** CI/deploy truthfulness (#885, merged), **B** §3d hard-gate violations (#886), **C** surviving duplicate owners + the `tests/picks/` owner-guard that was missing (#887), **D** missing→zero on live decision paths (#888), **E** governance/docs reconciliation (this row). Deliberately NOT claiming, and forbidden to: `C2-U2`, `C2-U3`, `C2-U4`, `C2-U6`, `C1-U7`, any `C3+`, any new product feature, methodology, UI surface or opportunistic lane — and no scope expansion under the label of "repair". **Path overlap is broad by construction** (an audit reads everything), but every WRITE is inside a named batch and listed here. | `.github/workflows/{deploy,release-candidate}.yml`, `tests/deploy/`, `tests/identity/test_dual_read_zero_divergence.py`, `tests/api/test_pick_completeness.py`, `tests/picks/test_single_owner.py` (new), `tests/identity/test_pick_grammar_frontend_parity.py`, `src/api/data_contract.py` (tier-centre literal → owner call only), `src/trade/faab_history.py`, `frontend/lib/league-analysis.js`, `tests/trade/test_faab_history_budget.py` (new), `frontend/__tests__/league-analysis.test.js`, `config/coercion_baseline.json`, `docs/ops/STABILIZATION_2026-08-16.md`, `docs/EXECUTION_PLAN.md`, `docs/C_SERIES_EXECUTION_MAP.md`, `docs/C_SERIES_SCOPE_MANIFEST.md`, `docs/WORK_CLAIMS.md` | audit batches A–E | `claude/audit-batch-{a,b,c,d,e}-*` | open — **SCOPE NARROWED 2026-08-20.** The feature freeze this row records is **superseded for isolated implementation by the POST-V1 mass-build lanes** (Claude 9-13) per `docs/EXECUTION_PLAN.md` §0, *POST-V1 C-SERIES MASS-BUILD CAMPAIGN*. The freeze's CONDITIONS still bind those lanes (production proof may not be substituted; an unreachable check is `BLOCKED-EXTERNAL`, never a pass; scope may not be expanded under the label of repair) and the freeze still governs the shipping tree, which only Claude 5 touches. A mass-build lane reading this row is **not** frozen |
| **C1-U6-D1 — a source that did not publish a year contributes MISSING.** THE DEPLOY BLOCKER: Deploy Production had failed 12 consecutive times since 2026-08-17T00:35Z at the hard-gate 2029 pick test, so none of the six merged C-Series units was in production. Root cause is in `Dynasty Scraper.py`, not the contract — `_build_site_pick_map` had FOUR paths that forced a value for a year the source never published: three sharing an uncapped, undirected `_nearest_year` (so asking for 2031 emitted 2031 from 2028 rows), plus a `(year, None)` un-yeared bucket the #877 diagnosis missed and which survives any repair that only deletes `_nearest_year`. Extracted the builder and its five helpers to `src/picks/site_pick_map.py` (they were closures with no test, no fixture and no import seam anywhere in the repo) and PROVED the extraction byte-identical over 36 source boards / 8,640 keys BEFORE changing behaviour; RED-authored 8 failing tests there, then repaired. Measured: 18/18 tier-round cells now strictly below their 2028 equivalent by derivation (never a clamp), no 2029 row stamps `direct_market_blend`, per-source values stepped with the model value. Found and fixed a SECOND instance on published years — `idpTradeCalc`'s 2027/2028 slot rows were 2026's slot rows verbatim (96 keys, median 11%, max 37.9%). Also: `pick_anchors_raw` no longer overwritten with the rebuilt model board, and `_tier_for_slot`/`_slot_range_for_tier` (12-team duplicates of the owner's tables) routed through the owner. Board movement measured and explained, NOT claimed inert: offense 1/551 at 0.021%, IDP 277/397 p50 0.113% with two rows ~29.5% (the documented backbone-filter coupling), picks 50/162 p50 6.2%. Deliberately NOT claiming: a full browser scrape (egress blocked in the sandbox — the real proof is the scheduled refresh), hardening `direct_market_blend` (measured inert AND unable to catch this defect — recorded rather than added), the pick-label grammar consolidation with `src/identity/picks.py` (C1-ID-02's deferred migration), C1-U7, or any C2 unit. | `Dynasty Scraper.py`, `src/picks/` (new), `tests/picks/` (new), `tests/pick_board_fixture.py` (new, self-retiring), `tests/api/test_pick_completeness_red.py` (input only — no assertion changed), `docs/picks/C1_U6_D1_FABRICATED_FUTURE_YEAR_ANCHORS.md`, `docs/EXECUTION_PLAN.md`, `docs/WORK_CLAIMS.md` | C1-U6-D1 | `claude/c-series-c1-u6-d1-repair` | done — merged `a4007aec4` (#883); Deploy Production run 32062696830 SUCCESS at 20:22Z, first since 00:35Z. Verification #884 (`5a5f1507f`) measured the committed board **0 of 18** violating and retired the fixture shim on its own declared condition |
| **C2-U1 / `C2-LINE-01` — one canonical lineup / slot assignment.** Re-measured the census at HEAD rather than trusting the manifest's 6/2: **3** competing greedy assignment engines, **all three serving production** (`starter-slots.js` on /terminal + /rosters, `team_impact.py::project_starters` on `/api/trade/simulate`, `bdvm/roster.py` on `/api/bdvm/roster`), plus **6** private slot-eligibility tables and **5** duplicate slot-demand derivations. Verified the named owner before trusting it — against Sleeper's OWN awarded best-ball lineups it is 10/10 where the two production greedies are 0/10 and 5/10 — and repaired two defects in it: a missing-objective coercion (`float(ros_value or 0.0)`, baselined) and a `WRRB_FLEX` alias that let tight ends into a WR/RB flex. Frontend is now a materializer over a server-stamped `sleeper.teams[].optimalLineup`, fail-closed, NOT a JS port. `Dynasty Scraper.py` now keeps Sleeper's `fantasy_positions` (already fetched, previously discarded) so eligibility can be absolute. Consumes `src/ros/lineup.py`, `src/identity/*` unchanged; does NOT touch `_build_site_pick_map`, Team Strength, replacement level or roster simulation. | `src/ros/lineup.py`, `src/trade/team_impact.py`, `src/trade/suggestions.py`, `src/trade/faab_engine.py`, `src/bdvm/roster.py`, `src/bdvm/league_config.py`, `src/intel/roster_shape.py`, `src/scoring/replacement_level.py`, `src/league_intel/config.py`, `src/league_comparison/metrics.py`, `src/api/data_contract.py`, `src/api/sleeper_overlay.py`, `Dynasty Scraper.py`, `frontend/lib/starter-slots.js`, `frontend/lib/portfolio-insights.js`, `frontend/lib/league-analysis.js`, `tests/lineup/*`, `config/coercion_baseline.json`, `docs/lineup/C2_U1_CANONICAL_LINEUP.md` | `C2-LINE-01` | `claude/c-series-c2-u1` | done |
| **C1-U9 / `C1-SRC-01`+`-02` — multi-format dynasty source archive.** `C1-SRC-02` was recorded COMPLETE; verification found no `game_type` field, no fail-closed path and no test — the dynasty-only property held only because each of the 21 sources was hand-verified in a COMMENT. Implemented for the first time: closed `GAME_TYPES` vocabulary, per-source `game_type` + `game_type_evidence` (source-specific, drawn from each entry's documented endpoint), and `_validate_source_game_types_invariant` raising AT IMPORT on absent/unknown/non-dynasty/unevidenced; plus the spec §16-item-8 fixture (a trusted provider's REDRAFT endpoint is refused). `C1-SRC-01`: new `src/source_archive/` append-only store keyed `(provider, endpoint, format_key, run_id, captured_date)` so one cycle's variants are PAIRED evidence. Archive eligibility and production eligibility are separate sets enforced structurally — `data_contract` does not import the archive, and KTC's four TEP states are ONE provider family proven not to change the independent-family set. Board measured inert: 0/1111. Deliberately NOT claiming: the KTC ladder scraper capture, any format-normalization model or learned curve, universal-league adaptation, production activation of any variant, the capability registry's four availability states, or `_build_site_pick_map` (the 2029 defect is C1-U6's). | src/api/data_contract.py (game-type vocabulary + per-source annotation + import gate + registry export), src/source_archive/ (new), tests/sources/, docs/sources/, docs/C_SERIES_SCOPE_MANIFEST.md, docs/EXECUTION_PLAN.md, docs/WORK_CLAIMS.md | C1-SRC-01, C1-SRC-02 | claude/c-series-c1-u9 | merged to `main` 2026-08-17 — pending production proof |
| **C1-U8 / `C1-ACQ-01`+`-02`+`-03` — acquisition history, cost basis and pick lineage.** New PRIVATE owner `src/acquisition/`: three tables (`acquisition_events` INSERT-only with conflicts surfaced-not-applied; `holdings` and `pick_lineage` pure replay derivations rebuilt wholesale). Closes the two capture gaps at the source — `_build_waivers_block` recorded nothing (capture now sits ahead of BOTH the window cutoff and the `seen` dedupe, via one shared `_flush_transaction_ledger`), and `record_transactions` was called with `league_key`/`season` unset. Draft selections captured with `amount is None` != `0` preserved. Cost basis via `value_known_before` with the clock passed as of the EVENT; `IMPORT_UNKNOWN` fail-closed. Consumes `LeaguePickIdentity` + `market_resolution` (C1-U3), `src/history/asof.value_known_before`, `data_contract.rookie_draft_year_on` (the as-of clock, factored out of `current_rookie_draft_year` by this unit so there is no second rollover rule), and `sleeper_overlay`'s `_league_rid_lookup` / `_league_draft_slot_lookup` at the collector. Deliberately does NOT consume `build_pick_ownership`: that fold answers *current ownership from /traded_picks*, which cannot express a chain — lineage comes from the transaction feed. Duplicates and retires NO existing pick-ownership fold. Deliberately NOT claiming: any UI or HTTP route, acquisition grades / profit-loss / trade-tree screens, FAAB valuation methodology, retiring the six duplicate folds, the intel-ledger re-key (DEFERRED with its measurement), `_normalize_live_pick`'s `amount` collapse, C1-U7/U9, C2+. Retention's "no decision path reads this" invariant is preserved: one named collector script crosses the boundary and reads payloads, never a value. | src/acquisition/ (new), src/api/sleeper_overlay.py (waiver ledger flush + shared flush helper + league_key/season threading), scripts/build_acquisition_ledger.py, scripts/acquisition_status.py, deploy/backup/riskit-state-backup.sh, tests/acquisition/, docs/acquisition/, docs/EXECUTION_PLAN.md, docs/C_SERIES_SCOPE_MANIFEST.md, docs/WORK_CLAIMS.md | C1-ACQ-01, C1-ACQ-02, C1-ACQ-03, C1-RET-06 waiver gap | claude/c-series-c1-u8 | merged to `main` 2026-08-17 — pending production proof |
| **C1-U5 / `C1-CONF-01` — confidence naming migration.** INFRA, explicitly NOT a methodology change: `CONFIDENCE_LEVELS` stays at four and the five-axis bottleneck is preserved verbatim per calibration policy §7. Adds the orthogonal `confidenceBasis` (closed set) so the four states `"none"` was covering are distinguishable; makes the bad state unrepresentable via a `validate_api_data_contract` ERROR on any priced row without a valid basis; stamps the basis at `_anchor_current_year_picks_to_rookies` (scoped to rows nothing assessed, so the 48 picks the dispersion rule already judged keep their verdict); moves `_compute_pick_confidence` to its owner and removes the circular import; dual-writes `identityResolutionConfidence`/`...Method` and `marketBreadthAgreementIndex` (+ the two halves the scraper discarded) behind a declared, three-way-pinned `meta.deprecations` block. Board measured before and after: **0 values moved, 24 buckets none->low**, all 2026 round-5/6 slot picks. Deliberately NOT claiming: whether a rookie-tethered value should still report pick-market confidence (recorded as a follow-up), alias REMOVAL (breaking, bumps CONTRACT_VERSION), any C1-U7/U8/U9 or C2+ work. | src/api/confidence.py, src/api/data_contract.py (confidence writers + dual-write + deprecations + validator), Dynasty Scraper.py (`_market_confidence` identifier rename + two extra return values; `conf`/`cv` byte-identical), frontend/lib/dynasty-data.js (both materializers + override synthesis), tests/api/test_confidence_naming*.py, tests/api/test_confidence_rename_aliases.py, docs/confidence/ | C1-CONF-01 | claude/c-series-c1-u5 | merged to `main` 2026-08-17 — pending production proof |
| **C0-R — governance reconciliation for the continuous C-Series campaign** (owner directive 2026-08-17). Records the campaign authorization in the one authorization record; repairs measured governance drift (execution-map §18 listing a closed unit as unauthorized, §12 totalling 153 against the CI-verified 163, manifest rows for closed C1-U1/C1-U2 still carrying pre-work statuses, `PLANNING_DOCUMENT_STATUS` §6 calling three closed PRs open); adds the directive→manifest reconciliation with the owner **methodology authority** findings (meaningful-core caps superseded by #839; FLEX rule resolved from T-NEW-19; KTC VA consolidation target is the validated `processV` port, not the directive's V12 constants); repairs two provenance defects (#839 mis-cited to intake decisions 47–49; superseded-rule census 5→6). **Documentation and standing-invariant tests only — no source behaviour changes.** Deliberately NOT claiming: any C1/C2/C3+ implementation unit, valuation or trade methodology, `CANONICAL_V2` activation, or anything under `X-01`…`X-07`. | docs/EXECUTION_PLAN.md, docs/C_SERIES_EXECUTION_MAP.md, docs/C_SERIES_SCOPE_MANIFEST.md, docs/C_SERIES_ZERO_LOSS_TRACEABILITY.md, docs/C_SERIES_DIRECTIVE_RECONCILIATION_2026-08-17.md (new), docs/PLANNING_DOCUMENT_STATUS.md, docs/OWNER_FEATURE_INVENTORY.md, docs/MASTER_PRODUCT_PLAN.md, docs/OWNER_REQUESTED_TODO.md, docs/WORK_CLAIMS.md, tests/docs/ | governance drift (execution-map §18/§12, manifest closed-unit statuses, planning-status §6); #839 provenance mis-citation; superseded-rule census | claude/c-series-c0-r | merged to `main` 2026-08-17 — pending production proof |
| Stabilization + C1-U6 closure (bounded repair pass, NOT a feature unit). CI lane separation (deterministic code correctness vs external source health), the contract gate that had never run turned on, a source-health lane (fetch freshness reused from the canonical watchdog + NEW content-staleness measurement), release-candidate discipline against the merge/data-refresh race, correction of the false run-31965928453 CI history in every record, and disposition of all eleven C1-U6 follow-ups (ten fixed, one proven an intentional contract and pinned). One repair moves canonical values — derived far-future pick rows withdrawn from the cross-market evidence pool — measured p50 0.085% / p90 0.152% over 297 rows with the mechanism recorded. Deliberately NOT claiming: any next C-Series unit, valuation methodology, the pick-value model, a second pricer, projections, UI. | src/api/data_contract.py (validator partition, backbone evidence filter, per-build derivation threading, provenance basis stamps), src/roster_intel/engine.py, src/api/trade_simulator.py, Dynasty Scraper.py (pick-model year derivation + vendor-key honesty), scripts/{validate_api_contract,check_source_health,check_release_candidate,check_product_plan_governance}.py, .github/workflows/{pr-validation,deploy,release-candidate}.yml, config/source_staleness.json, frontend/lib/{league-analysis,portfolio-insights}.js, frontend/app/trades/page.jsx, tests/, docs/ops/STABILIZATION_2026-08-16.md, docs/picks/, docs/EXECUTION_PLAN.md, docs/C_SERIES_EXECUTION_MAP.md, docs/C_SERIES_SCOPE_MANIFEST.md, docs/WORK_CLAIMS.md, CLAUDE.md | C1-U6 follow-ups 1-11; CI source-health contamination; release/data-refresh race | claude/c1-u6-stabilization-closure-gmrlj7 | done |
| C1A unit 6 — pick completeness through 2029 (`C1-PICK-01`/`C1-PICK-02` / map unit C1-U6). Owner-authorized 2026-08-16 at the C1-U4 checkpoint (C1-U5 deliberately deferred). Canonical future-pick value completeness in the `data_contract` pick pipeline: finite provenance-stamped canonical values for every valid pick through 2029 (year-step derivation replacing the uncalibrated clone-x0.53, round-step completion for future rounds 5-6, generic-grade board rows, cap-immune pick value stamping), the canonical MarketPickRef->value resolver, the generic<->exact-slot transition test, and the authorized trade-facing lookup repairs (draft-capital future-year pricing, simulator roster-pick identity join). Calibration per MATH_MODEL_CALIBRATION_POLICY §3.1: challenger-tested year-step family, PRIOR-classified, evidence pinned in docs/picks/. Deliberately NOT claiming: C1-U5 confidence renames, C1-U7 owned-pick distributions, C1-U8 lineage/re-key, trade-engine methodology, projections, UI redesign, scraper pick-model rebuild (rollover literal defect recorded as follow-up). NOTE path overlap: `src/api/data_contract.py` also named by the open `claude/dynasty-audit-consolidation-e75vdy` claim (curve routing / coordinate consumption — disjoint regions from the pick pipeline; EXECUTION_PLAN §4 serial-writer rule honored: this is the only active data_contract pipeline writer). | src/api/data_contract.py (pick pipeline), src/api/pick_value_resolution.py (new), src/identity/picks.py (generic-grade board-row name + parser only), src/api/draft_capital_fallback.py (future-year value resolution), src/api/trade_simulator.py (roster-pick identity join only), src/canonical/calibration.py (dormant pick pricer retirement), config/weights/pick_year_discount.json, scripts/calibrate_pick_year_step.py (new), tests/api/, tests/identity/, tests/trade/, docs/picks/, docs/EXECUTION_PLAN.md, docs/C_SERIES_EXECUTION_MAP.md, docs/C_SERIES_SCOPE_MANIFEST.md, docs/WORK_CLAIMS.md, PRODUCT_PLAN.md, CLAUDE.md | C1-PICK-01, C1-PICK-02, V-12/C-11 (the uncalibrated clone-discount prior) | claude/c-series-fable-implementation-u2ghlv | done |
| C1A unit 4 — one immutable as-of value/provenance ledger (`C1-HIST-01`/`-02`/`-03` / map unit C1-U4). Owner-authorized 2026-08-16 at the C1-U3 checkpoint. New canonical temporal owner under `src/history/` (as-of lookup, fidelity labels, missing semantics, rankChange derivation, player+pick namespaced keys via C1-U2/C1-U3 identity), backfill from `exports/archive/` (2026-07-14 boundary permanent, pre-boundary = missing), rankChange determinism repair at `data_contract._stamp_rank_changes`. Deliberately NOT claiming: valuation methodology, trade grading/replay (C3-U9), pick valuation (C1-U6/U7), acquisition lineage (C1-U8), confidence renames (C1-U5), projection/backtest engines (C5), any UI. | src/history/, src/api/data_contract.py (_stamp_rank_changes only), src/api/rank_history.py (adapter seam only), src/snapshots/board_store.py (read-owner integration only), server.py (recording call sites only), scripts/ (backfill + census helpers), tests/history/, docs/history/, docs/EXECUTION_PLAN.md, docs/C_SERIES_EXECUTION_MAP.md, docs/C_SERIES_SCOPE_MANIFEST.md, docs/WORK_CLAIMS.md, CLAUDE.md | C1-HIST-01, C1-HIST-02, C1-HIST-03 | claude/c1-u4-temporal-ledger-57beor | done |
| C1A unit 3 — one pick identity, end to end (`C1-ID-02` / map unit C1-U3). Owner-authorized 2026-08-16 at the C1-U2 checkpoint. Canonical owner `src/identity/picks.py` (league-pick identity = league_key+season+round+origin; market-pick refs at slot/tier/generic grades; generic→exact transition as pure state change; every legacy label grammar parsed, both legacy label grammars formatted by the owner with byte-parity pinned). Consumers adapted: data_contract pick regex/parse helpers delegate; sleeper_overlay fold+labels delegate (+additive `assetId` on pickDetails, fail-closed on unregistered league); scraper pick block delegates (S3 in-run map retired); draft_capital_fallback name formatting delegates; intel crawler pick asset-id strings route through the owner (persisted generic grade UNCHANGED — re-key is C1-U8). Board proven byte-inert (0/1093 rows, 0 values, 0 ranks via golden_board+board_diff). Deliberately NOT claiming: pick valuation (C1-PICK-01/02/03 pricing halves), the intel-ledger re-key (C1-ACQ-01/03), frontend lookup migration (needs C1-U6 generic rows; grammar-parity test pins lockstep), public_league fold (bespoke multi-season semantics, origin retained), C1-U4+, any trade-engine behavior. | src/identity/picks.py, src/api/data_contract.py (pick grammar delegation only), src/api/sleeper_overlay.py (pick fold/labels only), src/api/league_registry.py (league_key_for_sleeper_id only), src/api/draft_capital_fallback.py (_normalize_pick_name only), src/intel/crawler.py (asset-id strings only), Dynasty Scraper.py (pick block only), tests/identity/test_pick_identity*.py, tests/fixtures/pick_identity_live_subset.json, docs/identity/C1_ID_02_PICK_IDENTITY.md, docs/EXECUTION_PLAN.md, docs/C_SERIES_EXECUTION_MAP.md, docs/C_SERIES_SCOPE_MANIFEST.md, docs/WORK_CLAIMS.md, CLAUDE.md | C1-ID-02 | claude/c1-u3-pick-identity-c05p7s | done |
| Stage-A audit consolidation, now through B3: Phase A truth re-baseline + verified quick wins, the W30-F008 percentile-coordinate repair (fit/holdout/serving share one coordinate; **no production constant changed**), the /trade multi-team crash hotfix, and model-governance hardening (scope-specific promotion gate, snapshot pinning, complete input fingerprinting, appliedAt/measuredAt lifecycle, registry self-destruction fix), and the B2 curve-routing root cause (W02-F001: the Hill master is now chosen from the rank's coordinate pool, `src/canonical/rank_coordinates.py`, never from the source's registry declaration; **no Hill constant changed, nothing promoted or applied**), and the B3 market-corridor methodology repair (W02-F003: the arbitrary IDP 0.15 hard band cap removed so the corridor's board-derived per-bucket P90 decides again; three residuals tracked as W02-F015/F016/F017 rather than closed into the finding's narrative). Deliberately NOT claiming server.py, frontend/app/api/* bridge routes, or src/api/sleeper_overlay.py — those belong to the live `claude/bridge-timeout-root-cause` session (last push 2026-08-10 22:33Z); verified untouched by path intersection. **Overlap note:** `src/api/data_contract.py` is also touched by `origin/claude/league-intel-projections` and `origin/claude/league-intel-sim`; our change there is the `rank_to_percentile` consumption (~22 lines, B1) plus the B2 curve-routing repair (~120 lines in the Phase 1/1b/1c/1d translation passes and `_curve_for_rank`). Verified non-overlapping by diff: neither branch touches curve routing, the translation flags or the Hill constants — both are +43/-10 in unrelated regions. No shared defect. `frontend/__tests__/draft-logic.test.js` overlaps `origin/claude/rookie-draft-optimizer-386qyu` — same file, different defect (stale phantom imports). | docs/master-site-audit/ (claims-frozen file, re-baseline, W08+W30 evidence), docs/ARCHITECTURE_HANDOFF.md, docs/WORK_CLAIMS.md, docs/BRANCH_DISPOSITION_2026-08-11.md, docs/OWNER_FEATURE_INVENTORY.md, .github/workflows/refit-hill-curves.yml, src/canonical/player_valuation.py, src/canonical/rank_coordinates.py, src/api/data_contract.py (coordinate consumption + curve routing), src/model_registry/*, scripts/fit_hill_curve_percentile.py, tests/{canonical,model_registry,audit,api}/, frontend/__tests__/, frontend/components/waivers/ManualAddDrop.jsx, frontend/app/trade/page.jsx, frontend/lib/trade-logic.js | W31-F001, W11-F006, W08-F004, W30-F008 (coordinate half; challenger NOT promoted), W30-F023 (open, measured), W30-F024 (fixed), W02-F001 (fixed), W02-F001b (fixed), W02-F002 (resolved as a consequence; direction partially remains), W02-F003 (fixed in B3), W02-F015 / W02-F016 / W02-F017 (new, open) | claude/dynasty-audit-consolidation-e75vdy | done — **the branch no longer exists on the remote** (verified 2026-08-17 against the full 78-branch listing) and its work is on `main`: `src/canonical/rank_coordinates.py` exists and `_apply_market_corridor_clamp` is gone, retired via #794/#795/#796. This row was left `open`, so the claim protocol reported a live editor for a branch nobody could read. Its still-open FINDINGS are not tracked here — `scripts/audit_status.py` probes them (43 critical, 18 open, "no drift: every recorded status matches its probe") and is the record for status |
| C1A unit 2 — one player-identity owner (`C1-ID-01` / map unit C1-U2). Owner-authorized 2026-08-16. Canonical resolution engine (`src/identity/resolution.py`) + scraper name primitives moved verbatim into the owner (`src/identity/name_primitives.py`); dual-read adapters at the two consolidation sites (scraper `_resolve_sleeper_identity`, contract `_enrich_from_source_csvs`) serving the LEGACY answers — the board is provably inert (b5 metrics identical; contract-join dual-read 24,046/0). Deliberately NOT claiming pick identity (C1-ID-02/C1-U3), the confidence renames (C1-CONF-01/C1-U5), matcher.py's scaffold lane, pool/ros/playerctx/sharp resolver lanes (deferred per the census dispositions), or any C2+ work. | src/identity/, Dynasty Scraper.py (primitives block + _resolve_sleeper_identity + dual-read artifact), src/api/data_contract.py (_enrich_from_source_csvs instrumentation + identityDualRead stamp), src/utils/name_clean.py (registry pointer), tests/identity/, tests/fixtures/identity_directory_subset.json, tests/utils/test_team_codes_parity.py, scripts/identity_parity.py, docs/identity/, docs/EXECUTION_PLAN.md, docs/C_SERIES_EXECUTION_MAP.md, docs/WORK_CLAIMS.md, CLAUDE.md | C1-ID-01, W06-F006 (closed at the canonical API; legacy rung retained as named V1 compat pending consumer migration) | claude/c-series-c1a-u2-kergfj | done |
| **[CLOSED 2026-08-17 by C0-R — this row was stale.** The unit closed at the owner checkpoint 2026-08-16 (merge `47d7d243`, deploy run 31869441040); the file's own header banner already said pre-B-completion rows are closed, but the row still read `open`, which is exactly the "someone is in there" signal the protocol exists to give.] C1A unit 1 — irreversible-evidence retention (`C1-RET-01`…`C1-RET-08`). Authorized by `docs/EXECUTION_PLAN.md`; scope is `docs/C_SERIES_SCOPE_MANIFEST.md`. New append-only substrate under `src/retention/` (INTERNAL evidence store for per-observation scoring-card history + Sleeper trending; a SEPARATE **private** ledger for own-league transactions), a health probe covering all eight streams with a schedulable CI check, backup coverage for the seven previously-unbacked stores, and honest freshness labelling on `/api/scaffold/identity`. **No decision path reads any of it**; no valuation, ranking, trade or FAAB behaviour changed. Deliberately NOT claiming `C1-HIST-01`, Trade History, identity consolidation, or any C1B/C2+ work — all unauthorized. Touch points in existing files are narrow and additive: `league_registry.write_scoring_snapshot` (record before the overwrite), `sleeper_overlay._build_trades_block` (emit `transactionId`, record before the window cutoff), `server.py` (post-scrape trending record + the identity freshness block). | src/retention/, tests/retention/, scripts/retention_health.py, deploy/diagnostics/retention_health_probe.sh, .github/workflows/retention-health.yml, deploy/backup/riskit-state-backup.sh, docs/retention/RETENTION_REGISTER.md, src/api/league_registry.py (write_scoring_snapshot only), src/api/sleeper_overlay.py (_build_trades_block only), server.py (post-scrape warm + /api/scaffold/identity only) | C1-RET-01 … C1-RET-08 | claude/master-reconciliation-post-audit-ewgl00 | done |
| Work-claim protocol | docs/WORK_CLAIMS.md, scripts/check_work_claims.py, ASSISTANT_COORDINATION.md | — | claude/work-claim-protocol | done |
| PR-backlog audit: repair of the /edge market-gap display half | frontend/app/edge/page.jsx, frontend/app/edge/edge-columns.jsx, frontend/lib/edge-helpers.js | C09 (S-3) | claude/fix-plan-uex7ug | done |
| **V1-45 — Trade Calculator: render the canonical `rosterCapacity` forced-drop consequence (L4 gap repair).** Verification-first pass over the deployed Trade Calculator user path (UI -> `/api/trade/simulate` -> `src/trade/roster_capacity.py::assess_roster_capacity` -> response -> render) found VA/multi-team/missing-value/manual-override handling all clean and well-evidenced (L1/L2), but the backend's genuinely-computed `rosterCapacity` block (forced releases, cost, upper-bound flag) was rendered by zero frontend files anywhere in the app — confirmed by repo-wide grep before and after. Narrow, additive fix: `SimulationPanel` (`frontend/app/trade/trade-sections.jsx`) now renders the three `requiresDrops` states the backend already distinguishes (`true` — names forced releases and their cost, flags upper-bound/ladder-exhausted; `false` — silent, matching "clean fit imposes no forced-drop cost"; `null` — an explicit uncertainty note, distinguishing unknown cap from unknown taxi occupancy) — pure display of backend-stamped fields, no trade math added to the file. RED-before/GREEN-after verified by re-running the new test file against the pre-fix component (5 of 7 cases failed pre-fix, exactly the cases depending on the new render). Evidence: `frontend/__tests__/components/trade-simulation-panel-roster-capacity.test.jsx` 7 passed; `frontend/__tests__/trade-logic.test.js` 179 passed (unaffected). Does not close L3 (deployed-SHA checklist) or full L4 (no production/auth access from this environment) — that remains for whoever holds deploy access. Deliberately NOT touched: the duplicate VA port in `src/public_league/trade_grading.py` (different surface — public-league retro-grading, already owned by open PR #962, not this row). FEATURE_GREEN / READY_FOR_INTEGRATION, not self-merged. | `frontend/app/trade/trade-sections.jsx`, `frontend/__tests__/components/trade-simulation-panel-roster-capacity.test.jsx`, `docs/WORK_CLAIMS.md` | V1-45 | `claude/v1-45-roster-capacity-render` | open — FEATURE_GREEN, not yet merged; Claude 5 owns integration and promotion |
| **V1-46 — Manual override UX: visually silent, one global reset (`C3-CALC-02` / #781).** `docs/C_SERIES_SCOPE_MANIFEST.md` final state: "No per-player badge; top-level Reset Values; removal clears the override; canonical truth unchanged." Measured ABSENT before this fix: the asset-value override `<input>` carried a conditional `overridden` CSS class (amber text + border, `frontend/app/globals.css`) — a per-player badge — whenever a value was overridden, and no top-level control existed to clear every override at once; only a scattered set of internal `setValueOverrides({})` calls fired as a side effect of unrelated actions (team change, side add, etc.), never a user-facing "reset everything" button. Narrow, additive fix, both halves of the row's final state: (1) dropped the conditional `overridden` class from the input in `AssetRow` (`trade-sections.jsx`) and deleted the now-dead `.asset-value-override-input.overridden` CSS rule, so an overridden value renders identically to a computed one; (2) added one "Reset Values" button to the top-level trade-controls toolbar (`page.jsx`, beside "Clear Trade"), calling `setValueOverrides({})` directly, disabled when no override exists. Left the existing PER-PLAYER ghost "Reset <name>" button next to each overridden input untouched — the row forbids a per-player visual *badge*, not a working per-player undo control, and removing it would be a functional regression the row does not ask for. Does not touch `customValue`-first resolution in `trade-logic.js` (proven elsewhere) or any canonical field. RED-before/GREEN-after: new test file run against the pre-fix component first (all 3 cases failed — badge present, no Reset Values button reachable), then against the fix (all 3 pass). Evidence: `frontend/__tests__/components/trade-value-override-ux.test.jsx` 3 passed; full `frontend` vitest suite 2292 passed, 0 failed (no collateral damage). Real-collision check run first (`scripts/check_work_claims.py`): `origin/claude/v1-45-roster-capacity-render` (this session's own concurrent PR #1019) also touches `trade-sections.jsx` in a different function (`SimulationPanel`, not `AssetRow`) — no logical overlap; `origin/claude/v1-trade-roster-rehearsal(-2)` also flagged but carry no open PR and are already recorded elsewhere in this file as "unverified whether stale." FEATURE_GREEN / READY_FOR_INTEGRATION, not self-merged. | `frontend/app/trade/trade-sections.jsx`, `frontend/app/trade/page.jsx`, `frontend/app/globals.css`, `frontend/__tests__/components/trade-value-override-ux.test.jsx`, `docs/WORK_CLAIMS.md` | V1-46, C3-CALC-02 | `claude/v1-46-manual-override-ux` | open — FEATURE_GREEN, not yet merged; Claude 5 owns integration and promotion |

---

## Why this file exists

On 2026-08-05, five sessions independently solved the **same eight defects**
inside one working window, and in no case did anyone notice until a branch had
already merged:

| Defect | Solved in | And independently in |
|---|---|---|
| Market gap computed in rank space | #740 | #722 **and** #742 — three times |
| `tepMultiplier` default forces the override path | #740 | #722 |
| `_sanitize_next_path` accepts a backslash | #740 | #722 |
| FAAB budget-regime mixing | #740 | #707 |
| ROS absence coerced to 0.0 odds | #740 | #736 |
| Compact view drops `appliedWeight` | #740 | main |
| SSR streaming "duplicate" (#716) | #741 | #747 |

**Eight duplicated repairs across five sessions**, and the market-gap defect was
solved three separate times.

**#742 was pushed AFTER #740 merged.** Its branch does not contain `cef17703`, and
it touches the same three files the merged fix landed in. So this is not a tidy
retrospective about a bad afternoon — the collisions were still happening while
this document was being written.

Every individual collision was resolved sensibly — the better-documented
version was kept each time. The aggregate was not sensible: roughly a third
of a session's output was discarded, and **which version survived came down
to which branch happened to be mergeable first, not which was better.** In
at least one case nobody compared the two implementations at all.

`ASSISTANT_COORDINATION.md` already required branching, and already required
`git pull --ff-only origin main` before starting. Neither prevented any of
this, and it is worth being precise about why: **both rules are about where
your work goes, and none of them tells you what someone else is already
doing.** A branch you never listed is a branch you will duplicate.

So the missing step is not another rule about branching. It is a place to
look, and something worth looking at when you get there.

## The format, and why it is a markdown table

A markdown table is editable in the same commit as the work, readable in a
PR diff, and mergeable by hand. A JSON registry would conflict on every
concurrent claim — which is exactly the failure this is meant to reduce.

Columns:

- **Claim** — one line a human recognises. "FAAB budget regimes", not "fix bug".
- **Paths** — comma-separated files you expect to change. Approximate is fine;
  the point is overlap detection, not a manifest.
- **Defect ids** — `C12`, `W22-F001`, audit finding ids, `—` if none.
- **Branch** — where the work lives, so a collision has somewhere to go.
- **Status** — `open` while in flight, `done` once merged or abandoned.

## What to do when you hit an overlap

Not "stop". Overlap is often legitimate — two people fixing different bugs
in one large file collide on paths and not on meaning.

1. **Read the other branch.** `git diff origin/main..origin/<branch> -- <path>`.
2. **If it is the same defect:** do not write it again. Either take theirs and
   move on, or say concretely why yours should replace it — on the PR, before
   the work, not after.
3. **If it is a different defect in the same file:** carry on, and say so in
   your claim row so the next person does not have to re-derive it.

## Honest limits

- Nothing enforces this. `check_work_claims.py` exits 0 unless you pass
  `--strict`. A gate that cries wolf gets ignored, which is roughly how the
  previous coordination rules failed.
- It only helps if run *before* the work. Run afterwards it is a conflict
  report.
- A session that never fetches sees stale branches. The branch scan is only
  as current as your last `git fetch`.
