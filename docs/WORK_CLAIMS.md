# Work Claims

> **2026-08-14 — post-B master reconciliation.** Any claim row below referencing pre-B-completion state is
> closed. The reconciliation touched planning and documentation only, on
> `claude/master-reconciliation-post-audit-ewgl00`. No C implementation is authorized.


**What you are about to work on, recorded before you start.**

One row per piece of work in flight. Add yours in your first commit; set it
`done` in your last. Check it before you begin:

```bash
python scripts/check_work_claims.py --files <paths you will change> --defect <ids>
```

That script also scans remote branches, because claims are voluntary and
branches are evidence.

---

## Open claims

| Claim | Paths | Defect ids | Branch | Status |
|---|---|---|---|---|
| **CI reliability lane — CI's own side effects, not the seven red ticks.** Audited the seven named failure families against the live Actions API. Four are correctly red or already resolved and this lane changes none of them: Bootstrap Sharp Records (0/19, runaway prod collector — owner), Retention health (0/8 ever, `C1-RET-07` dead since 2026-04-20 — owner), Rank-Form Drift (real drift from #787; constants are an ADR-008 human act — owner), E2E (another lane landed `1af1d2b3`; PR #762 open). Scheduled Data Refresh is 23 consecutive greens and PR Validation's 2-in-60 failures were both real. What IS repaired is a class the 2026-08-18 triage did not look at: **a verification workflow writing to `main` 42 times a day** (42 of 66 bot commits in 24h, all recording an unchanged status — class-C drift under every open PR), four superseded workflows still holding production SSH credentials (one with a confirmed `exit 0`-on-missing-secrets path), three gates that render green while asserting nothing (job-level `if` skip = neutral = success; a gate with no credential warning green forever; trackers that cannot find their own issue because the `calibration` label does not exist — #777/#895 are the same request seven days apart). Deliberately NOT claiming: `deploy.yml`, `release-candidate.yml`, `config/coercion_baseline.json`, `CLAUDE.md`, `frontend/**`, `e2e.yml`, `requirements*.txt`, `package.json`, the rank-form constants, the Bootstrap trigger cadence, a shared concurrency group (measured to add a failure mode and remove none), SHA-bound production verification (needs `deploy.yml`), or the `mergeable_state: dirty` → zero-checks-scheduled hole (a repository-settings act no PR can land). **Path overlap, declared:** `tests/deploy/` is named by the audit-batch row above. Four files there are NEW; `test_sharp_smoke_commit_order.py` is EXTENDED because it is the canonical guard for the workflow this lane changes, and putting the extension elsewhere would create a second owner for one invariant. No false-green mechanism introduced; zero retries added. | `.github/workflows/{verify-sharp-production,health-check,smoke-test,audit-rank-form-drift,refit-hill-curves}.yml`, deletion of `.github/workflows/{check-sharp-production-now,force-sharp-production-now,trigger-sharp-now-via-merge,trigger-sharp-no-environment}.yml`, `scripts/sharp_smoke_record.py` (new), `tests/deploy/{test_no_workflow_exits_green_on_missing_secrets,test_production_checks_do_not_skip_green,test_bootstrap_reports_the_units_real_outcome,test_calibration_issue_tracking_is_self_healing}.py` (new), `tests/deploy/test_sharp_smoke_commit_order.py` (extended), `tests/ops/test_sharp_smoke_record.py` (new), `docs/ops/CI_FAILURE_MATRIX_2026-08-20.md` (new), `docs/CI_RELIABILITY_LANE.md`, `docs/WORK_CLAIMS.md` | main-movement churn from `verify-sharp-production.yml`; unconditional-success path in `trigger-sharp-no-environment.yml`; job-level skip-as-green in `health-check.yml` / `smoke-test.yml`; missing `calibration` label + mis-keyed tracker dedup | `claude/ci-reliability-hardening` | open — `READY_FOR_INTEGRATION`, Claude 5 owns the merge |
| **POST-MERGE C-SERIES AUDIT AND STABILITY GATE — feature freeze** (owner directive 2026-08-17, later the same day as the campaign directive). Judges the actual repository and production behaviour at audit base `96ecc22a9` — not PR text, not prior green CI, and explicitly not this session's own earlier claims. Ends in a binary **PASS / FAIL** plus one recommendation, and stops there: **`C2-U2` does not begin even on `PASS`.** Repairs land as repair-only PRs in labelled batches — **A** CI/deploy truthfulness (#885, merged), **B** §3d hard-gate violations (#886), **C** surviving duplicate owners + the `tests/picks/` owner-guard that was missing (#887), **D** missing→zero on live decision paths (#888), **E** governance/docs reconciliation (this row). Deliberately NOT claiming, and forbidden to: `C2-U2`, `C2-U3`, `C2-U4`, `C2-U6`, `C1-U7`, any `C3+`, any new product feature, methodology, UI surface or opportunistic lane — and no scope expansion under the label of "repair". **Path overlap is broad by construction** (an audit reads everything), but every WRITE is inside a named batch and listed here. | `.github/workflows/{deploy,release-candidate}.yml`, `tests/deploy/`, `tests/identity/test_dual_read_zero_divergence.py`, `tests/api/test_pick_completeness.py`, `tests/picks/test_single_owner.py` (new), `tests/identity/test_pick_grammar_frontend_parity.py`, `src/api/data_contract.py` (tier-centre literal → owner call only), `src/trade/faab_history.py`, `frontend/lib/league-analysis.js`, `tests/trade/test_faab_history_budget.py` (new), `frontend/__tests__/league-analysis.test.js`, `config/coercion_baseline.json`, `docs/ops/STABILIZATION_2026-08-16.md`, `docs/EXECUTION_PLAN.md`, `docs/C_SERIES_EXECUTION_MAP.md`, `docs/C_SERIES_SCOPE_MANIFEST.md`, `docs/WORK_CLAIMS.md` | audit batches A–E | `claude/audit-batch-{a,b,c,d,e}-*` | open |
| **C1-U6-D1 — a source that did not publish a year contributes MISSING.** THE DEPLOY BLOCKER: Deploy Production had failed 12 consecutive times since 2026-08-17T00:35Z at the hard-gate 2029 pick test, so none of the six merged C-Series units was in production. Root cause is in `Dynasty Scraper.py`, not the contract — `_build_site_pick_map` had FOUR paths that forced a value for a year the source never published: three sharing an uncapped, undirected `_nearest_year` (so asking for 2031 emitted 2031 from 2028 rows), plus a `(year, None)` un-yeared bucket the #877 diagnosis missed and which survives any repair that only deletes `_nearest_year`. Extracted the builder and its five helpers to `src/picks/site_pick_map.py` (they were closures with no test, no fixture and no import seam anywhere in the repo) and PROVED the extraction byte-identical over 36 source boards / 8,640 keys BEFORE changing behaviour; RED-authored 8 failing tests there, then repaired. Measured: 18/18 tier-round cells now strictly below their 2028 equivalent by derivation (never a clamp), no 2029 row stamps `direct_market_blend`, per-source values stepped with the model value. Found and fixed a SECOND instance on published years — `idpTradeCalc`'s 2027/2028 slot rows were 2026's slot rows verbatim (96 keys, median 11%, max 37.9%). Also: `pick_anchors_raw` no longer overwritten with the rebuilt model board, and `_tier_for_slot`/`_slot_range_for_tier` (12-team duplicates of the owner's tables) routed through the owner. Board movement measured and explained, NOT claimed inert: offense 1/551 at 0.021%, IDP 277/397 p50 0.113% with two rows ~29.5% (the documented backbone-filter coupling), picks 50/162 p50 6.2%. Deliberately NOT claiming: a full browser scrape (egress blocked in the sandbox — the real proof is the scheduled refresh), hardening `direct_market_blend` (measured inert AND unable to catch this defect — recorded rather than added), the pick-label grammar consolidation with `src/identity/picks.py` (C1-ID-02's deferred migration), C1-U7, or any C2 unit. | `Dynasty Scraper.py`, `src/picks/` (new), `tests/picks/` (new), `tests/pick_board_fixture.py` (new, self-retiring), `tests/api/test_pick_completeness_red.py` (input only — no assertion changed), `docs/picks/C1_U6_D1_FABRICATED_FUTURE_YEAR_ANCHORS.md`, `docs/EXECUTION_PLAN.md`, `docs/WORK_CLAIMS.md` | C1-U6-D1 | `claude/c-series-c1-u6-d1-repair` | done — merged `a4007aec4` (#883); Deploy Production run 32062696830 SUCCESS at 20:22Z, first since 00:35Z. Verification #884 (`5a5f1507f`) measured the committed board **0 of 18** violating and retired the fixture shim on its own declared condition |
| **C2-U1 / `C2-LINE-01` — one canonical lineup / slot assignment.** Re-measured the census at HEAD rather than trusting the manifest's 6/2: **3** competing greedy assignment engines, **all three serving production** (`starter-slots.js` on /terminal + /rosters, `team_impact.py::project_starters` on `/api/trade/simulate`, `bdvm/roster.py` on `/api/bdvm/roster`), plus **6** private slot-eligibility tables and **5** duplicate slot-demand derivations. Verified the named owner before trusting it — against Sleeper's OWN awarded best-ball lineups it is 10/10 where the two production greedies are 0/10 and 5/10 — and repaired two defects in it: a missing-objective coercion (`float(ros_value or 0.0)`, baselined) and a `WRRB_FLEX` alias that let tight ends into a WR/RB flex. Frontend is now a materializer over a server-stamped `sleeper.teams[].optimalLineup`, fail-closed, NOT a JS port. `Dynasty Scraper.py` now keeps Sleeper's `fantasy_positions` (already fetched, previously discarded) so eligibility can be absolute. Consumes `src/ros/lineup.py`, `src/identity/*` unchanged; does NOT touch `_build_site_pick_map`, Team Strength, replacement level or roster simulation. | `src/ros/lineup.py`, `src/trade/team_impact.py`, `src/trade/suggestions.py`, `src/trade/faab_engine.py`, `src/bdvm/roster.py`, `src/bdvm/league_config.py`, `src/intel/roster_shape.py`, `src/scoring/replacement_level.py`, `src/league_intel/config.py`, `src/league_comparison/metrics.py`, `src/api/data_contract.py`, `src/api/sleeper_overlay.py`, `Dynasty Scraper.py`, `frontend/lib/starter-slots.js`, `frontend/lib/portfolio-insights.js`, `frontend/lib/league-analysis.js`, `tests/lineup/*`, `config/coercion_baseline.json`, `docs/lineup/C2_U1_CANONICAL_LINEUP.md` | `C2-LINE-01` | `claude/c-series-c2-u1` | done |
| **C1-U9 / `C1-SRC-01`+`-02` — multi-format dynasty source archive.** `C1-SRC-02` was recorded COMPLETE; verification found no `game_type` field, no fail-closed path and no test — the dynasty-only property held only because each of the 21 sources was hand-verified in a COMMENT. Implemented for the first time: closed `GAME_TYPES` vocabulary, per-source `game_type` + `game_type_evidence` (source-specific, drawn from each entry's documented endpoint), and `_validate_source_game_types_invariant` raising AT IMPORT on absent/unknown/non-dynasty/unevidenced; plus the spec §16-item-8 fixture (a trusted provider's REDRAFT endpoint is refused). `C1-SRC-01`: new `src/source_archive/` append-only store keyed `(provider, endpoint, format_key, run_id, captured_date)` so one cycle's variants are PAIRED evidence. Archive eligibility and production eligibility are separate sets enforced structurally — `data_contract` does not import the archive, and KTC's four TEP states are ONE provider family proven not to change the independent-family set. Board measured inert: 0/1111. Deliberately NOT claiming: the KTC ladder scraper capture, any format-normalization model or learned curve, universal-league adaptation, production activation of any variant, the capability registry's four availability states, or `_build_site_pick_map` (the 2029 defect is C1-U6's). | src/api/data_contract.py (game-type vocabulary + per-source annotation + import gate + registry export), src/source_archive/ (new), tests/sources/, docs/sources/, docs/C_SERIES_SCOPE_MANIFEST.md, docs/EXECUTION_PLAN.md, docs/WORK_CLAIMS.md | C1-SRC-01, C1-SRC-02 | claude/c-series-c1-u9 | merged to `main` 2026-08-17 — pending production proof |
| **C1-U8 / `C1-ACQ-01`+`-02`+`-03` — acquisition history, cost basis and pick lineage.** New PRIVATE owner `src/acquisition/`: three tables (`acquisition_events` INSERT-only with conflicts surfaced-not-applied; `holdings` and `pick_lineage` pure replay derivations rebuilt wholesale). Closes the two capture gaps at the source — `_build_waivers_block` recorded nothing (capture now sits ahead of BOTH the window cutoff and the `seen` dedupe, via one shared `_flush_transaction_ledger`), and `record_transactions` was called with `league_key`/`season` unset. Draft selections captured with `amount is None` != `0` preserved. Cost basis via `value_known_before` with the clock passed as of the EVENT; `IMPORT_UNKNOWN` fail-closed. Consumes `LeaguePickIdentity` + `market_resolution` (C1-U3), `src/history/asof.value_known_before`, `data_contract.rookie_draft_year_on` (the as-of clock, factored out of `current_rookie_draft_year` by this unit so there is no second rollover rule), and `sleeper_overlay`'s `_league_rid_lookup` / `_league_draft_slot_lookup` at the collector. Deliberately does NOT consume `build_pick_ownership`: that fold answers *current ownership from /traded_picks*, which cannot express a chain — lineage comes from the transaction feed. Duplicates and retires NO existing pick-ownership fold. Deliberately NOT claiming: any UI or HTTP route, acquisition grades / profit-loss / trade-tree screens, FAAB valuation methodology, retiring the six duplicate folds, the intel-ledger re-key (DEFERRED with its measurement), `_normalize_live_pick`'s `amount` collapse, C1-U7/U9, C2+. Retention's "no decision path reads this" invariant is preserved: one named collector script crosses the boundary and reads payloads, never a value. | src/acquisition/ (new), src/api/sleeper_overlay.py (waiver ledger flush + shared flush helper + league_key/season threading), scripts/build_acquisition_ledger.py, scripts/acquisition_status.py, deploy/backup/riskit-state-backup.sh, tests/acquisition/, docs/acquisition/, docs/EXECUTION_PLAN.md, docs/C_SERIES_SCOPE_MANIFEST.md, docs/WORK_CLAIMS.md | C1-ACQ-01, C1-ACQ-02, C1-ACQ-03, C1-RET-06 waiver gap | claude/c-series-c1-u8 | merged to `main` 2026-08-17 — pending production proof |
| **C1-U5 / `C1-CONF-01` — confidence naming migration.** INFRA, explicitly NOT a methodology change: `CONFIDENCE_LEVELS` stays at four and the five-axis bottleneck is preserved verbatim per calibration policy §7. Adds the orthogonal `confidenceBasis` (closed set) so the four states `"none"` was covering are distinguishable; makes the bad state unrepresentable via a `validate_api_data_contract` ERROR on any priced row without a valid basis; stamps the basis at `_anchor_current_year_picks_to_rookies` (scoped to rows nothing assessed, so the 48 picks the dispersion rule already judged keep their verdict); moves `_compute_pick_confidence` to its owner and removes the circular import; dual-writes `identityResolutionConfidence`/`...Method` and `marketBreadthAgreementIndex` (+ the two halves the scraper discarded) behind a declared, three-way-pinned `meta.deprecations` block. Board measured before and after: **0 values moved, 24 buckets none->low**, all 2026 round-5/6 slot picks. Deliberately NOT claiming: whether a rookie-tethered value should still report pick-market confidence (recorded as a follow-up), alias REMOVAL (breaking, bumps CONTRACT_VERSION), any C1-U7/U8/U9 or C2+ work. | src/api/confidence.py, src/api/data_contract.py (confidence writers + dual-write + deprecations + validator), Dynasty Scraper.py (`_market_confidence` identifier rename + two extra return values; `conf`/`cv` byte-identical), frontend/lib/dynasty-data.js (both materializers + override synthesis), tests/api/test_confidence_naming*.py, tests/api/test_confidence_rename_aliases.py, docs/confidence/ | C1-CONF-01 | claude/c-series-c1-u5 | merged to `main` 2026-08-17 — pending production proof |
| **C0-R — governance reconciliation for the continuous C-Series campaign** (owner directive 2026-08-17). Records the campaign authorization in the one authorization record; repairs measured governance drift (execution-map §18 listing a closed unit as unauthorized, §12 totalling 153 against the CI-verified 163, manifest rows for closed C1-U1/C1-U2 still carrying pre-work statuses, `PLANNING_DOCUMENT_STATUS` §6 calling three closed PRs open); adds the directive→manifest reconciliation with the owner **methodology authority** findings (meaningful-core caps superseded by #839; FLEX rule resolved from T-NEW-19; KTC VA consolidation target is the validated `processV` port, not the directive's V12 constants); repairs two provenance defects (#839 mis-cited to intake decisions 47–49; superseded-rule census 5→6). **Documentation and standing-invariant tests only — no source behaviour changes.** Deliberately NOT claiming: any C1/C2/C3+ implementation unit, valuation or trade methodology, `CANONICAL_V2` activation, or anything under `X-01`…`X-07`. | docs/EXECUTION_PLAN.md, docs/C_SERIES_EXECUTION_MAP.md, docs/C_SERIES_SCOPE_MANIFEST.md, docs/C_SERIES_ZERO_LOSS_TRACEABILITY.md, docs/C_SERIES_DIRECTIVE_RECONCILIATION_2026-08-17.md (new), docs/PLANNING_DOCUMENT_STATUS.md, docs/OWNER_FEATURE_INVENTORY.md, docs/MASTER_PRODUCT_PLAN.md, docs/OWNER_REQUESTED_TODO.md, docs/WORK_CLAIMS.md, tests/docs/ | governance drift (execution-map §18/§12, manifest closed-unit statuses, planning-status §6); #839 provenance mis-citation; superseded-rule census | claude/c-series-c0-r | merged to `main` 2026-08-17 — pending production proof |
| Stabilization + C1-U6 closure (bounded repair pass, NOT a feature unit). CI lane separation (deterministic code correctness vs external source health), the contract gate that had never run turned on, a source-health lane (fetch freshness reused from the canonical watchdog + NEW content-staleness measurement), release-candidate discipline against the merge/data-refresh race, correction of the false run-31965928453 CI history in every record, and disposition of all eleven C1-U6 follow-ups (ten fixed, one proven an intentional contract and pinned). One repair moves canonical values — derived far-future pick rows withdrawn from the cross-market evidence pool — measured p50 0.085% / p90 0.152% over 297 rows with the mechanism recorded. Deliberately NOT claiming: any next C-Series unit, valuation methodology, the pick-value model, a second pricer, projections, UI. | src/api/data_contract.py (validator partition, backbone evidence filter, per-build derivation threading, provenance basis stamps), src/roster_intel/engine.py, src/api/trade_simulator.py, Dynasty Scraper.py (pick-model year derivation + vendor-key honesty), scripts/{validate_api_contract,check_source_health,check_release_candidate,check_product_plan_governance}.py, .github/workflows/{pr-validation,deploy,release-candidate}.yml, config/source_staleness.json, frontend/lib/{league-analysis,portfolio-insights}.js, frontend/app/trades/page.jsx, tests/, docs/ops/STABILIZATION_2026-08-16.md, docs/picks/, docs/EXECUTION_PLAN.md, docs/C_SERIES_EXECUTION_MAP.md, docs/C_SERIES_SCOPE_MANIFEST.md, docs/WORK_CLAIMS.md, CLAUDE.md | C1-U6 follow-ups 1-11; CI source-health contamination; release/data-refresh race | claude/c1-u6-stabilization-closure-gmrlj7 | done |
| C1A unit 6 — pick completeness through 2029 (`C1-PICK-01`/`C1-PICK-02` / map unit C1-U6). Owner-authorized 2026-08-16 at the C1-U4 checkpoint (C1-U5 deliberately deferred). Canonical future-pick value completeness in the `data_contract` pick pipeline: finite provenance-stamped canonical values for every valid pick through 2029 (year-step derivation replacing the uncalibrated clone-x0.53, round-step completion for future rounds 5-6, generic-grade board rows, cap-immune pick value stamping), the canonical MarketPickRef->value resolver, the generic<->exact-slot transition test, and the authorized trade-facing lookup repairs (draft-capital future-year pricing, simulator roster-pick identity join). Calibration per MATH_MODEL_CALIBRATION_POLICY §3.1: challenger-tested year-step family, PRIOR-classified, evidence pinned in docs/picks/. Deliberately NOT claiming: C1-U5 confidence renames, C1-U7 owned-pick distributions, C1-U8 lineage/re-key, trade-engine methodology, projections, UI redesign, scraper pick-model rebuild (rollover literal defect recorded as follow-up). NOTE path overlap: `src/api/data_contract.py` also named by the open `claude/dynasty-audit-consolidation-e75vdy` claim (curve routing / coordinate consumption — disjoint regions from the pick pipeline; EXECUTION_PLAN §4 serial-writer rule honored: this is the only active data_contract pipeline writer). | src/api/data_contract.py (pick pipeline), src/api/pick_value_resolution.py (new), src/identity/picks.py (generic-grade board-row name + parser only), src/api/draft_capital_fallback.py (future-year value resolution), src/api/trade_simulator.py (roster-pick identity join only), src/canonical/calibration.py (dormant pick pricer retirement), config/weights/pick_year_discount.json, scripts/calibrate_pick_year_step.py (new), tests/api/, tests/identity/, tests/trade/, docs/picks/, docs/EXECUTION_PLAN.md, docs/C_SERIES_EXECUTION_MAP.md, docs/C_SERIES_SCOPE_MANIFEST.md, docs/WORK_CLAIMS.md, PRODUCT_PLAN.md, CLAUDE.md | C1-PICK-01, C1-PICK-02, V-12/C-11 (the uncalibrated clone-discount prior) | claude/c-series-fable-implementation-u2ghlv | done |
| C1A unit 4 — one immutable as-of value/provenance ledger (`C1-HIST-01`/`-02`/`-03` / map unit C1-U4). Owner-authorized 2026-08-16 at the C1-U3 checkpoint. New canonical temporal owner under `src/history/` (as-of lookup, fidelity labels, missing semantics, rankChange derivation, player+pick namespaced keys via C1-U2/C1-U3 identity), backfill from `exports/archive/` (2026-07-14 boundary permanent, pre-boundary = missing), rankChange determinism repair at `data_contract._stamp_rank_changes`. Deliberately NOT claiming: valuation methodology, trade grading/replay (C3-U9), pick valuation (C1-U6/U7), acquisition lineage (C1-U8), confidence renames (C1-U5), projection/backtest engines (C5), any UI. | src/history/, src/api/data_contract.py (_stamp_rank_changes only), src/api/rank_history.py (adapter seam only), src/snapshots/board_store.py (read-owner integration only), server.py (recording call sites only), scripts/ (backfill + census helpers), tests/history/, docs/history/, docs/EXECUTION_PLAN.md, docs/C_SERIES_EXECUTION_MAP.md, docs/C_SERIES_SCOPE_MANIFEST.md, docs/WORK_CLAIMS.md, CLAUDE.md | C1-HIST-01, C1-HIST-02, C1-HIST-03 | claude/c1-u4-temporal-ledger-57beor | done |
| C1A unit 3 — one pick identity, end to end (`C1-ID-02` / map unit C1-U3). Owner-authorized 2026-08-16 at the C1-U2 checkpoint. Canonical owner `src/identity/picks.py` (league-pick identity = league_key+season+round+origin; market-pick refs at slot/tier/generic grades; generic→exact transition as pure state change; every legacy label grammar parsed, both legacy label grammars formatted by the owner with byte-parity pinned). Consumers adapted: data_contract pick regex/parse helpers delegate; sleeper_overlay fold+labels delegate (+additive `assetId` on pickDetails, fail-closed on unregistered league); scraper pick block delegates (S3 in-run map retired); draft_capital_fallback name formatting delegates; intel crawler pick asset-id strings route through the owner (persisted generic grade UNCHANGED — re-key is C1-U8). Board proven byte-inert (0/1093 rows, 0 values, 0 ranks via golden_board+board_diff). Deliberately NOT claiming: pick valuation (C1-PICK-01/02/03 pricing halves), the intel-ledger re-key (C1-ACQ-01/03), frontend lookup migration (needs C1-U6 generic rows; grammar-parity test pins lockstep), public_league fold (bespoke multi-season semantics, origin retained), C1-U4+, any trade-engine behavior. | src/identity/picks.py, src/api/data_contract.py (pick grammar delegation only), src/api/sleeper_overlay.py (pick fold/labels only), src/api/league_registry.py (league_key_for_sleeper_id only), src/api/draft_capital_fallback.py (_normalize_pick_name only), src/intel/crawler.py (asset-id strings only), Dynasty Scraper.py (pick block only), tests/identity/test_pick_identity*.py, tests/fixtures/pick_identity_live_subset.json, docs/identity/C1_ID_02_PICK_IDENTITY.md, docs/EXECUTION_PLAN.md, docs/C_SERIES_EXECUTION_MAP.md, docs/C_SERIES_SCOPE_MANIFEST.md, docs/WORK_CLAIMS.md, CLAUDE.md | C1-ID-02 | claude/c1-u3-pick-identity-c05p7s | done |
| Stage-A audit consolidation, now through B3: Phase A truth re-baseline + verified quick wins, the W30-F008 percentile-coordinate repair (fit/holdout/serving share one coordinate; **no production constant changed**), the /trade multi-team crash hotfix, and model-governance hardening (scope-specific promotion gate, snapshot pinning, complete input fingerprinting, appliedAt/measuredAt lifecycle, registry self-destruction fix), and the B2 curve-routing root cause (W02-F001: the Hill master is now chosen from the rank's coordinate pool, `src/canonical/rank_coordinates.py`, never from the source's registry declaration; **no Hill constant changed, nothing promoted or applied**), and the B3 market-corridor methodology repair (W02-F003: the arbitrary IDP 0.15 hard band cap removed so the corridor's board-derived per-bucket P90 decides again; three residuals tracked as W02-F015/F016/F017 rather than closed into the finding's narrative). Deliberately NOT claiming server.py, frontend/app/api/* bridge routes, or src/api/sleeper_overlay.py — those belong to the live `claude/bridge-timeout-root-cause` session (last push 2026-08-10 22:33Z); verified untouched by path intersection. **Overlap note:** `src/api/data_contract.py` is also touched by `origin/claude/league-intel-projections` and `origin/claude/league-intel-sim`; our change there is the `rank_to_percentile` consumption (~22 lines, B1) plus the B2 curve-routing repair (~120 lines in the Phase 1/1b/1c/1d translation passes and `_curve_for_rank`). Verified non-overlapping by diff: neither branch touches curve routing, the translation flags or the Hill constants — both are +43/-10 in unrelated regions. No shared defect. `frontend/__tests__/draft-logic.test.js` overlaps `origin/claude/rookie-draft-optimizer-386qyu` — same file, different defect (stale phantom imports). | docs/master-site-audit/ (claims-frozen file, re-baseline, W08+W30 evidence), docs/ARCHITECTURE_HANDOFF.md, docs/WORK_CLAIMS.md, docs/BRANCH_DISPOSITION_2026-08-11.md, docs/OWNER_FEATURE_INVENTORY.md, .github/workflows/refit-hill-curves.yml, src/canonical/player_valuation.py, src/canonical/rank_coordinates.py, src/api/data_contract.py (coordinate consumption + curve routing), src/model_registry/*, scripts/fit_hill_curve_percentile.py, tests/{canonical,model_registry,audit,api}/, frontend/__tests__/, frontend/components/waivers/ManualAddDrop.jsx, frontend/app/trade/page.jsx, frontend/lib/trade-logic.js | W31-F001, W11-F006, W08-F004, W30-F008 (coordinate half; challenger NOT promoted), W30-F023 (open, measured), W30-F024 (fixed), W02-F001 (fixed), W02-F001b (fixed), W02-F002 (resolved as a consequence; direction partially remains), W02-F003 (fixed in B3), W02-F015 / W02-F016 / W02-F017 (new, open) | claude/dynasty-audit-consolidation-e75vdy | done — **the branch no longer exists on the remote** (verified 2026-08-17 against the full 78-branch listing) and its work is on `main`: `src/canonical/rank_coordinates.py` exists and `_apply_market_corridor_clamp` is gone, retired via #794/#795/#796. This row was left `open`, so the claim protocol reported a live editor for a branch nobody could read. Its still-open FINDINGS are not tracked here — `scripts/audit_status.py` probes them (43 critical, 18 open, "no drift: every recorded status matches its probe") and is the record for status |
| C1A unit 2 — one player-identity owner (`C1-ID-01` / map unit C1-U2). Owner-authorized 2026-08-16. Canonical resolution engine (`src/identity/resolution.py`) + scraper name primitives moved verbatim into the owner (`src/identity/name_primitives.py`); dual-read adapters at the two consolidation sites (scraper `_resolve_sleeper_identity`, contract `_enrich_from_source_csvs`) serving the LEGACY answers — the board is provably inert (b5 metrics identical; contract-join dual-read 24,046/0). Deliberately NOT claiming pick identity (C1-ID-02/C1-U3), the confidence renames (C1-CONF-01/C1-U5), matcher.py's scaffold lane, pool/ros/playerctx/sharp resolver lanes (deferred per the census dispositions), or any C2+ work. | src/identity/, Dynasty Scraper.py (primitives block + _resolve_sleeper_identity + dual-read artifact), src/api/data_contract.py (_enrich_from_source_csvs instrumentation + identityDualRead stamp), src/utils/name_clean.py (registry pointer), tests/identity/, tests/fixtures/identity_directory_subset.json, tests/utils/test_team_codes_parity.py, scripts/identity_parity.py, docs/identity/, docs/EXECUTION_PLAN.md, docs/C_SERIES_EXECUTION_MAP.md, docs/WORK_CLAIMS.md, CLAUDE.md | C1-ID-01, W06-F006 (closed at the canonical API; legacy rung retained as named V1 compat pending consumer migration) | claude/c-series-c1a-u2-kergfj | done |
| **[CLOSED 2026-08-17 by C0-R — this row was stale.** The unit closed at the owner checkpoint 2026-08-16 (merge `47d7d243`, deploy run 31869441040); the file's own header banner already said pre-B-completion rows are closed, but the row still read `open`, which is exactly the "someone is in there" signal the protocol exists to give.] C1A unit 1 — irreversible-evidence retention (`C1-RET-01`…`C1-RET-08`). Authorized by `docs/EXECUTION_PLAN.md`; scope is `docs/C_SERIES_SCOPE_MANIFEST.md`. New append-only substrate under `src/retention/` (INTERNAL evidence store for per-observation scoring-card history + Sleeper trending; a SEPARATE **private** ledger for own-league transactions), a health probe covering all eight streams with a schedulable CI check, backup coverage for the seven previously-unbacked stores, and honest freshness labelling on `/api/scaffold/identity`. **No decision path reads any of it**; no valuation, ranking, trade or FAAB behaviour changed. Deliberately NOT claiming `C1-HIST-01`, Trade History, identity consolidation, or any C1B/C2+ work — all unauthorized. Touch points in existing files are narrow and additive: `league_registry.write_scoring_snapshot` (record before the overwrite), `sleeper_overlay._build_trades_block` (emit `transactionId`, record before the window cutoff), `server.py` (post-scrape trending record + the identity freshness block). | src/retention/, tests/retention/, scripts/retention_health.py, deploy/diagnostics/retention_health_probe.sh, .github/workflows/retention-health.yml, deploy/backup/riskit-state-backup.sh, docs/retention/RETENTION_REGISTER.md, src/api/league_registry.py (write_scoring_snapshot only), src/api/sleeper_overlay.py (_build_trades_block only), server.py (post-scrape warm + /api/scaffold/identity only) | C1-RET-01 … C1-RET-08 | claude/master-reconciliation-post-audit-ewgl00 | done |
| Work-claim protocol | docs/WORK_CLAIMS.md, scripts/check_work_claims.py, ASSISTANT_COORDINATION.md | — | claude/work-claim-protocol | done |
| PR-backlog audit: repair of the /edge market-gap display half | frontend/app/edge/page.jsx, frontend/app/edge/edge-columns.jsx, frontend/lib/edge-helpers.js | C09 (S-3) | claude/fix-plan-uex7ug | done |

---

## Why this file exists

On 2026-08-05, five sessions independently solved the **same eight defects**
inside one working window, and in no case did anyone notice until a branch had
already merged:

| Defect | Solved in | And independently in |
|---|---|---|
| Market gap computed in rank space | #740 | #722 **and** #742 — three times |
| `tepMultiplier` default forces the override path | #740 | #722 |
| `_sanitize_next_path` accepts a backslash | #740 | #722 |
| FAAB budget-regime mixing | #740 | #707 |
| ROS absence coerced to 0.0 odds | #740 | #736 |
| Compact view drops `appliedWeight` | #740 | main |
| SSR streaming "duplicate" (#716) | #741 | #747 |

**Eight duplicated repairs across five sessions**, and the market-gap defect was
solved three separate times.

**#742 was pushed AFTER #740 merged.** Its branch does not contain `cef17703`, and
it touches the same three files the merged fix landed in. So this is not a tidy
retrospective about a bad afternoon — the collisions were still happening while
this document was being written.

Every individual collision was resolved sensibly — the better-documented
version was kept each time. The aggregate was not sensible: roughly a third
of a session's output was discarded, and **which version survived came down
to which branch happened to be mergeable first, not which was better.** In
at least one case nobody compared the two implementations at all.

`ASSISTANT_COORDINATION.md` already required branching, and already required
`git pull --ff-only origin main` before starting. Neither prevented any of
this, and it is worth being precise about why: **both rules are about where
your work goes, and none of them tells you what someone else is already
doing.** A branch you never listed is a branch you will duplicate.

So the missing step is not another rule about branching. It is a place to
look, and something worth looking at when you get there.

## The format, and why it is a markdown table

A markdown table is editable in the same commit as the work, readable in a
PR diff, and mergeable by hand. A JSON registry would conflict on every
concurrent claim — which is exactly the failure this is meant to reduce.

Columns:

- **Claim** — one line a human recognises. "FAAB budget regimes", not "fix bug".
- **Paths** — comma-separated files you expect to change. Approximate is fine;
  the point is overlap detection, not a manifest.
- **Defect ids** — `C12`, `W22-F001`, audit finding ids, `—` if none.
- **Branch** — where the work lives, so a collision has somewhere to go.
- **Status** — `open` while in flight, `done` once merged or abandoned.

## What to do when you hit an overlap

Not "stop". Overlap is often legitimate — two people fixing different bugs
in one large file collide on paths and not on meaning.

1. **Read the other branch.** `git diff origin/main..origin/<branch> -- <path>`.
2. **If it is the same defect:** do not write it again. Either take theirs and
   move on, or say concretely why yours should replace it — on the PR, before
   the work, not after.
3. **If it is a different defect in the same file:** carry on, and say so in
   your claim row so the next person does not have to re-derive it.

## Honest limits

- Nothing enforces this. `check_work_claims.py` exits 0 unless you pass
  `--strict`. A gate that cries wolf gets ignored, which is roughly how the
  previous coordination rules failed.
- It only helps if run *before* the work. Run afterwards it is a conflict
  report.
- A session that never fetches sees stale branches. The branch scan is only
  as current as your last `git fetch`.
