# Blueprint Gap Analysis (Repo-Grounded Truth)

Date: 2026-03-19  
Scope: Gap map between blueprint intent and verified implementation/runtime behavior.

## Source-of-truth baseline used for this gap pass
- Planning blueprint: `docs/BLUEPRINT_EXECUTION.md`
- Runtime authority truth: `server.py`, `docs/RUNTIME_ROUTE_AUTHORITY.md`, `/api/runtime/route-authority`
- Value authority truth: `Dynasty Scraper.py`, `src/api/data_contract.py`, `/api/data`
- Release safety/gating: `src/api/promotion_gate.py`, `server.py`, `deploy/verify-deploy.sh`
- Tests: `tests/api/*`, `tests/e2e/specs/smoke-api.spec.js`

## Gap Matrix

| Workstream | Blueprint target | Current status | Evidence | What works now | What does not work / is incomplete | Exact closure work required | Priority | Dependencies / risk notes |
|---|---|---|---|---|---|---|---|---|
| Runtime authority clarity | Explicit, testable route ownership with no ambiguous fallback ownership | mostly complete | `server.py:621`, `server.py:2406`, `tests/e2e/specs/smoke-api.spec.js:176` | Runtime map endpoint exists; response headers stamp authority; docs now call out static authority | `/calculator` route expectation still drifts vs runtime reality | Decide `/calculator` policy (alias or remove from blueprint references) and enforce in tests/docs | high | Route map + docs + smoke must all match |
| Landing -> League public flow | Public League journey works without auth and remains stable | complete (runtime), partial (deploy safety) | `server.py:2712`, `server.py:2731`, `tests/e2e/specs/smoke-api.spec.js:97` | Landing loads, League button works, all top-level League routes return 200 and no auth redirect | Full League shell depends on `Static/league/*`, currently untracked in this workspace | Resolve artifact policy: tracked in git, generated in build, or remove runtime dependency | critical blocker | Untracked local artifacts can mask deploy failures |
| League failure mode hardening | `/league` never raw-500s on missing shell; controlled public fallback | complete | `server.py:2731`, `server.py:2748`, `tests/api/test_league_route_resilience.py:48` | Missing shell now serves explicit inline fallback with route authority marker | Fallback is safe but still degraded UX if assets missing | Keep fallback; ensure deploy gate blocks shipping degraded shell unexpectedly | high | Already enforced in `deploy/verify-deploy.sh` strict mode |
| League tab implementation depth | Tabs exist with real data-backed modules (standings/franchises/awards/draft/trades/records/money/constitution/history/media) | partial | `Static/league/league.js:39`, `server.py:1739`, `docs/league-page/data-availability-matrix.md` | Route shells and nav exist; home/franchise summary/recent trades summary are live | Most tabs are scaffold/blocked by missing historical/manual datasets | Build historical backbone + manual content stores; keep blocked labels truthful until data exists | high | Depends on ingestion/backfill + commissioner content workflows |
| Public/private boundary | Public league surfaces consume a dedicated public-safe contract; private app remains gated | partial | `server.py:2270`, `server.py:2213`, `server.py:2809`, `docs/league-page/public-private-boundary.md:9` | `/api/league/public` exists and excludes private valuation internals; `/app` `/rankings` `/trade` are auth-gated | `/api/data` remains public and broad | Define and enforce policy for `/api/data` public exposure (gate, reduce, or explicitly accept and document) | high | Boundary ambiguity is a trust and security narrative risk |
| Auth boundary integrity | League public, Jason/private routes gated, no accidental auth weakening | mostly complete | `server.py:2759`, `server.py:2809`, `tests/e2e/specs/smoke-api.spec.js:234` | Auth redirects for private routes work; League remains public | Credential defaults hardcoded in runtime fallback env (`server.py:98-101`) are risky for prod hygiene | Remove default password fallback requirement in production mode and document secure env setup | high | Security hardening task; avoid breaking local dev |
| Shared value authority (rankings + calculator) | One backend-authoritative adjusted-value path across surfaces | mostly complete | `server.py:1379`, `src/api/data_contract.py:640`, `Static/js/runtime/40-runtime-features.js:1418` | Live path is scraper -> contract -> `/api/data`; static runtime consumes backend bundle for known assets | Frontend still carries fallback computation logic for manual/unknown assets (intentional but drift-prone) | Keep fallback for manual assets but add strict diagnostics and versioning policy for fallback constants | medium | Drift risk increases when fallback grows |
| IDPTradeCalc OFF + IDP coverage | IDPTradeCalc contributes for offense + IDP where available | complete (in ingestion), mostly complete (surface observability) | `Dynasty Scraper.py:8801`, `Dynasty Scraper.py:9057`, `Dynasty Scraper.py:12121`, `tests/e2e/specs/smoke-api.spec.js:469` | IDPTradeCalc excluded from IDP-only filters; coverage diagnostics by QB/RB/WR/TE/DL/LB/DB exist; smoke enforces presence | Coverage diagnostics live in raw/settings diagnostics and not first-class in `valueResolverDiagnostics` contract block | Promote essential source-column coverage diagnostics into contract-level diagnostics for easier downstream consumption | medium | Primarily an observability clarity gap |
| Deterministic identity merge + diagnostics | Deterministic merge with explicit unmatched/duplicate/conflict diagnostics and no silent drop | partial | `Dynasty Scraper.py:9059`, `Dynasty Scraper.py:9106`, `Dynasty Scraper.py:12080`, `data/dynasty_data_2026-03-19.json:970` | Deterministic strategy + counters + sample diagnostics exist; promotion gate enforces thresholds | Non-zero unresolved/conflicting identities remain (`unmatchedRows=21`, `conflictingSourceIdentities=159`) | Triage top conflict buckets and reduce thresholds over time; add dedicated mismatch trend dashboard | high | Quality risk is controlled but not solved |
| Identity mismatch before/after evidence | Explicit before vs after mismatch counts | partial | `data/dynasty_data_2026-03-19.json:970`, historical payloads `data/dynasty_data_2026-03-04.json`..`03-14` | After instrumentation: sourceRows 4682, matched 4661, unmatched 21, dupes 2, conflicting identities 159 | Before instrumentation: counts unavailable in older snapshots (no equivalent diagnostics fields) | Backfill trend baseline from archived exports or start durable daily metrics store for longitudinal before/after reporting | high | Current limitation is historical instrumentation coverage |
| Source coverage breadth (Yahoo, DynastyNerds, IDPTradeCalc) | All major sources represented and materially affect outputs | mostly complete | `data/dynasty_data_2026-03-19.json` site counts, `tests/e2e/specs/smoke-api.spec.js:264`, `:352`, `:469` | Current payload has Yahoo, DynastyNerds, IDPTradeCalc rows; tests guard disappearance signaling | Source volatility still possible run-to-run; a source can degrade and still pass if above thresholds | Continue tightening source thresholds by season phase and add stronger anomaly alerts for sudden collapses | high | External dependency instability remains |
| Formula authority consolidation | No hidden/duplicate/conflicting formula paths | partial | `docs/FORMULA_AUTHORITY_MAP.md`, `Static/js/runtime/00-core-shell.js`, `src/api/data_contract.py:816` | Backend formula path is explicit; compatibility fields documented | Duplicate fallback math remains in frontend; constants can drift vs backend | Keep one authority for known assets; isolate fallback and make drift visible in diagnostics and tests | medium | Avoid hard removal that breaks manual asset entry |
| Rankings correctness/layout stability | Rankings surface stable and aligned with backend values | mostly complete (static runtime) | `Static/js/runtime/10-rankings-and-picks.js`, `tests/e2e/specs/smoke-api.spec.js:11` | Core tab and data contract checks pass; no crash in smoke on desktop/mobile | Next rankings page is limited shell (not runtime authority); no full parity guarantee there | Keep static as authority until Next parity is intentionally cut over and tested | medium | Migration seam risk if ownership becomes ambiguous |
| Trade calculator flagship path | Reliable calculator with shared value source and realistic workflows | mostly complete (static runtime), partial (Next runtime) | `Static/js/runtime/20-data-and-calculator.js`, `frontend/app/trade/page.jsx` | Static calculator path is live and tested; parity diagnostics exist | Next `/trade` remains shell-level and not authoritative feature parity | Keep static authority; define explicit cutover criteria for Next calculator parity | high | Do not split logic authority during migration |
| Mobile parity | Phone + tablet parity for public league + core app smoke | partial | `tests/e2e/playwright.config.js:60`, `tests/e2e/specs/smoke-api.spec.js:147`, `tests/e2e/utils/app.js:3` | Desktop + phone + tablet League smoke passes; top-level public routes and auth boundary checks are green | Deeper mobile workflow parity (dense calculator/rankings interactions) is still not fully covered by smoke-level assertions | Keep cross-device smoke mandatory and add deeper mobile workflow coverage incrementally | medium | Passing smoke reduces route risk but does not prove full UX parity |
| Deployment/publish safety | Broken scrape/formula runs cannot poison live payload | complete | `server.py:1760`, `server.py:1935`, `src/api/promotion_gate.py`, `tests/api/test_promotion_gate.py` | Promotion gate blocks bad candidates; last-known-good fallback active; operator endpoints available | Gate tests rely on date-pinned fixture (`data/dynasty_data_2026-03-19.json`) | De-pin fixture dependency for CI portability (stable fixture copy under `tests/fixtures`) | high | CI stability and long-term maintainability |
| Route/deploy artifact safety | No runtime dependency on local-only artifacts | partial/blocker | `git status --short` (`?? Static/league/`), `deploy/verify-deploy.sh:13` | Runtime has readiness check and fallback behavior | Workspace currently has untracked League shell assets; deployment provenance unclear | Track `Static/league/*` or generate deterministically in deploy/build; do not rely on local untracked files | critical blocker | This is the main current release-safety risk |
| Frontend/runtime narrative coherence | Docs and code clearly separate live runtime from migration scaffold | mostly complete | `frontend/README.md:24`, `src/README.md:6`, `docs/league-page/repo-architecture-findings.md:83` | Core READMEs and league architecture docs now state static authority truth and Next non-authority for League | Remaining ambiguity is mostly roadmap-level (`/calculator` policy still unresolved) | Resolve `/calculator` policy and keep route ownership docs linked from operator docs | medium | Narrative drift risk is now narrower and easier to monitor |
| Mike Clay offseason integration | Projection guide ingestion + weighting + seasonal gating | planned but not started | No parser/module/runtime wiring found in repo | No runtime impact currently | Entire ingestion+contract+formula+gating stack absent | Build ingestion first, then formula integration with offseason gating and diagnostics | high | Should not be bolted in before current route/runtime cleanup |

## Contradictions / drift hotspots
- `docs/BLUEPRINT_EXECUTION.md` still carries `/calculator` parity intent not matched by live route handlers.

## Bottom-line closure targets
1. Remove deploy ambiguity: resolve `Static/league/*` tracking/generation policy.
2. Keep cross-device smoke mandatory (`desktop-1366`, phone, tablet) so route/auth regressions are caught early.
3. Resolve `/calculator` route policy and align docs/tests/runtime map.
4. Tighten public/private payload boundary policy for `/api/data`.
5. Keep reducing identity conflicts with measurable trend reporting.
