# Version 1 Completion Contract

**Status:** **RATIFIED** as to scope (owner, 2026-08-18) — the boundary in §1, the vocabulary in §2 and the classification in §3-§6 are settled and frozen. **Statuses stay live** and are reconciled against the open six-lane PRs after every integration. Three genuinely irreducible product questions remain in §7.2; they do not hold up the rest.
**Published:** 2026-08-18 by the Integration Authority lane (Claude 5).
**Companions:** `docs/EXECUTION_PLAN.md` §0 (authorization) · `docs/C_SERIES_SCOPE_MANIFEST.md`
(scope) · `docs/OWNER_FEATURE_INVENTORY.md` (owner product record) ·
`docs/audit/POST_MERGE_C_SERIES_AUDIT_2026-08-18.md` (measured defects).

> **This document owns exactly one question: what must be true for V1 to be complete.**
> It does not authorize work — `docs/EXECUTION_PLAN.md` §0 does. It does not define the product —
> the owner records do. It does not replace the Scope Manifest; it *classifies* it.

---

## 0. How to read this

**The denominator is the V1 REQUIRED list in §3, and nothing else.** Completion is
`VERIFIED items ÷ V1 REQUIRED items`. Items in §4, §5 and §6 are out of the denominator by
classification, not by omission — every one of them is named, with the record it came from.

**Two failure modes this document exists to prevent**, both of which have already happened in
this repository:

1. **Manufacturing 100% by shrinking the denominator.** Moving a hard item to POST-V1 the week
   it turns out to be hard. Every classification below carries a rationale traceable to an owner
   record or to the boundary the owner set on 2026-08-18; a reclassification is an owner
   decision, recorded here with a date, never a silent edit.
2. **Counting a thing as done because code for it exists.** See §2.

**Nothing silently disappears.** Where this document classifies an item in a single line, the
itemised requirement remains in its canonical record and is cited. This is a classification
layer over those records — deliberately not a second copy of them, because a second copy is a
second roadmap and it will drift.

---

## 1. The V1 boundary (owner decision, 2026-08-18)

V1 REQUIRED is the **bounded V1-required subset** assigned across the six lanes — **not** every
capability those lanes are authorized to keep building afterwards. The lane assignments
deliberately include post-V1 continuation work so that no lane goes idle after finishing its V1
responsibilities. **That continuation work is not in the denominator.**

**V1 REQUIRED covers:**

- stability and false-green repairs
- canonical identity, value and history
- exact lineup and FLEX-first assignment
- replacement level, meaningful roster core, Team Strength, Team Weakness, basic Young Core
- canonical trade package / Value Adjustment / capacity / forced drops / team context /
  Analyze Trade **V1**
- trustworthy FAAB and Sharp **core**
- exact scoring **including individual special teams**
- required current-season-model correctness
- auth / admin
- mobile-desktop parity
- high-use Premium UI
- rankings performance / virtualization
- truthful degraded states
- source-health correctness
- CI / E2E
- production verification

**Explicitly NOT V1 REQUIRED**, even though one of the six lanes eventually owns it: the full
projection ensemble · Game Day · podcast / YouTube ingestion · the full analyst-intelligence
ecosystem · Manager Scout · full Ask Brisket / AI Front Office · advanced trade-market systems ·
full Public League Experience v3 · Wrapped / reporting · advanced WAR / VORP · other explicitly
post-V1 continuation work.

**Method.** All 290 owner-approved capabilities and the 288 C-Series units/items that carry them
were reconciled **individually** against that boundary. Where an item did not resolve cleanly it
is flagged **individually in §7** rather than absorbed by broadening the denominator.

---

## 2. Status vocabulary, and what may not be counted

| status | meaning |
|---|---|
| `NOT STARTED` | no implementation |
| `IN PROGRESS` | implementation begun, not merged |
| `IMPLEMENTED_UNVERIFIED` | merged, and its required verification has **not** been satisfied |
| `VERIFIED` | required verification satisfied at the stated level |
| `BLOCKED` | cannot proceed — dependency, external permission, or owner decision |
| `DEFERRED` | out of V1 by classification |
| `RETIRED` | superseded or withdrawn; must stay absent |

**Only `VERIFIED` counts toward V1 completion.**

**None of the following is verification**, and each has previously been mistaken for it here:

- the code exists;
- a PR exists;
- the PR merged;
- unit tests pass;
- CI was green;
- a document says it is done.

**The false-green test every item must answer:**

> Is the intended production consumer actually using the canonical implementation, with truthful
> data semantics?

Precedents this rule was written from, all real: the exact lineup solver existed while the
normal overlay discarded its output; DraftSharks sat 12.6 days stale while health read green; a
status denominator reported 2/2 for a board carried by 21 voters; the scrape-promotion gate
watched the retired `ktc` while the anchor that moves hundreds of offense values was
`ktcSfTep`; features shipped behind flags with no production consumer; `rosValue` looked like a
projection and was mostly rank-derived.

### Verification levels

| level | what satisfies it |
|---|---|
| **L1 — deterministic** | RED→GREEN test at exact head, plus green CI on the merge tree |
| **L2 — board/contract inert or measured** | L1 plus a measured statement of the effect on the live board or contract (0 rows, or the exact rows) |
| **L3 — production** | L1 plus the named checklist executed against the **deployed SHA**, evidence recorded |
| **L4 — production consumer** | L3 plus proof the intended user-facing surface consumes the canonical implementation with truthful semantics |

---

## 3. V1 REQUIRED — the denominator

Lane key: **L1** Roster Intelligence · **L2** Trade Intelligence · **L3** Season / Scoring /
Projections · **L4** Market / FAAB / Analyst · **L5** Integration / QA / CI / Governance ·
**L6** Premium UI / Frontend.

### 3.1 Canonical identity, value and history

| # | capability | canonical id | lane | status | level | in V1 because |
|---|---|---|---|---|---|---|
| V1-01 | One player-identity owner | `C1-ID-01` / inv 7.1 | L5 | `VERIFIED` | L3 | canonical identity. Cut over 2026-08-16; prod gate 2,016/2,016 and 24,024/24,024, board 0/1,092 |
| V1-02 | One pick identity, end to end | `C1-ID-02` / inv 2.7 | L5 | `VERIFIED` | L2 | canonical identity. PR #867; board inert 0/1,093 |
| V1-03 | One immutable as-of value/provenance ledger | `C1-HIST-01` | L5 | `VERIFIED` | L3 | canonical history. Prod run 31960075629, 169,896 rows / 34 dates |
| V1-04 | Historical pick values first-class | `C1-HIST-02` | L5 | `VERIFIED` | L1 | canonical history |
| V1-05 | Deterministic board-history `rankChange` | `C1-HIST-03` | L5 | `VERIFIED` | L1 | truthful degraded state — no comparator yields `None`, never 0 |
| V1-06 | Canonical value scale 1–9999 enforced | `F-VAL-01` / inv 7.5 | L5 | `VERIFIED` | L1 | canonical value. B9a |
| V1-07 | No second canonical board | `F-VAL-02` | L5 | `VERIFIED` | L2 | canonical value. B9a; measured 491/507 disagreement removed |
| V1-08 | `valuation_mode` withdrawn, ignored, stamped | `F-VAL-03` | L5 | `VERIFIED` | L1 | canonical value |
| V1-09 | Five-axis confidence, weakest axis wins | `F-CONF-01` / inv 7.4 | L5 | `VERIFIED` | L1 | canonical value. B11 |
| V1-10 | Provider families, one vote each | `F-SRC-01` / inv 7.6 | L5 | `VERIFIED` | L1 | canonical value. 21 keys → 13 families |
| V1-11 | Confidence naming migration | `C1-CONF-01` / `C1-U5` | L5 | `IMPLEMENTED_UNVERIFIED` | L3 | canonical value. PR #876 merged. §6 checklist **executed 2026-08-18** (§ verification record): 5 of 8 PASS incl. item 3 (0 of 857 priced rows without a `confidenceBasis`), 2 `BLOCKED-EXTERNAL` on auth, 1 checklist path corrected. Reached **L2**, not L3 — the passes are on a rebuilt real board, not a deployed response  **Item 3 re-verified 2026-08-18 23:00 on the post-#910 SHA**, which mattered because `#910` added a NEW `confidenceBasis` value (`derived_year_step`) to the closed set: production's `contract.health.structuralErrors` is empty, and `validate_api_data_contract` ERRORS on any priced row carrying a basis outside that set — so the new value was registered rather than smuggled. The two `BLOCKED-EXTERNAL` items are unchanged; this does not move the row. **Item 8 re-investigated 2026-08-27**: `scripts/verify_v1_authenticated.py::check_v11_item8` (added since the above record) was hitting bare `/api/terminal` with the guest_pass verification session, which has no `sleeper_user_id` and no `?team=` param — so `resolved_team` stays `None`, `roster_rows` is empty, and `signals: []` naturally, independent of whether confidence is wired correctly. The consistent `V11-8` FAIL across every run so far was reading "no signal rows to check" as "signal rows lack confidence" — the same class of verifier defect already found and fixed for V1-56. Fixed the verifier to resolve a real team from `contract.sleeper.teams` (same pattern `check_v27_item3` already uses) and pass `?team=<id>`, distinguishing "team resolved, zero signals" (`unmeasurable`) from "team resolved, signals present, none carry confidence" (`fail`, a real regression this check should still catch — `src/api/terminal.py::_build_signal_context` does spread a `confidence` key into every signal entry, per code review). Mutation-verified (`tests/ops/test_v1_verification_primitives.py`, 5 new tests). **Not yet re-verified against production with the fixed check** — pending a fresh authenticated verification run before this can move past the existing L2 status. |
| V1-12 | Pick value completeness through 2029 | `C1-PICK-01` / MFB-89 / inv 2.8 | L5 | `VERIFIED` | L3 | canonical value; owner hard requirement. PR #871 + D1 repair. **DOWNGRADED from `VERIFIED` 2026-08-18 (owner instruction)** — audit `F-30`: `main` left 2029 rounds 2-6 unpriced when a pick market truncated, failing the hard gate and **skipping a production deploy**. A previously verified capability is allowed to regress; leaving it `VERIFIED` during a known live failure is the false green this contract exists to prevent. Restoration needs all four: exact-head deterministic tests, a clean 2029 completeness census, board-diff measurement, and deployed evidence. Repair is in #910. **Three of four satisfied 2026-08-18** (strengthened after lane 7's `#916` measured the first repair too narrow — see `F-30`; the shipped rung searches the board for the nearest priced earlier future year instead of consulting the injection's record, which also covers a PARTIALLY published horizon year: 24 census errors → 0 on that case) — deterministic tests green at exact head; census clean (0 errors, 24/24 2029 rows priced with explicit provenance); board diff `main`→repair shows **0 values moved, 0 ranks changed, 15 newly priced, 5 rows added**, with the 6 quarantine flips proven to be 2029 matching the treatment 2027 and 2028 already had. **Deployed evidence landed 2026-08-18 23:00 UTC and the fourth condition is met, so this returns to `VERIFIED`**: on a board built entirely by post-#910 code, production's own `contract.health` reports `ok: true`, `structuralErrors: []`, `sourceHealthErrors: []`, `errorCount: 0`. The pick-completeness census is a STRUCTURAL check, so an empty `structuralErrors` is the census passing — every valid pick through the horizon priced with explicit provenance. The 2029 rows are demonstrably on that board: the same endpoint listed `2029 Round 6` among `normalizationHealth`'s samples an hour earlier, so this is not a vacuous pass over an empty population |
| V1-13 | Generic ↔ exact-slot transition, one asset | `C1-PICK-02` | L5 | `VERIFIED` | L1 | canonical identity |
| V1-14 | Per-source pick boards: no fabricated year anchors | `C1-U6-D1` | L5 | `VERIFIED` | L3 | false-green repair. Deployed `5a5f1507f`; 0 of 18 cells violating |
| V1-15 | Acquisition history / cost basis | `C1-ACQ-01` / inv 7.11 | L5 | `IMPLEMENTED_UNVERIFIED` | L3 | canonical history. PR #878; §8 checklist **attempted 2026-08-18 and BLOCKED-EXTERNAL on all seven** (§8a) — `data/retention/` and `data/intel/` do not exist outside prod, and running the builder here would report `0` transactions for the uninteresting reason that the store is absent. **RE-CONFIRMED FRESH 2026-08-24 (§9a)** rather than carried forward, and the access question was re-tested from scratch, not assumed stale: `~/.ssh` holds no keys, no `.env` and no deploy/Sleeper/admin credentials are present, and the ledger's own read projections are auth-gated on the live host — `GET /api/market/waivers` and `GET /api/market/trades` both return **401 `auth_required`**. All seven §8 items remain `BLOCKED-EXTERNAL`; none became reachable, and none was downgraded to a pass |
| V1-16 | Historical roster reconstruction | `C1-ACQ-02` | L5 | `VERIFIED` | L3 | canonical history. **PROMOTED 2026-08-26 on fresh deterministic + production evidence.** Implementation remains the merged C1-U8 owner from #878 (merge `af4dad9b5`). Fresh L1 proof on current product tree `28b3c8eaf`: workflow run `32943730840` first ran `tests/acquisition/test_roster_and_privacy.py` GREEN, then mutated `_covers()` so a future acquisition incorrectly covered the earlier instant and required the focused suite to go RED, then restored the file to a clean diff and GREEN again with planning integrity passing. L3 production proof: corrected on-box run `32919655291` against deployed SHA `a82898fdbe73ea2effd295132590dff35a45ac98` passed C1-U8 §8 item 5 on the real retention store: 492 `free_agent`, 294 `trade`, and 744 `waiver` transactions, proving historical reconstruction is fed by the full transaction vocabulary rather than trades-only. Missing/unavailable semantics remain distinct from empty in the same mutation-proven roster suite. |
| V1-17 | Pick lineage / trade trees | `C1-ACQ-03` / CE-18 | L5 | `IMPLEMENTED_UNVERIFIED` | L3 | canonical history |
| V1-18 | Irreversible-evidence retention | `C1-U1` / `C1-RET-01…08` | L5 | `VERIFIED` | L3 | perishable evidence; loss is unrecoverable. `C1-RET-07` honestly STALE |
| V1-19 | Multi-format dynasty source archive | `C1-SRC-01` / `C1-U9` | L5 | `VERIFIED` | L3 | canonical value provenance. §7 checklist **executed 2026-08-18** (§7a): items 1-2 PASS at **L3** on live production (21/21 `DYNASTY` with evidence), item 3 PARTIAL (healthy post-deploy scrape; strict inertness needs a pre-deploy snapshot nobody captured), item 4 N/A. **Items 1-2 RE-VERIFIED FRESH 2026-08-24 (§9a)** against the current deployment rather than carried forward: `/api/rankings/sources` 200 with **21** sources, **0** non-`DYNASTY`, **0** empty `gameTypeEvidence`. **PROMOTED 2026-08-25 on an owner decision plus evidence already in hand, not on new engineering.** Owner decision (recorded verbatim from this session's dispatch): *"If no already-authorized substitute exists, ACCEPT the missing pre-deployment snapshot as permanently irrecoverable. Do not fabricate it."* The substitute search was performed first and an authorized substitute DOES exist — exactly the one the §9a packet B recommended accepting: the **pre-merge on-box inertness measurement** (`docs/sources/C1_U9_MULTI_FORMAT_SOURCE_ARCHIVE.md` §4: `golden_board.py` before/after + `board_diff --expect-no-value-change` → `rows 1111 → 1111 · VALUES: 0 moved · RANKS: 0 changed`) plus the **healthy post-deploy scrape** (§7a item 3: `overall_status: complete`, `partial_run: false`, `contract.health.ok: true`, 0 structural / 0 source-health errors over 1109 players). Item 3 therefore closes on the accepted substitute; the strict pre-deploy production snapshot is recorded as owner-accepted irrecoverable and was NOT fabricated. Items 1-2 are L3 fresh; item 4 N/A. The recurring process gap (nothing captures a pre-deploy snapshot) remains named in §7a and in packet B's recommendation |
| V1-20 | Game type proven per feed, fails closed | `C1-SRC-02` | L5 | `IMPLEMENTED_UNVERIFIED` | L3 | canonical value — dynasty lane purity. **Proven-per-feed RE-VERIFIED FRESH 2026-08-24 at L3 (§9a)** on the live deployment: `/api/rankings/sources` 200, **21/21** `gameType: "DYNASTY"`, **0** empty `gameTypeEvidence` — every feed states its game type with evidence rather than inheriting a default. What that measurement does **not** establish is the *fails-closed* half, and it cannot: an all-`DYNASTY` registry contains no `UNKNOWN` feed, so production offers no live instance of a refusal to observe. The refusal is proven on this box (`C1-U9`) and remains **L2 for the fails-closed clause specifically** — recorded rather than rounded up, because a population with no negative case cannot evidence the negative behaviour |
| V1-21 | Model provenance stamps | inv 7.12 | L5 | `VERIFIED` | L1 | canonical value provenance; partial (W04-F011). **PROMOTED 2026-08-22 (Integration) — shipping-tree L1 confirmation on merged `main` (`#1057`, squash `8ba570dd8`, reconciling `#1055`).** `_hill_master_provenance()` (`src/api/data_contract.py`) stamps `hillCurves.provenance` read-only from the existing `ModelRegistry`/`ModelVersion` owner, reusing the vocabulary already established by `src/bdvm`/`src/consensus_edge`: `verified_champion` only when live constants byte-match the recorded champion's params exactly; `unverified` (not the stale champion's version) on drift; `unavailable` on a missing/corrupt registry or no champion — `modelVersion` stays `None` on both non-verified states. `src/history/provenance.py`'s `pipeline_version()` hash is scoped to exclude the new `provenance` sub-block, so a cosmetic registry correction cannot move temporal-board pipeline identity. Re-verified fresh on the shipped tree (not trusted from the PR): `tests/api/test_data_contract.py -k provenance` 6/6 passed (corrupt registry, missing registry, drifted constants, no champion, paramSetId stability, real verified champion); a live invocation of `_hill_master_provenance()` against the real production `hill_scope_masters` registry returned a genuine `status: "verified_champion"`, `modelVersion: 2`, real `paramSetId`/`asOf`/`producer` — not a synthetic mock. **Independent mutation performed this pass**: forced the `matches` branch to never take the `unverified` path ⇒ RED on `test_drifted_constants_report_unverified_not_the_stale_champion`; restored, `git diff --stat` empty. Zero `server.py`/`frontend/` changes — confirmed against the true `git merge-base` (`faafefb71`, not the PR's stated stale base) that no source-weight, bridge, calibration, voting, player-value or auth path is touched, and `HillCurveExplorer` does not interpret the new object. `check_planning_integrity.py` and `check_decision_coercions.py` both clean on the shipped tree. |
| V1-22 | Hill curve / percentile→value correctness | inv 7.2 | L5 | `IN PROGRESS` | L2 | canonical value. B1 investigated, not implemented. **STANDING CHALLENGER ADJUDICATED 2026-08-25 — REFUSED; champion stays v2; no constant changed.** Full record: `docs/valuation/HILL_CHALLENGER_PROMOTION_ADJUDICATION_2026-08-25.md`. The open question was whether v5's held-out criterion (683.1 vs champion v2's 787.8, **−13.3%**) licenses promotion. It does not, on two independent measured grounds. **(a) Scope coverage.** `hill_masters.VALIDATED_PARAMS` is literally the OFFENSE pair and `holdout.evaluate_offense_master` reads offense boards only, while a promotion moves all eight constants. Running `scope_validation.classify_scopes` over the real registry: v5 is `GLOBAL=UNVALIDATED_NO_HOLDOUT`, `IDP=UNVALIDATED_NO_HOLDOUT`, `OFFENSE=VALIDATED_EXTERNAL_HOLDOUT`, `ROOKIE=NOT_ROUTED` — and its unscored moves are the large ones (`IDP_HILL_PERCENTILE_C` 0.083→0.038, −54%; `IDP_HILL_PERCENTILE_S` 1.110→0.870, −22%; `HILL_GLOBAL_PERCENTILE_C` 0.112→0.089, −21%). v3 and v4 are refused on the same ground. **(b) Measured blast radius (L2).** `scripts/measure_hill_version_board.py` (added here, evaluation only — promotes nothing, writes no config) prices the real board through the real `build_api_data_contract` with one version's params substituted; both captures in the pair carry identical `inputSha256`/`sourceCsvSha256`/`freshnessSha256`, so `board_diff.py` reports curve effect and nothing else. v2→v5 over 1,111 rows / 849 priced: **741 values moved** (87%), p50 15.4% / p90 25.7% / max **32.1%**; **758 ranks changed**; **36 newly unpriced** (all offense) and 36 newly priced; 738 tier flips; 264 rows voting differently; top-25 membership NOT preserved. Median move by class: offense **−22.99%**, IDP **−9.42%**, picks 0.00% on 72 of 144. The deciding finding is that the B11 **agreement** axis moves the wrong way: **300 `confidenceBucket` flips, 205 worse against 95 better** (367 `confidenceLabel` flips, e.g. `A.J. Brown: High — every axis high → Medium — limited by agreement`) — the challenger that describes six external boards better agrees **worse** with the eleven families voting on our own board. For contrast v2→v4 moves 771 values at p50 **0.8%** / max 4.1%. Evidence: `docs/valuation/evidence/HILL_V2_TO_V5_BOARD_DIFF_2026-08-25.txt`, `…V2_TO_V4…`. **NOT rejected, and deliberately so** — `reject()` records that a challenger lost, and nothing here establishes that; the honest state is "unpromotable on current evidence", which is `challenger`, so v5's registry entry is untouched. **Row stays `IN PROGRESS`, tally unchanged** — this measures what a promotion would COST, not that the champion curve is correct, and `holdout.py` states plainly that no instrument here can establish the latter. **Governance defect found while measuring, and repaired.** `ModelRegistry.promote()` did not consult `scope_validation` at all: `assert_promotable`/`classify_scopes` had **zero callers** outside their own test file, `scopeValidation` was `{}` on all five recorded versions, and `scripts/model_registry.py` carried a comment saying `cmd_promote` "has NO holdout gate". Measured before the repair: `promote(3)`, `promote(4)` and `promote(5)` all **succeeded** — the refusals above were available and were never asked for. The gate is now wired into `promote()`, with evidence **derived** (OFFENSE validated iff the challenger carries a holdout record — no caller may assert `validated_scopes`), an owner escape hatch (`--override-scope`/`--override-reason`) recording `OVERRIDDEN_BY_OWNER` as a decision never a `VALIDATED_*` measurement, and `rollback()` deliberately ungated so the documented undo is not blocked. Pinned by `tests/model_registry/test_promote_scope_gate.py` (10 tests, incl. a parametrized live-registry case asserting all three standing challengers are refused). RED-before/GREEN-after executed: with the wiring reverted the refusal tests fail `Failed: DID NOT RAISE RegistryError`; restored, 10/10 and `tests/model_registry/` 167/167. One pre-existing fixture (`test_round_trip_returns_to_the_original_params`) gained a holdout record so its scaffolding promotion is legal — the fixture was made valid, not exempt |
| V1-23 | IDP valuation correctness | inv 7.3 | L5 | `IN PROGRESS` | L2 | canonical value. W02-F001/F002 |
| V1-24 | Factual scoring identity, fails closed | `F-SCORE-01` / inv 7.7 | L5 | `VERIFIED` | L2 | canonical value; cross-league correctness. B6 |
| V1-25 | League-config consistency | inv 7.8 | L5 | `VERIFIED` | L1 | canonical value; residual drift W18-F005/F011. **PROMOTED 2026-08-25 (#1124, `cd3c82fb2`) at L1.** Both named defects re-verified already-closed on current `main` (W18-F005 by #1056/#1114 — "my league" resolves through `league_registry.get_default_league()` and fails closed on an empty registry, per-position sample sizes derive from `rosterSettings`; W18-F011 by #1114 — `dynasty_new` registry matches its committed host snapshot). The one live residual (W30-F006 shape) is fixed here: seven hardcoded-`dynasty_main` consumers in `suggestions.py` (`_generate_sell_high`/`_buy_low`/`_consolidation`/`_positional_upgrades`, `rank_score`, `rank_score_breakdown`, `_roster_balancer_candidates`) read `DEFAULT_STARTER_NEEDS` unconditionally, so a 1-TE league flagged a TE surplus then never offered the TE2. Fix: `analyze_roster` stamps the resolved league demand on `RosterAnalysis.starter_needs`; consumers read it; `DEFAULT_STARTER_NEEDS` is private to the three declared fallback sites, enforced by an AST guard. RED→GREEN mutation: reintroducing the sell-high hardcode turns 3 tests RED; dynasty_main is an exact no-op (derived needs reproduce the constant). 1001 focused + 205 league-scoped api tests green; green CI on the merge tree |
| V1-26 | KTC playerID → identity, one owner | `C4-KTC-01` | L4 | `VERIFIED` | L2 | canonical identity. Join 200/200, board inert |

### 3.2 Exact lineup, replacement, roster core, Team Strength / Weakness, Young Core

| # | capability | canonical id | lane | status | level | in V1 because |
|---|---|---|---|---|---|---|
| V1-27 | One lineup / slot assignment owner | `C2-LINE-01` / `C2-U1` | L1 | `VERIFIED` | L3 | named V1 scope. PR #880; 10/10 vs Sleeper truth. §10 checklist **executed 2026-08-18** (§10a): items 1, 3a, 5 PASS at **L2** (12/12 lineups solved from `sleeper_roster_positions`; 4 hybrids started in slots their primary alone forbids), items 2-3 `BLOCKED-EXTERNAL` on auth, item 4 PARTIAL. **Auth blocker RE-TESTED FRESH 2026-08-24 (§9a) and unchanged:** `GET /api/data` on the live host returns **401 `auth_required`**, and there is no session to mint — the E2E bootstrap is correctly invisible in production (`POST /api/test/create-session` → **404**, its documented fail-closed behaviour). The row's required level is **L3**; its passing checks are **L2** against a rebuilt export. That gap is exactly what an authenticated deployed response would close, so this is credential-blocked rather than incomplete — the production-consumer chain (real league config → canonical solver → FLEX/SF/IDP FLEX → starter removal → reserve demand) is unobservable from here, and fixture parity is explicitly not accepted in its place **PROMOTED 2026-08-26 to `VERIFIED` at L3 after the merged authenticated production packet `docs/lineup/evidence/V1_27_AUTHENTICATED_PROD_2026-08-26.md` closed the remaining consumer chain: the canonical guest-pass/login path authenticated against production; `/terminal` and `/rosters` rendered the server-stamped `optimalLineup`; and a real bench-for-bench trade simulation reported zero `starterDelta` at every position while preserving starter assignments. The already-recorded owner-approved substitute for the irrecoverable pre-deploy snapshot remains the evidence for that historical check; nothing was fabricated or inferred from a merge alone.** |
| V1-28 | FLEX / SF / IDP-FLEX starters assigned before reserve depth | `#899` addendum | L1 | `VERIFIED` | L1 | **VERIFIED 2026-08-20 on merged `main` `547e51bc6` (post-#914/#922).** L1 bar is deterministic evidence, and it is satisfied AND proven non-vacuous. `tests/roster_intel/test_core.py` carries eight named acceptance tests covering exactly what this row names — `ac1` flex starters before reserves, `ac2` flex starter not also positional depth, `ac3`/`ac4` RB/WR flex shift, `ac5` flex count from config not a constant (parametrised), `ac6` SUPER_FLEX folds into QB demand once AND the SF starter is not also a QB reserve, `ac7` IDP_FLEX assigns before IDP reserves. **34 passed** on the shipping tree. **Mutation proof:** deleting the starter-exclusion from the reserve pool (`core.py:448 remaining = [p for p in pool_list if p.player_id not in starter_ids]`) fails 6 tests including `ac4`, `ac6` and `ac8_every_core_member_is_unique`; restored, 34 pass. The suite is demonstrably sensitive to the ordering it asserts |
| V1-29 | One replacement level / PAR owner | `C2-REPL-01` / `C2-U2` | L1 | `VERIFIED` | L2 | named V1 scope. **#933 merged 2026-08-20 (`6eb847c39`) and it does NOT verify this row — measured, not assumed.** What #933 genuinely proves: the RETIREMENT half. `scripts/replacement_census.py` runs clean at head (1 retired symbol deleted and unreachable, 0 DEAD rows, every OWNER consumed, every OWNER unit distinct) and 18 tests pass; mutation **MR3** — reintroducing the retired `vorp_table` as a real call site in `src/`, confirmed APPLIED — produces 3 named REDs (`test_the_census_runs_clean_at_head`, `test_retired_implementations_are_not_reachable`, `test_a_docstring_mention_does_not_trip_the_census`). **What it does NOT prove, and this is the row's actual capability:** that there is exactly ONE replacement-level owner. Two mutations refute it. **MR1** — a new module `src/league_intel/_dup_replacement_probe.py` defining a second `replacement_per_game` ⇒ census **clean**, 18 passed. **MR2** — the same duplicate **plus a call site**, so it is live code rather than dead ⇒ census still **clean**, 18 passed, exit 0. Cause: the census scans `src`/`scripts` for CALL SITES of its **declared** rows and of `RETIRED_SYMBOLS`; it has no rule that fails on an undeclared new implementation. It is a registry-consistency check, not a single-owner check. Contrast `V1-34`/`V1-130`'s guard, which caught an equivalent decoy on the first try. **To close:** a discovery rule that fails when a replacement/PAR implementation appears outside the declared owner set (the vocabulary-scan shape already used in `test_there_is_exactly_one_constraint_owner`), plus the L2 board statement — **BOTH DELIVERED AND VERIFIED 2026-08-20 (#987, merge `faa50ba9a`); PROMOTED at L2.** The discovery rule is `undeclared_owner_definitions()` in `scripts/replacement_census.py`: an AST scan over `FunctionDef` / `AsyncFunctionDef` / `ClassDef` nodes asserting every declared OWNER symbol is defined at exactly the one path its CENSUS row declares, flagging a second definition **whether or not anything calls it** — which is what closes MR1 and MR2 with one mechanism rather than two, since both exploited a call-site scan that could tell you a NAME was used but never whose CODE answered. **L1 half, verified at Integration by execution:** baseline `tests/roster_intel/test_replacement_owner.py` 21/21 at the exact merged head `ee34859e9`; **MG-A** forcing `sites = {}` inside the guard ⇒ RED on exactly the right three (`test_no_owner_symbol_is_defined_outside_its_declared_path`, and the MR1/MR2 positive controls); **MG-B**, the one that matters, wrote a genuine second definition of `replacement_per_game` into a tracked `src/utils/_v129_probe.py` with no caller and got `::error … an undeclared second owner: {'replacement_per_game': ['src/utils/_v129_probe.py']}` and `exit 1` — MR1's exact shape now failing closed where it previously exited 0. The `git add` is load-bearing: `definition_sites()` scans TRACKED files, so an untracked probe is invisible and a mutation run that way proves nothing (a mistake made earlier in this session and discarded rather than counted). **L2 half, measured rather than inferred:** a pinned-input golden-board capture either side of the merge (`0ac262b02` → `faa50ba9a`, both worktrees pinned to the same `CSVs/` + `data/scrape_state/` so the diff measures code and not the 2-hourly refresh) reports **0 values moved, 0 ranks changed, 0 newly priced, 0 newly unpriced** across 1,111 rows / 740 ranked / 849 priced / 162 picks / 398 IDP. That is the honest L2 statement for this row: the census has zero importers outside its own test file and is wired into no workflow or runtime (grepped, not assumed), so "no effect on the published board" is what there is to measure, and it is now measured instead of argued. Also independent of this row's own claim: the lane flagged that its assignment mislabelled V1-29 as lineup assignment (that is V1-27) and built against the real row instead of the brief |
| V1-30 | Canonical meaningful roster core | `C2-CORE-01` / `C2-U6` / `#839` | L1 | `VERIFIED` | L2 | **VERIFIED 2026-08-20 on merged `main` `547e51bc6` (post-#914/#922).** The row's stated bar was "`M=1.5` ships as V1 champion labelled PRIOR, **challenger pass required**" — the challenger pass HAS now been run and is recorded at `docs/roster-intelligence/evidence/C2_CORE_M_CHALLENGER_2026-08-18.{json,txt}`. L2 (board measured) is satisfied on a real 12-team board across **four** formats (live 21-slot, no_idp, no_superflex, +1 more) at M ∈ {1.25, 1.50, 1.75} with k=1/2/3 retention, data-derived cutoffs and strength-order stability. It rules out 1.25 and places 1.50 inside the defensible band in every measured format (live: 99.95% mean retention at k=3; strength order vs M=1.75 differs in 0 of 12 seats). `scripts/challenge_core_multiplier.py` stamps **"EVALUATION ONLY — this script promotes nothing and writes no config"**, so champion/challenger separation held. The champion is deliberately NOT frozen — correct methodology, not incompleteness |
| V1-31 | Canonical Team Strength | `C2-STR-01` / `C2-U4` | L1 | `VERIFIED` | L2 | named V1 scope. 4 competing notions to retire. **#914** builds the owner. **PROMOTED 2026-08-21.** `docs/roster-intelligence/V1_35_METRIC_SEPARATION_AUDIT.md` measured the census and found exactly two live frontend duplicates: F-1 (`scoreTeamTiers` on `/rosters`) — already closed, `/rosters` renders `TeamStrengthCard.jsx` off `GET /api/roster/intelligence`, guarded by `no-frontend-team-strength-methodology.test.js` — and F-3 (`/phases`' classifier, `frontend/lib/team-phase.js`), which independently computed a client-side top-25 `rankDerivedValue` sum and a raw per-player age scan. #1002 redirects `/phases`' two input axes onto the canonical `strengthTotal` / `valueWeightedCoreAge` fields `GET /api/roster/intelligence`'s `leagueContext` already serves (the same fields `/rosters` consumes) — zero `.py` files touched (`git diff --stat main -- '*.py'` empty against both the PR's own base and current `main`), the classification RULE (4-quadrant bucketing) is unchanged, only its input SOURCE moved. Reconciled at Integration: rebased onto exact head `0a5267d06` (only conflict was `config/coercion_baseline.json`, regenerated from the rebased tree rather than hand-merged — `check_decision_coercions.py` clean, 0 new coercions in touched files). Reperformed rather than trusted: frontend suite 147 files / 2286 tests green (no regressions); the PR's own 3 touched test files 18/18; an independent manual mutation — appending a new exported function to `team-phase.js` that reads `rawData?.sleeper?.teams` — trips `no-frontend-team-strength-methodology.test.js`'s `NO_RAW_ROW_SCOPE` check (RED, names the exact injected line), reverted (GREEN restored, clean tree). The backend single-owner guard (#1004, same mechanism V1-32 uses) also covers `TeamStrength`/`PositionStrength`/`build_team_strength`/`rank_team_strengths` and passes 5/5 on this same rebased tree. Zero known live Team Strength duplicates remain. **L2:** structural zero on canonical values (`.py` diff empty — no rank, lineup or Team Strength computation changed); `/phases`' displayed classification LABELS can change per-team (the intended repair — old labels were computed from non-canonical inputs), which is a display consequence of fixing the duplicate, not a board-value movement; no automated harness captures "old vs new `/phases` labels" without a live browser session, named as a gap rather than asserted away. |
| V1-32 | Canonical Team Weakness / Need Priority | `C2-WEAK-01` / `C2-U5` | L1 | `VERIFIED` | L2 | named V1 scope. ≥5 need definitions to retire. **PROMOTED 2026-08-21.** The 2026-08-20 refusal named one precise blocker: the L2 measurement (`scripts/verify_roster_intelligence.py --offline` — 216 rungs PASS, 215 rung credits PASS at EVIDENCE-L2, 0 violations, 11/0/2 passed/failed/unmeasurable, plus `tests/roster_intel/test_verify_offline.py` 5/5) already stood, but the backend single-owner discovery guard proving "≥5 need definitions to retire" was #1004, not yet on `main`. **#1004 is now on `main`** (merged `25ec0826a`, `tests/roster_intel/test_strength_weakness_single_owner.py`). Reperformed at Integration against exact head `0a5267d06`, not assumed from the merge alone: the pinned suite's own two positive controls pass (`test_the_guard_catches_an_undeclared_second_strength_owner`, `test_the_guard_catches_an_undeclared_second_weakness_owner`, 5/5 total), and an INDEPENDENT manual mutation outside the test file confirms the same mechanism live — writing `src/roster_intel/_v132_manual_probe.py` with a second `build_team_weakness` makes `undeclared_owner_definitions()` return `{'build_team_weakness': ['src/roster_intel/_v132_manual_probe.py']}` (RED); deleting the probe returns `{}` (GREEN restored, working tree clean). Both halves of the row's bar are now discharged by measured evidence: the L2 board statement (unchanged from 2026-08-20) and the single-owner guard (new).
| V1-33 | Basic Young Core Index **+ the age-value portfolio it is computed from** | `C2-AGE-02` + `C2-AGE-01` / inv 1.6 / `#838` | L1 | `VERIFIED` | L1 | **VERIFIED 2026-08-20 on merged `main` `547e51bc6` (post-#914/#922).** L1 satisfied, and by stronger evidence than L1 requires: `scripts/validate_young_core.py` exists precisely because "unit tests cannot discharge that" — the `C2-AGE-01` addendum §5 requires validation against real league examples, not fixtures. Run on the shipping tree against a REAL contract: **exit 0, all four properties hold** — cheap young bench players cannot game the index; meaningful-core selection matches the league's own slot config; a +5.00 age shift moves the age statistics and nothing else on 12/12 teams; strength ranks 1..12 complete with YCI spanning 0.0-100.0 and 16 of 66 rank/YCI inversions, so youth is a genuinely separate axis from strength. **Recorded caveat, in the script's own words:** this validates BEHAVIOUR, not calibration — the 0-100 weighting remains a PRIOR. That is the champion/challenger posture, and this row scopes the **basic** index |
| V1-34 | Untouchable / excluded-player control | inv 1.5 | L2 | `VERIFIED` | L1 | **VERIFIED 2026-08-20 on merged `main` (#929, merge `817d9abe4`).** Target level is **L1** — §2: a RED→GREEN test at exact head plus green CI on the merge tree — and both halves hold. `src/trade/constraints.py` is the single owner (`test_there_is_exactly_one_constraint_owner`); `tests/trade/test_recommendation_constraints.py` + `test_constraint_generation.py`: **41 passed**. **Three mutations performed at Integration, each confirmed APPLIED with the changed line quoted before being counted:** (MC1) `constraints.py:266` `protected = _as_key_set((persistent or {}).get("untouchables"))` → `set()` ⇒ **6 named REDs** (`test_an_individual_rule_survives_the_player_leaving_the_team`, `test_picks_are_never_swept_up_by_a_team_rule`, `test_persistent_protection_outranks_a_temporary_lock`, `test_multiple_constraints_are_honoured_together`, `test_partition_returns_the_blocked_half_rather_than_dropping_it`, `test_every_outgoing_asset_constrained_is_an_honest_no_result`); (MC2) `:188` `return "protected_nfl_team_unknown"` → `return None`, i.e. an unknown NFL team fails **OPEN** ⇒ RED `test_an_unknown_nfl_team_fails_closed` **and** `TestUnknownFailsClosed::test_an_unknown_nfl_team_is_treated_as_protected` — the fail-closed invariant is genuinely pinned, on both surfaces; (MC3) `:184` `return "excluded_temporary"` → `return None` ⇒ RED `test_exclude_blocks_and_lock_requires`, `test_lock_and_exclude_on_the_same_player_resolves_to_exclude`, `test_multiple_constraints_are_honoured_together`. All restored; 41 pass. Two further mutation attempts were **refused by the harness** for matching 2 sites rather than 1 and are NOT counted — recorded because a silently-unapplied mutation is the false green this campaign keeps finding. CI: Validate PR GREEN on exact head `e057c55a3`; train tree `1c1c52e1b` green across six suites (1936 passed) \| owner control over recommendations; W09-F011 |
| V1-130 | One recommendation-constraint owner | `C3-CON-01` | L2 | `VERIFIED` | L2 | **VERIFIED 2026-08-20 on merged `main` (#929, merge `817d9abe4`).** Target level **L2** — §2: L1 plus a measured statement of the effect on the live board or contract — and both halves hold. **L1:** `src/trade/constraints.py` is the sole owner; 41 pass across `tests/trade/test_recommendation_constraints.py` + `test_constraint_generation.py`. Four mutations, each confirmed APPLIED with the changed line quoted: (MC1) drop persistent untouchable protection ⇒ **6 named REDs**; (MC2) `:188` unknown NFL team fails OPEN ⇒ RED `test_an_unknown_nfl_team_fails_closed` + `TestUnknownFailsClosed::test_an_unknown_nfl_team_is_treated_as_protected`; (MC3) `:184` temporary exclusion stops blocking ⇒ 3 named REDs; (MV1) **positive control on the single-owner guard** — a second module `src/league_intel/_mutation_decoy.py` containing the constraint vocabulary ⇒ RED `test_there_is_exactly_one_constraint_owner`, with the decoy path named in the assertion message. So the owner guard is a real scan over `src/`, not a decorative one. **L2:** Board measurement, code the ONLY variable (train source reverted to pre-train `8b9c28289` while `data/` `CSVs/` `exports/` were held at current `main`, both captured with `scripts/golden_board.py`): 1111 → 1111 rows, 740 ranked, 849 priced, 162 picks, 398 idp; `scripts/board_diff.py --expect-no-value-change` reports **0 values moved, 0 newly priced, 0 newly unpriced, 0 ranks changed**, exit 0. The capture covers the whole Wave A train (#933 + #929 + #937 + #934), which entails 0 for each member individually. That is the relevant measurement for this row, because the invariant it carries is that constraint resolution never touches canonical value — pinned at L1 by `test_resolution_never_reads_or_writes_a_canonical_value` and confirmed at the board by 0 rows moving |
| V1-35 | Metric separation (asset value / roster strength / lineup / depth / power / playoff / championship) | decision 69 | L1 | `VERIFIED` | L1 | binding owner decision — may not collapse into one team score. **PROMOTED 2026-08-22 (Integration) — `#1049` reconciled onto current `main` (zero conflicts).** Both live violations the audit doc (`docs/roster-intelligence/V1_35_METRIC_SEPARATION_AUDIT.md`) originally found are confirmed already retired in production: F-1 (`/rosters`' `scoreTeamTiers` weighted composite, `starterValue×0.7 + depthValue×0.2 − pickValue×0.1`, tercile-cut into contender/mid-tier/rebuilder) — deleted, `TeamStrengthCard.jsx` reads `GET /api/roster/intelligence` instead; F-3 (`/phases`' raw top-25-`rankDerivedValue` + raw-age derivation) — redirected onto canonical `strengthTotal`/`valueWeightedCoreAge`. Both fixes landed on `main` independently of the two PRs that separately claimed them (`#1002`, closed this session as superseded; this PR's own predecessor). **The genuine gap this PR closes is evidentiary, not behavioral**: this lane's own `tests/roster_intel/test_metric_separation.py` previously disclaimed ability to prove separation on `/rosters`/`/phases` ("other lanes' files"), and the audit doc's own §5 still said "not closed." 7 new tests (10→17) read the two real frontend files directly from disk — 2 absence checks against production files, 2 mutation-proof positive controls, and a positive-control divergence test (Team Strength rank vs. Young Core Index rank genuinely diverge on 11 of 12 teams on the newest real archived board — the asserted separation is not vacuous). **Independently re-verified this pass**: `tests/roster_intel/test_metric_separation.py` 17/17 passed fresh; a fresh, self-performed mutation (reintroduced the exact retired `scoreTeamTiers` formula into `frontend/lib/league-analysis.js`, a real tracked production file) sent `test_rosters_surface_has_no_weighted_composite_team_score` RED naming the exact line, restored clean (`git diff --stat` empty); `npx vitest run` on the three related JS test files — 18/18 passed, matching the PR's own claim; `ruff format`/`ruff check` clean; `check_decision_coercions.py` clean, 660/660 unchanged. Test-only change — no production code touched by this PR (the F-1/F-3 fixes themselves landed earlier, independently). |

### 3.3 Canonical trade intelligence (Analyze Trade V1)

| # | capability | canonical id | lane | status | level | in V1 because |
|---|---|---|---|---|---|---|
| V1-36 | ONE shared package generator | `C3-PKG-01` / `C3-U1` | L2 | `VERIFIED` | L2 | named V1 scope. **Moved 2026-08-21 (Integration, reconciling open PR `#962`, Claude 9's C3 Trade Substrate lane, base `cca488cd7` reconciled onto current `main`) — real progress, not `VERIFIED`.** Re-measured, not trusted from the PR body: `finder.py` and `angle.py` already fully consumed the shared `src/packages/construction.py` mechanics substrate before this PR; `roster_intel/packages.py` consumed only the identity helper with zero `TradeConstraints`/`roster_capacity` integration — the one generator this PR closes, gaining a `constraints: TradeConstraints | None = None` parameter wired to LOCK/EXCLUDE, filtered before stage 1, proven a byte-identical no-op on the one production caller (which passes no `constraints` today) by a golden-fixture equality test. `tests/roster_intel/test_packages.py` 55 passed (was smaller pre-PR); a narrowed pre-existing over-broad guard test (which banned the entire `src.trade` namespace when its docstring named only three specific engines) is mutation-proven to still catch a real reintroduction. **Not `VERIFIED`: still genuinely NOT one generator.** `roster_intel/packages.py` remains disconnected (no frontend caller) even after this fix, and `suggestions.py` — the live `/api/trade/suggestions` endpoint — remains a deliberate, un-migrated holdout with hand-rolled fixed 1v1/2v1 shapes carrying tuned product logic (including an in-code-flagged deferred owner decision) that must not be silently changed by a mechanical migration. `docs/C_SERIES_SCOPE_MANIFEST.md`'s own corrected `C3-PKG-01` row states this plainly: "PARTIAL, not the prior '4 independent generators'". **CORRECTED 2026-08-24: `suggestions.py` is NO LONGER an un-migrated holdout** — `#1079` (squash `a3e7bdb82`) migrated it onto `src/packages`' identity primitives. The sentence above is preserved as the record of what was true before that merge. **HELD at `IN PROGRESS` after a four-guard integration audit (2026-08-24), and the reason is specific: three of four guards passed and the fourth is disqualifying.** PASSED — (1) **no arbitrage leak**: `equal_count_only` / `ARBITRAGE_CONTROL_KEY` / `excludePlayers` appear 17 times repo-wide and all 17 are in `finder.py`; ten arbitrage symbols probe absent from the loaded `suggestions` module; and behaviourally **89 of 176** measured live suggestions are shape `(2,1)`, unequal-count packages that could not exist under finder's equal-count rule. `#1076` never touched `src/packages` (owner's last commit predates it by five days), so indirect inheritance is impossible by construction. (2) **methodology unchanged, MEASURED**: the production path (`build_asset_pool_from_contract` + `generate_suggestions_from_pool`) over all 12 real `dynasty_main` teams × pool caps 150/300/800 from the pinned golden fixture yields **176 suggestions** (sellHigh 78, buyLow 9, consolidation 3, positionalUpgrades 86) with payload sha256 `90ea6110…` **byte-identical** before (`a3e7bdb82^`) and after — 0 changed, and 0 of the file's 24 module constants moved. The null is proven non-vacuous by a positive control: reinstating the one retired hand-rolled key at `:1161` moves the sha and makes the engine recommend acquiring **Nik Bonitto, already on that team's roster**. (4) **CI green on the merge tree**: `PR Validation` `32765081070` success on the PR's exact final head `7822a2a4b3`, with `git log fa45be6bc..a3e7bdb82^` = **0 commits**, so the validated tree is the landed tree. FAILED — (3) **the single-ownership guard does not enforce single ownership.** `tests/trade/test_suggestions_package_identity.py` was defeated in one edit, independently by two agents: delete line 27's `from src.packages import …`, reimplement `_identity_key` and `_side_identity` locally, keep the docstrings — then `'src.packages' in sys.modules` is **False**, the module has zero dependency on the canonical owner, and **11/11 guard tests pass with 1608 relevant tests byte-identical to baseline**. The exact capability this row claims can be wholly removed with everything green. Three independent root causes: the routing assertions scan `inspect.getsource`, which **includes the docstring** (both helpers name the owner only in prose once the body is stripped); `_code_of` uses `ast.unparse`, which normalises literals to single quotes while the guard hunts **double-quoted** spellings, making 3 of its 5 patterns structurally unmatchable; and both scans iterate hardcoded function-name lists with a positive check any decoy call satisfies. **Remaining acceptance** — engineering (blocking, no owner decision): assert over the docstring-stripped AST body, match node structure rather than quoted literals, assert `src.packages` is imported and its symbols appear in EXECUTABLE code, scan every module-level function rather than two name lists, and **prove the repair by requiring the exact bypass above to go RED**. Owner adjudication (blocking, not closable by measurement): the row's literal acceptance names `package_key`, which has **no call site** in `suggestions.py` — `side_key` was substituted with an explicitly deferred product question ("should duplicate packages collapse across categories?"); and whether identity-only consumption satisfies "ONE shared package generator" at all, given the file keeps its own 1v1/2v1 shapes (which guard 1 was tasked with PRESERVING — the row's two blockers pull in opposite directions). Any future promotion must also carry two qualifications: `src/bdvm/roster.py::scan_double_positive_trades` is a deliberate SECOND two-sided generator consuming nothing from `src/packages`, so the claim is domain-scoped; and consumption is at the **name-fallback grade** (`asset_id=""` on every call — the branch the owner's own docstring calls "a real defect"), unlabelled in the payload. **GUARD B — ADJUDICATED AND CLOSED 2026-08-25 (Integration). The canonical records settle it mechanically; it is NOT an owner decision.** The question was whether V1-36 requires whole-package `package_key(send, receive)` to replace `suggestions.py`'s category-sensitive local dedup identity. **It does not**, on two independent grounds read straight from the records rather than from judgement. **(1) The acceptance criterion forbids it.** `docs/C_SERIES_SCOPE_MANIFEST.md`'s `C3-PKG-01` row states its acceptance verbatim as **`zero-diff on finder + suggestions`**. Imposing whole-package identity moves production-default `buyLow` **3 → 2** (measured by Claude 2 on the pinned golden fixture across all 12 real teams, and the mechanism is reproducible: two proposals sharing one receive side collide under the receive-side key and survive under the whole-package key). A change that moves shipped output is by definition not zero-diff, so the row's own acceptance excludes it. **(2) It would hold `suggestions.py` to a standard no other consumer meets.** Measured at HEAD: `src/trade/finder.py` contains **0** references to `package_key` and `src/trade/angle.py` contains **0** — and the manifest counts both as generators that "**fully consume the substrate**". They reach the owner through `enumerate_packages` / `enumerate_sides`, which dedup internally. `package_key` has exactly **one** direct consumer repo-wide (`roster_intel/packages.py`, 7 references) and it uses the whole-package form for genuine whole-package identity — Pareto-frontier membership — which is a different question from `suggestions.py`'s per-category tightest-gap dedup. **Ruling: consuming the owner's primitives (`PackageAsset.key`, `side_key`) at every keying site IS the accepted consolidation boundary for this row**, and every one of `suggestions.py`'s five keying sites does so post-`#1079`. The grain is a product property of each generator's search, not an ownership violation. `package_key`'s deliberate absence stays recorded in-code at `suggestions.py:308`. Whether duplicate packages should collapse ACROSS categories remains a genuine open product question — but it is a **feature question, not a V1-36 acceptance item**, and it must not be imposed silently precisely because it is not behaviour-inert. **Consequence for this row: Guard B is no longer a blocker.** The sole remaining engineering blocker is the identity-guard bypass in the paragraph above — re-tested 2026-08-25 against `#1089`'s own tree (`dc4ecdbf6`), where the full bypass (owner import removed, both helpers reimplemented locally, module fully functional, `src.packages` absent from `sys.modules`) still leaves **17/17 guard tests green**. `#1089`'s Guard A is a real and credited improvement — it takes `tests/packages/test_single_owner.py` from 2 to 6 tests and closes the precomputed-topology false negative — but it hardens the package-MECHANICS guard, not the IDENTITY guard (`tests/trade/test_suggestions_package_identity.py`), which it does not touch. Routed, NOT folded in: `_opponent_fit_label` (`:1107`, `:1125`) joins over a **set**, so `/api/trade/suggestions` is genuinely nondeterministic across processes (6 `opponentFit` fields differ across 3 teams at `PYTHONHASHSEED` 0/1/7) — proven pre-existing, byte-identical across `a3e7bdb82^..a3e7bdb82`, fixed by a one-line `sorted()`; consequence that travels is that every measurement of this endpoint is valid only seed-pinned. **PROMOTED `IN PROGRESS` → `VERIFIED` (L2) 2026-08-25 (Integration).** The sole remaining engineering blocker — the identity-guard bypass — is closed by `#1100` (squash `de492be54`), which rewrote `tests/trade/test_suggestions_package_identity.py` to assert ownership over executable structure: docstring-stripped AST dataflow (`OwnershipAnalysis.owner_derived`), an `ImportFrom(src.packages)`-plus-executable-symbol assertion, an every-`FunctionDef` scan (no name lists), quote-normalised needle matching, and a clean-interpreter import probe. **Independently re-proven fresh on merged `main` (`b5bacf402`), not trusted from the PR:** the Integration Authority self-performed the EXACT historical bypass — line 27's owner import deleted, `PackageAsset` / `adapt_assets` / `side_key` reimplemented verbatim from `src/packages/construction.py` with docstrings kept, module fully importable — and the guard went **4 RED** on precisely the ownership assertions (`test_there_is_a_real_executable_dependency_on_the_owner`, `test_every_identity_helper_returns_an_owner_derived_value[_identity_key]` and `[_side_identity]`, `test_importing_suggestions_imports_the_canonical_owner`) while the 25 functional tests stayed green — proving the guard, not collateral breakage, caught a WORKING bypass, which is the failure mode the retired prose guard waved through 17/17. Restored to an empty diff → 29/29 green. Guard B was already adjudicated closed above (2026-08-25); the routed `_opponent_fit_label` set-join nondeterminism has its fix up as `#1106` (one-line `sorted()` + a four-seed subprocess pin) — routed, and not part of this row's acceptance.

**Reconciled `#1048` 2026-08-22 (Integration) — still `IN PROGRESS`, not `VERIFIED`; the PR's own proposed shortcut is corrected here.** `#1048` adds the required L1 single-owner discovery guard (`tests/packages/test_single_owner.py`): an AST-shaped scan of `src/trade/` + `src/roster_intel/` for the owner's own signature (`itertools.combinations` co-occurring with an explicit `abs(...) <= N` topology bound) — no second combinatorial mechanics implementation exists outside `src/packages/construction.py`, with `roster_intel/packages.py`'s bespoke Pareto-frontier search and BDVM's independent-value-concept scan both documented exceptions rather than silent gaps. **Independently re-verified this pass**: `tests/packages/test_single_owner.py` 2/2 passed fresh; a fresh, self-performed mutation (added a real tracked decoy file, `src/trade/_decoy_mechanics_mutation.py`, replicating the owner's exact signature) sent the guard RED naming the decoy file, deleted and restored clean (`git status --short` empty); `tests/packages/ tests/roster_intel/ tests/trade/` — 1395 passed, 0 failed; `ruff format`/`ruff check` clean; `check_decision_coercions.py` clean. **The PR poses a real adjudication question and proposes an answer — the answer is wrong, corrected here rather than merged as-is.** Question: is the `roster_intel/packages.py` precedent (consume the owner's package-identity primitives, keep a bespoke selection algorithm) the accepted second form of consolidation? **Yes** — the manifest's own `C3-PKG-01` row already counts `roster_intel/packages.py` among the "3 of 4" consolidated generators on exactly that basis. The PR then claims `suggestions.py` "already qualifies via its existing `src.trade.constraints.blocked_outgoing` consumption" — **independently verified false by direct grep**: `roster_intel/packages.py` imports `PackageAsset`/`package_key` from `src.packages` (the package-mechanics owner, `C3-PKG-01`) at lines 368/379; `suggestions.py` has zero references to `PackageAsset`, `package_key`, or `src.packages` anywhere in the file. `blocked_outgoing` is imported from `src.trade.constraints` — a *different* canonical owner (`C3-CON-01`, recommendation constraints) — and consuming it says nothing about package-mechanics consolidation. Conflating the two owners is exactly the kind of "ONE CONCEPT, ONE CANONICAL OWNER" collapse CLAUDE.md warns against. **`suggestions.py` genuinely does not yet qualify under the accepted precedent.** The real, bounded remaining gap: wire `PackageAsset`/`package_key` from `src/packages/construction.py` into `suggestions.py`'s four `_generate_*` functions for package IDENTITY/representation only, without discarding their needs-driven heuristic search logic (weakest-starter selection, sweetener-tolerance widening, tightest-gap dedup) — mirroring `roster_intel/packages.py`'s own precedent exactly. That is real code work, not documentation, and not performed in this Integration pass. `V1-36` stays `IN PROGRESS`. |
| V1-37 | ONE Value Adjustment per runtime, parity proven | `C3-VA-01` / `C3-U2` / decision 70 | L2 | `VERIFIED` | L2 | named V1 scope. **PROMOTED 2026-08-21 (Integration, reconciling `#962` onto current `main`) — re-verified independently in this environment, not trusted from the PR body.** Re-measured at HEAD before trusting the row's stale "5 implementations, one via import-time monkeypatch" framing: 2 of those 5 (the finder's import-time monkeypatch — `src/trade/__init__.py` is now a documented-empty package, `finder.py` calls the adjusting scorer directly; a second Python port in `market_value_adjustment.py`) were already retired by an earlier, undocumented pass before this PR, confirmed by direct read. The one genuine survivor was a third, independently-maintained Python port in `src/public_league/trade_grading.py`, structurally forced to exist by the public/private import boundary (`trade_grading.py` may not import `src.trade` internals). This PR extracts the pure, stdlib-only algorithmic core to `src/valuation_math/ktc_va_core.py` (outside every forbidden import prefix); both `src/trade/ktc_va.py` (private, dataclass API) and `trade_grading.py` (public, dict API) now wrap that one core instead of maintaining separate derivations. **Independently re-verified, not merely re-read:** (1) `tests/public_league/test_trade_grade_parity.py` — 8 tests / 50 subtests, all passing, matching the PR's own stated count exactly, including `VaEngineParity` cases proving `trade_grading.py`'s wrapped output is unchanged; (2) `tests/valuation_math/test_single_owner.py` — 5/5 passing, an AST fingerprint scan over `src/trade/` + `src/public_league/` for KTC's exact magic-constant signature, with the scan's own positive controls re-run; (3) **an independent mutation performed fresh this pass** (not the test file's built-in positive control): appended a standalone reimplementation of `processV`'s exact fingerprint (`{1.3, 1.05, 6, 0.1, 0.15, 0.6}`) as a new function in `trade_grading.py` ⇒ RED, `test_no_second_python_implementation_of_ktc_value_adjustment`, correctly naming the offending file; reverted, confirmed 5/5 green and `git diff --stat` empty. JS side (`frontend/lib/trade-logic.js`) was already single-owner with the dead V2/V12/V13 exports removed (unrelated prior work), so this closes the row's full stated evidence bar: **one Python algorithmic core (two thin, boundary-forced wrappers), one JS port, parity-tested, monkeypatch deleted, dead exports removed.** Zero production/value impact: both parity fixtures (139-observation KTC fixture + the 30,000-random-package fixture referenced in `market_value_adjustment.py`'s own docstring) pass unmodified. |
| V1-38 | KTC VA stays an explicit labelled market lens | `C3-VA-02` | L2 | `VERIFIED` | L1 | truthful semantics; already complete |
| V1-39 | Roster capacity / forced-drop trade analysis | `C3-CAP-01` / `#843` | L2 | `VERIFIED` | L1 | named V1 scope. **VERIFIED 2026-08-20 at Integration.** Target level **L1**. `tests/trade/test_roster_capacity.py`: **50 passed, 0 skipped**. **Single resolver confirmed by reading both former duplicates, not by trusting the claim:** `src/draft/context.py::_roster_size_for` and `src/api/gameplan.py::_roster_limit` both now import and return `src.trade.roster_capacity.league_roster_limit`, so there is one answer to "what is this league's roster cap". **Two mutations on DIFFERENT rules, each confirmed APPLIED with the changed line quoted:** (MR-39a) `roster_capacity.py:332` `return None` → `return False`, i.e. an unknown cap answering "not over the limit" ⇒ RED `test_an_unknown_cap_is_unknown_and_never_unlimited` — UNKNOWN is not unlimited; (MR-39c) `:761` restoring the retired flat subtraction `size_before - len(outgoing)` in place of `- held_out` ⇒ **2 REDs**, `test_an_outgoing_player_the_roster_does_not_hold_frees_no_spot` and `test_a_partly_held_outgoing_multiset_frees_only_what_is_held` — the defect that hid roster pressure by freeing a spot that never existed. **A latent false-green is recorded rather than left to be discovered:** two `pytest.skip` calls survive at `:836` ("fixture produced fewer than two forced drops") and `:861` ("fixture forced no drops"). They do **not** fire today — the run reports 0 skips, so the forced-drop behaviour genuinely executes — but they are conditional on FIXTURE OUTPUT rather than on fixture availability, which is the shape that silently stops testing when a fixture drifts. Unlike the availability skips elsewhere in the suite, these would hide whether the behaviour ran. Worth converting to assertions when the file is next touched |
| V1-40 | Dropability / cut candidates consolidation | `C2-DROP-01` / `C2-U8` / inv 1.4 | L2 | `VERIFIED` | L2 | named V1 scope (forced drops); complete but not consolidated. **PROMOTED 2026-08-21.** `src/trade/waiver.py::find_drop_candidates` — a second, independent droppability computation (raw `rankDerivedValue` bottom-sort, no lineup guard, no waiver-value cost basis, no scarcity, exactly the `package delta - lowest raw player value` rule `C3-CAP-01` forbids by name) — had zero production callers (grepped against `server.py` and all of `src/`) and is deleted; `tests/roster_intel/test_droppability.py::test_no_independent_drop_candidate_selector_exists` is a vocabulary scan over `src/**/*.py` closing the concept to reintroduction under any matching name, mutation-proved (a tracked decoy second implementation turns it RED, removing it restores GREEN). **L2 measured, not argued at Integration:** a pinned-input `golden_board.py` before/after capture bracketing the deletion (identical CSV/export inputs — only `src/trade/waiver.py` differed) reports 1111 rows before and after, 0 values moved, 0 ranks changed, 0 newly priced/unpriced; `board_diff.py --expect-no-value-change` exit 0. **ZERO BY MEASUREMENT, reperformed 2026-08-21 on the reconciled shipping tree** (restored the pre-deletion `find_drop_candidates` from `main`, captured, restored the deletion, captured again; `inputSha256`/`sourceCsvSha256`/`freshnessSha256` identical both sides, confirming the pin held): identical result (1111/1111 rows, 0 values, 0 ranks) **plus** the two captures' top-level contract key sets are identical (`freshnessSha256, freshnessStampCount, inputExport, inputSha256, rows, scrapeTimestamp, sourceCsvCount, sourceCsvSha256, totals` — no field added or dropped) and a 50-row per-row field-key sample shows zero symmetric difference — no API/contract field changed. **Roster-intelligence and trade roster-capacity outputs directly measured, not left to the zero-callers argument alone** (Integration, 2026-08-21, on the fully reconciled tree): confirmed neither `src/roster_intel/droppability.py` nor `src/trade/roster_capacity.py` imports anything from `src/trade/waiver.py` (read both modules' import blocks directly), then ran the two real entry points against the live tracked contract (`tests/fixtures/golden/input_export.json.gz`, the same pinned input `golden_board.py` uses) bracketing the deletion exactly as above: `team_droppability(contract, owner_id=<real owner from the live league>)` and `assess_roster_capacity(build_capacity_context(...), outgoing_players=[<a real rostered player>])` — a genuine forced-drop simulation, not a synthetic stub — serialized to JSON before and after. **Byte-for-byte identical output both times** (`diff` reports zero difference on both captures). This is a literal measurement of Team Strength's and roster intelligence's and trade roster-capacity's real outputs, not an inference from an import graph. The "zero production callers, therefore inert" argument is now a measured zero on every axis the row's L2 bar asks for — board values/ranks, full contract field set, and the two actual downstream consumer functions' real output — not an inference from absence on any of them. |
| V1-41 | "Use Team Context" toggle, ON by default | `C3-CTX-01` / `#842` | L2 | `VERIFIED` | L1 | named V1 scope. **PROMOTED 2026-08-22 (Integration) — `#1043` merged (`6a1b0f69b`), shipping-tree L1 confirmation performed on current `main`.** Census before this PR found zero canonical team-context consumption anywhere in the trade surface: `finder.py`'s only roster-fit logic was a raw position **count** (`<= ROSTER_WEAK_THRESHOLD`), never the canonical Team Weakness owner (`src.roster_intel.weakness.build_team_weakness`, `C2-WEAK-01`), and there was no OFF path — nothing to gate. Scoped to `src/trade/finder.py::find_trades`: `use_team_context: bool = True`; ON resolves the requesting team's real `ownerId` and calls `src.api.roster_intelligence.build_league_roster_intelligence` — the same canonical entrypoint `/api/roster/intelligence` itself calls, not a re-derivation — to get that team's `TeamWeakness.needs`/`urgentPositions`; the "fills a need" fit-bonus arm now fires on `need.level in ("critical", "high")` instead of the raw count. OFF, or any owner/contract/computation failure, skips the whole block and fails closed (never a silent fallback to the retired count rule) — confirmed by direct code read of the `except Exception` branch, which sets `unavailableReason` rather than defaulting `applied` to `True`. `metadata.teamContext = {applied, urgentPositions, unavailableReason}` is stamped unconditionally, and `applied` / `off` (`unavailableReason: "context_off"`) / `unavailable` (owner/contract/computation failure, each with its own reason string) are three distinct, never-inferred states. `ROSTER_WEAK_THRESHOLD` is confirmed genuinely dead — declared but referenced nowhere else in the file, not merely deprioritized. **Independently re-verified this pass, not trusted from the PR body**: `tests/trade/test_finder_team_context.py` 5/5 passed fresh; a fresh, self-performed mutation (default `True` → `False`) sent `test_omitting_use_team_context_behaves_as_on` RED (`assert False is True`), restored clean (`git diff --stat` empty); exact-head CI on `#1043`'s feature head (`eca8d3e9c`) confirmed green (`Validate PR`, run `32532261611`, success) via a fresh API check, not reused from session history. Deliberately NOT done, matching the PR's own stated scope: `suggestions.py` is not wired (separate integration); Competitive Posture / playoff-bye-championship counterfactuals / pick-ownership forecasts are not wired (no canonical owner exists for them on `main`, and inventing one would be exactly the "second context methodology" this row forbids) — only positional need (Team Weakness) is wired, because that owner already exists and is already correct. |
| V1-42 | Exact before→apply→re-solve→after roster simulation | `C2-SIM-01` / `C2-U3` / inv 1.3 | L2 | `VERIFIED` | L2 | trade simulation, named V1 scope. **PROMOTED 2026-08-21 (Integration, reconciling `#962` onto current `main`).** The engine itself, `src/roster_intel/simulation.py`, is confirmed **pre-existing lane-1 work** (single commit `cec5c9904`, not part of this PR) — its own docstring opens "Exact before → apply → re-solve → after roster simulation (C2-SIM-01)", answering promotions/displacements/need-changes as roster information rather than a value subtraction, per owner decision 26 / inventory row 1.3. **The genuine gap this PR closes**: the live `/api/trade/simulate` endpoint never called the already-built, already-tested `src/trade/roster_capacity.py::simulate_final_legal_roster` composition — it reported only a forced-drop cost estimate, never which player, the re-solved lineup, or the Team Strength delta. Wired in as a new, additive `finalRosterSimulation` response field. **Re-verified independently in this environment**: `tests/trade/test_trade_consumes_roster.py` — 18 passed (P1-P9 structural properties: configured slot eligibility reaches the cut path, no duplicate lineup/roster-intel/cut-ladder ownership, before→apply→resolve→after is the actual order, a forced drop is never also retained, an unknown roster limit stays unknown, an unpriced forced drop reports null rather than zero); `tests/roster_intel/test_simulation.py` — 15 passed; `tests/api/test_trade_simulator.py -k final_roster` — 4 passed. **An independent RED-before/GREEN-after mutation performed fresh this pass** (not merely re-reading the PR's own claim): removed the entire `finalRosterSimulation` wiring block from `src/api/trade_simulator.py` ⇒ 3 of 4 targeted tests fail with `KeyError: 'finalRosterSimulation'`; restored, confirmed 4/4 green and the restored diff matches the PR's own stated shape (+59/-2 lines net on that file). Additive-only: no existing `rosterCapacity` field, forced-drop cost estimate, or canonical value changed. |
| V1-43 | Analyze Trade canonical recommendation (V1) | `C7-DESK-01` / `#792` | L2 | `VERIFIED` | L1 | named V1 scope, explicitly at V1 depth. No double-counting of overlapping signals. **PROMOTED 2026-08-22 (Integration) — `#1045` reconciled onto current `main` (only conflict was `docs/WORK_CLAIMS.md`, resolved by keeping both this and `V1-41`'s rows; code merged clean).** `src/trade/analyze_trade.py` composes two already-canonical, already-`VERIFIED` dimensions — equity (`src.trade.ktc_va.adjusted_pair_totals`, the same function `suggestions._va_gap` wraps) and roster marginal impact (V1-42's `finalRosterSimulation` Team Strength before/after delta) — into one `MAKE`/`LEAN_MAKE`/`TOO_CLOSE`/`LEAN_PASS`/`PASS` recommendation via an explicit rule table over each dimension's sign plus the equity dimension's existing canonical `_fairness_label` magnitude bucket — never a tuned numeric weight or a normalized sum of two differently-scaled quantities, per `docs/trade/TRADE_DECISION_SYNTHESIS_PLAN_2026-08-11.md`'s own governance rule against naive weighted averaging. New `POST /api/trade/analyze`, additive; `/api/trade/simulate`'s league/team resolution factored into a shared `_build_trade_simulation` helper (behavior-preserving) so Analyze Trade composes on the identical simulation rather than re-deriving it. **Dependency adjudication performed this pass, from current canonical owner/dependency records rather than assumed:** does V1-43 need to separately wire V1-41's newly-`VERIFIED` "Use Team Context" toggle? **No** — three independent lines of evidence converge: (1) `C7-DESK-01`'s declared `deps` in `docs/C_SERIES_SCOPE_MANIFEST.md` (line 338) are `C3-CALC-01`, `C2-SIM-01`, `C4-MTL-03` — `C3-CTX-01` is not among them; (2) the synthesis plan's own dimension-4 ("roster marginal impact") explicitly bundles "Team Strength / starting-group displacement / Team Weakness / roster construction" as ONE unique-information dimension, already wired via V1-42; (3) direct code inspection of `src/roster_intel/core.py` confirms the "meaningful population" Team Strength aggregates over is lineup-slot-aware (`assign_lineup`, the exact solver, over starters + reserves) — so the Strength delta structurally captures positional-need effects (filling a weak/empty slot produces a larger marginal delta than stacking a surplus position), meaning V1-41's finder-side Team Weakness fit-bonus and V1-42's roster marginal impact are two different-precision measurements of substantially the same underlying fact, not independent evidence. Wiring both would violate this row's own "No double-counting of overlapping signals" bar. **`C4-MTL-03` (market corroboration) legitimately remains explicitly unavailable, not a blocker**: it depends on entirely-absent `C4-MTL-01` (a whole new CE-01 real-market-trade ledger, its own spec doc, `IMPLEMENT` status) — genuinely "truly absent" infrastructure, not a bounded V1-depth gap. Confirmed via direct code read: `_UNAVAILABLE_DIMENSIONS` is a real, non-vacuous, named tuple stamping `marketCorroboration` (`reason: no_backing_ledger`) and `uncertainty` (`reason: unaudited_model`, Monte Carlo's open revalidation items per the plan's own §A) — genuinely reported, never silently dropped, never defaulted to neutral/zero. **Independently re-verified this pass, not trusted from the PR body:** `tests/trade/test_analyze_trade.py` 17/17 passed fresh, including the AST guard proving `_roster_impact_dimension` never calls `adjusted_pair_totals`; a fresh, self-performed mutation (injected an `adjusted_pair_totals` call into `_roster_impact_dimension`) sent `test_source_reads_no_shared_helper_between_dimensions` RED, restored clean (`git diff --stat` empty); broader regression `tests/trade/ tests/api/test_trade_simulator.py tests/api/test_trade_simulate_mc.py tests/roster_intel/` — 1389 passed, 0 failed, confirming the `_build_trade_simulation` refactor is behavior-preserving; `ruff format`/`ruff check` clean; `check_decision_coercions.py` clean, 660/660 unchanged; exact-head CI on the feature head (`900d702e8`) confirmed green (`Validate PR`, run `32529449613`, success) via a fresh API check. Deliberately NOT done, matching the PR's own stated scope: no frontend UI (backend contract only); no Monte Carlo dimension (unaudited per plan §A); no owner-constraint (LOCK/EXCLUDE) participation — Analyze Trade reads the manual, deliberately-unconstrained Trade Calculator surface, so `C3-CON-01` does not apply here by design. |
| V1-44 | Equalizers rank on the post-VA gap | `C3-EQ-01` / `#800` | L2 | `VERIFIED` | L1 | P1 live trade correctness. **VERIFIED 2026-08-20 at Integration.** Target level **L1**. The row's own concern was that `findBalancers` was **divergent in two runtimes**, so both were proven, not one: `tests/trade/test_balancer_uses_adjusted_gap.py` **24 pass** and `frontend/__tests__/trade-logic.test.js` **179 pass**. **Two mutations, each confirmed APPLIED with the changed line quoted, each reintroducing the EXACT retired #800 rule — ranking candidates by raw value against an adjusted target:** (MV1, Python) `suggestions.py:1474` scoring by `abs(value - abs(gap))` instead of re-running `_va_gap` ⇒ REDs including `test_chosen_balancer_is_the_best_available_from_the_pool[2-for-1]`, `[3-for-2]`, `test_published_residual_matches_a_recomputation`, and critically `test_overshoot_does_not_hand_the_lead_to_the_other_side` — the 701-flip half of the defect; (MV2, JS) `trade-logic.js:1317` `imbalanceAfter: after.imbalance` → `Math.abs((row.value \|\| 0) - Math.abs(gap))` ⇒ **5 REDs**, including one named for the defect itself (*"scores against the ADJUSTED gap, not the raw value (defect #800)"*), plus *"returns players that actually close the gap — which is not the one whose value matches it"*, *"never leaves the trade further apart than it started"*, *"picks the best available candidate, not merely a better one"* and the 3-team sweetener routing. So the invariant is enforced **independently in each runtime**, which is what the divergence concern required — a single-runtime proof would have left exactly the gap this row was opened on. Both restored; 24 and 179 pass |
| V1-45 | Trade calculator | inv 2.1 | L2 | `IMPLEMENTED_UNVERIFIED` | L4 | high-use surface; owner status "ALREADY COMPLETE — VERIFY ONLY". **Boundary ruling recorded 2026-08-25 (Integration adjudication from canonical records; NO status change):** this row's L4 clause binds the EXISTING `/trade` surface (inv 2.1) to consume the canonical implementation it already integrates with — it does **not** require `POST /api/trade/analyze` to gain a V1 frontend consumer. Grounds: (1) the contract's own bar for Analyze Trade is V1-43's required level **L1** — the owner set the V1 depth at the backend decision contract, not at a rendered surface; (2) §4.2 defers `CE-05` Trade Desk to lane continuation ("not V1-required"), `OWNER_FEATURE_INVENTORY.md` §12 gates CE-05 execution on CE-11, and `EXECUTION_PLAN.md` §6 lists Trade Desk on the do-not-opportunistically-begin list; (3) the calculator end-state that names Analyze Trade as an absent piece is `C3-CALC-01`'s TC-30 gate — C-Series maturity work, not this row's inv 2.1 scope; (4) inv §13.1's "`/trade` and CE-05 must consume the same decision contract" is a same-owner constraint on whoever builds a consumer (no second decision engine), not a V1 sequencing requirement. The demonstrated, measured L4 gap is **`finalRosterSimulation`** — V1-42's canonical block, stamped on the `/api/trade/simulate` response `/trade` already reads, with **0 frontend consumers** (`docs/trade/V1_45_TRADE_CALCULATOR_L4_EVIDENCE_RECIPE.md`, every fact independently re-verified on current `main` before #1082 merged at `aa8896fc7`) — the same gap class as this row's earlier `rosterCapacity` repair (#1025). **R1 routed to Claude 6**: display-only render mirroring the `rosterCapacity` block, preserving the three distinguishable states (`capacity_uncertain` range / `unavailable` error / populated simulation) so unknown never renders as healthy; no second simulation and no second analyze engine. Promotion requires: R1 merged; non-vacuous shipping-tree tests green; the deployment containing it identified; L4 observation of the real `/trade` surface consuming canonical truth with the three states visibly distinct. |
| V1-46 | Manual override UX: visually silent, one global reset | `C3-CALC-02` / `#781` | L6 | `VERIFIED` | L1 | binding owner UX requirement, decisions 3–7. **PROMOTED 2026-08-21** (merged `b78dd858e`, #1025). `docs/C_SERIES_SCOPE_MANIFEST.md`'s final state — "No per-player badge; top-level Reset Values; removal clears the override; canonical truth unchanged" — measured absent before this fix: the asset-value override `<input>` carried a conditional `overridden` CSS class (a per-player badge), and no top-level control cleared every override at once. Fixed: dropped the conditional class from `AssetRow` (`trade-sections.jsx`) and its now-dead `.overridden` CSS rule; added one "Reset Values" button to the trade-controls toolbar (`page.jsx`), disabled when nothing is overridden. Does not touch `customValue`-first resolution in `trade-logic.js` or any canonical field. Pinned by `frontend/__tests__/components/trade-value-override-ux.test.jsx` (3/3). Reperformed at Integration: restored the conditional `overridden` class ⇒ RED (1 of 3, the exact badge-absence assertion); reverted ⇒ GREEN, clean tree. |
| V1-47 | Multi-team (3+) trade modelling preserved | `BS-3TEAM` | L2 | `VERIFIED` | L1 | owner requirement: "must never be simplified away" |

### 3.4 Exact scoring and required current-season-model correctness

| # | capability | canonical id | lane | status | level | in V1 because |
|---|---|---|---|---|---|---|
| V1-48 | Realized scoring correctness | `F-SCORE-02` / inv 7.9 | L3 | `VERIFIED` | L2 | exact scoring. B7 |
| V1-49 | Individual special-teams scoring | `C5-ST-01` / `#802` | L3 | `IMPLEMENTED_UNVERIFIED` | L2 | **named explicitly** in the V1 boundary. **#915** — measuring `#802` found its headline already closed (B7/W18-F003 wired `kr_yd`/`pr_yd`/`st_td`, re-verified at 1,280 rows / 2,239.40 pts) and three larger defects underneath, incl. **safeties scoring a well-formed 0.000** in the canonical engine because `SAF` was in neither `POSITION_ALIASES` nor `_IDP_POSITIONS` — a **10,842.89-point engine defect** (2025 REG) that was **0.00 consumer-reachable**, because both consumers already normalized `SAF` in their own private position tables (which is the second-owner finding). The consumer-visible delta of the champion repairs is **218.12 pts / 0.167%**, all of it blocked kicks. **`#1031` merged 2026-08-21 (squash `2970cc07f`) — status corrected from `IN PROGRESS`, not promoted to `VERIFIED`.** Reconciled two prior sessions' disjoint audits (`#1020` vs `#1027`) rather than treating them as contradictory, then closed `#1020`'s residual: four Sleeper scoring keys with no canonical realized-scoring path. `punt_ret_td` — real, repaired: nflverse's weekly feed carries it as a bare column (`pt_return_tds`, confirmed against the live 2025 release header), added to `realized_points._SIMPLE_KEYS`. `kick_ret_td` — real, repaired: no weekly-feed column exists, so it's derived from raw PBP (`play_type=="kickoff"` + `return_touchdown`, excluding `own_kickoff_recovery_td`), mirroring the existing `pass_int_td` predicate. `idp_def_pr_td`/`idp_def_kr_td` — CLOSED as unsupported, not fabricated: Sleeper's own scoring UI has no IDP return-TD category, and no sampled 2025 host dump (~330 player-entries) ever carries either key nonzero, so there is no host-truth instance to validate a predicate against. Host-truth validated against real 2025 events: Charlie Jones (Sleeper `10228`) week 9 and Rashid Shaheed (`8676`) week 14 both reconcile exactly to `kick_ret_td=1` against the real PBP release; week 14's other two `st_td` scorers independently confirmed as punt returns on the same release, proving the identity `kick_ret_td + punt_ret_td == st_td` (the reconciliation test this repair pins, since Sleeper never publishes the split itself). L2: current-league point delta is provably **0** on both live leagues (neither `dynasty_main` nor `dynasty_new` configures any of the four keys — derivable from config alone, since `audit_scoring_settings` skips zero-rated keys); synthetic fully-configured-fixture delta is exact, **+6.0** per scored event. **Integration reconciliation** onto current `main` (base moved from `c68921d47` to `2970cc07f`'s pre-merge tip): one real conflict, `docs/WORK_CLAIMS.md`, resolved by keeping this branch's own claim row plus every row already on `main`. Re-verified post-merge: `tests/nfl_data/` 515 passed/1 skipped (matches the PR's own count exactly); `ruff format`/`ruff check` clean; `check_decision_coercions.py` clean, 660/660 unchanged; `check_planning_integrity.py` clean; `validate_api_contract.py --lane structural` `ok=True, 0 errors`. **NOT promoted to `VERIFIED` — merge does not verify.** Does not touch or resolve `host_native_scoring` (still `PRODUCTION_ONLY`, the PR's own count of 4 of 10 promotion-gate items remains open) or any position-normalization table (separate, prior-session finding). That checklist — run against a real deployment, not this environment — is what the row still needs before `VERIFIED`. **Production state MEASURED 2026-08-24 (§9a), and it changes the classification from "unexecuted" to "unexecutable as things stand":** public `/api/status.featureFlags` reports `host_native_scoring` as `{"enabled": false, "gateStatus": "LIVE"}` — **the gate is OFF in production**. A promotion checklist for a capability that is switched off cannot be executed against it, so this row is `STATE_BLOCKED` on a production configuration decision, not merely awaiting an engineer with a terminal. Enabling it is an owner action with its own blast radius and is **not** taken here; it is raised in the owner-decision packet (§9a) as the precondition. Recorded this way deliberately: "the checklist has not been run" and "the feature is not on" are different statements, and only the second one is true. **Packet C ANSWERED by owner 2026-08-25 — controlled production activation AUTHORIZED** (verbatim conditions: exact-head CI green, blast radius measured first, rollback ready, any unexpected scoring behavior triggers rollback, no scoring-methodology change). The row is therefore no longer `STATE_BLOCKED` on the configuration decision: the remaining work is the controlled enable + the 4 open promotion-gate items executed against the live deployment, under those conditions |
| V1-50 | ROS projections honest about what they are | `F-ROS-01` | L3 | `VERIFIED` | L1 | truthful semantics — `rosValue` precedent |
| V1-51 | One playoff-probability engine | `C5-PLAY-01` / inv 6.1 | L3 | `VERIFIED` | L2 | required current-season-model correctness. **VERIFIED 2026-08-20 at Integration on the DEPLOYED build.** Target level **L2**, and both halves hold. #956 (feature head `1bea81bca`, merge `4157adfe4`) gave `src/ros/championship.py`'s `if not distributions:` branch the `unsimulable` shape with `n_simulations: 0`. **L2 — measured on production, same league, same preseason state, before and after, 29 minutes apart:** BEFORE `2026-08-20T09:17:40Z` (build `b5f339d2d`) — `rosChampionship` served `championshipOdds: []` beside **`n_simulations: 10000`**, no `unsimulable`, `rosStrengthAvailable: true` and a fully resolved 7-team bracket: every signal saying ten thousand seasons were simulated and nobody won one. AFTER `2026-08-20T09:47:17Z` (deployed `cca488cd7`, which contains `4157adfe4` and `1bea81bca`) — `championshipOdds: []`, **`n_simulations: 0`**, and `unsimulable: {reason: no_scored_weeks_in_league}`. State identical across both captures: `season 2026`, `weeksPlayed 0`, `playoffSpots`/`playoffSeeds 7`, `byeSeeds 1`, `playoffStructure {7,1,15,league_settings}`, `rosStrengthAvailable true`. **All three endpoints now answer the same state with the same number and the same word:** `playoffOdds` `numSims 0` + `simulated false` + `no_scored_weeks_in_league`; `rosPlayoffOdds` `n_simulations 0`; `rosChampionship` `n_simulations 0` + `no_scored_weeks_in_league`. The shared reason string is the invariant `playoff_odds.py` states in its own comment — *two engines must not invent different words for one state* — and it is now held. **No methodology moved:** `playoffStructure`, `rosStrengthAvailable`, `bestBallVarianceMode: depth_aware` and `pointsModelSource: fallback-constants` are byte-identical before and after; only the count and the missing-state block changed. **DEPLOYED IS NOT VERIFIED, and this row proves it.** The deploy of the merge itself (`4157adfe4`, run `32352065385`, success 09:28:55) shipped the fixed CODE and production still served **`n_simulations: 10000`** for nineteen more minutes. `rosChampionship` is served from `data/ros/sims/latest_championship.json` under a 6 h TTL, and that deploy shipped its own tree's artifact — written 07:32:41 by the PRE-fix scrape — with a fresh mtime, so the deploy *extended* the stale contradiction rather than clearing it. Production only corrected when the 09:21 scheduled refresh regenerated the artifact under the new code (`computedAt 09:21:47.977371`) and deploy `32353505254` shipped it. A merge-plus-deploy check would have passed here while the defect was still live on the public API. **Residual, recorded rather than buried:** `rosPlayoffOdds` reports `n_simulations: 0` with `playoffOdds: []` but publishes no `unsimulable` block or `simulated` flag, so it is truthful and non-contradictory yet does not NAME the reason its two siblings now share. Pre-existing, unchanged by #956, and outside this row's stated close condition (which names `:215`). Whether that warrants its own row is an owner call |
| V1-52 | One weekly power-rankings engine | `C5-POW-01` / inv 6.2 | L3 | `IMPLEMENTED_UNVERIFIED` | L2 | required current-season-model correctness: 2 competing engines. **Item D CLOSED 2026-08-21 (`#996` squash `c8ee6fcba`, `#1009` squash `f92a465ed`) — status corrected from `IN PROGRESS`, not promoted to `VERIFIED`.** #979 (merge `6c235cb51`) had already shipped items A/B/C (one engine, two lenses; refuse-to-rank; both lenses reachable end-to-end). #996 redirected `overview.currentPowerLeader` off the legacy engine onto `power_v2` (additively surfacing `record`/`teamName`, the two fields the landing card needed and the canonical engine didn't yet publish). #1009 closed item D fully: `src/public_league/power.py`, `frontend/app/league/sections/power.jsx`, `tests/public_league/test_power.py`, `_SECTION_BUILDERS["power"]` and the three-layer `useRosPowerRankings` toggle are all DELETED, not deprecated — confirmed by direct grep post-merge, not assumed from the PR body. `LeagueClient.jsx` now renders the canonical `RosPowerSection` unconditionally for the Power tab (one reachable consumer). `components.pointsPerGame`/`components.recentAvg` (the two remaining "genuinely unrecoverable" raw magnitudes #979's own body claimed were lost — re-derived as false: `_score_state` already computes them as an intermediate before converting to the percentiles it publishes) are now surfaced on every row, display-only, excluded from `WEIGHTS` by construction so `powerScore` cannot move. The Playoff Odds Monte Carlo card (the only other live consumer `power.jsx` carried) was ported unchanged onto `ros-power.jsx`, still reading `/api/public/league/playoffOdds` (existing v1 source) — the dormant `rosPlayoffOdds` lazy section is untouched, not activated. `weekRankDelta` stays permanently absent under any name — no per-week `team_ros_strength` history exists to diff against, and `ros-power.jsx`'s pre-existing `trendDelta` already answers the same question honestly (`null` on missing/unrankable, never a fabricated `0`). Historical trend weeks stay results-only by construction and are labelled as such when browsed, never silently switched to a forward-looking value that was never observed at that week. **Integration reconciliation for both PRs** (#996 onto post-#1032 `main` for the season-scoping fix; #1009 onto post-#996 `main` for the same fix plus #992's independent `public_contract.py`/`overview.py` changes) verified rather than trusted: `tests/ros/`+`tests/public_league/` 650 passed/1 skipped, frontend vitest 2321/2321, `next build --webpack` clean with 14/14 bundle budgets under limit, ruff/coercion/planning-integrity/structural-contract all clean. **NOT promoted to `VERIFIED` — merge does not verify.** The stated L2 bar is production-scale 12-owner lens-agreement per `docs/power/V1_52_L2_VERIFICATION_RECIPE.md`, which needs the gitignored `data/ros/team_strength/latest.json` for a real league; that recipe has not been run from this environment. **`all_play` RESIDUAL ADJUDICATED 2026-08-25 — INSIDE this row's acceptance; ONE defect returned to Claude 3.** `#1081` states honestly that it pins `all_play`'s *reset* but does not claim its *missing-value* semantics. Determination, from the code rather than the claim: `src/ros/power_v2.py:369` computes `recent` as `sum(rb)/len(rb) if rb else None` under the comment *"UNMEASURED IS None, NOT 0.0 (owner invariant: missing != zero)"*, and **four lines later `:373` reads `all_play = state["allplay"].get(oid, 0.0)`** — a per-owner placeholder, in the same loop, for a component carrying weight **0.08** in `WEIGHTS`. It is INSIDE the acceptance because (a) this row's L2 bar is production-scale 12-owner **lens agreement** and `all_play` is a weighted term of the `powerScore` both lenses publish, so a placeholder moves the very quantity the bar compares; (b) the machinery to do it correctly already exists and is used one line above — `None` propagates to `missing_inputs`, the weight is renormalised away, and `_percentile` excludes it from the comparison population (`eligible`), so an unmeasured owner cannot drag the scale; (c) the triggering state is **the one `#1081` itself names** — an owner absent from the last scored season, whose `season_state` correctly goes empty. The PR replaced that owner's *stale prior-season share* with a *zero share*, which is a different wrong number, not a right one: `all_play` is an expected share on [0,1], so `0.0` scores a missing owner as the worst all-play performer in the league rather than dropping them from the weighting. **Not a merge blocker** — the defect is pre-existing and `#1081` strictly improves on it (`recent` was the larger leak, 12/12 owners at the 0.5 midpoint); holding a real repair hostage to an adjacent one would be the wrong call. Returned as ONE exact defect: *`power_v2.py:373` must use `None` + the existing renormalisation path, exactly as `:369` does, and the existing `test_all_play_also_survives_the_scoreless_season` must be strengthened — it passes under `#1081`'s own mutation and is therefore a regression guard, not a proof* |

**Recipe executed 2026-08-21 (Integration) against the real deployed `chaseupside.com` — 7 of 8 sections hold, 1 is genuinely inapplicable this cycle, not failed.** `data/ros/team_strength/latest.json` is confirmed absent from `main` (not merely gitignored-and-present locally), so this ran against the live public API rather than a local file, per the recipe's own §0 fallback. Sections 1, 2, 4, 5, 6, 7, 8 all passed verbatim against production: both canonical lenses reachable, the real 12-owner census present and every manager truthfully represented, unrankable-team refusal reasons correct (not fabricated), production-scale timing recorded, no legacy power engine reachable anywhere (confirmed live, matching the code-level grep already in this row), no `weekRankDelta` fabricated on either surface, and Playoff Odds presentation preserved unchanged. **Section 3 (rank agreement/disagreement) could not be executed**: the real league is presently in its natural preseason `unrankable`/refusal state — `n_simulations: 0`, no forward-looking scored input yet — so neither lens publishes numeric ranks to compare. This is a season-timing condition, not a code defect, and it is itself corroborating evidence that Section 4's refuse-rather-than-fabricate contract is doing its job. Per standing instruction, an inapplicable check is recorded as a causal gap rather than silently counted as passed: **V1-52 is NOT promoted to `VERIFIED` this pass.** Re-run Section 3 once the season produces real scored weeks and ranks exist on both lenses to compare; every other section of the L2 bar is already satisfied against real production. |
| V1-53 | Redraft / ROS seasonal lane stays separate from dynasty valuation | `C5-ROS-01` | L3 | `VERIFIED` | L1 | canonical value — source-domain boundary. **VERIFIED 2026-08-20 at Integration on merged `main` (#964, merge `a13615bad`).** Target level **L1**, and the manifest's stated evidence for `C5-ROS-01` is literally a **lane-separation test** — that is what is now in place, mutation-proven, and it covers **both directions** of the boundary. **Direction 1 — the seasonal lane cannot mutate the dynasty path by import side effect:** pre-existing, `tests/ros/test_isolation.py::TestRosIsolation` digests `_RANKING_SOURCES` and the dynasty path before and after loading every `src.ros` module. **Direction 2 — no seasonal module writes canonical dynasty value:** #964 widened `_CANONICAL_WRITE` from `rankDerivedValue` alone to the **three proven-identical aliases** `overall` / `finalAdjusted` / `displayValue`, which is the gap that mattered: the previous scan matched the canonical name only, so a module assigning `values["displayValue"]` passed silently while writing the same quantity. **Mutations re-performed at Integration on the RECONCILED tree (post-#954), not merely at the feature head:** (MG1) a **tracked** module under `src/ros/` assigning `values["displayValue"]` ⇒ **2 REDs** — `test_only_approved_modules_assign_the_canonical_value_field` and the new `test_no_seasonal_lane_module_assigns_a_canonical_alias`; restored, 20 pass. (MG2) `frontend/lib/ros-data.js` assigning `row.displayValue` ⇒ **RED**, which proves coverage is **not** confined to `src/ros/` — the whole-repo scan applies the same widened pattern with only two approved writers (`data_contract.py`, `overlay.py`), neither seasonal. A third probe was **discarded rather than counted**: an **untracked** file passed, because `_production_sources()` reads `git ls-files` and cannot see one — a pass over an inapplicable mutation proves nothing. **No product change:** the diff touches **zero** files under `src/` or `frontend/`, so methodology and value invariance are structural rather than measured. 222 passed / 1 skipped across the ownership + full ROS suites on the merge tree. **Named residual, recorded rather than implied:** the guard is a static scan of *assignment syntax*, so it catches subscript and attribute writes in Python and JS but **not** indirect construction — a dict literal `{"displayValue": v}`, `dict.update(...)`, or `setattr(row, "displayValue", v)`. That limitation is **inherited, not introduced**: it applied identically to the pre-existing `rankDerivedValue` guard, and #964 strictly widens coverage (4 field names for 1) using the same mechanism. It is a candidate for a later strengthening unit, not a V1-53 blocker — the row's stated evidence bar is a lane-separation test, and one exists, is non-vacuous, and is broader than it was |
| V1-54 | BDVM fundamentals stay a separate concept | `C5-BDVM-01` / inv 8.1 | L3 | `VERIFIED` | L1 | canonical value separation; complete for declared scope |

### 3.5 Trustworthy FAAB and Sharp core

| # | capability | canonical id | lane | status | level | in V1 because |
|---|---|---|---|---|---|---|
| V1-55 | One FAAB engine (ceiling vs recommended bid) | `F-FAAB-01` / inv 3.1 | L4 | `VERIFIED` | L2 | FAAB core. 247 tests; backtest 166/166 → 0/166 low-value overbids |
| V1-56 | FAAB league context panel | inv 3.2 | L4 | `VERIFIED` | L4 | FAAB core; owner status "VERIFY ONLY". **VERIFIED 2026-08-27.** Backend (`faab_analytics.py`) and frontend (`ManualAddDrop.jsx`, `FaabRecommendation.jsx`) were already correct; the gap was `scripts/verify_v1_authenticated.py::check_v56` reading a `"faabAnalytics"` key that never exists in the real `{contractVersion, league, section, data}` envelope (same defect class as historical finding W11-F006). Fixed and merged `#1150` (`935ba4f`→`b194316f`, deployed via `Deploy Production` run `33066619784`, conclusion `success`). Fresh authenticated production evidence, run `33098047781` (`v1-authenticated-verification.yml`, 2026-08-27T17:24:19Z, deployed SHA `b194316f0f1cd76e5eca4e0d73b20031a1d5f6cb`): `GET /api/public/league/faabAnalytics` with a real guest_pass session → `leagueMedianWinningBid=0.0` (a real typed number, not absent — the zero-bid-inclusive figure), alongside `leagueAvgWinningBid`, `budgetNormalization`, `positionBids`, `tierBids`, `teamAggression`, `recentWins`, `playerHistory` all present. This is the L4 recipe: real deployed endpoint, real session, the exact field the row exists to guarantee is served correctly. **Noted, not blocking**: the same 2026-08-27T18:26-18:31Z verification run (`33103452227`) found `tests/e2e/specs/prod-auth/v1-56-waivers-faab-strip.spec.js` failing on the deployed `/waivers` page — but the cause is the TEST never selecting a team before asserting on the FAAB stat tiles, and `FaabHeaderStat` (`ManualAddDrop.jsx:423`) is deliberately gated behind `selectedTeam` (it also renders "Your FAAB", a team-specific figure, alongside the league-wide average/median). This is a test-completeness gap, not a value-correctness defect — the backend evidence above is what this row's requirement actually guarantees. Follow-up: give the test a team-selection step. |
| V1-57 | FAAB bid-history collection is scheduled, not a manual step | `C4-FAAB-02` | L4 | `VERIFIED` | L3 | **#911 merged 2026-08-19 (`edc726ef1`).** The scheduler exists: `deploy/systemd/dynasty-faab-history.{service,timer}.template` plus the `install_simple_timer "faab-history"` line, verified in the diff and verified to match the installer's own naming convention (`dynasty-${stem}.{service,timer}.template`, unit `${SERVICE_NAME}-${stem}`). Fires 07:40 UTC, clear of the 2-hourly scrape, the crowd-FAAB pass and the sharp discovery/records/roster chain. **L3 needs the unit installed and firing on prod** — not claimable from here. **RE-TESTED 2026-08-25 (§9b): `BLOCKED-EXTERNAL`, unobserved rather than failed.** `systemctl` is unavailable in this environment (*System has not been booted with systemd as init system*), so the verifier's `V57`/`V57b` checks report `blocked`. Whether `dynasty-faab-history.timer` is installed and has fired in production is **not known**; the templates are present in `deploy/systemd/`. **PROMOTED → `VERIFIED` (L3) 2026-08-25 (Integration), Lane 4 on-box run `32843495391` (2026-08-25, deployed `8537aa4c2`, read from the step log and report JSON):** `V57` PASS — the timer is installed and FIRED 4h10m before the run (last 2026-08-25 09:42:30 CEST, next 2026-08-26 09:46:01); `V57b` PASS — `data/faab/bid_history_dynasty_main.json` written 4.2h before the run at the deployed path. The unit is installed and firing on prod, which is this row's stated L3 verbatim |
| V1-132 | The horizon pick year is not a single-vendor dependency | audit `F-34` | L5 | `VERIFIED` | L2 | **added 2026-08-18** — tracked DEFECT against already-required canonical value + signal independence, not new product scope. Measured: 2026/2027/2028 tier rows blend `idpTradeCalc` + `ktcSfTep`; the horizon year blends `idpTradeCalc` **alone** on all 12 cells, because the injection clones from the RAW payload while `ktcSfTep` pick values arrive via the later CSV enrichment. `F-30` made the horizon *guarantee* independent of this; the blended *value* still is not. **PROMOTED → `VERIFIED` (L2) 2026-08-25 (Integration) — `#1118` merged with the row's board measurement.** The horizon year blended `idpTradeCalc` alone on all 12 tier cells because the injection runs against the RAW payload, before `ktcSfTep`'s CSV enrichment; a post-enrichment completion pass now extends the SAME derivation (same `derivedYearModel` cell step, same compounding, `derived_year_step` provenance, missing stays missing, never re-stepped) to the enriched evidence. Measured on pinned inputs: all 12 horizon cells gain exactly `ktcSfTep`; 24 `pick:2029` rows move −4.3%..+3.5%; 2027/2028 byte-identical; IDP players zero; ranked 740→740. The named coupling — 17 offense rookies (p50 −0.15%, max −0.78%) + 3 tethered 2026 slot picks (−0.11%) via the rookie ladder's ordinal pool — was ADJUDICATED as the KTC-side mirror of the coupling `C1_U6` §8 already accepted on the IDPTC side at larger magnitude (max −1.3%); §8 is amended with these numbers and §12's translation-pool-exclusion question stays OPEN, now with symmetric evidence. The implementing pass STOPPED on the wrong-shaped diff per its instructions and the acceptance was decided at Integration, on the records. A fabricated-basisYear coercion the CI gate caught was replaced with a refusal (missing basis → skip, never `or 0`) |
| V1-129 | External crowd-FAAB evidence is comparable, fresh and position-capable | audit `F-33` / `FAAB_MARKET_SIGNAL_NORMALIZATION_2026-08-14` §3/§5/§7/§10 | L4 | `VERIFIED` | L2 | **#911 merged 2026-08-19.** `src/trade/faab_comparability.py` is the single owner, called at fetch AND read time; multi-copy and degenerate-budget leagues excluded; IDP refused against an offense-only population; stale ledgers refused. Adversarial review confirmed the headline invariant across 30 (crowd pct × value) combinations with every leaf key diffed, and `faab_engine.py` has a zero-byte diff. **L2 needs the crowd ledger, which is prod-only (gitignored)** — and see the deploy note: the accumulated legacy ledger hard-excludes on read, so `crowdMarket` reports `missing` until the fetcher re-accumulates. **FIVE CHECKS RE-ATTEMPTED 2026-08-25 (§9b) — all five `BLOCKED-EXTERNAL`, none simulated.** 129a/129b need the real `data/faab/crowd_history_dynasty_main.json`, absent here (verifier `C7`/`C10` = `blocked`); 129c needs an authenticated `POST /api/waiver/faab-recommend` with a real rostered IDP player; 129d must read the real current ledger and **no file was artificially aged** to manufacture a stale state; 129e needs the live `crowdMarket.targetFormatUnknown`. Separately corrected in this pass: `CLAUDE.md:1357` named `/api/faab/recommend`, which **does not exist** — the only registered route is `/api/waiver/faab-recommend` (`server.py:5770`), and a verifier following the doc would have chased a 404 and recorded a false negative. **PROMOTED → `VERIFIED` (L2) 2026-08-25 (Integration), Lane 4 on-box run `32843495391` (2026-08-25, deployed `8537aa4c2`, read from the step log and report JSON) — the prod-only crowd ledger this row's L2 named is now measured.** `C4` PASS: the target TEP derives from the FRESH scoring card and the card DISAGREES with the label (card tep=false, edges `bonus_rec_te -0.02` / `bonus_fd_te 0.0`, vs the `superflex_tep15_ppr1` label — the served rule is `card`, live). `C7` PASS: over the real 1,027-row crowd ledger the retired label rule admitted 133 comparable rows, the card-derived rule admits **146** (exclusion census live: `budget_unknown` 748, `roster_exclusivity_unknown` 748, `tep_mismatch` 881), evidence state `fresh`. `C10` PASS: the offense-only population REFUSES DL/LB/DB while pricing QB/RB/WR/TE over the 146 retained rows. `C6` PASS. **Residuals named, not smoothed:** `C5` is honestly UNMEASURABLE (the fresh card means the stale-evidence branch did not execute, and manufacturing a stale card is forbidden — the fail-closed branch stays L1-pinned by its tests); `C8`/`C9` remain session-gated (they read `server.py`'s assembled response; unlocked by the `PROD_VERIFY_GUEST_PASS` grant, §9b) — both are endpoint-assembly reads whose logic is L1-pinned by `tests/trade/test_faab_crowd.py` |
| V1-58 | Sharp cohort proven populated in production | `C4-SHARP-01` / `C4-U2` | L4 | `VERIFIED` | L3 | Sharp core. Verification artifacts end at 502/401/"unverifiable_unauthenticated". **RE-TESTED 2026-08-25 (§9b) and the blocker is worse than "no session for Claude":** `data/ops/sharp-production-smoke.json` — written by `verify-sharp-production.yml` against real production, committed at `a55d1eaf0` — reads `status: "unverifiable_unauthenticated"`, `measured: false`, `cohort: {}`, `lastError: 401 from https://chaseupside.com/api/sharp/cohort`, and **`lastMeasuredOn: null`**. That null is load-bearing: the standing production-verification workflow has **never once measured**. `BLOCKED-EXTERNAL`. **PROMOTED → `VERIFIED` (L3) 2026-08-25 (Integration), Lane 4 on-box run `32843495391` (2026-08-25, deployed `8537aa4c2`, read from the step log and report JSON):** `V58` PASS — the cohort resolves ON THE PRODUCTION STORES to **2,389 members** (2,362 sleeper `automated_qualified` + 27 ffpc `provisional_public`; methodology `sharp-v2.1`; evidenceManagers 45,159), through the ONE canonical owner (`cohort_members`) with the identical call `market_payload` makes. **Adjudication, recorded:** in-process-on-box is the same L3 grade this ledger already accepted for `V1-63` (the verifier's C1–C3); the row's question — populated in production? — is answered on the deployed stores, and the HTTPS read variant adds only serialization+auth, both separately verified (route registered per R0; the 401 gate measured 80/80). The prior blocker record stands corrected in one respect: `verify-sharp-production.yml` still has never measured (its `lastMeasuredOn: null` remains true), but the Lane 4 instrument now HAS measured, which is what the row needed |
| V1-59 | Sharp bootstrap stops failing | `C4-SHARP-02` | L4 | `VERIFIED` | L3 | Sharp core. FFPC timeouts + SQLite locking. **DEFECT NAMED 2026-08-25 (§9b) — still `IN PROGRESS`, not promoted.** Measured from real production journald via `Bootstrap Sharp Records` run **32813417583** (job `97697004287`, head `a9e1c1de2`), step *"Install units and populate the Sharp cohort"*: **failure after 30 m 11 s**. Two of the four candidate causes are present and one is upstream of the other. (1) `chaseupside-ffpc-sharp.service` **crashloops on a 30-minute `TimeoutStartSec`** — repeated `start operation timed out. Terminating.` → `code=killed, status=15/TERM` → `Failed with result 'timeout'`, restarting immediately, each cycle reporting `Consumed 29 min 25-45 s CPU time`. CPU rather than wall-clock means it is **spinning, not blocked on a slow remote read**. (2) When it does reach work it dies on `sqlite3.OperationalError: database is locked` at `src/intel/platform_ledger.py:1219` (`register_asset_alias`), via `hydrate_sleeper_asset_catalog` (`:1820`) from `scripts/crawl_ffpc_sharp.py:111`. (3) **The error path dies on the same lock** — `could not persist FFPC failure report`, `record_ingestion_run` (`:1862`) raising `database is locked` while handling (2) — so the service cannot record its own failure, which is why the ledger shows nothing rather than a failed run. Not a blip: of the 20 most recent runs every non-skipped one is a failure, back to 2026-08-22. Explicitly NOT promoted on the grounds that other Sharp surfaces look healthy — they return 401, so nothing about them is observed at all. **Run-4 note (2026-08-25, run `32843495391`):** `B59` remains blocked — the row still needs a clean scheduled journalctl run of the discovery → records → rosters chain on the box. Both repairs are now merged AND deployed ahead of it: `#1102` (ingestion index + busy_timeout + pre-claimed running row) and `#1113` (the records crawl commits per season instead of holding one write transaction across its whole network budget — the other half of the 05:20 lock collision). The next scheduled bootstrap is the evidence **PROMOTED 2026-08-26 on the naturally scheduled production-chain evidence already preserved in `docs/ops/V1_59_SCHEDULED_CHAIN_PROOF_2026-08-26.md` (#1144). The production proof is bound to deployed `c30b8d6ba1ef2fa604ecb1cfe1e44dd6130ede44`: discovery, records, and rosters ran in scheduled order (`discovery < records < rosters`); the systemd units accepted only the documented success semantics (`SuccessExitStatus=0 2`); the named crash/lock/watchdog signatures were absent in the captured slices; FFPC remained successful with zero restarts; and the verifier emitted `V1_59_SERVICE_CHAIN_PROOF=PASS`. Upstream 404/read-timeout warnings and resumable budget exhaustion remain visible rather than being relabelled healthy.** |
| V1-60 | FFPC roster lane real or honestly empty | `C4-SHARP-03` | L4 | `VERIFIED` | L2 | **#911 merged 2026-08-19.** `CollectResult` gains `status` / `unavailable_reason` with an explicit `unavailable()` constructor, and the FFPC lane returns `no_cohort_managers_on_platform` instead of an empty success — so "the lane ran and found nothing" and "the lane did not run" are structurally distinct and both published. Verified in the diff. **L2 board measurement is prod-side** (FFPC contributes zero rosters today). **RE-TESTED 2026-08-25 (§9b): `BLOCKED-EXTERNAL` — and the state is `unavailable`, NOT honestly empty.** A truthful `no_cohort_managers_on_platform` may satisfy this row only when measured on the deployed real population. It cannot be claimed: per `V1-59`'s measured defect the FFPC ingestion service never completes a run, so the lane did not run. Coercing that to "empty" is the exact defect `#911` built `CollectResult.unavailable()` to prevent. **PROMOTED → `VERIFIED` (L2) 2026-08-25 (Integration), Lane 4 on-box run `32843495391` (2026-08-25, deployed `8537aa4c2`, read from the step log and report JSON):** `V60` PASS on the deployed real population — cohort 2,389 managers, 9,637 eligible rosters, `ffpcRosters: 0` with `collectResultCarriesStatus: true` and `boardStatus: ok`. The zero is HONEST and structurally distinguishable from a lane that did not run — exactly what `#911` built `CollectResult` to guarantee, now observed live. Nuance recorded rather than smoothed: 27 FFPC cohort MEMBERS exist, so the zero rosters is the documented roster-bearing-URL gap (`docs/sharp-roster-percentage/METHODOLOGY.md`), truthfully reported — not `no_cohort_managers_on_platform` and not a coerced empty |
| V1-61 | Sharp Roster Percentage | inv 4.5 | L4 | `IMPLEMENTED_UNVERIFIED` | L4 | Sharp core; owner status "VERIFY ONLY". **STALE CLAIM CORRECTED 2026-08-27**: the 2026-08-25 §9b note above ("`BLOCKED-EXTERNAL`, 401 unauthenticated, no session mintable") no longer reflects the mechanism — an ephemeral guest_pass session is now mintable via `v1-authenticated-verification.yml`'s on-box SSH path, and `GET /api/sharp/roster-percentage` does **not** require a session at all (no auth gate in `server.py::get_sharp_roster_percentage`). The real blocker is different and ENGINEERING-typed: two independent runs against the current deployed SHA (`32919648642` attempt 6 2026-08-27T14:36-14:42Z, and `33098047781` 2026-08-27T17:22-17:28Z, both post-`#1150` deploy) both hit a genuine **>60s server-side timeout** (`TimeoutError: The read operation timed out`) on this exact endpoint — `check_v61` never got a response. **`#1150`'s V1-61 fix did NOT target this endpoint**: it removed a redundant `platform_records.build_manager_records()` call from `src/sharp/service.py::cohort_status()`, which backs `/api/sharp/cohort` — a different function. The verifier's actual target, `/api/sharp/roster-percentage` → `src/sharp/service.py::roster_percentage_payload()` → `src/sharp/roster_percentage.py::build_board()`, has no caching/memoization on its own expensive calls (`roster_store.load_rosters()`, `_buy_sell_index()` over 4 baseline windows, `_trend()`) and was not touched by that PR. Needs its own profiling + fix as a separate follow-up; not authority-blocked, not re-attempted here this session for lack of remaining budget. |
| V1-62 | Sharp Tracker | inv 4.4 | L4 | `IMPLEMENTED_UNVERIFIED` | L4 | Sharp core. **W15-F017 CLOSED at the code level 2026-08-25 (#1129, `e378550e0`)**: `cohort_members()` memoized behind a correctness-preserving cache — key `(qualification, resolved_ledger_path, ffpc_config_signal)` guarded by a `(mtime_ns, size)` fingerprint of the platform ledger (the one per-trade-moving input; the curated tables live in the same db), so a stale cohort is structurally unservable: a changed ledger forces recompute, mutation-proved (stripping the fingerprint check turns the anti-stale test RED). Selection/qualification/dedup untouched — `cohort.py` stays the single owner. 307 sharp tests green. What L4 still needs: the deployed user-facing Sharp Tracker consuming it with truthful semantics — the authenticated-verification pass |
| V1-63 | Manager-level Sharp concentration | inv 4.6 | L4 | `VERIFIED` | L1 | Sharp core; the denominator counts rosters, not people. **VERIFIED 2026-08-20 at Integration on merged `main` (#977, merge `ef41523a8`).** Target level **L1**, and that is what this rests on — deterministic RED→GREEN at the exact merged head, nothing inferred from CI or from the merge. The blocker was the one the previous note named precisely, and it was NOT the defect the row's title suggests: #927 (`df93e5ac1`) had already fixed `personManagerQuality` returning `1.0` for zero voters, but that lives in `src/sharp/market.py` (the Buy/Sell Tracker), while `docs/OWNER_FEATURE_INVENTORY.md:132` (inv 4.6 / `W15-F009`) names `src/sharp/roster_percentage.py` — the board where one manager's five teams could render as an 83% sharp roster percentage with nothing on the row saying it is one human. That surface now emits `distinctManagers` and `managerConcentration` per player, and `formatSample` shows the manager count only when it differs from the roster count, so the rosters-not-people rule is visible where the percentage is read. **Mutation-verified at Integration by execution, not accepted on the lane's report:** baseline `tests/sharp/test_roster_percentage.py` 45/45 at head `4dacf1c40`; **MG1** replacing `max(manager_counts.values()) / len(counted)` with `1 / len(counted)` (concentration blind to manager identity) ⇒ RED, 2 failed / 43 passed, including `test_manager_concentration_flags_one_person_behind_several_rosters`; **MG2** replacing `len(manager_counts)` with `len(counted)` (distinctManagers reporting rosters) ⇒ RED, the same 2 tests; restore ⇒ 45/45, clean tree. Non-vacuous in both directions. The lane's "`counted` is never empty" claim was checked rather than trusted, because `max()` over an empty Counter and a zero divisor would both be live faults: `holders` comes from `_tally(filtered)` and `roster_keys` from that same `filtered` list, so `held_by ⊆ roster_keys` by construction, `counted == held_by`, and `roster_manager` is total over it — no missing-manager state to coerce. Cohort qualification, scoring and weighting are untouched, and no canonical value surface is involved. |
| V1-64 | Sharp event ledger surfaces adds/drops | inv 4.7 | L4 | `VERIFIED` | L1 | **VERIFIED 2026-08-20 on merged `main` (#936, merge `5bdee44db`).** Target level is **L1** — deterministic evidence — and it is now satisfied on BOTH producers, which is what the row was missing. #911 confirmed the behaviour existed; #936 pins it. `tests/sharp/test_record_queue.py` + `tests/sharp/test_transactions.py`: **27 passed** on the shipping tree. **Four mutations re-performed at Integration, each confirmed APPLIED before being counted:** `record_queue.py:110` `min`→`max` ⇒ RED `test_oldest_crawl_is_the_minimum_not_the_most_recent`; `record_queue.py:110` `None`→`0` ⇒ RED `test_no_crawled_league_publishes_none_never_zero`; `transactions.py` SQL `MIN(`→`MAX(` ⇒ RED (minimum test); `transactions.py:363` `else None`→`else 0` ⇒ RED (never-zero test). Restored: 27 pass. So `oldestCrawlMs` is provably the MINIMUM, and provably `None` rather than `0` when nothing was crawled — missing is not zero, on both code paths. A fifth attempt (Python `min(` in `transactions.py`) matched nothing because that producer aggregates in SQL; it applied no change and is NOT counted |
| V1-65 | Insider Trading / cross-league ownership | `C4-INS-01` / inv 4.8 | L4 | `VERIFIED` | L2 | **#911 merged 2026-08-19. L1 COMPLETE and mutation-proven at Integration 2026-08-20; L2 UNMEASURABLE in this environment — NOT promoted.** The consolidation already existed (ledger / signals / two products); what was missing was the guarantee. `tests/intel/test_insider_never_claims_sharp.py` AST-scans the three insider modules for non-docstring "sharp" literals, with a positive control so it cannot pass by matching nothing and a negative control so docstrings stay allowed. 5 pass. **Mutation MI1, confirmed APPLIED with the changed line quoted:** appending a real emitted literal `_MUTATION_LABEL = "sharp manager signal"` to `src/intel/leads.py` ⇒ RED `test_the_insider_surface_emits_no_sharp_vocabulary[src/intel/leads.py]`, naming the offending module and line. So the scan is a genuine guard over the real tree, not only over its own fixture. The boundary itself is real in code: Sharp is a SKILL claim (`SHARP_ELIGIBLE_TYPES = {2}` — dynasty only — plus `SHARP_MIN_LEAGUE_AGE_SEASONS = 2`); Insider admits `ELIGIBLE_TYPES = {1, 2}` — keeper AND dynasty — with no age floor. **Why L2 is not satisfied:** §2 requires a measured statement of the effect on the live board or contract, and the population statement this row needs (how many leagues each filter actually admits) cannot be made here — `data/intel/ledger.sqlite3` is present but **schema-only**: `leagues`, `platform_leagues`, `league_memberships`, `transactions`, `asset_movements`, `sharp_people` and `manager_seasons` all hold **0 rows**. A count of 0 from an empty ledger is an artifact of the environment, not a finding — the same trap #927 named for the Sharp consensus row. The insider surface is also not on the public allowlist, so unlike `V1-95` it cannot be read off production unauthenticated, and that gate must not be relaxed to make verification easier. **To close:** run the eligibility census on the deployed ledger (on-box) and record how many leagues each filter admits and how the two sets differ. **L2 RE-ATTEMPTED 2026-08-25 (§9b) — `UNMEASURABLE`, not failed.** The sandbox `data/intel/ledger.sqlite3` (458 KB) censuses to **29 tables, 0 rows in 28 of them** (`meta` = 4). Per the standing prohibition that is an ENVIRONMENT ARTIFACT, not evidence the production feature is broken, and it was **not** used as a population measurement. Sharp-admitted league count is **UNKNOWN**, never 0; the Insider-admitted count and the set difference were not computed, and were not inferred from source code. Needs the deployed ledger. **PROMOTED → `VERIFIED` (L2) 2026-08-25 (Integration), Lane 4 on-box run `32843495391` (2026-08-25, deployed `8537aa4c2`, read from the step log and report JSON):** the population census the row's L2 named is measured on the deployed ledger — `V65b` PASS: **14,264 discovered leagues; the Insider (signal) gate admits 6,204; the strictly-narrower Sharp gate admits 5,035; the 1,169-league difference is FULLY explained** (`too_new` 785, `keeper` 384, via the canonical `sharp_exclusion_reason` ladder re-run over stored evidence — no second rule, no fabricated `too_new` for unrecorded ages); sharp ⊆ signal holds with 0 violations; independent cross-check: 11,180 distinct leagues carry sharp-eligible complete season records; graph 18,496 observed users / 44,059 memberships / 8,060 discovery-only leagues. `V65` (single owner) re-confirmed |

### 3.6 Source-health correctness and stability / false-green repairs

| # | capability | canonical id | lane | status | level | in V1 because |
|---|---|---|---|---|---|---|
| V1-66 | 2029 pick tier ordering not inverted | audit `F-1` | L5 | `VERIFIED` | L2 | data integrity |
| V1-67 | E2E board diagnostic reports a true root cause | audit `F-2` | L5 | `VERIFIED` | L1 | false-green repair (#893) |
| V1-68 | E2E suite verdict is read and true | audit `F-3` / `F-3a` / `F-3b` | L5 | `VERIFIED` | L2 | CI/E2E. **Premise corrected 2026-08-25 (Integration): "red 7 consecutive days" was stale.** The suite was GREEN on `main` 2026-08-21 → 2026-08-24 (run `32701911086` green), then red again from 2026-08-24 — a NEW regression, diagnosed (F-3a) to exactly one test: `#1071` (`79d2d0310`) rewrote `/arbitrage`'s pre-scan empty state, scan button ("Find arbitrage" → "Find trade packages") and no-result copy ("No arbitrage found" → "No package arbitrage found") without touching `tests/e2e/specs/journey-trade.spec.js`, which asserted all three retired strings. Measured on the `#1105` CI run: 189 passed / 1 failed / 62 skipped — the spec drift is the WHOLE red. Fix in flight as `#1105` (spec-only, both commits); the row promotes on the next green `main` run, not on the diagnosis **PROMOTED `IN PROGRESS` → `VERIFIED` (L2) 2026-08-25 (Integration) — that run has now happened.** `#1105` merged (`307e9fb06`; a second round of the same drift — the scan button and no-result copy — was found by the PR's own first CI run and fixed on the same branch); dispatched main run `32837722538` on exactly that head: conclusion SUCCESS, `Run Playwright suite` green with 0 failed, and the verdict is READ — step 19 closed tracker `#1094` on the strength of it (see `V1-86`). The L2 statement: the suite's verdict is now both true (the one diagnosed failure — `#1071`'s three-string spec drift, measured 189 passed / 1 failed / 62 skipped — is repaired at its cause, the spec, with no selector loosened) and read (a green run drains the tracker; a red run files exactly one). F-3a's diagnosis requirement is discharged by the measured single-cause census on the `#1105` CI run |
| V1-69 | `value_as_of` accepts an ISO datetime | audit `F-5` | L5 | `VERIFIED` | L1 | stability. **PROMOTED 2026-08-21; the row was stale.** `src/history/store.py::as_of_date`'s string path used to be a bare `strptime(s, "%Y-%m-%d")`, so an ISO-8601 *datetime* string escaped as a raw `ValueError` the caller could not catch as `ObservationError` and said nothing about the UTC-awareness rule that governs the object path. Already fixed (commit `8f7c1197b`) and pinned by `tests/history/test_asof_string_dates.py`: 7/7 on this tree, covering plain-date passthrough, UTC/offset/Zulu datetime strings resolving identically to the equivalent `datetime` object, midnight-crossing offset conversion, naive-datetime refusal matching the object path, and garbage inputs raising the module's own `ObservationError` rather than a bare `ValueError`. Reperformed rather than trusted: reverted the string path to the old bare `strptime` ⇒ RED on 8 of 11 (4 tests + 4 subtests, exactly the ISO/offset/Zulu/naive/garbage cases); restored ⇒ GREEN, clean tree. |
| V1-70 | A source cannot go unfetched while every surface reads OK | audit `F-6` | L5 | `VERIFIED` | L2 | source-health correctness. 12.6-day precedent |
| V1-71 | Source-health headline counts the real voter population | audit `F-7` / `C4-SRC-02` | L5 | `VERIFIED` | L2 | false-green repair — 2/2 for a 21-voter board |
| V1-72 | Build-check suppressions rest on sources that exist | audit `F-8` | L5 | `VERIFIED` | L1 | evidence integrity |
| V1-73 | Board-diff harness hashes all three inputs | audit `F-9` | L5 | `VERIFIED` | L1 | test integrity |
| V1-74 | The board's retail anchor is watched | audit `F-10` | L5 | `VERIFIED` | L2 | source-health correctness. Verified 2026-08-18 from the post-deploy committed export: `coverageAudit.expectedSites: {offense: [ktcSfTep], idp: [idpTradeCalc]}`. Not from `/api/status`, which does not expose the field — the first check read `anchor_row_counts`, a different question, and was withdrawn |
| V1-75 | A source losing all evidence stays visible to the watchdog | audit `F-11` | L5 | `VERIFIED` | L1 | truthful degraded state |
| V1-76 | Failure attribution compares one vocabulary | audit `F-12` / census `S-2` | L5 | `VERIFIED` | L2 | source-health correctness. **PROMOTED 2026-08-25 after `#1132` (merge `7e73a97b4`, feature head `bd3127a1b`).** The refuted `playerCount: null` proposal remains refuted and absent. The accepted replacement is the evidence-driven design §8.1 required: one registry-owned `run_source` → registry-key resolver, additive key-space failure projections, a frontend that consumes those resolved keys, and fail-closed unattributable failures when no mapping is proven. L1: exact-head `PR Validation` run `32891194471` SUCCESS; `tests/api/test_failure_attribution_vocabulary.py` RED→GREEN with 11 cases plus three frontend resolved-attribution cases, including explicit guards that the refuted design never re-enters the promotion path. L2: the real 2026-08-25 board built with and without the repair held **1,116 rows with 0 value changes, 0 rank changes, 0 tier changes**. This closes the vocabulary false-green without moving canonical valuation. |
| V1-77 | `/api/dynasty-data` cannot answer 200 off disk when the backend says 401 | audit `F-14` | L5 | `VERIFIED` | L2 | security / data integrity |
| V1-78 | Row-floor guard covers every registered voter | audit `F-15` | L5 | `VERIFIED` | L2 | source-health correctness. 8 of 21 had opted out |
| V1-79 | A blocking gate bounds the production payload | audit `F-16` | L6 | `VERIFIED` | L2 | performance / test integrity. **VERIFIED 2026-08-21.** F-16's own third sub-finding (fake `--strict` flag on the bundle-size gate) was already repaired on `main` by `8cd89fbe5` and needed no further work — confirmed genuinely blocking in `pr-validation.yml` (no `continue-on-error`). The two payload sub-findings named in F-16 were still open: `test_launch_readiness.py::test_gzipped_payload_under_2mb` sits in `_LIVEDATA_MODULES` (`tests/conftest.py`), so it runs only in the `continue-on-error: true` advisory step and cannot block a PR, and it measures `json.dumps(contract)` at default separators + `gzip.compress` default level 9 on the **full** contract — a shape `server.py` never serves (production always uses `separators=(",", ":")` + `compresslevel=5`, and clients receive `?view=app`/`runtime` or `?view=array`, never the full dict+array-duplicated payload). Repaired by `tests/api/test_production_payload_ceiling.py`, new and additive (the advisory test is untouched — a livedata calibration claim, not a defect, per the same reasoning already applied to `test_faab_calibration.py`). Built from the tracked export archive (`tests/archive_fixtures.newest_complete_raw_payload()`, the same deterministic-and-skips-not-passes-vacuously discipline `test_compact_view_byte_budget.py` established) so it stays in the **blocking** gate, not livedata. Asserts a bytes-per-row budget (60% headroom over the measured rate) rather than a hardcoded byte count, so organic per-row growth cannot false-fail it while a doubling-class bug (e.g. reintroducing a dropped duplicate encoding — the exact historical failure `test_compact_view_byte_budget.py` was written for) still trips it. **Exact measured production-sized payload** (archive `dynasty_export_20260820_230504.zip`, 1,111 rows, gzip level 5 matching `server.py`): `view=app` (runtime) **592.3 KB gz, 545.95 bytes/row**; `view=array` **722.1 KB gz, 665.51 bytes/row**. **L1 mutation-proved**: reintroducing the dropped `players` dict into the runtime view (`payload["_MUTATION_TEST_duplicate_blob"] = contract.get("players")`) → `test_runtime_view_under_bytes_per_row_budget` RED (`1123.4 KB gz, 1035.4 bytes/row` against an 873.5 bytes/row budget); reverting → GREEN, both tests pass |
| V1-80 | The critical-source gate can fire for DLF | audit `F-17` | L5 | `VERIFIED` | L2 | source-health correctness. **PROMOTED 2026-08-25 (#1126, `adb224c8e`) at L2.** F-17 confirmed: `_CRITICAL_PRIMARY_SOURCES` contains `"DLF"`, but the scraper registers DLF under run name `"DLF_LocalCSV"` (`Dynasty Scraper.py:3092`, `SITES.get("DLF")`), and the gate matched run names verbatim — so a DLF failure matched nothing and fell through to `partial_run_unknown:DLF_LocalCSV` (a warning, not an error), `ok` stayed `True`, and neither the deploy-blocking `--lane full` gate nor `check_source_health.py` could ever see a critical DLF failure. Latent today (`SITES["DLF"]=False`) but would ship with the gate disabled on re-enable. Fix (smallest real, per F-17's own constraint that a fourth hand-maintained list is the defect): one owner `critical_primary_for_run_source(run_source)` — exact match or `<primary>_<qualifier>` on the scraper's underscore convention; `tests/archive_fixtures.py`'s inline copy now delegates to it. RED→GREEN: reverting the gate wiring to the verbatim match turns 3 tests RED; `partial_run_critical:DLF_LocalCSV` now appears as a source-health-lane error with `ok:False`. **L2 measured**: contract built from `exports/latest/dynasty_data_2026-08-25.json` with and without the repair — 1116 rows either way, 0 rows changed value/rank/tier; the live run summary is `complete` so 0 error strings move on the live board. Validation-only gate. new test 23 + `test_contract_health_lanes.py` 23 green |
| V1-81 | Freshness budgets bound what the signal measures | audit `F-18` | L5 | `VERIFIED` | L2 | source-health correctness. #909. **VERIFIED 2026-08-20 at Integration.** Target level **L2** — §2: L1 plus a measured statement of the effect on the live board or contract — and both halves hold. **L1:** `tests/api/test_freshness_budget_not_laxer_than_alerts.py` **23 pass**, pinning the RELATION (contract budget ≤ alert threshold) rather than a number. Mutation **MF1**, confirmed APPLIED with the changed line quoted: `data_contract.py:746` `"ktc": 6` → `"ktc": 9999` ⇒ RED `test_contract_budget_is_not_laxer_than_the_alert_threshold[ktc]`. So the relation is genuinely enforced per source, not asserted once in aggregate. **L2 measured on the live board, twice, because the first measurement alone would have been vacuous.** (a) *Is the repair inert today?* Relaxing the five repaired 24h budgets back to 720h and rebuilding: **0 confidenceBucket, 0 confidenceLabel, 0 rankDerivedValue changed across 1111 rows.** That reproduces the F-18 audit's own finding (0 across 1109 rows) and is the honest answer — every source is currently fresh (production ages 1.24–1.62 h, measured the same night), so nothing is near any budget. (b) *But does the budget bound anything at all?* A 0 on a fresh board cannot distinguish a working gate from a dead one, so the mechanism was exercised directly: aging **all 27** `data/scrape_state/*_last_success` stamps by 1000 h — far past every budget — and rebuilding moves **583 of 1111 rows**: `high` **269 → 24**, `medium` **338 → 0**, `low` **242 → 825** (transitions: 245 high→low, 338 medium→low; the 262 `none` rows are unaffected because they carry no confidence). That is the five-axis gate behaving exactly as specified — freshness is one axis and **the weakest axis wins**, so total staleness collapses the board to `low` and cannot be bought back by source count. Recorded because it is easy to get backwards: aging a **single** source (`yahooBoone`, 100 h against a 24 h budget) moves **0** buckets. That is not the gate failing — one of 27 families is simply never pivotal to a weakest-axis outcome. Stamps DO participate in the build even though they are not among `golden_board.py`'s two hashed inputs (the pinned export and `CSVs/site_raw/`), which is why (b) is measurable at all |
| V1-82 | Health/status/metrics/alerts report board age, not process age | audit `F-19` | L5 | `VERIFIED` | L3 | false-green repair. #909, completed by #910's `F-28` repair. **Production-verified 2026-08-18 23:00 UTC** on the deployed SHA: `producedAt 22:55:34+00:00` < `loadedAt 22:55:40+00:00` < `last_scrape 22:55:49+00:00`, `data_age_hours 0.1`. The age now tracks the BOARD. It was `IMPLEMENTED_UNVERIFIED` rather than verified because #909 shipped the repair onto a host whose clock made it unmeasurable — see `V1-128` |
| V1-83 | Alert cooldown keyed on delivery | audit `F-20` | L5 | `IMPLEMENTED_UNVERIFIED` | L3 | observability correctness. #909 |
| V1-84 | 503 is not exempt from production health failure | audit `F-21` | L5 | `VERIFIED` | L3 | false-green repair. #909 | **PROMOTED 2026-08-25 from natural L3 production evidence.** The repaired F-21 health workflow from `#909` was exercised by scheduled production run `32700949074` on head `b1066ec98a93b417d3801935142543885e94230f`; that head is 818 commits after `#909` merge `82b25bd7d`. The first attempt observed HTTP 503 on all three `/api/health` probes with `contract_ok=false` and opened rolling production-health issue `#1069` instead of warning/exempting the degraded state. A later canonical data cycle recovered; attempt 2 returned HTTP 200 with `contract_ok=true` and the workflow closed the same tracker. This is the real production failure/recovery path, not a synthetic status or workflow-green inference.
| V1-85 | `pickAnchors` reporting consistent with the contract | audit `F-22` | L5 | `VERIFIED` | L1 | P2 reporting consistency — **reclassified, see §8. PROMOTED `NOT STARTED` → `VERIFIED` (L1) 2026-08-25 (Integration), `#1111` merged (squash `a9d1328f9`).** The inconsistency was measured live before the fix (2026-08-25 payload): `pickAnchorsProvenance` stamped `ktcSfTep` (180 keys) while `pickAnchors` carried no `ktcSfTep` at all, and 180 `ktc` keys against 84 emitted — the stage-1 vendor-build provenance survived the model board's replacement of the anchor maps, so two surfaces described the same anchors differently (F-22 exactly; the served values were never implicated — the contract reads the vendor CSVs independently, all 36 rows confirmed). Fix: `reconcile_emitted_anchor_provenance` in the pick-map OWNER (`src/picks/site_pick_map.py`; the scraper stays its adapter, per the C1-U6-D1 arrangement) makes the provenance's site/key sets equal the emitted map's exactly, carries stage-1 evidence classes for vendor-carried values, and names the deliberate C1-U6-follow-up-3 model injection `model_injected_composite` instead of letting it wear a vendor class — extending `SitePickMap`'s own stated contract (a key absent from `values` is absent from `provenance`) to the payload level. Value-inert by construction: no anchor value is read, written or moved; W05-F006's value-affecting recommendation (stamp `pickAnchors` under `ktcSfTep` to widen the pick anchor set) was deliberately NOT done under this P2 row. L1 evidence: `tests/picks/test_anchor_provenance_reconciliation.py` — the invariant test proves the stage-1 fixture VIOLATES the emitted-key-set invariant (the pre-fix behaviour) and the reconciled map satisfies it, plus AST wiring pins on the scraper. Two mutations self-performed and restored: the function passing stage-1 through → 5 RED; the scraper wiring removed → the AST pin RED; restored, `tests/picks/` 65 passed |
| V1-86 | The E2E tracker identity works | audit `F-23` | L5 | `VERIFIED` | L3 | CI/E2E false-green. 14 duplicate trackers; close step never fired — **PROMOTED `IMPLEMENTED_UNVERIFIED` → `VERIFIED` (L3) 2026-08-25 (Integration), on BOTH directions executing against real state in production CI, read from the primary source (step logs), never from step conclusions.** An earlier candidate proof was REFUTED at the log: green run `32701911086`'s close step logged `no open e2e-failures issue; nothing to close` — conclusion `success` meant the step ran, not that it closed anything, and tracker `#1094` was filed AFTER that run. The genuine proof: (1) ALERT direction — the 2026-08-24/25 real red (the `#1071` spec drift) produced exactly ONE open tracker, `#1094`, with zero duplicates stacked across six further red main runs (measured 2026-08-25: the open `e2e-failures` census is exactly `[#1094]`), against F-23's baseline of 15 duplicates — the dedup lookup matches the bot's real spelling; (2) CLOSE direction — after `#1105` merged, dispatched main run `32837722538` (head `307e9fb06`, conclusion success, 0 failed) step 19 logged `✓ Closed issue jasonleetucker-code/riskittogetthebrisket#1094 (E2E safety net failing)` — a REAL open tracker drained by a genuinely green run, the exact non-vacuity statement this row required. The board now reads "broken now", not "broke once, ever" |
| V1-87 | Every live feature flag is visible to operators | audit `F-24` | L5 | `VERIFIED` | L2 | truthful degraded state — a defaulted-ON flag absent from `/api/status`. **The blocker is now MEASURED rather than predicted (2026-08-24, §9a).** The live instance is confirmed present: `RISKIT_FEATURE_LEDGER_RANK_CHANGE` is read straight from the environment in `data_contract._stamp_rank_changes`, defaults ON, and is in neither `_DEFAULTS` nor `_GATE_STATUS` — production `/api/status` returns exactly the **18** registered flags and not this one, so the registry and the operator surface agree with each other while both omit a live gate. Registering it is refused by `tests/api/test_feature_flags.py` until its blast radius is measured, and the read site predicts that measurement would be vacuous without a populated ledger. **That prediction was tested and holds literally.** A real ledger was built here from all 42 tracked `exports/archive` dates — 169,911 rows — and the ON/OFF pair was run over the real 2026-08-24 board (992 rows, 740 ranked): `rankChange` non-null is **0 with the flag ON and 0 with it OFF**, a diff of exactly **0 rows**. The cause is structural, not a build error: the local ledger carries `scraper_blend` (89,638) and `source_value` (80,273) and **zero `canonical_board` rows, with zero non-null ranks in any lane**, because that lane is written only by the live scrape-promotion site (`src/history/record.py:159`) and by legacy-store migrations (`src/history/migrate.py:114`/`:285`) reading `data/rank_history.jsonl` / `data/board_history.sqlite` — **neither present outside production**, which carries `rank_history.jsonl` at 36 snapshots (public `/api/status.rankHistoryCoverage`). The archive cannot supply it: exports carry the raw scraper payload, not the served canonical board, and rebuilding an old board with today's curve is forbidden by `src/history`'s own rule. Unblocking needs the production ledger, i.e. owner-held credentials — same class as `V1-15`/`V1-16`/`V1-17`, not more engineering. **UNBLOCKED AND PROMOTED → `VERIFIED` (L2) 2026-08-25 (Integration).** The blocking measurement was taken by Lane 4 on-box run `32843495391` (2026-08-25, deployed `8537aa4c2`, read from the step log and report JSON): `V87` PASS — with the temporal ledger present (37 recorded board dates, 241,344 canonical rows), flag ON derives a non-null `rankChange` for **743 of 749 ranked rows** on the 2026-08-25 board (comparator 2026-08-24, 1,342 ranked assets); OFF is structurally all-None; no value/rank/tier moves either way. The repair then landed as `#1119`: `ledger_rank_change: True` registered in `feature_flags._DEFAULTS` carrying that blast radius, classified `LIVE` + `value_moving_on`, and the `data_contract` read moved onto the ONE registry owner — the flag is now visible in `snapshot()`, `effective_flags()`, `/api/status` and the reachability suite, with `RISKIT_FEATURE_LEDGER_RANK_CHANGE` behaving identically via the registry's env override |
| V1-88 | Flag documentation names the endpoint it actually gates | audit `F-26` | L5 | `VERIFIED` | L1 | evidence integrity. **NOT promoted 2026-08-20 — refuted by mutation at Integration.** The F-26 repair itself is present and correct on `8b9c28289`: `src/api/feature_flags.py:228-237` now names **`/api/valuation/league-adjusted`** and explains why, instead of the wrong `/api/gameplan`. But L1 is defined in §2 as a **RED→GREEN test at exact head**, and there is none for this claim. **Measured:** reintroducing F-26's exact defect — flipping that one comment back to `/api/gameplan`, mutation confirmed APPLIED with the changed line quoted — leaves `tests/league_intel/test_flag_docs_match_reality.py` + `tests/api/test_feature_flags.py` + `tests/league_intel/test_reception_fit.py` at **35 passed, 0 failed**. Nothing goes red. The existing guard is real but answers a **different claim**: it compares a module docstring's *ON/OFF default* against `_DEFAULTS`, not the *endpoint named*. A route-registration check would also be vacuous here — `/api/gameplan` **is** a registered route; the defect was reachability, not existence. **What would satisfy L1:** a guard that resolves flag → gate call site → enclosing function → route handler, so a comment naming an endpoint the flag does not gate goes RED. That is Lane 5 implementation work, not an Integration edit; recorded rather than hand-rolled at the merge desk **PROMOTED 2026-08-20 at L1 (#991, merge `d87eefae9`).** This row was previously REFUSED at Integration on a measurement, and the refusal is what defined the bar: reintroducing F-26's exact defect left the three existing suites at 35 passed / 0 failed, so the repair was correct but unguarded. `src/api/flag_reachability.py` now resolves the chain Integration asked for — flag → `is_enabled()` call site → enclosing function → transitive callers → FastAPI route handler and its path(s). Verified at Integration on the SHIPPING TREE (this head merged with main, `c09bdaaa5`), by execution: baseline **82 passed**; **F-26 reperformed verbatim** by replacing the bold-marked ``**`/api/valuation/league-adjusted`**`` with ``**`/api/gameplan`**`` in `feature_flags.py` ⇒ **RED, 3 failed / 79 passed**, including the primary guard `test_the_documented_endpoint_is_actually_reachable`; restored ⇒ **82 passed**, `git diff` empty. **The decisive detail is which tests did NOT fail:** under the mutation all 79 pre-existing tests still passed — reproducing the exact result that justified the original refusal — so this is not "a test was added" but the specific non-vacuity gap being closed and re-measured. A route-EXISTENCE check would also have been vacuous, because `/api/gameplan` IS a registered route; the defect was reachability FROM THIS FLAG. Two false negatives found while building the guard are worth keeping visible because both would have shipped a silently-useless check: the caller graph originally followed only `Call.func`, so the real handler's `run_in_threadpool(_gameplan.get_league_adjusted_values, ...)` — where the target is a positional ARGUMENT — was invisible and the BFS found zero routes; and a naive "every `/api/...` token" doc extraction returned both endpoints, because the real comment narrates its own history in prose while naming the live route in bold. Test-and-tooling only: the module is read-only static analysis, gates nothing, and touches no `_DEFAULTS` entry or `is_enabled` behaviour.
| V1-89 | DraftSharks staleness resolved | `C4-SRC-01` | L4 | `VERIFIED` | L3 | source-health correctness. **`OD-04` RESOLVED by owner 2026-08-25: ACCEPT — HEALTHY_CURRENT.** Do not re-mint, do not retire; proceed with remaining L3 deployment/verification work (owner decision recorded verbatim in this session's dispatch). Grounds: `docs/lane4/V1_89_DRAFTSHARKS_DECISION_PACKET.md` — authenticated access works (username/password login mints the session; no owner credential action required), content is demonstrably current (fetch 2.7 h, content changed 4.6 h, re-confirmed byte-identical against upstream, vendor publication marker `2026-08-25T00:15:02Z` — cadence lag, not staleness), consumption real (412 + 245 players against floors 190/85). `BLOCKED` → `IMPLEMENTED_UNVERIFIED`: the decision unblocks the row; it does not verify it. What L3 still needs is the packet's §6 recipe executed on the deployed box (steps 2-9: fetch stamps advance, league-scoped proof, ROS fail-closed proof, all four files under `site_raw/` in the newest archive + `siteRawMirrored` manifest, `measure_content_staleness()` returning the four DraftSharks keys, no false alert while the vendor publishes, the stale-content control alerting, `idpTradeCalc` reading unchanged) | **PROMOTED 2026-08-25 from fresh L3 production evidence.** On-box verification run `32919655291` against deployed SHA `a82898fdbe73ea2effd295132590dff35a45ac98` executed the packet §6 recipe: production DraftSharks stamps are fresh and advance only on successful fetch; the existing dynasty/IDP league-scoped proof and the independent ROS fail-closed proof are both pinned by the deployed implementation; the newest archive contains all four DraftSharks files under `site_raw/` and names them in `siteRawMirrored`; `measure_content_staleness()` returns all four DraftSharks keys; currently publishing content does not false-alert; the synthetic stale-content control does alert; and the pre-existing `idpTradeCalc` content-age reading remains present. The verifier reported each packet group PASS rather than inferring health from workflow green.
| V1-90 | FootballGuys ghost stamps removed | `C4-SRC-03` | L5 | `VERIFIED` | L1 | source-health correctness — stamps with no fetcher since 2026-05-24. **PROMOTED 2026-08-21; the row was stale.** Measured clean rather than assumed: zero `footballGuys`/`footballGuysSf`/`footballGuysIdp` references anywhere in `_SOURCE_CSV_PATHS`, `_SOURCE_MAX_AGE_HOURS`, `_DEFAULT_SOURCE_ROW_FLOORS`, `config/`, or `data/scrape_state/` (grepped, not read from a doc's self-description); no directory-scanning mechanism exists that could pick up a stray untracked stamp file even if one appeared on a live box, since freshness is driven off the fixed registry rather than a `data/scrape_state/` glob. The real ghost was in `SINGLE_SOURCE_ALLOWLIST`: 18 of 52 entries said "only ranked by FootballGuys" while the pipeline had already dropped the source, and 14 of the 18 were demonstrably NOT single-source at all (the worst, Zavion Thomas, carried 13 real sources under a "FootballGuys-only" excuse). Fixed and pinned by `tests/api/test_single_source_allowlist_integrity.py` (6/6): every allowlist explanation must name sources the registry actually has, both in its machine-readable prefix and its human prose. Reperformed rather than trusted: injected a decoy allowlist entry citing `footballGuys` ⇒ RED on 3 of 6 tests, naming the offending entry; reverted ⇒ GREEN, clean tree. |
| V1-91 | Partial runs cannot report as healthy | `C4-SRC-02` | L5 | `VERIFIED` | L2 | false-green repair. **PROMOTED `IN PROGRESS` → `VERIFIED` (L2) 2026-08-25 (Integration) — the row's premise was stale: the repair and its health contract test already shipped (`#905`, `tests/api/test_source_health_population.py`, 7 passed fresh), and the row was never reconciled.** The manifest defect (`total_sources: 2` while the payload carries 21 keys, run called "healthy") does NOT reproduce at HEAD, and the L2 statement was measured fresh on the live 2026-08-25 payload by running `server._build_source_health_snapshot` directly: `total_sources: 21` (the REGISTRY population via `get_ranking_source_keys()` — "the population we are entitled to expect, not what one run happened to emit") against a payload whose `sites` block carries exactly the 2 anchor markets; with no served-board coverage handed in, all 21 report as `unmeasured_sources` — "we were not given the served board and therefore cannot say" — structurally distinct from `missing_sources` (known to have contributed nothing) and from healthy; `partial_run` is computed from the scraper's own run summary plus failure records, distinct from freshness; and per-source freshness (`sources[src].lastFetched`, CSV mtimes) is present for 22 of 22, independent of the full-pipeline scrape time. Freshness, coverage and degraded state are honest and DISTINCT — the row's acceptance verbatim. The contract-level degraded/structural split is separately pinned by `tests/api/test_contract_health_lanes.py` (the CI-lanes work). Missing is never zero at the health layer |
| V1-92 | Freshness indicators complete | inv 6.8 | L6 | `VERIFIED` | L1 | truthful degraded state; W08-F011. **VERIFIED 2026-08-21, narrowly scoped to avoid the prior REFUSAL's vague-measurement problem.** Census of W08-F011's three named gaps on `/trade` (`page.jsx:1757-1800`): (1) board-level data freshness — already covered site-wide by `StaleDataBanner` (mounted in `AppShellWrapper`, present on every page including `/trade`), confirmed no trade-page-specific work needed there; (2) per-asset source count — was absent, `AssetRow` showed no `sourceCount`; (3) per-asset normalization confidence — was absent, `AssetRow` showed no `confidenceBucket`/`confidenceLabel`. Both (2) and (3) repaired in `trade-sections.jsx::AssetRow`: the meta line now reads `{sourceCount} src`, and a `Badge` carrying the real `confidenceLabel` renders for any non-"high" `confidenceBucket` (silent for "high" — the expected default — to avoid badging every row). Both fields are backend stamps already threaded by `buildRows` (`dynasty-data.js`); no client freshness computation was added — `confidenceBucket` already folds freshness in as one of its five axes (`src/api/confidence.py`), satisfying "use backend freshness truth, no Date.now-based invented freshness" by construction. Closed by `frontend/__tests__/components/trade-asset-confidence-source-count.test.jsx` (6 tests): source count renders; high confidence stays silent; medium/low/none each render the correct badge text carrying the real backend label; and no client-side "…ago"-style freshness text is ever rendered. **L1 mutation-proved**: `git stash` of the `AssetRow` diff (reverting to the pre-fix component) ⇒ 4 of 6 tests RED (`getByText("no confidence data")` etc. fail to find the element). `git stash pop` ⇒ GREEN, `6 passed`. Full frontend suite re-run clean after restore: 149 files, 2291 tests, 0 failures |
| V1-131 | Nav does not offer a page whose endpoints all 503 | audit `F-25` / `C6-EDGE-01` (gating only) | L6 | `VERIFIED` | L3 | **VERIFIED 2026-08-27.** The third defect's theory (test-side hydration race, not a product bug) is confirmed: `#1155` (`7181264d`) hardened `v1-131-nav-gating.spec.js`'s mobile-drawer test (bounded `networkidle` wait + one click retry) with no product code change. A clean re-verification run — `33103452227` (2026-08-27T18:26-18:31Z), deployed SHA `b73613de` (unchanged app code from `b194316f`; this run's head commit was a routine automated data-refresh, confirming no app-code deploy was needed) — produced **zero failure artifacts anywhere in `v1-131-nav-gating.spec.js`** (previously the mobile-drawer test alone produced `test-failed-*.png`/`error-context.md` on every run; this run's whole browser suite had exactly one unrelated failure, in a different spec — `v1-56-waivers-faab-strip.spec.js`, a test-setup gap unrelated to this row). `AUTH0`/`V131A` both pass on the same run (real `guest_pass` session, `features.consensusEdge.available` agrees with the board's 503). L3 recipe satisfied: the checklist executed against the deployed SHA with evidence recorded. Full defect history preserved below. **added 2026-08-18, §7.1 A-7** — follows mechanically from "truthful degraded states", which the boundary names. Nav currently offers Consensus Edge while its three endpoints 503. **Gating only**: the Consensus Edge FEATURE stays POST-V1. **RECLASSIFIED `NOT STARTED` → `IMPLEMENTED_UNVERIFIED` 2026-08-25 (Integration) — `#1086` merged.** The nav capability gate is implemented across all five offer surfaces (`/consensus-edge` gated on the `consensus_edge` flag via the shell's existing `/api/auth/status` probe, failing closed on an unresolved capability) and mutation-proved twice — including the repair of a vacuous menu-closed negative found in the PR's own tests (now 7 RED across 2 files under the same mutation). NOT `VERIFIED`: the row's L3 needs the gate observed on the deployed site; the recipe is `docs/master-site-audit/evidence/V1-131/L3_PRODUCTION_RECIPE.md`. **`#1149` merged 2026-08-27** (`frontend/components/shell/MobileChrome.jsx` — guard the pathname-close effect so a late shell-mount effect can't race a fresh Menu click and immediately close the drawer) and deployed (Deploy Production run `#33052902637`); the next authenticated verification attempt (run `32919648642`, attempt 5, 2026-08-27T09:36-09:42Z) still failed `tests/e2e/specs/prod-auth/v1-131-nav-gating.spec.js:289` (`.shell-drawer-group` never became visible at 390×844) — **a second, distinct defect**, root-caused this session from the run's own Playwright trace (screencast frames + action log, not speculation): the mobile tab bar's `authenticated` check collapsed `useAuth`'s deliberately three-valued `true`/`false`/`null` state into a boolean, so a real signed-in guest-pass session rendered the PUBLIC tab set ("Sign in" included) for as long as the client-side `/api/auth/status` probe was in flight, then swapped to the real authenticated tab set the instant it resolved — and the test's Menu click can land in that window. Desktop's `TopBar.jsx::visibleGroups` already had the correct three-way handling (`authenticated === null/undefined` → render nothing, established and tested); `MobileChrome.jsx` did not. Repaired same session by mirroring that exact pattern (`MOBILE_TABS_UNKNOWN`, `frontend/lib/nav-model.js`), mutation-verified (reverting the fix reproduces a real, non-vacuous test failure). Merged in `#1150` (`b194316f`) and deployed (`Deploy Production` run `33066619784`, `success`, 2026-08-27T11:37Z). **Re-verified against production 2026-08-27, and the auth-tristate fix genuinely worked** (tabs correctly show the authenticated set, not "Sign in") — **but a third, distinct defect surfaced**, reproduced identically on two independent post-deploy runs (`32919648642` attempt 6 at 14:36-14:42Z, `33098047781` at 17:22-17:28Z): `.shell-drawer-group` never becomes visible after clicking Menu — `tests/e2e/specs/prod-auth/v1-131-nav-gating.spec.js:301` times out with zero drawer elements found, not merely hidden ones. Traced by code review (not speculation): the component logic itself reproduces correctly in an RTL/jsdom unit test with the real `NAV_MODEL` and identical props (`TopBar.test.jsx`'s existing "renders the tabs + a Menu button" test passes), and `itemIsOffered`/`navGroupsFor` only gate the single capability-flagged `/consensus-edge` item — never whole groups like Market — so `groups` cannot legitimately be empty once `authenticated === true`. Leading theory: a **test-side hydration race**, not a product defect — the spec does `page.goto(..., {waitUntil: "domcontentloaded"})`, confirms the SSR-painted tabbar is visible, then clicks Menu immediately with no wait for React hydration to attach the click handler; a native click landing in that gap is inert with no replay once hydration completes. Hardened the test (bounded `networkidle` wait + one click retry) rather than touching product code, since the theory implies no product defect exists — this is a test-only change, still pending its own merge + a fresh verification run to confirm the theory. **Still NOT VERIFIED**: do not promote until a subsequent run passes this spec cleanly. |
| V1-127 | `normalizationHealth` reports the board it actually has | audit `F-27` | L5 | `VERIFIED` | L3 | **added 2026-08-18** — false red in production since C1-U6; see §10 note below. **Production-verified 2026-08-18** on the deployed SHA: `playersArray.pickNameMalformed` **18 → 0** and `healthy` **false → true**, with the 18 generic-grade pick rows (`2027 Round 1` … `2029 Round 6`) still present on the board — the rows were never wrong, the grammar reading them was |
| V1-128 | Board age is measured in a timezone that exists | audit `F-28` | L5 | `VERIFIED` | L3 | **added 2026-08-18** — host is UTC+2, so reported age was always ≤ 0 and `data_stale` structurally unreachable. Fixed at the source (tz-aware `scrapeTimestamp`) plus a future-board UNKNOWN guard. **Production-verified 2026-08-18 on the deployed SHA `92469d32`.** The 22:55 scrape is the first board built by the repaired scraper and stamps `producedAt: 2026-08-18T22:55:34.252784+00:00` — tz-aware, and earlier than both `loadedAt` and `last_scrape` rather than two hours later. Three samples against that instant: 23:00:04 → `0.1` (true 0.075 h), 23:14 → `0.3`, 23:20:28 → `0.4` (true 0.415 h). Monotonic, each matching the true elapsed time to the published rounding, so the age tracks the clock and the 6 h `data_stale` threshold is reachable — which is the property `V1-82`/`F-19` needed and this defect was blocking. Was `-1.0`, i.e. below every threshold by construction |

### 3.7 Truthful degraded states

| # | capability | canonical id | lane | status | level | in V1 because |
|---|---|---|---|---|---|---|
| V1-93 | Missing is never zero, on display | `F-MISS-01` | L5 | `VERIFIED` | L1 | governing invariant (#836) |
| V1-94 | `teamAssignment` degraded/missing is not served as `assignments: []` | `R-TEAMASSIGN` / `#815` | L1 | `VERIFIED` | L2 | named correctness defect; HTTP 200 on a degraded snapshot. **#914** fixes it and reports a second missing-as-zero found alongside. Note on `tests/e2e/specs/public-league.spec.js:533`: it was a deliberate red written against this defect, but it over-asserted — `nflTeams.length > 0` for every manager is not what this capability promises (an empty list with a resolved availability contract is a pinned-legitimate state, and the data is live Sleeper, so it flapped in the blocking E2E gate — runs 140/142). The E2E stabilization rewrote it to pin the availability/no-fabrication contract this row actually names: explicit `available`/`unavailableReason`, one slot per current manager, resolved favorites preserved, nothing listed below threshold. The favorite-resolution defect itself stays tracked here and in #815. **PROMOTED 2026-08-21** on #1015's evidence (merged `bc21bd3c3`, which itself never flipped this row — Integration's job, done here). #815's repair is genuinely complete on current `main`: `src/api/team_assignment.py` models three distinct failure states, the frontend consumer branches on `available`/`unavailableReason` honestly, and no path converts a degraded snapshot into a fabricated 200. What #1015 added: `test_team_assignment_degraded_snapshot_reports_unavailable_not_empty`, which exercises the degraded path through the REAL assembly pipeline (`build_public_snapshot` → `build_section_payload` → `assert_public_payload_safe`) rather than a hand-constructed `PublicLeagueSnapshot` — closing a verification gap, not a defect. Reperformed independently at Integration: reintroduced the pre-#815 bare `{"assignments": [], "available": True}` shape in `build_section`'s no-current-season branch ⇒ RED on 2 of 38 (`test_no_current_season_is_unavailable_not_empty` and #1015's own integration-level test, `KeyError`-shaped exactly as #1015 reported); restored ⇒ GREEN, clean tree. |
| V1-95 | Awards do not exist before games are played | `C9-AWARD-01` | L3 | `VERIFIED` | L2 | **VERIFIED 2026-08-20 on the DEPLOYED build.** Target level **L2** — §2: L1 plus a measured statement of the effect on the live board or contract — and both halves now hold. **L1 (#937, merge `5feade0eb`):** `tests/public_league/test_awards_need_games.py` 9 pass; two mutations, each confirmed APPLIED with the changed line quoted: (MA1) `awards.py:2327` rename `row["awardsUnavailable"]` so an explicit refusal degrades to an empty list ⇒ RED `test_no_games_is_an_explicit_refusal_not_an_empty_success`; (MA2) `awards.py:2310` `if not _has_begun(season):` → `if False:` ⇒ **4 named REDs** including `test_a_season_with_zero_games_manufactures_no_awards` and `test_the_named_defect_awards_are_specifically_gone`. **L2 measured on PRODUCTION**, `GET https://chaseupside.com/api/public/league/awards`, after the deploy of `e37d2786e` completed 03:22 UTC (post-deploy smoke + live-contract validation both green): season **2026 → `awards: 0` with `awardsUnavailable.reason = "season_has_not_played_a_game"`**; season **2025 → 32 awards**; season **2024 → 29 awards**, both untouched. So the refusal fires exactly on the season with no games played and changes nothing on completed seasons — the live defect this row names (awards manufactured with zero games) is closed at the surface that had it. **The deployed-SHA question is answered by the payload itself**, which matters because no endpoint publishes a build identifier (the gap #932 recorded): the constant `AWARDS_UNAVAILABLE_NO_GAMES` is **absent** from `src/public_league/awards.py` at `5feade0eb^1` and present at `5feade0eb`, so production returning that exact string is only possible from a build containing #937. **The refusal is coherent across the whole section, not just the awards list** — checked because refusing awards while still publishing a fabricated race would be the same defect one field over: `awardRaces: []`, `hottestRace: null`, `currentSeason: 2025` with `currentSeasonStatus: "complete"`, `featuredSeason: 2025`, `upcomingSeason: 2026`. So nothing is invented for the unplayed season and the prior season stays featured, which is exactly what `test_the_prior_season_stays_featured_until_the_new_one_begins` and `test_featuring_moves_once_the_new_season_plays` pin. Earlier the same day this row was explicitly NOT promoted because L2 was **unmeasurable locally** — `data/public_league/snapshot.json` holds only fully played seasons (2025, 2024) and the gate measured provably inert there. Production is where the case arises, and it does |
| V1-96 | Historical franchise continuity | `C9-HIST-01` | L5 | `VERIFIED` | L2 | live defect: 2024 declares ten teams, carries eight standings rows. **VERIFIED 2026-08-24 at L2 — and the row's own `NOT STARTED` was FALSE for four days.** The repair merged 2026-08-20 in squash `854598d9a`, whose subject is *"C8-PSI-01/C8-A11Y-01: elevate the Chase Upside Market Ticker (#965)"* — a premium-UI PR that mentions neither `C9-HIST-01` nor franchise continuity anywhere in its subject or opening body. Only `git log -S"is_retired"` finds it. Recorded because an auditor grepping commit subjects for this capability would have concluded it was never built: **bundling unrelated lane work into one squash is a live traceability hazard**, not a cosmetic one. **Mechanism**, traced in code rather than taken from the delivery note: pre-fix `build_manager_registry` hit `if owner_id in _RETIRED_OWNER_IDS: continue` BEFORE populating `roster_to_owner` / `by_owner_id`, so a retired owner's historical roster had no mapping, and `metrics.season_standings` (`resolve_owner` → falsy → `continue`) silently dropped that row. 2024 fielded 10 rosters, two held by retirees, giving exactly 8. `#965` deleted the `continue`, added `Manager.is_retired`, and moved exclusion to the single point `ManagerRegistry.ordered_managers(include_retired=False)`. **L1** — `tests/public_league/test_identity_retirement.py` 8 passed at exact head; module regression `tests/public_league/` 422 passed + 50 subtests, `tests/ros/` 245 passed/1 skipped; green CI on the merge tree (`PR Validation` run `32374369686` success on head `31ad70058`, squash-merged as `854598d9a`, confirmed ancestor of `main`). **Non-vacuity — two mutations EXECUTED, not asserted**, each anchor verified to match exactly one site: reinstating the pre-`#965` `continue` turns **5** tests RED including the two that name this defect (`test_season_standings_reports_every_roster_including_retired_owners`, `test_retired_owner_roster_to_owner_mapping_is_preserved`); forcing `include_retired` always-on turns **2** RED (`test_public_list_excludes_retirees`), so the privacy half is pinned in the opposite direction and the fix did not simply delete the exclusion. **L2, measured on the LIVE production public-league contract** (`GET /api/public/league/history`, 200, `public-league/2026-08-23.v1`, snapshot `generatedAt 2026-08-24T20:52:05Z` — re-fetched and re-derived by the Integration Authority independently of the assessing agent): 2026 `numTeams 12`/12 rows, 2025 `10`/10, **2024 `numTeams 10` / 10 standings rows / 10 unique `ownerId`s** — the named defect is closed. Non-vacuous at the surface: the two restored rows are exactly the retirees carrying real records — `714976074907336704` standing 7, 11-15, PF 3313.69; `720849338183548928` standing 9, 10-16, PF 3255.81. The privacy half still holds in the same payload (`league.managers` = 12, both retirees absent), and `/api/public/league/franchise` — the row's namesake — serves index 14 and detail 14 with both retirees present and correctly non-current (`currentRosterId: null`). **Deployed-build proof** follows the `V1-95` argument: no public endpoint publishes a build id, so the payload proves the SHA — under pre-`#965` code those two `ownerId`s could not appear as an `ownerId` anywhere, which mutation 1 demonstrates directly. Evidence therefore EXCEEDS the row's L2 bar; deliberately **not** rounded up to L3, because L2 is what the row requires and no §-numbered L3 checklist exists for it. **TWO ADJACENT RESIDUALS, found during this verification, confirmed live in production by direct fetch, and NOT folded into this promotion** — they are a narrower directory inconsistency on other sections, not the standings defect this row names, and hiding them would be the exact failure this ledger exists to prevent: (1) `archives.py:248::_manager_index` is the ONE caller of `ordered_managers()` in `src/public_league/`, so `/api/public/league/archives` serves `seasonResults` = **32** rows *including* both retirees' 2024 rows while its own `managers` index = **12** *excluding* them — the archive contains their season but its own index cannot find them, set difference exactly those two ids; `docs/WORK_CLAIMS.md` states archives.py "needed no change", which is measurably the wrong call on the only one of the three audited files where the call site mattered. (2) `overview.py:388` counts `len(by_owner_id)`, so production serves `leagueVitals.managers = 14` while the SAME payload's `league.managers` directory lists **12** — pre-fix both were 12; 14 is arguably the truthful all-time figure, but two unlabelled numbers disagree on one page and the change appears in no delivery note. Both routed to the C9 lane as a bounded follow-up |
| V1-97 | Historical Trade Replay does not leak hindsight | `C3-REPLAY-01` | L2 | `VERIFIED` | L2 | **wrong semantics in production** **PROMOTED 2026-08-20 at L2 (#992, merge `a7ccdec3f`).** **L1 — four of the row's named hindsight leaks pinned, verified by mutation at Integration:** (MV1) `trade_instant_from_created_at` falling back to `datetime.now()` on unparseable/zero input — the exact leak — ⇒ RED `test_trade_with_no_parseable_instant_is_unavailable_on_every_side`; (MV3) a missing historical value coerced to `0.0` instead of `None` ⇒ RED on 2 (`test_missing_evidence_produces_an_honest_unavailable_side`, `test_one_sides_missing_evidence_does_not_poison_another_side`); (MV4) an incomplete side silently totalling the assets it DID resolve ⇒ RED on the same 2; (MV5) opening the strictly-before boundary in `src/history/asof.py::_instant_at_or_before` so a FUTURE observation becomes selectable ⇒ **48 RED** across `tests/history/`. All restored, 7 passed, tree clean. **L2 — measured on the LIVE PRODUCTION contract** (`GET https://chaseupside.com/api/public/league/activity`, 429 KB): **200 trades; 187 predate the 2026-07-14 history floor and 13 do not; 12 sides GRADED, 399 sides UNAVAILABLE, and 0 sides carrying neither.** Every unavailable side gives the reason `no_historical_evidence`. 2.9% grade coverage is LOW AND CORRECT — the row's standard is that a wrong present number is worse than an honest absence, and the coverage tracks the floor rather than the board. The production payload literally serves `{"grade": null, "color": null, "label": "Insufficient historical evidence", "available": false, "reason": "no_historical_evidence", "missingAssets": [{"kind": "pick", "name": "2028 R2"}, ...]}` — `null`, never `0`, and it names WHICH assets could not be resolved. The label is owned by the BACKEND (`activity._unavailable_grade`) and consumed by `TradeCard`, so the string cannot drift between the two. **ONE GAP RECORDED, not papered over:** (MV2) making `_normalize_instant` guess a naive timestamp as UTC instead of refusing it left all 7 GREEN — that refusal is real in the code and correct, but nothing pins it, so a future change could reopen it silently. It does not affect the four properties this row names, and it is a narrower hole than the row's own bar; worth a test when the history lane is next touched.
| V1-98 | Blended source rank renders or is honestly removed | inv 6.7 | L6 | `VERIFIED` | L1 | truthful degraded state. **VERIFIED 2026-08-21.** The "renders blank" premise was stale, not live: `data_contract.py` (`row["blendedSourceRank"] = round(mean(effectiveSourceRanks), 2) if rank_values else None`), the frontend materializer (`dynasty-data.js`: `blendedSourceRank: player.blendedSourceRank ?? null`, both code paths) and `players/compare/page.jsx` (`blended != null ? blended.toFixed(1) : "—"`) were all already correct — a real formatted number for a source-covered player, an honest "—" for one outside the ranked pool, never a fabricated client-side rank. Empirically confirmed against a live `/api/data?view=app` response: 800 of 1109 players carried a non-null, sensible `blendedSourceRank`. The actual gap was that none of it was pinned: `/players/compare` had zero test coverage. Closed by `frontend/__tests__/components/players-compare-blended-rank.test.jsx` (3 tests): real value renders formatted to one decimal (never the raw unrounded number); no-coverage row renders "—" (never blank/`undefined`/`NaN`); and a row whose materializer forgot to thread the backend stamp still falls to "—" rather than the page computing its own average from `sourceOriginalRanks` — the "invent a client rank" failure this row explicitly forbids. **Mutation-proved**: `Stat label="Blended rank" value={blended != null ? blended.toFixed(1) : "—"}` → `value={blended && blended.toFixed(1)}` (reintroduces exactly the historical blank-render defect, since `null && ...` renders nothing) ⇒ RED, 2 of 3 tests fail (`expected [ '12.3', '' ] to include '—'`). Reverted ⇒ GREEN, `3 passed`. Full frontend suite re-run clean after revert: 148 files, 2285 tests, 0 failures |
| V1-99 | Public/private boundary holds on both channels | `F-PRIV-01` / `PL-BOUNDARY` | L5 | `VERIFIED` | L2 | governing product split. B8 |
| V1-100 | Unauthenticated draft-capital redaction | inv 9.7 | L5 | `VERIFIED` | L2 | owner decision 2026-08-11 |

### 3.8 Auth / admin

| # | capability | canonical id | lane | status | level | in V1 because |
|---|---|---|---|---|---|---|
| V1-101 | `/admin` `fmtPassExpiry` crash repaired | `#779` | L6 | `IMPLEMENTED_UNVERIFIED` | L4 | named V1 scope; live user-facing crash. **#912.** **Checked 2026-08-21**: the fix is real and complete on `main` — `fmtPassExpiry` lives in `frontend/lib/guest-pass-format.js` (a proper module export, not the orphaned-extraction private helper that produced `Can't find variable: fmtPassExpiry`), both `GuestPassPanel.jsx` call sites import from it, and `frontend/__tests__/components/admin/GuestPassPanel.test.jsx` reproduces the exact reachability condition (a non-empty pass list — the crash was invisible to a naive empty-state smoke test) and covers both call sites. `3 passed` confirmed this session. Left at `IMPLEMENTED_UNVERIFIED` rather than `VERIFIED` because L4 requires proof the deployed production surface consumes it — a live `/admin` check — which this session has no path to perform; the component-level regression evidence is real but is L2/L3, not L4 |
| V1-102 | Temporary-password generator with configurable expiry | `#780` | L6 | `IMPLEMENTED_UNVERIFIED` | L4 | named V1 scope; owner workflow must work end-to-end. **RECLASSIFIED 2026-08-25 `NOT STARTED` → `IMPLEMENTED_UNVERIFIED` — the row was FALSE, and the tally does not move.** `#1086` reported the staleness; the evidence below was re-derived **independently on the shipping tree** (`main` `8200d4c18`) rather than taken from that PR body, since a status edit sourced from an unmerged branch would be exactly the assertion this ledger forbids. `src/api/guest_passes.py` (present since 2026-08-18, i.e. before the row was written) satisfies every stated requirement: tokens from `secrets.token_urlsafe(_TOKEN_BYTES)`; **SHA-256-only persistence** (`hashlib.sha256`, column `token_hash TEXT NOT NULL UNIQUE`) with the plaintext shown to the owner exactly once and never stored; **configurable expiry** bounded `_MIN_DURATION_HOURS` 1 minute → `_MAX_DURATION_HOURS` 30 days, validated on creation; full create/list/revoke surface at `POST /api/admin/guest-pass`, `GET /api/admin/guest-passes`, `POST /api/admin/guest-pass/{id}/revoke` (`server.py:12852/12915/12936`); server-side enforcement via `is_expired` / `is_revoked` / `purge_expired()` called on every `validate`; `tests/api/test_guest_passes.py` present. **The guest-pass system IS the temporary-password generator** — building a second one would have created the duplicate auth subsystem this row forbids, so the correct outcome was that nothing was implemented. **NOT `VERIFIED`, and deliberately so:** the required level is **L4**, whose bar is one authenticated `/admin` run on a deployed SHA minting a pass at a **non-default** duration (a run at the default 12 h leaves a hardcoded 12 indistinguishable from a wired control). That is unreachable from this environment — same `BLOCKED-EXTERNAL` class as §9b. `IMPLEMENTED_UNVERIFIED` is not counted in the numerator, so **89 / 136 is unchanged** |
| V1-103 | Security repairs | inv 9.6 | L5 | `VERIFIED` | L2 | named V1 scope. W22-F002/F003/F005/F007. **PROMOTED → `VERIFIED` (L2) 2026-08-25 (Integration) — all four named findings repaired, merged, and mutation-proven.** `W22-F002` (`#1112`): the rate limiter keyed on the FIRST `X-Forwarded-For` entry while nginx APPENDS — an attacker-chosen key; trust order is now `X-Real-IP` (nginx REPLACES it) → last XFF entry → socket peer, rotation-invariance pinned (5 spoofed requests → exactly 1 limiter key; mutation 3 RED). `W22-F005` (`#1112`): `/api/scaffold/status` de-allowlisted after a caller census found zero anonymous readers; pinned in `PRIVATE_API_PATHS` (mutation RED). `W22-F007` (`#1115`): `POST /api/scrape`, `POST /api/test-alert` and `POST /api/intel/refresh` gate on `_require_admin_session` — a guest-pass session gets 403, never a fired scrape/email/crawl; full mutating-endpoint census in the PR (5 already admin-gated, 2 bearer/owner-gated left alone, product features kept user-level, zero adjudication-needed; mutation: gate dropped → 403 test RED with the scraper spy quiet). `W22-F003` (`#1115`): the login endpoint gains a failure throttle in the ONE rate-limit owner — 5 free attempts per 15-min window then 1s→60s exponential backoff as 429+`Retry-After`, checked BEFORE credential comparison; keyed `ip` and `(ip, username)`, NEVER username alone (remote owner-lockout structurally impossible, isolation test-pinned); success clears only `(ip, user)` so one valid guest pass cannot reset the IP-wide counter (mutation: recorder no-op'd → 5 RED). `tests/api/` 2,193 passed on the M-parts head. No allowlist entry added anywhere; no auth path weakened — every change narrows or brakes |
| V1-104 | Human review / admin controls | inv 6.9 / `#779`-adjacent | L5 | `VERIFIED` | L1 | named V1 scope; narrow today (sharp-identities only). **PROMOTED 2026-08-25 (#1123, `c53cf1eb2`) at L1.** Scope held to the stated boundary — sharp identities only, no general admin system. The review layer already existed (`/api/sharp/review*` in `src/sharp/curated_service.py`, `curated.review_candidate`, the `sharp_review_decisions` audit table, `curated_cohort_members` feeding `src/sharp/cohort.py`). One genuine defect found and repaired: the audit "who" was a body-supplied `reviewer` claim defaulting to the constant `"admin"` — and the admin UI never sends one, so every real decision was audited as `"admin"`. `decide_candidate` now attributes the decision to the authenticated admin session's username (new `_admin_session()` helper; the body `reviewer` is ignored). RED→GREEN: attribution test RED at pre-fix head; deleting the guard from `decide_candidate`/`refresh_curated` turns the guest-403 tests RED; approval promotes into `curated_cohort_members` with a real audit row (`reviewer`/`reason`/`decided_ms>0`). 6 new tests + `tests/sharp/`/operator-admin-gate/admin-endpoints 336 passed; green CI |

### 3.9 Performance, mobile parity, high-use Premium UI

| # | capability | canonical id | lane | status | level | in V1 because |
|---|---|---|---|---|---|---|
| V1-105 | Performance baselines captured before feature work | `C0-PERF-01` / `C0-U2` | L6 | `VERIFIED` | L2 | a baseline captured after the work cannot show what the work cost. **Packet B ANSWERED by owner 2026-08-25 — option (A) accepted** (verbatim: *"ACCEPT that the true pre-feature `/rankings` p95 baseline is permanently irrecoverable. Do not fabricate or retroactively relabel a current measurement as the old baseline. Record this as an explicit owner-accepted irrecoverable exception and preserve real current/prospective measurements."*). Closure route: merge `#1006` (the baseline audit — the canonical classification of every candidate source) so the record is on `main`, then close at L2 on the baselines legitimately captured for still-unmodified surfaces (`/trade`, `/league`, `/` — prospective baselines measured 2026-08-24 at `131abf9f9`, 5 runs/cell, p95, both viewports, `docs/master-site-audit/evidence/C0-PERF-01/CURRENT_STATE_2026-08-24.md`), with the `/rankings` gap preserved explicitly as owner-accepted irrecoverable (its 2026-08-24 numbers are CURRENT-STATE only, never a baseline) and the unmeasured routes (`/waivers`, sharp pages) recorded `BLOCKED_EXTERNAL` rather than dropped. Status moves only when `#1006` is merged and the closure statement is recorded here — decision received is not closure. **CLOSED 2026-08-25: `#1006` merged (`1b491feba`) — the baseline audit (every candidate source classified VALID_PRE_FEATURE_BASELINE / POST_FEATURE_MEASUREMENT / UNATTRIBUTABLE / MISSING) is now in the canonical record — and the row closes exactly on the owner-accepted terms: the `/rankings` pre-feature p95 baseline is PERMANENTLY IRRECOVERABLE (owner-accepted exception; its 2026-08-24 numbers are CURRENT-STATE only and are never relabeled a baseline); the legitimately captured prospective baselines stand for the still-unmodified surfaces (`/trade`, `/league`, `/` — 2026-08-24 at `131abf9f9`, 5 runs/cell, p95, both viewports, `docs/master-site-audit/evidence/C0-PERF-01/CURRENT_STATE_2026-08-24.md`, raw JSON committed); the unmeasured routes (`/waivers`, sharp pages) stay recorded `BLOCKED_EXTERNAL`, never dropped; and the one target miss (desktop `/rankings` warm p95 useful 1018 ms > 1000 ms) is preserved rather than rounded away. The L2 bar for a baseline row is the measured artifact + canonical record, both now on `main`; the instrument's own false-green finding (a verdict computed over an empty set) is recorded in the report and routed to the instrument owner |
| V1-106 | Rankings board windowing / virtualization | `C8-PERF-03` / inv 9.1 | L6 | `VERIFIED` | L2 | **named explicitly** in the V1 boundary. PR #760 **PROMOTED 2026-08-20 at L2 (#1003, merge `dda306cfe`).** **The interpretation was settled from the canonical record, not chosen.** §2 defines L2 as *"L1 plus a measured statement of the effect on the live board or contract"* — a MEASURED STATEMENT, with no threshold in it — and this row's own text carries no FPS number at all. The `>=50 @1x AND >=30 @4x` conjunction lives in the engineering ledger, not in the canonical V1 contract, so it is not this row's bar. No third threshold was invented. **The inconvenient number is recorded verbatim: 24.0 FPS @4x, which does NOT meet the old engineering target** (59.7 @1x, 24.0 @4x, 12.6 @6x), alongside the windowing measurement that does: Show All = **983 rows, 37 mounted, 1,536 DOM nodes**. **L1 verified at Integration by execution on a real stack** (backend + Next production build + authenticated session, Chromium at 1366x768 and 390x844), not from the merge record: `tests/e2e/specs/rankings-windowing.spec.js` **3 passed** — bounded mounted DOM on the full board, scrolling moving the window while `aria-rowcount` stays constant, and mobile bounded too. **The mutation the spec itself only CLAIMED:** its header says `virtualize` off was *"verified manually during development"*, which is not a committed proof, so Integration reperformed it — `frontend/app/rankings/page.jsx` `virtualize` -> `virtualize={false}`, full rebuild, **all 3 RED** (desktop bounded-DOM, desktop scroll-window, mobile bounded-DOM); restored, rebuilt, **3 passed**, `git diff` empty. That is what makes these tests non-vacuous rather than a pair of assertions that pass whether or not windowing is on.
| V1-107 | Mobile payload smaller than desktop | `C8-PERF-01` / audit `F-13`, `F-32` | L6 | `VERIFIED` | L2 | mobile-desktop parity. **VERIFIED 2026-08-20 at Integration on merged `main` (#912, merge `b3ea4cf5a`).** Target level **L2**, and both halves hold. **L2 measured INDEPENDENTLY at Integration**, not read off the PR — rebuilt from archive `dynasty_export_20260820_033106.zip`, 1,109 rows, gzip level 6, serialized as `server.py` prepares a view: full **1,098.4 KB** · array (desktop) **635.2 KB** · compact (mobile) **492.9 KB** — **−22.4% against desktop**, and `"players" in compact` is **False**. The row's opening measurement was mobile **+16.3% LARGER**; it is now decisively smaller while carrying MORE per-player fields. **L1: two mutations, each confirmed APPLIED with the changed line quoted.** (MP1) `compact_view.py:196` `if k == "players" and isinstance(v, dict):` → `if False:`, i.e. carry the legacy dict again ⇒ **2 named REDs**, `test_compact_is_smaller_over_the_wire_than_array` (reporting compact **955.4 KB vs array 635.0 KB, +50.4%** — the original defect reproduced) and `test_compact_carries_the_array_encoding_and_not_the_dict`. (MP2) adding `blendedSourceRank` to `_PRUNED_PLAYER_FIELDS` — the LOSSY half, and the exact sort key this row names ⇒ **2 named REDs**, `test_compact_prunes_no_field_the_materializer_reads` and `test_the_surviving_prunes_are_genuinely_unread`. The byte test asserts the **relation** (`compact < array`), never an absolute, so it cannot rot as the contract grows. One earlier mutation attempt produced invalid syntax (`tuple + set`) and is **NOT counted** |
| V1-108 | Non-data routes stop fetching the contract | `C8-PERF-02` | L6 | `VERIFIED` | L2 | rankings/app performance. PR #759, shipped in **#912** (merge `b3ea4cf5a`). **NOT promoted — L1 holds, L2 has no committed instrument.** `AppShell.jsx::isNoPlayerDataRoute` is the gate and `frontend/__tests__/app-shell-route-gates.test.js` pins it well: descendants match (`/admin/sharp-identities`), a shared prefix does not, a null pathname does not throw, and — the part that makes it non-vacuous — the two routes deliberately NOT gated are pinned with their reasons (`/settings` calls `useDynastyData()` itself; `/tools/trade-coverage` reads `useApp().rawData`). **But that tests the PREDICATE, not the behaviour.** §2's L2 needs a measured statement, and the PR's two measurements — a transitive scan over the 73 reachable modules, and a browser probe finding 0 contract requests on all six routes against 1 each on a control — are **one-off findings that are not committed as tests**. Verified at Integration: no E2E spec asserts a contract request count, and no frontend test walks the route import graph. So a route re-acquiring a data consumer would be caught by neither, which is exactly the regression this row exists to prevent. **To close:** commit one of the two measurements as a guard — the transitive import-graph scan is the stronger and needs no browser — then record its result **PROMOTED 2026-08-20 at L2 (#989, merge `08f47f61d`).** The row's own "to close" named the instrument — *commit one of the two measurements as a guard; the transitive import-graph scan is the stronger and needs no browser* — and that is exactly what shipped: `frontend/scripts/route-import-graph.mjs` plus `frontend/__tests__/route-gate-no-player-data-transitive.test.js`. Verified at Integration on SHIPPING MAIN (`36b7033f8`), by execution: **16/16 green**, covering all 7 gated page modules (`/login`, `/more`, `/design`, `/admin`, `/admin/sharp-identities`, `/tools/source-health`, `/tools/ros-data-health`) plus vacuity guards on both the gate list and each route's page set. **Mutation was deliberately TRANSITIVE, not a direct import**, because a direct-import check would not test the property this guard exists for: a tracked two-hop chain (`app/tools/source-health/page.jsx` → `lib/_v108_probe_outer.js` → `lib/_v108_probe_inner.js` → `useApp()`) ⇒ **RED**, 1 failed / 15 passed, and the failure NAMED the reachable path — *"transitively reaches a player-data consumer it must not have: useApp() reachable via lib/_v108_probe_inner.js"* — identifying the inner hop, two modules from the page. Restored ⇒ 16/16, working tree clean. **L2 statement, re-measured on the current tree rather than carried forward:** the 7 gated page modules reach **53 distinct modules** and **0 player-data consumers**; the two deliberate exclusions reach **3** between them — `/settings` 1 (`useDynastyData` directly) and `/tools/trade-coverage` 2 (`useApp` directly AND transitively via `components/useTeam.js`). That transitive control hit is what proves the scanner walks real code, not only a synthetic probe. Note the 53 does not reproduce the one-off "73 reachable modules" from #759's original finding and is not presented as reproducing it — different tree, and the union is counted distinctly here rather than summed per route (the per-route sum is 94). **#989 changed no runtime behaviour**, structurally: the merge touched exactly two files, a test and a dev-tooling scanner, and a repo-wide grep finds no production module importing `route-import-graph.mjs`. **Deliberately NOT claimed:** the browser half of the original measurement (0 contract requests on the six routes against 1 each on the control) could NOT be reproduced in the Integration rig — the positive controls fell silent, registering 0 API requests where `/rankings` registered 8, so the instrument was unfalsifiable and its numbers are withheld rather than reported. The promotion rests on the committed structural guard, which is what the row asked for.
| V1-109 | Mobile usability for roster-heavy views | inv 9.3 / MFB-67 / MFB-93 | L6 | `IMPLEMENTED_UNVERIFIED` | L4 | mobile-desktop parity. **`#1086` merged (2026-08-25): honest partial**, then **`#1131` merged (2026-08-25) closed the remaining two** (`docs/master-site-audit/evidence/V1-109/REPAIR_2026-08-25.md`) — this cell previously went stale describing only `#1086`'s partial state and contradicted the row's own status column; corrected 2026-08-27 (Integration, session reconciliation), no new engineering. All four named defects are now fixed at code level with mutation-proved E2E coverage: the /trade tray↔FAB occlusion (44×44px overlap → 0), the /draft stacking defect (0px document overflow), the watchlist touch targets (17×13px → 44×44 hit-area pseudo-element, `frontend/app/rankings/board.module.css::.watchStar`, verified present on `main`), and the sub-10px type floor on /rankings, /rosters, /draft (raised onto `--font-size-2xs` / 11px). Row stays `IMPLEMENTED_UNVERIFIED` (matches the status column, not `IN PROGRESS`) — required level is L4, a production-consumer statement, which no local measurement satisfies |
| V1-110 | Design primitives / tokens / shell / focus | `C8-PSI-01` | L6 | `VERIFIED` | L2 | high-use Premium UI. **PROMOTED `IN PROGRESS` → `VERIFIED` (L2) 2026-08-25 (Integration).** The recorded blocker was stale: **`OD-05` is RESOLVED 2026-08-18** (`docs/DESIGN-SYSTEM.md:3` — the North Star doc supersedes the war-room-terminal direction), and the resolved direction is what shipped. Measured on `main`, with every contrast claim **independently recomputed, not transcribed**: `frontend/app/tokens.css` carries 241 tokens, imported FIRST in `globals.css`; the accent family's inline measurements reproduce exactly (base 12.44/11.68/10.92/10.06 across surfaces 0-3, hover 13.76/12.92/12.08/11.13, pressed 9.47/8.90/8.32/7.66, `--text-on-accent` 12.82:1) — every accent state clears the 3:1 UI floor with ≥2.5× headroom and 4.5:1 as text; the text ladder's worst case across all four surfaces is primary 13.55 / secondary 7.21 / tertiary 5.11, so every text token clears AA 4.5:1 on every surface it may sit on, confirming the inline "still AA on every surface" claims by computation. SHELL: `AppShellWrapper` mounted at `frontend/app/layout.jsx:84`, `components/shell/` (TopBar / NavMenu / MobileChrome / CommandPalette) styled by `shell.css` — 167 `var(--…)` token references, imported via globals. FOCUS: one `:focus-visible` ruleset in `ds.css` covering the ds-* interactive classes (buttons, inputs, selects, tabs, segmented options, table sorts, interactive rows). Scope note: this row is the PRIMITIVES layer; per-page migration onto it is tracked by its own rows (`V1-109`, `V1-131`, Lane 6's `#1086`) and is not claimed here |
| V1-111 | Premium migration of the high-use routes (Rankings first) | `C8-PSI-02` / `R-PREMIUM` | L6 | `VERIFIED` | L4 | **high-use** Premium UI only — route-by-route migration is post-V1. **Status corrected 2026-08-20, NOT promoted.** `NOT STARTED` became false when `C8-PSI-02` merged (#984, `5c43da9e8`): the Rankings reference route and the Universal Player Profile both ship, on an additive `.psi-editorial` token scope, with the shell restyled across every route. **This is a status correction, not a promotion — the row's target is L4 (a production consumer), and merging is not deploying, let alone consuming.** What IS established, by measurement at Integration rather than from the PR text: browser-rendered evidence on desktop 1366×768 and mobile 390×844 against a real backend and the real board, for a rich-data player (Josh Allen 9,991 / #1 / QB1 / 14 sources) and a partial-data player (AJ Dillon — `not priced`, `None — unranked`, no position-rank tile at all, "Value history unavailable"); zero prototype constants; no frontend value, rank or weighting computation; WCAG contrast measured from 11 failing colour groups (worst 1.04:1) back to **0** on both viewports, agreeing with the lane's independent axe run of 187 nodes → 0; and both CI gates green at the exact merged head, E2E genuinely applicable because `components/shell/**` is in its path filter. **What L4 still needs:** the deployed build serving these routes on production, and a real consumer of them. Recorded separately and not inferred from this merge. Full record: `docs/psi/PR_A_VISUAL_VERIFICATION_2026-08-20.md` **PROMOTED 2026-08-25 from fresh production evidence.** Fresh post-#1135 authenticated production verification run 32919648642 against deployed SHA `a82898fdbe73ea2effd295132590dff35a45ac98` passed the dedicated `v1-111-premium-rankings.spec.js` consumer proof at both production desktop and 390x844 mobile. The spec first reads the authenticated `/api/data?view=app` contract, proves `.psi-editorial` is actively applied, and verifies rendered rank/name/value tuples are the backend stamps rather than client recomputation; partial/missing states remain explicit. This supplies the previously missing L4 deployed-consumer evidence.
| V1-112 | Streaming SSR does not leave a duplicate DOM copy | `#730` | L6 | `VERIFIED` | L2 | stability; page markup renders twice. **VERIFIED 2026-08-21.** Old PR #758 (still open, never merged) carried the dedicated invariant test (`tests/e2e/specs/ssr-duplication.spec.js`), a 2,400-load statistical harness (`frontend/scripts/measure-duplication.mjs`), and an unrelated flake fix in `journey-tools-health.spec.js`. Per instruction, NOT merged wholesale: only the spec — the smallest current-valid instrument — was ported (verbatim invariant, updated provenance header). Confirmed still valid on current main before porting: both helpers it depends on (`awaitStreamSettled` in `tests/e2e/helpers/journey.js`, landed by #747 well before #758) and `pageUrl`/`../helpers/auth-fixture` are present unchanged, and all three target routes (`/rankings`, `/waivers`, `/arbitrage`) still exist. The rate-measuring harness and the unrelated flake fix were deliberately left out of scope. **L2 — measured on the live board**: ran against a real production build (worktree HEAD `0a5267d06`, `next build`+`next start` on :3000, backend on :8000) — `6 passed` across `desktop-1366` + `mobile-chromium` × 3 routes, 0 duplicate ids, 0 surviving staging containers, 1 `<h1>` per route on every run. (`mobile-390`/`mobile-430` could not launch Chromium in this sandboxed root container — `Running as root without --no-sandbox is not supported` — a pre-existing environment limitation unrelated to the app or this spec; not run, not counted as pass or fail.) **L1 mutation-proved**: `components/AppShell.jsx`'s `InnerAppShell` — `{children}` → `{children}{children}` (the literal shape of the historical `dynamic()`-around-`{children}` defect: the entire routed page duplicated). Confirmed live via raw HTML before running the suite (`<h1>` count 1 → 2, `id="main"` count 1 → 2 on `/rankings`). Rebuilt, re-ran the suite ⇒ **6 of 6 RED**, each failure naming its exact duplicate ids (`/rankings`: `main×2`; `/waivers`: `main×2, waiver-pos×2, waiver-strength×2, waiver-min-gain×2`; `/arbitrage`: `main×2`). Reverted the mutation, rebuilt, re-ran ⇒ **6 of 6 GREEN** again, confirmed via a second raw-HTML check (`<h1>` back to 1) before the suite ran |
| V1-113 | Accessibility instrumentation in CI | `C8-A11Y-01` | L6 | `VERIFIED` | L1 | accessibility not measured in CI regresses silently. **VERIFIED 2026-08-20 at Integration on merged `main` (#912, merge `b3ea4cf5a`).** Target level **L1** — §2: a RED→GREEN test at exact head plus green CI on the merge tree. **The CI half is the one that mattered, and it was confirmed from the run log rather than inferred.** `e2e.yml` invokes `npx playwright test` with `testDir: ./specs` and **no spec filter**, so `a11y-axe.spec.js` runs with everything else; it is path-triggered because this PR touches `tests/e2e/**`. Downloaded the `Boot stack + run journeys` job log for the reconciled head `677442ea5`: **20 lines mention `a11y-axe.spec.js`, 20 passed, 0 skipped, 0 failed** — 10 routes (`/`, `/admin`, `/league`, `/login`, `/rankings`, `/rosters`, `/settings`, `/trade`, `/trades`, `/waivers`) × 2 viewports (`desktop-1366`, `mobile-chromium`). Job green at 05:53:44. This check was made because a spec that silently skips is indistinguishable from one that passes, and the run reported 59 skips overall — none of them axe. **The RED→GREEN half** is the row's own history: the first scan found **84** WCAG A/AA violations (`/settings` 4 unlabelled inputs + 3 unnamed selects + 21 `aria-prohibited-attr`; `/rosters` 57 contrast failures to 2.19:1 and a keyboard-unreachable scroll region; `/rankings` a 4.494:1 near-miss), and the baseline is now `{}` — **empty by measurement**. The ratchet fails on new or worsened violations **and** on a stale allowance, so an entry nobody re-checks cannot absorb the next regression |

### 3.10 CI / E2E, governance and production verification

| # | capability | canonical id | lane | status | level | in V1 because |
|---|---|---|---|---|---|---|
| V1-114 | One authorization record matching merged reality | `C0-GOV-01` | L5 | `VERIFIED` | L1 | governance; this contract + EXECUTION_PLAN §0. **VERIFIED 2026-08-20 at Integration.** Target level **L1**. Enforced by `scripts/check_planning_integrity.py::check_single_authorization_record`, which is **blocking on every PR** (`Validate PR`) and passes on `main` at `5cc65a3ab`. It scans every `docs/*.md` plus `PRODUCT_PLAN.md` for the authorization-record heading and requires the claimant list to be exactly `[EXECUTION_PLAN.md]`. **Bidirectional, which is what makes it a guarantee rather than a spelling check** — two mutations, each confirmed APPLIED with the changed line quoted: (MG-114) a second document carrying that heading ⇒ RED `[authorization] expected exactly one authorization record (EXECUTION_PLAN.md); found ['EXECUTION_PLAN.md', 'MUTATION_PROBE_AUTH.md']`; (MG-114b) altering the heading inside `EXECUTION_PLAN.md` so NO document claims it ⇒ RED `…found none`. Neither a second owner nor a vanished owner can pass. **Demonstrated in anger on this very edit:** the first draft of this row quoted the heading verbatim, which made THIS document a claimant, and the gate failed the tree with `found ['EXECUTION_PLAN.md', 'VERSION_1_COMPLETION_CONTRACT.md']` before the change could ship. The row is therefore worded to describe the marker rather than reproduce it |
| V1-115 | One CE identifier namespace | `C0-GOV-02` | L5 | `VERIFIED` | L1 | governance. **VERIFIED 2026-08-20 at Integration.** Target level **L1**. Enforced by `scripts/check_planning_integrity.py`, which is **blocking on every PR** (`Validate PR`) and passes on `main` at `5cc65a3ab`. `check_ce_registry` enforces BOTH halves of the claim, each mutation-proven and confirmed APPLIED: **uniqueness** — (MG-115c) inserting a CONFLICTING duplicate (`CE-01` a second time with a different capability) ⇒ RED `[ce-unique] CE-01 defined twice in the registry: 'Market Trade Ledger / Trade Database' vs 'A Totally Different Capability'`; **mirroring** — (MG-115b) renaming `CE-01`'s capability in the registry ⇒ **two** REDs, `[ce-mirror]` against `MASTER_PRODUCT_PLAN.md` *and* `OWNER_FEATURE_INVENTORY.md`, so a rename cannot be applied to one copy only. Recorded so it is not mistaken for a gap: (MG-115a) a **verbatim** duplicate row passes, and that is correct — an identical restatement is a typo, not a namespace collision; the guard fires on conflicting definitions, which is the property the row asserts |
| V1-116 | Zero-loss scope census | `C0-GOV-03` / `C0-GOV-04` | L5 | `VERIFIED` | L1 | governance; this document is part of the deliverable. **VERIFIED 2026-08-20 at Integration.** Target level **L1**. Enforced by `scripts/check_planning_integrity.py`, which is **blocking on every PR** (`Validate PR`) and passes on `main` at `5cc65a3ab`. The census is a claim of EXHAUSTIVENESS, which is the kind that decays silently, so two independent mutations were performed and both confirmed APPLIED with the changed line quoted: (MG-116) setting `docs/C_SERIES_ZERO_LOSS_TRACEABILITY.md`'s **Unexplained unmapped** total from `0` to `7` ⇒ RED `[traceability] unexplained unmapped is 7, not 0`; (MG-116b) inflating `<!-- MANIFEST-ROW-COUNT -->` from 163 to 172 ⇒ RED `[manifest-count] MANIFEST-ROW-COUNT declares 172 but 163 rows are present`. So neither an admitted loss nor a miscounted denominator can pass, and the count is measured from the row table rather than typed forward — the same rule the contract's own §3.11 tally now lives under |
| V1-117 | One intake mechanism for owner instructions | `C0-GOV-05` | L5 | `VERIFIED` | L1 | governance. **PROMOTED 2026-08-21; the row's stated blocker was itself stale.** "A doc the index calls superseded" was true before 2026-08-14 and is not true now: `docs/OWNER_REQUESTED_TODO.md` line 3 self-declares its status as active and names itself the live owner-instruction ledger (reclassified by the post-B master reconciliation), and `docs/PLANNING_DOCUMENT_STATUS.md` §2 + its governance-index table agree — measured, not read from prose, by grepping both files rather than trusting either one's self-description. **No structural L1 guarantee existed for this**, so one was built rather than assumed: `scripts/check_planning_integrity.py::check_single_intake_ledger` (check 10), mirroring `check_single_authorization_record`'s exact mechanism — a marker regex anchored on that self-declaring status line's own exact wording (see the function's docstring for the literal pattern; deliberately not reproduced here — quoting it verbatim in this row would itself trip the guard, exactly the trap V1-114 records for the authorization-record heading) specifically so a *cross-reference* to it (`PLANNING_DOCUMENT_STATUS.md`'s own index-table row naming `OWNER_REQUESTED_TODO.md`) is not miscounted as a second claimant. **Blocking on every PR** via the same `Validate PR` step that already runs `check_planning_integrity.py`. Two mutations, both confirmed APPLIED: adding a decoy doc carrying the marker ⇒ RED `[intake-ledger] expected exactly one live owner intake ledger (OWNER_REQUESTED_TODO.md); found ['MUTATION_PROBE_INTAKE.md', 'OWNER_REQUESTED_TODO.md']`; replacing the marker text inside `OWNER_REQUESTED_TODO.md` so no document claims it ⇒ RED `…found none`. Both restored, `check_planning_integrity.py` GREEN. |
| V1-118 | Governance index names every planning doc | `C0-GOV-06` | L5 | `VERIFIED` | L1 | governance. **VERIFIED 2026-08-20 at Integration.** Target level **L1**. Enforced by `scripts/check_planning_integrity.py`, which is **blocking on every PR** (`Validate PR`) and passes on `main` at `5cc65a3ab`. (MG-118) adding `docs/MUTATION_PROBE_SPEC.md` — an in-scope planning document classified nowhere — ⇒ RED `[governance-index] docs/MUTATION_PROBE_SPEC.md is a planning document but is classified nowhere in docs/PLANNING_DOCUMENT_STATUS.md`, naming the offending file. **A first attempt did NOT count and is recorded rather than dropped:** `docs/MUTATION_PROBE_PLAN.md` left the gate green, because the guard scopes itself with `PLANNING_NAME_PATTERNS` and that filename matches none of them. That was a defective probe, not a defective guard — but a silently-unapplied mutation reading as a pass is exactly the false green this campaign keeps finding, so the retry used a filename actually in scope. Known and deliberate boundary: a planning document whose name matches no pattern is not policed, so the index's completeness is relative to that pattern list |
| V1-119 | Planning-integrity validation in CI | `C0-GOV-07` | L5 | `VERIFIED` | L1 | **VERIFIED 2026-08-20 on merged `main` `547e51bc6`.** L1 satisfied on all three required checks. (1) The gate is genuinely BLOCKING, not advisory: `pr-validation.yml:168` "Planning integrity gate" carries no `continue-on-error`, and it was observed green as step 12 of every PR Validation job run this session. (2) **Mutation proof**, performed rather than asserted: changing the declared RET-row count in `docs/C_SERIES_SCOPE_MANIFEST.md` from **12 to 19** makes `check_planning_integrity.py` exit **1** with `[manifest-count] the counts table declares 19 RET rows but 12 carry the RET flag`; restoring the file returns exit **0**. (3) Merge-tree CI green. Two earlier mutation attempts of mine silently missed their targets and returned exit 0 — a pass over an unapplied mutation proves nothing, and only the confirmed-applied one is counted here |
| V1-120 | Governance / directive reconciliation | `C0-R` | L5 | `VERIFIED` | L3 | production verification. **§7.2 checklist re-run 2026-08-18 against the post-#910 deployed SHA `92469d32` (§7.2b): 7 of 7 PASS.** The earlier 6-of-7 at `8ec1978e` recorded item 5 as UNRUN because `pytest` was believed absent from this session; it was installed as a `uv` tool binary, so `python -m pytest` failed while `pytest` worked — the verifier tested a proxy for the question instead of the question. Re-run on a worktree at the deployed SHA: `pytest tests/docs/ -q` → **22 passed**, all three modules. Ancestry, both governance gates, the census figures and the reserved-phrase check all hold there, and the deploy itself was green end to end including the post-deploy smoke test and the live data-contract validation |
| V1-121 | Release-gate classification: every check has a category | release discipline | L5 | `VERIFIED` | L1 | CI. A real blocker must not be treated as noise, nor a detector block releases. **Correction 2026-08-21 (owner comment on #1021):** this row was briefly promoted on `#1021` citing `validate_api_data_contract`'s `sourceHealthErrors`/`structuralErrors` partition (`tests/api/test_contract_health_lanes.py`) as evidence. That partition is a real, separately-useful invariant, but it is a different one — it classifies *error messages inside one contract-validation function*, not "every CI check has a category" (i.e. every CI *workflow step* across the release gate is classified as a real blocker vs. advisory noise, which is what the 2026-08-16 incident and this row's canonical id are actually about). Demoted back to `NOT STARTED`, correctly, until the right mechanism landed. **PROMOTED 2026-08-21, extracted from `#1030`.** `#1030` was a 17-file, +2012/−42 branch mixing this row's real mechanism with V1-123/124/125/126 work that is NOT promotion-ready (production matrix/jobs unexecuted, duplicate census incomplete, regression command unrun) and a stale base (predating `#1025`/`#1029`) that left it `mergeable_state: dirty`. Rather than merge the whole branch for one row, Integration extracted the three-file V1-121 mechanism verbatim onto a fresh branch off current `main`: `config/ci/release_gate_classification.json` (the manifest — 235 workflow-step entries, 31 workflow files, 33 jobs), `scripts/ci_gate_classification.py` (`enumerate_workflow_steps()` reads ONLY two structural GitHub Actions signals — `continue-on-error: true` and a `vars.`/`secrets.` inequality `if:` conditional-skip flag — never heuristic inference; the module docstring names a prior heuristic attempt measured wrong, 23 hits narrowed to 5, all 5 false positives, and this design deliberately does not repeat it), and `tests/deploy/test_release_gate_classification.py` (4 tests: every live step has a manifest entry, no manifest entry names a step that no longer exists, declared category matches the live `continue-on-error` structure, and the manifest is well-formed). Reperformed independently at Integration on the freshly-extracted tree, not trusted from the source branch: all 4 tests GREEN; manifest's declared 235-entry count re-verified against a live `enumerate_workflow_steps()` count on today's workflow files (not assumed still current after several intervening merges this session) — matches exactly. Three mutation proofs run fresh: (1) removed one manifest entry ⇒ `test_every_workflow_step_has_a_manifest_entry` RED, naming the exact orphaned step; restored ⇒ GREEN. (2) added a fake entry for a nonexistent step ⇒ `test_no_manifest_entry_names_a_step_that_no_longer_exists` RED, naming it; restored ⇒ GREEN. (3) flipped one real `blocking` entry to `advisory` ⇒ `test_declared_category_matches_the_live_workflow_structure` RED, reporting the exact mismatch; restored ⇒ GREEN, clean tree. Sample classification cross-checked by hand against this session's own first-hand CI observations: `pr-validation.yml`'s three known advisory steps (`Release-candidate freshness`, `Run live-data tests`, `Source-health check`) classify `advisory`; lint/build/unit-test/contract/planning-integrity/coercion gates all classify `blocking` — matches every CI run seen this session. `#1030` itself is left open and untouched for a future session to evaluate V1-123/124/125/126 on their own terms — this promotion claims only the 3-file mechanism, nothing else from that branch. |
| V1-122 | Structural / source-health CI lanes stay separated | `docs/ops/STABILIZATION_2026-08-16.md` | L5 | `VERIFIED` | L1 | CI correctness |
| V1-123 | Browser / workflow matrix | `C10-CLOSE-03` | L5 | `NOT STARTED` | L4 | production verification |
| V1-124 | Background jobs and data proven in production | `C10-CLOSE-04` | L5 | `VERIFIED` | L3 | production verification **PROMOTED 2026-08-25 from fresh production evidence.** Fresh post-#1135 on-box production verification run 32919655291 against deployed SHA `a82898fdbe73ea2effd295132590dff35a45ac98` returned `V1-124: PASS`. The canonical on-box verifier checks the real production timer units/job timestamps and their actual data artifacts, and records unavailable artifacts truthfully rather than coercing missing to zero. This supplies the row's L3 production proof.|
| V1-125 | Duplicate owners retired (every `retires` line zero) | `C10-CLOSE-02` | L5 | `NOT STARTED` | L2 | ONE CONCEPT, ONE CANONICAL OWNER |
| V1-126 | Final V1 regression green | `C10-CLOSE-07` | L5 | `NOT STARTED` | L1 | production verification |

### 3.12 Source acquisition and cross-position bridge (owner decision, 2026-08-20)

The owner decided on 2026-08-20 that the **multi-bridge / source-acquisition program** is V1
and top priority. That is a denominator change, and it is recorded here rather than folded in
silently — §10 makes adding a V1 REQUIRED item an owner decision, and this is one.

**It is +4, not +6.** The owner named six capabilities; two of them already have owners in this
contract, and admitting them again would inflate the denominator with work that is already
counted. Integration's reconciliation, stated so it can be corrected:

| owner-named capability | representation | denominator |
|---|---|---|
| multi-bridge cross-position translation | **new** — no row covers the mechanism. `V1-23` names IDP valuation *correctness* (the outcome); `V1-132` names one pick-year vendor dependency. Neither is a bridge | **+1** (`V1-133`) |
| truthful no-bridge failure | **new** — and it gets its own row for the same reason `V1-20` did: a fail-closed rule folded into a feature row is the half that gets quietly dropped | **+1** (`V1-134`) |
| source-native ordinal vs cardinal semantics preserved | **new** — nothing covers it, and it is what makes a bridge meaningful rather than a rescale | **+1** (`V1-135`) |
| secure public / authenticated source acquisition | **new** — §3.6's rows are freshness, coverage and attribution. None is about how a source is *reached* or what credential reaches it | **+1** (`V1-136`) |
| raw source preservation | **covered** by `V1-19` (`C1-SRC-01` / `C1-U9`, multi-format dynasty source archive, `IMPLEMENTED_UNVERIFIED` at L3). A raw-archive **schema extension** is that capability continuing, not a second one | **0** |
| source-family / common-mode protection | **covered** by `V1-10` (provider families, one vote each — **`VERIFIED`**, 21 keys → 13 families). The bridge does not need a new row for this; it needs to **hold** the invariant `V1-10` already owns, which is an acceptance criterion of `V1-133`, not a denominator item | **0** |

**Granularity is Integration's judgement and is the part most open to correction.** Four rows is
the smallest set that keeps each independently verifiable; the owner may prefer three (folding
`V1-134` into `V1-133`) or five. What is *not* negotiable under §0 is that these are not silently
absorbed into existing rows — that would report the program as complete when it has not started.

| id | capability | source record | lane | status | required level | note |
|---|---|---|---|---|---|---|
| V1-133 | Multi-bridge cross-position translation | owner decision 2026-08-20 / `#954` | L8 | `VERIFIED` | L2 | canonical value. A bridge translates a source's ordinal or cardinal observations into combined-market space. Acceptance carries `V1-10`'s invariant: bridges from one provider family are **one** vote, not several — the KTC TE+/TE++/TE+++ error in a new population. **PROMOTED 2026-08-21.** `#954` (merged `31724a8d4`) built the bridge owner (`src/bridges/`); `#993` "Lane 8 PR B" (merged `000a9f14a`) built the multi-bridge ladder on top of it — both already on `main`, not #1012's doing (#1012's `idpShowCombined` reuses this existing mechanism via `is_cross_market=True`/`needs_shared_market_translation=False` rather than building a second one — confirmed by registry inspection, and it does not appear in `_VALUE_BASED_SOURCES`, so it votes ordinal-only as required). Family dedup: `assess_bridges` claims one bridge per `descriptor.family` in declared registry order; Draft Sharks' offense+IDP halves count as ONE family throughout. Pinned by `tests/bridges/` (22/22) + `tests/api/test_curve_routing_coordinate_pool.py` (17/17) + `tests/consensus_edge/test_fair_value.py` (34/34), all green on exact head `0a5267d06`. Reperformed rather than trusted: removed the `descriptor.family in set(claimed_families)` check in `assess_bridge` ⇒ RED (`test_a_second_bridge_from_one_family_does_not_count_twice`, a second same-family bridge counted as `VALID` instead of `DEPENDENT_SOURCE_FAMILY`); restored ⇒ GREEN, clean tree. L2 (from #993's own measurement, not re-run here): healthy-board `crossPositionBridges.ladder.contributors: ["idpTradeCalc"]`, depth 370, 0 withheld, `contractHealth.ok: True`; with `idpTradeCalc` excluded, Draft Sharks automatically takes over the ladder-seed role with 0 votes withheld. |
| V1-134 | A missing or unproven bridge fails closed, and says so | owner decision 2026-08-20 / `#954` | L8 | `VERIFIED` | L2 | truthful degraded state. `PENDING` may not vote. An IDP-only ordinal rank may **never** enter combined-market space untranslated, and an untranslatable source is reported as untranslatable — never defaulted, never ceiling-substituted. **PROMOTED 2026-08-21**, same `#954`/`#993` mechanism as `V1-133`. `assess_bridge` refuses a `PENDING`-comparability descriptor before it can vote (`NOT_COMPARABLE`, "an unproven bridge does not vote"); with no bridge available at all, `_compute_unified_rankings`'s Phase 1 withholds the vote (`withheld_no_bridge[source_key] += 1; continue`) rather than passing the untranslated raw rank through — reported per-source on the contract as `crossPositionBridges.withheldNoBridge`, never silently dropped and never defaulted. Reperformed rather than trusted, two independent mutations: (1) removed the `PENDING` gate in `assess_bridge` ⇒ RED, 3 of `tests/bridges/`'s 22 (`test_pending_does_not_vote`, `test_a_pending_bridge_is_still_capable_and_still_refused`, `test_an_unusable_bridge_does_not_claim_its_family`); restored ⇒ GREEN. (2) In `data_contract.py`'s withhold branch, replaced `continue` with forcing `rank_pool = RANK_POOL_SHARED_MARKET` (i.e. let the untranslated fallback rank reach the global master anyway) ⇒ RED on 2 of `test_curve_routing_coordinate_pool.py`'s 17 (`test_with_no_bridge_at_all_the_vote_is_withheld`, `test_no_idp_only_source_is_ever_priced_on_the_global_master`), reproducing the EXACT pre-repair defect signature verbatim (`method: fallback`, `rawRank: 1`, `valueContribution: 9999`, `rankCoordinatePool: shared_market`); restored ⇒ GREEN. Both mutations left the tree clean. L2 (#993's own measurement): fully-degraded scenario (no cardinal bridge at all) withholds 702 specialist votes, `ladder.available: False`, 0 untranslated votes reach the board, `contractHealth.ok: True` — refuses cleanly rather than crashing or fabricating. **Post-#1012 reconciliation check (2026-08-21):** independently confirmed `idpShowCombined`'s later cutover to sole-voting-source status does not invalidate this evidence — `config/bridges/bridges_v1.json` registers exactly three bridge families (`idpTradeCalc`, `draftSharks`, `dynastyDealer`); `idpShowCombined` has no bridge registry entry at all and is instead flagged `is_cross_market` in `_RANKING_SOURCES`, reading its own native combined rank through a wholly separate crosswalk in `data_contract.py` (the pre-existing shared-market backbone, not the `src/bridges/` owner). The two mechanisms operate on disjoint source sets; #1012 changed neither. |
| V1-135 | Source-native ordinal vs cardinal semantics are preserved | owner decision 2026-08-20 / `#954` | L8 | `VERIFIED` | L2 | canonical value provenance. A rank is not a value. What a source published is preserved end to end, and any conversion is explicit, named and reversible in the record. **PROMOTED 2026-08-21**, same `#954`/`#993` mechanism. `_VALUE_BASED_SOURCES` (`{ktcSfTep, idpTradeCalc}`) is the ONLY cardinal-vote population; every other registered source — including `idpShowCombined` — votes via rank → percentile → Hill, and `sourceRankMeta` stamps `method` (`direct`/`fallback`), `rawRank`, `effectiveRank`, `ladderDepth`, `backboneDepth` and `rankCoordinatePool` per row, so a ladder-translated conversion is explicit and named rather than silently folded into the published value. Confirmed by registry inspection on this tree (`idpShowCombined` absent from `_VALUE_BASED_SOURCES`) and by the mutation-#4 evidence under `V1-134` above: forcing an untranslated ordinal rank into `rankCoordinatePool: shared_market` is exactly the "rank presented as a value" defect this row exists to prevent, and the guarding test catches it by name. |
| V1-136 | Source acquisition is secure and its auth state is explicit | owner decision 2026-08-20 / `#954` | L8 | `VERIFIED` | L2 | source-health correctness. Public vs authenticated acquisition is a recorded property of a feed, not an accident of which fetcher ran; a credential-dependent source that loses its credential is `UNVERIFIABLE`, never healthy and never zero. **PROMOTED 2026-08-21 (Integration) — real production cycle measured, L2 board/contract measurement complete.** `#1034` merged (squash `25304c9bf`).

**Real production evidence, not synthetic.** `deploy/idpshow_fetch_and_push.sh` ran on the actual VPS timer and pushed two consecutive real cycles to `main` (commits `171e31d89` at 16:32 UTC and `7445e2a26` at 18:32 UTC, author `IDP Show Fetch (prod) <idpshow-fetch@brisket-prod-1.local>` — the exact bot identity this branch's own deploy-script edit configured, not fabricated). Both cycles: `idpShow_last_status.json` `HEALTHY`/350 rows, `idpShowCombined_last_status.json` `HEALTHY`/665 rows — matching the working tree's own `CSVs/site_raw/idpShowCombined.csv` (666 lines = 1 header + 665 rows) and `idpShow.csv` (351 lines = 350 rows) exactly.

**Board/contract measurement — ZERO BY MEASUREMENT, not "status-only by construction."** `git diff` across `#1034`'s merge commit (`d0e89e482`..`25304c9bf`) touching `src/api/data_contract.py`, `src/bridges/`, `config/bridges/`, `src/canonical/`, `src/api/feature_flags.py` returns **0 lines** — the entire valuation/ranking code path is byte-identical before and after, which is why canonical values changed = 0, canonical ranks changed = 0, voter-eligibility changed = 0, source-contributor-set changed = 0, contract fields changed = 0 (the only new artifacts are the two status JSON files, which are not contract fields at all). Confirmed positively, not just by absence of diff: built the real board from today's production CSVs (`scripts/golden_board.py`, 1,111 rows, 398 IDP) — 636 rows carry `idpShowCombined` as an active `_sourceKeys` contributor with sane priced values (e.g. A.J. Brown `rankDerivedValue: 4982`); `validate_api_contract.py --lane structural` on the same board: `ok=True errors=0`. Direct registry inspection confirms `_RANKING_SOURCES` has exactly one `correlation_group: "idpShow"` entry (`idpShowCombined`) — the plain `idpShow` key is absent, unable to vote by construction.

**The six required proofs, each independently re-verified fresh on the current merged tree** (not trusted from the original PR): (1) HEALTHY grants no voting eligibility — `_RANKING_SOURCES` inspection above plus the static guard test, both re-run; (2) `idpShowCombined` remains the sole voting source — registry inspection above; (3) plain-board `HEALTHY` cannot overwrite combined-board failure — `TestBoardsDoNotContaminateEachOther` (2/2) re-run fresh; (4) the shared-status mutation is non-vacuously RED — **re-performed independently this pass** (not the historical PR's proof): collapsing the board-aware `status_path` split back to a shared file sends 11 of 39 tests RED including both contamination tests, restored clean (`git diff` empty after restore); (5) last-good combined CSV survives failure — `TestLastGoodPreservation` (13/13) re-run fresh; (6) no cookie/session/token anywhere — `TestNoSecretValuesLeak` (2/2) re-run fresh, **plus** the real production commit (`7445e2a26`) diff itself greped for `cookie|session|token|connect\.sid|password|secret`: 0 matches. Full suite: 39/39 passed.

**Honest gaps, not silently rounded up**: production has been HEALTHY on both boards every cycle observed (2 of 2) — no natural failure has occurred yet to observe live, so items 3/4/5's PRODUCTION behavior (as opposed to code/test-level behavior, which is fully verified) remains to be confirmed the first time a real failure happens. This does not block promotion — the code is mutation-proven and the production evidence that DOES exist (two consecutive real HEALTHY cycles with correct, board-consistent row counts) is exactly what the row's L2 bar asks for. `#1001`'s Slice 3 instrumented `scripts/fetch_idpshow.py`'s PLAIN board only — its branch predates PR #1008's `--combined` machinery, so the board that actually VOTES (`idpShowCombined`, sole voting source since `#1012`) had zero outcome-instrumentation on either branch; main's own row-floor guard on that path was a bare `return 2`. Per `docs/sources/LANE8_POST_1012_RECONCILIATION_AUDIT.md` Task C's 5-point plan, reconciled onto current main's copy of the fetcher: every `main()` return point on BOTH boards now constructs a real `src.sources.acquisition_state.AcquisitionOutcome`, persisted to a board-specific file (`idpShow_last_status.json` / `idpShowCombined_last_status.json` — two files, not one, because the deploy script invokes the fetcher twice per cycle as separate processes, and a shared file would let the second run's outcome silently overwrite the first's). Required states confirmed present with a passing test each: `AUTH_REQUIRED` (missing session file, expired/paywalled session), `UNAVAILABLE` (article/version/dataset fetch failure), `PARSE_FAILED` (no chart found, 0 rows parsed), `SCHEMA_CHANGED` (below the board's own row floor — 150 plain / 450 combined), `HEALTHY` (real success). Critical anti-false-green property directly tested (`TestBoardsDoNotContaminateEachOther`, 2 tests): plain-board success followed by a genuine combined-board failure leaves `idpShowCombined_last_status.json` reporting its own true failure state, never inheriting or defaulting to the other board's `HEALTHY`, and the reverse. Also tested: last-good CSV survives every failure path on both boards (13 parametrized cases); no cookie/session value in the status JSON or stdout/stderr; a static guard that no ranking/bridge module reads either status file (acquisition health grants no voting eligibility by construction); the written CSV never carries a value column (ordinal stays ordinal). 39/39 new tests green (`tests/sources/test_idpshow_acquisition_state.py`); mutation-proved by deliberately reverting the board-aware status-path split and confirming 11 tests including both contamination tests go RED, then restoring clean. `deploy/idpshow_fetch_and_push.sh` updated to commit the two new status files (existence-checked before `git add -f`, since — unlike the pre-existing `_last_success` stamps — these are new paths with no prior tracked history, and an uncaught pre-persist exception on a virgin path would otherwise abort the whole push under `set -Eeuo pipefail`, blocking the OTHER board's successful commit too). `config/coercion_baseline.json` unchanged (660/660). **Both preconditions this paragraph originally deferred are now satisfied** (see the four sub-headers above): actual production deployment happened (two real VPS-timer cycles landed on `main` via bot-authored commits) and the L2 board/contract-level measurement has now been run in this environment — hence the promotion to `VERIFIED` above. |

**Explicitly NOT authorized by this section:** any production valuation change. `#954` is
foundation — acquisition state, bridge lifecycle, family metadata, archive schema, measured
capability — and is required to leave production values untouched. The production-changing bridge
work is a separate unit behind the §9 preconditions.

**Denominator: 136 items.** (132 until 2026-08-20, when the owner's source-acquisition /
cross-position-bridge decision added `V1-133`…`V1-136` — see §3.12 for why that is +4 and not
+6. This line read **127** until 2026-08-18 — stale from before `V1-127`…`V1-132` were added, while §3.11 already said 132. Recounted from the table itself: 132 rows, 132 distinct ids. A contract that disagrees with itself about its own denominator is the failure mode this document exists to prevent, so it is corrected here and the count is derived, never typed forward.)

### 3.11 Standing tally

Measured 2026-08-20 03:30 UTC, counted from the §3 table itself rather than by
editing this block, and **enforced** by
`scripts/check_planning_integrity.py::check_standing_tally`. That gate earned
itself immediately: it was added because this block sat stale at 45 while the
table already said 47, and on its first real use it caught the same drift again
(47 declared vs 48 measured) before this edit went out.

| status | count |
|---|---|
| `VERIFIED` | 118 |
| `IMPLEMENTED_UNVERIFIED` | 13 |
| `IN PROGRESS` | 2 |
| `NOT STARTED` | 3 |
| `BLOCKED` | 0 |
| **denominator** | **136** |

**V1 completion: 118 / 136 = 86.8%.** Numerator moved +1 by `V1-131` production L3 closure (2026-08-27):
the third defect (mobile drawer never opening) was root-caused this session as a test-side hydration
race, not a product bug — `#1155` hardened the E2E test only, and a clean re-verification run
(`33103452227`) produced zero failures anywhere in `v1-131-nav-gating.spec.js`, confirming the theory.
Full three-defect history preserved in the row. **117 before this reconciliation.**
Numerator moved +1 by `V1-56` production L4 closure (2026-08-27):
the row's own gap was a bug in `scripts/verify_v1_authenticated.py::check_v56` (wrong envelope key),
not in the FAAB backend/frontend; fixed in `#1150` (deployed `b194316f`), then confirmed on fresh
authenticated production evidence (run `33098047781`, 2026-08-27T17:24:19Z) —
`GET /api/public/league/faabAnalytics` serves a real, typed `leagueMedianWinningBid` (zero-bid-inclusive)
alongside the rest of the analytics block. Full record in the row. **116 before this reconciliation.**
Numerator moved +1 by `V1-27` production L3 closure: the authenticated production packet merged in #1147 exercised the real canonical lineup consumers and a starter-neutral trade simulation, with the previously owner-approved substitute retained for the irrecoverable pre-deploy observation. **115 before this reconciliation.**  Numerator moved +1 by `V1-59` production L3 closure: the naturally scheduled chain proof preserved by #1144 verifies the deployed repair SHA `c30b8d6b...`, scheduled `discovery < records < rosters` ordering, documented systemd success semantics, absence of the named crash/lock/watchdog signatures in the captured slices, and `V1_59_SERVICE_CHAIN_PROOF=PASS`. **114 before this reconciliation.** Numerator moved +1 by `V1-16` production L3 closure: fresh current-tree GREEN → mutation RED → restored GREEN proof (`32943730840`) plus the real on-box C1-U8 §8 item 5 PASS (`32919655291`, deployed `a82898fd...`) with 492 free-agent / 294 trade / 744 waiver rows. **113 before this reconciliation.** Numerator moved +1 by `V1-84` production L3 closure (scheduled health run `32700949074`: three consecutive 503s produced a real `production-health` incident, and the green recovery attempt closed it). **112 before this reconciliation.** Numerator moved +1 by `V1-89` production L3 closure (fresh on-box run `32919655291` against deployed `a82898fdbe73ea2effd295132590dff35a45ac98`; the packet §6 DraftSharks acquisition/archive/staleness recipe passed). **111 before this reconciliation.** Numerator moved +1 in the 2026-08-25 `V1-76`
reconciliation (`NOT STARTED` → `VERIFIED` L2): `#1132`'s replacement satisfies the exact evidence-driven
posture preserved in §8.1, exact-head CI is green, and the 1,116-row L2 board measurement is inert
(0 values / 0 ranks / 0 tiers changed). **108 before this reconciliation.** The prior numerator movement
was +1 by `V1-105`'s closure (`NOT STARTED` → `VERIFIED` L2): `#1006` merged the baseline audit into the canonical record, and the row closes on
the owner-accepted terms — the `/rankings` pre-feature baseline permanently irrecoverable
(owner-accepted, never fabricated or relabeled), the prospective baselines for the still-unmodified
surfaces standing, the unmeasured routes staying `BLOCKED_EXTERNAL`, the one near-miss preserved.
Two engineering completions moved without touching the numerator, both honest halves of L4 rows:
`V1-62` (`IN PROGRESS` → `IMPLEMENTED_UNVERIFIED`, #1129 — W15-F017 memoization with a structural
anti-stale guard) and `V1-109` (`IN PROGRESS` → `IMPLEMENTED_UNVERIFIED`, #1131 — all four named
mobile defects now closed at code level with production-build mutation proofs). Both await the
deployed production-consumer statement. (107 before this batch. Numerator moved +3 in the 2026-08-25 bounded-engineering
batch: `V1-25` (`IN PROGRESS` → `VERIFIED` L1, #1124 — the seven hardcoded-`dynasty_main` starter-need
consumers now read the league's resolved demand, RED→GREEN + AST guard), `V1-80` (`NOT STARTED` →
`VERIFIED` L2, #1126 — the critical-source gate's run-name-vs-registry-key mismatch repaired via one
owner `critical_primary_for_run_source`, RED→GREEN + 0-row measured board effect), and `V1-104`
(`IN PROGRESS` → `VERIFIED` L1, #1123 — the sharp-identity review control's audit attribution fixed
from a constant `"admin"` to the authenticated session, RED→GREEN + guest-403 mutations). Each
promotion's full record is in its row. (104 before this batch. Numerator moved +1 in the
2026-08-25 owner-decision batch:
`V1-19` promoted to `VERIFIED` (L3) on the owner-accepted substitute for its one irrecoverable
observation — items 1-2 already L3-fresh on the live deployment, item 3 closed on the pre-merge
on-box inertness measurement plus the healthy post-deploy scrape, item 4 N/A; full record in the
row. `V1-89` moved `BLOCKED` → `IMPLEMENTED_UNVERIFIED` (OD-04 resolved: ACCEPT HEALTHY_CURRENT;
the decision unblocks, the packet §6 on-box recipe still verifies), so `BLOCKED` is now 0 and
`IMPLEMENTED_UNVERIFIED` holds at 18 (−V1-19, +V1-89). `V1-105`'s owner decision is recorded on
its row; its status moves only when `#1006`'s audit record is merged and the closure statement
lands. (103 before this batch. Numerator moved +8 in the Lane 4 harvest + merge-train batch — six rows promoted on the on-box run `32843495391` at deployed `8537aa4c2` (`V1-57` L3 timer-firing; `V1-58` L3 cohort 2,389 on the production stores; `V1-60` L2 honest-zero FFPC lane; `V1-65` L2 the 14,264-league census with the 1,169-league difference fully explained; `V1-87` L2 unblocked by the 743-of-749 blast-radius measurement and repaired by `#1119`; `V1-129` L2 the crowd ledger measured with residuals named), plus `V1-103` (all four security findings repaired across `#1112`/`#1115` with executed mutations) and `V1-132` (`#1118`'s two-market horizon with the full board measurement and the coupling adjudication). `V1-131` reclassified to `IMPLEMENTED_UNVERIFIED` (`#1086` merged; L3 recipe recorded). Every promotion's full record is in its row. (95 before this — numerator moved +4 in one Integration batch — `V1-85` (`NOT STARTED` → `VERIFIED` L1: `#1111`'s pickAnchors-provenance reconciliation, two executed mutations), `V1-91` (`IN PROGRESS` → `VERIFIED` L2: the health contract test shipped in `#905` and the row was never reconciled; fresh live measurement — 21 registered vs 2 emitted sites, unmeasured ≠ missing ≠ healthy), `V1-86` (`IMPLEMENTED_UNVERIFIED` → `VERIFIED` L3: real red filed exactly one tracker, dispatched green run `32837722538`'s step 19 logged closing `#1094` — an earlier vacuous candidate proof was refused at the log first), and `V1-68` (`IN PROGRESS` → `VERIFIED` L2: the `#1071` spec drift repaired by `#1105` and the suite green on `main` with its verdict consumed). Each promotion's full record is in its row. (91 before this — numerator moved +1 by `V1-110`'s promotion — `IN PROGRESS` → `VERIFIED` at **L2**: the recorded `OD-05` blocker was stale (resolved 2026-08-18, `docs/DESIGN-SYSTEM.md:3`), and the token / shell / focus primitives layer is shipped and measured with every inline contrast claim independently recomputed and reproduced exactly — full record in the row. (90 before this — numerator moved +1 by `V1-36`'s promotion — `IN PROGRESS` → `VERIFIED` at **L2**: `#1100`'s executable-structure identity guard merged (`de492be54`), and the exact historical bypass (owner import deleted, helpers faithfully reimplemented, docstrings kept, module functional) was self-performed fresh on merged `main` and went 4 RED on the ownership assertions with all 25 functional tests green, restored to an empty diff at 29/29 — the full record is in the row, including the Guard B adjudication that closed the last owner question and the routed-not-folded `#1106` determinism fix. (89 before this — numerator moved +1 by `V1-96`'s promotion — `NOT STARTED` → `VERIFIED` at **L2**, and the interesting part is that the row was *false*: the `C9-HIST-01` repair merged 2026-08-20 and had been live in production for four days while the ledger still read `NOT STARTED`. It was invisible to history because it rode in on squash `854598d9a`, a premium-UI PR (*"elevate the Chase Upside Market Ticker (#965)"*) that names neither the capability nor the row — only `git log -S"is_retired"` finds it, which is a traceability hazard recorded in the row rather than quietly fixed. Promotion evidence: 8 focused tests green at exact head with green CI on the merge tree, **two executed mutations** (reinstating the pre-`#965` `continue` turns 5 tests RED including the two naming the defect; forcing `include_retired` on turns 2 RED, pinning the privacy half in the opposite direction), and an **L2 statement measured on the live production contract and independently re-fetched by the Integration Authority** — 2024 now serves `numTeams 10` / **10** standings rows / 10 unique owners against the recorded defect of ten declared and eight carried, with the two restored rows carrying real records and the forward-facing directory still correctly at 12. Two adjacent residuals found during the verification — the `archives` manager index (32 season rows including both retirees, 12-row index excluding them) and `overview`'s `leagueVitals.managers 14` against the same payload's 12 — are confirmed live, recorded in the row, and **routed rather than folded into the promotion**, because they are a different defect from the one this row names. (88 before this — numerator moved +1 by `V1-21`'s promotion — `IN PROGRESS` → `VERIFIED` L1: `#1057` reconciled `#1055` (Claude 8's model provenance stamps, W04-F011) onto current `main` — `hillCurves.provenance` now stamps `verified_champion`/`unverified`/`unavailable` truthfully from the existing `ModelRegistry` owner, `modelVersion` staying `None` on both non-verified states. Shipping-tree L1 confirmation performed fresh: 6/6 focused tests, a live invocation against the real production registry returning a genuine `verified_champion`, and an independent mutation (forced the drift branch off, confirmed RED, restored clean). See the row for the full record. (87 before this — three independent, non-conflicting reconciliations landed in sequence this pass: `V1-41` (`#1043`/`#1047`, `NOT STARTED` → `VERIFIED` — the "Use Team Context" toggle onto `src/trade/finder.py::find_trades`, shipping-tree L1 confirmation performed fresh), `V1-43` (`#1045`/`#1050`, `NOT STARTED` → `VERIFIED` — Analyze Trade composing canonical equity (V1-37) and roster marginal impact (V1-42) via an explicit rule table, with `C4-MTL-03` market corroboration explicitly named unavailable and V1-41 dependency adjudicated as correctly excluded — see the row for the double-count reasoning), and `V1-35` (`#1049`/`#1051`, `NOT STARTED` → `VERIFIED` — both F-1/`/rosters` and F-3/`/phases` metric-separation violations were already retired in production; this lane's own test suite (`tests/roster_intel/test_metric_separation.py`, 10 → 17 tests) previously disclaimed proof on those two files directly, now closed with a fresh self-performed mutation — reintroduced the retired `scoreTeamTiers` formula into a real production file, confirmed RED, restored clean). Each landed as a separate merge onto a moving `main`, each independently re-verified rather than trusted from its own PR body. (84 on `main` before any of the three — numerator moved +2 by reconciling open PR `#962` (Claude 9's C3 Trade Substrate lane, stale base reconciled onto current `main`, docs-only conflicts resolved, code merged clean): `V1-37` (`NOT STARTED` → `VERIFIED` — the third standalone Python VA port collapsed onto one shared stdlib-only core, both remaining wrappers thin and boundary-forced, JS already single-owner, parity + single-owner-guard mutation independently re-verified this pass) and `V1-42` (`NOT STARTED` → `VERIFIED` — the pre-existing C2-SIM-01 engine finally wired to the live `/api/trade/simulate` endpoint via `finalRosterSimulation`, RED-before/GREEN-after independently re-verified this pass). `V1-36` moved `NOT STARTED` → `IN PROGRESS` only (one more generator gained constraint integration, but `suggestions.py` remains a deliberate, un-migrated holdout — genuinely not yet "ONE shared package generator"). (82 on `main` post-`#1024`(`V1-94`)/`#1029`(`V1-46`)/`#1021`(`V1-69`/`V1-90`)/`#1033`(`V1-121`)/`#1022`(`V1-79`/`V1-92`/`V1-98`/`V1-112`)/`#1023`(`V1-133`/`V1-134`/`V1-135`) + `V1-40` here, `IN PROGRESS` → `VERIFIED` on a measured `board_diff` + full-contract-key-set + direct `team_droppability`/`assess_roster_capacity` output L2 statement — see the row for the literal ZERO BY MEASUREMENT numbers. Numerator moved by `V1-136`'s promotion — `IMPLEMENTED_UNVERIFIED` → `VERIFIED`: `#1034` (squash `25304c9bf`) already merged the code; this pass added the L2 board/contract measurement the row's own bar required — a 0-line diff across the merge commit range for every valuation-path module, a real `golden_board.py` capture showing 636 rows actively voting `idpShowCombined`, a clean `validate_api_contract.py --lane structural` run, direct `_RANKING_SOURCES` registry inspection confirming `idpShowCombined` is the sole `idpShow`-family voter, all six of the row's required proofs independently re-verified fresh on the current merged tree (including a fresh, self-performed, self-reverted mutation of the board-aware status-path split), and two real production HEALTHY cycles read directly off bot-authored commits on `main` (`171e31d89`, `7445e2a26`) with row counts matching the working tree's own source CSVs. See the row for the full record, including the honestly-stated gap that production has not yet been observed to fail live. Numerator unchanged by `V1-52`'s status move either — `#996`+`#1009` merged, so `IN PROGRESS` → `IMPLEMENTED_UNVERIFIED`, not `VERIFIED`: item D (one canonical engine, fully retired) is real and verified at the code level, but the row's own L2 bar is a production-scale 12-owner measurement this environment cannot run. Numerator unchanged by `V1-49`'s status move either — `#1031` merged, so `IN PROGRESS` → `IMPLEMENTED_UNVERIFIED`, not `VERIFIED`: the return-TD scoring-key repair is real and code-verified, but the row's own `host_native_scoring` production checklist (4 of 10 items open per the PR's own count) has not been run.)))))))

The percentage went DOWN without any capability regressing, and that is the tally being honest
rather than flattering: the owner enlarged V1 on 2026-08-20 (§3.12, +4) and the denominator moved
with it. A completion figure that only ever rises is the first failure mode §0 names.

**Movement to 55 (2026-08-20 04:15 UTC), and the evidence classes differ row by row.**
60 → 61 (#987, merge `faa50ba9a`): `V1-29` at **L2** — two mutations at the exact merged head
(the decisive one writing a real tracked duplicate definition, not a fixture), plus a pinned-input
golden-board capture either side of the merge reporting 0 values moved and 0 ranks changed. The
board statement here is "nothing published moved", which for a static-analysis tool with no
importers is the whole of what L2 can mean — and it is measured rather than argued from the
absence of importers alone.
59 → 60 (#977, merge `ef41523a8`): `V1-63` (L1 — two mutations run at Integration against the exact
merged head, each confirmed RED and named, restore GREEN). Nothing else moved in that pass. The two
other PRs merged alongside it are deliberately NOT promotions: #961 (C4 waiver / Market Trade Ledger)
and #965 (C8 Market Ticker) are C-Series campaign work whose V1 rows, if any, are claimed separately
and on their own evidence — a merge is not a promotion, and batching three merges into one tally step
would be exactly that.
55 → 57 (#912, Lane 6): `V1-107` (L2 — payload re-measured independently at Integration) and `V1-113` (L1 — 20/20 axe checks confirmed from the CI run log). `V1-108` was REFUSED in the same pass: its predicate is tested, but the L2 measurement has no committed instrument. `V1-106` stays open per the owner ruling on the FPS harness; `V1-110` is gated on owner decision `OD-05`. Before that, 48 → 55: `V1-81` (L2 — board measured), `V1-44` and `V1-39` (L1 — mutation, `V1-44` in BOTH runtimes), and `V1-114` / `V1-115` / `V1-116` / `V1-118` (L1 — the governance guards mutated against the real tree). Immediately before them, 45 → 48: `V1-34` and `V1-130` (L1/L2 — mutation plus a 0-value/0-rank board diff) and `V1-95` (L2 — **deployed** evidence, read off `/api/public/league/awards` after the `e37d2786e` deploy). Six rows were REFUSED in the same pass on their own measurements (`V1-29`, `V1-51`, `V1-63`, `V1-65`, `V1-88`, `V1-92`).

Each row states the class it was promoted on; nothing here is promoted from a merge or from CI alone. **Only `V1-95` in this batch rests on deployed evidence** — the rest are deterministic or board-measured, which is what their stated levels require.

---

**HISTORICAL — the earlier 40 → 45 movement (2026-08-18/19).** The paragraphs below record that pass and are retained as evidence; they do **not** describe the tally above. At that time the claim *up five, and every one of them on deployed evidence rather than on a merge* was accurate for those five rows.
#910 merged at 21:29 UTC; the deploy that carried it completed at ~22:50 and the
2-hourly scrape at 22:55 produced the first board built entirely by post-#910
code. Measured against `chaseupside.com` at 23:00:

* `V1-127` (`F-27`) — `normalizationHealth.pickNameMalformed` **18 → 0**,
  `healthy` **false → true**.
* `V1-82` (`F-19`) — `producedAt 22:55:34+00:00` < `loadedAt 22:55:40+00:00` <
  `last_scrape 22:55:49+00:00`, `data_age_hours 0.1`. The ordering is what makes
  it a board age rather than a process age.
* `V1-12` (`F-30`) — production's own `contract.health`: `ok: true`,
  `structuralErrors: []`, `errorCount: 0`. This was **downgraded** from
  `VERIFIED` earlier the same day on owner instruction, and it returns only
  because the fourth of its four stated conditions is now satisfied. The
  round trip is the mechanism working: it left `VERIFIED` on a measured live
  failure and came back on a measured live pass, not on a merge.

The fourth is `V1-120` (`C0-R`), and it moved for an uncomfortable reason: **the earlier
verification was wrong about its own tooling.** Item 5 of that checklist was recorded UNRUN
because `pytest` was believed absent from this session's interpreter. It was installed as a
`uv` tool binary — `python -m pytest` failed while `pytest` worked — so the verifier tested a
proxy for the question instead of the question. Re-run on a worktree at the post-`#910`
deployed SHA `92469d32`: `pytest tests/docs/ -q` → **22 passed**, and all seven checks pass
(§7.2b of the reconciliation record). This failed in the direction of **under**-reporting
verification, which is the safer direction and still a false statement in a verification
record. §7.2a is left standing rather than rewritten, because it accurately records what was
measured with the tooling the verifier believed they had.

`V1-128` (`F-28`) was held back at first and is the fifth to move. Its stated bar
is "a deployed scrape showing a positive age **tracking the 2-hourly cadence**",
and one 0.1 h reading taken five minutes after a scrape shows a positive age but
not yet that it tracks — so it stayed `IMPLEMENTED_UNVERIFIED` while the fix was
already demonstrably live. Two further samples settled it: 0.3 at 23:14 and 0.4 at
23:20:28, against a board produced at 22:55:34, which is the true elapsed time at
the published rounding on all three. "The defect is fixed" and "the row's evidence
bar is met" are different statements, and holding the row for twenty minutes
between them is what the difference is worth.

Reconciled against every open six-lane PR — #910 (L5), #911 (L4), #912 (L6),
#913 (L2), #914 (L1), #915 (L3). Thirteen rows moved `NOT STARTED` →
`IN PROGRESS` because a lane is *demonstrably implementing them right now*;
leaving them `NOT STARTED` while an open PR builds them understates the work in
flight exactly as leaving a regressed row `VERIFIED` overstates the work
finished. `IN PROGRESS` means begun and unmerged — none of these has earned
`IMPLEMENTED_UNVERIFIED`, which requires the merge.

The percentage went **down** between publication and the first update, and that is
the contract working rather than failing. One item was added (`V1-127`), two
moved to `IMPLEMENTED_UNVERIFIED` (`V1-86` from `IN PROGRESS`, `V1-88` from
`NOT STARTED`), one moved to `BLOCKED` (`V1-87`), and `V1-74` was **downgraded**
from `VERIFIED` after its production check turned out to have read the wrong field.

It has since moved back up, on evidence rather than on reconsideration: `V1-74`
is verified from the post-deploy committed export, which carries the
`coverageAudit.expectedSites` block `/api/status` does not expose. `V1-128` moved
`NOT STARTED` → `IMPLEMENTED_UNVERIFIED` (fixed, deploy proof still owed), which
leaves the denominator untouched.

**And then down again, deliberately.** `V1-12` (pick value completeness through
2029) was `VERIFIED` and is now `IN PROGRESS`: audit `F-30` measured `main`
leaving 2029 rounds 2-6 unpriced when a pick market truncated, which failed the
hard gate and **skipped a production deploy**. `V1-107` moved `NOT STARTED` →
`IN PROGRESS` on `F-32`.

**A previously verified capability is allowed to regress, and saying so is the
point.** A ledger whose entries only ever ratchet upward is not measuring the
system; it is describing the intentions of the people editing it. `VERIFIED`
asserts a property holds *now*, so a measured live failure retracts it — and the
retraction is what distinguishes this contract from the status documents it was
written to replace. The denominator did not move: a regression is not a scope
change.

`V1-87` is the instructive one. Its repair is mechanically trivial and was written,
then **backed out**, because the guard that governs defaulted-ON flags demands a
measured blast radius and that measurement is not obtainable without the temporal
ledger — locally both branches stamp `None`, so a diff would report a vacuous
"0 rows changed" that reads as evidence. `BLOCKED` is the honest status for an
item whose repair is known and whose proof is not available. A ledger that only
ever ticks upward is not measuring anything.

Read that number carefully. The 17 `IMPLEMENTED_UNVERIFIED` items are *merged and deployed
code* — they are not missing, they are unproven, and five of them
(`V1-11`, `V1-15/16/17`, `V1-19/20`, `V1-27`, `V1-120`) are blocked behind exactly five
production checklists that have never been executed. Executing those five checklists is the
single largest available movement in this number, and it requires an authenticated production
session (§9).

---

## 4. POST-V1 DEFERRED

Approved product, deliberately out of the V1 denominator. **Every one of these is real, owner-
approved scope** and remains itemised in its canonical record; deferral is a scheduling
statement, never a withdrawal.

### 4.1 Named by the owner as out (2026-08-18)

| group | items | canonical record |
|---|---|---|
| Full projection ensemble | `C5-U1` and all six sub-units `C5-PROJ-A…F`; `#854` | Execution Map §7 |
| Game Day | `C5-GD-01` / `CE-20` / `#789`; `C5-GD-02` prediction archive | Manifest §4 C5 |
| Podcast / YouTube ingestion | inv 5.1–5.7; `C6-POD-01`; `C6-YT-01`; `#782` | Inventory §5 |
| Full analyst-intelligence ecosystem | `C6-ANA-01`; `C6-X-01`; `C6-FRESH-01`; `#788`; `#783`; freshness/decay decisions 33–40 | Manifest §4 C6 |
| Manager Scout | `C6-MGR-01` / `CE-03` | Manifest §4 C6 |
| Full Ask Brisket / AI Front Office | `C7-AI-01…05` | Manifest §4 C7 |
| Advanced trade-market systems | `C4-MTL-01/02/03` / `CE-01`; `CE-14` Market Pulse; `C4-FAAB-01` / `CE-19` / `#830`; `C4-WAIV-01`; MFB-77/78/79/80 | Manifest §4 C4 |
| Full Public League Experience v3 | `C9-V3-01`; `PL-IA6`; `PL-FRANCHISE`; `PL-RIVALRY`; `PL-RECEIPTS`; `PL-YEARBOOK`; `PL-HOF`; `PL-RINGHONOR`; `PL-CHAMPPATH`; `PL-ONTHISDAY`; `PL-WEEKHIST`; `PL-MILESTONE`; `PL-RECORDCHASE`; `PL-REUNION`; `PL-LEAGUECAST`; `PL-GOTW`; `PL-WPTIMELINE`; `PL-BADBEAT`; `PL-REDZONE`; `PL-PICKEM`; `PL-DRAFTCAST` | Backlog Spec §9 |
| Wrapped / reporting | `C9-RECAP-01` / `CE-21`; `C9-UR-01/02` Upside Report; `C9-WRS-01` / `#829`; `C9-SHARE-01` / `CE-10`; `weekly-narratives` | Manifest §4 C9 |
| Advanced WAR / VORP | `C5-WAR-01` / `CE-09`; `AW-VORP`; `AW-*` award family (`AW-FLAGSHIP`, `AW-POST`, `AW-TEAM`, `AW-MOTY`, `AW-GMOTY`, `AW-ALLBRISKET`, `AW-RACES`, `AW-TROPHY`, `AW-SECONDARY`, `AW-LEDGER`, `AW-COVERAGE`); `C9-AWARD-02` Brisket Honors v2 | Backlog Spec §10 |

> **Note on awards.** The award *family* is post-V1. `V1-95` — awards must not exist before games
> are played — is **in** V1, because it is a live truthful-degraded-state defect on a shipped
> surface, not new capability.

### 4.2 Lane continuation work (owned by a V1 lane, not V1-required)

| lane | deferred continuation |
|---|---|
| L1 | `C2-AGE-01` age-value portfolio · `C2-AGE-03` age/value trends · `C7-AGE-01` roster window surfaces · `C1-U7` / `C1-PICK-03` / `CE-02` owned-pick distributions (deps on Team Strength) |
| L2 | `C7-BEST-TRADE` / `#841` / `CE-05` Trade Desk · `C7-GOLD-01` Golden Upgrades · `C7-PKGB-01` Package Builder · `C7-POST-01` / `#840` Competitive Posture · `C7-PICKGEN-01` · `C3-CON-01/02/03` recommendation constraints · `C2-EXP-01` / `#786` NFL-team exposure · `C3-MC-01` / `#790` Monte Carlo revalidation · `C3-CALC-03` / `#791` Second Opinions tally · `C3-AGE-01` / `C3-U9` trade aging · `C3-TOPO-01` · `C3-XMKT-01` · `C7-WAIV-01` Perfect Waivers · `MFB-101` historical trade replay (beyond the `V1-97` semantics fix) |
| L3 | `C5-FIT-01` / `#803` player fit + college translation · `C5-U1` ensemble · projection-driven season models beyond `V1-51/52` |
| L4 | `C6-SIG-01/02` central Buy/Sell reconciler and ticker (`#784`, inv 4.2/4.3) · `C6-EDGE-01` Consensus Edge repair · `C7-ALERT-01` Edge Alerts · `#801` Establish The Run (owner-PAUSED) · `#830` FAAB Market Heat |
| L6 | `C8-PSI-03` route-by-route migration · `CE-25` compare multi-select · `CE-14A` personal rankings overlay · `CE-29` push (exists) · `MFB-94…98` informational/monetization surfaces |
| L5 | `C10-ML-01` adaptive source weighting (stays off by design) · `C10-U3` full prior census · `DEF-INTEL-REKEY` · `DEF-FE-PICKGRAMMAR` · `DEF-PUBLIC-PICKFOLD` · `DEF-KTC-STRAT2` · `DEF-TRADE-RETRO` · `DEF-RECONSTRUCTED` · `B4-SAFEGUARD` · `B9B-CONSTANTS` · `CANONICAL_V2` activation |

### 4.3 Remaining approved surfaces and ledger rows

`CE-04` Command Center · `CE-06` Portfolio · `CE-07` Market ADP · `CE-08` Projections & Stats Hub ·
`CE-11` Sleeper Action Gateway (`OD-02`) · `CE-12` Lineup Intelligence surface · `CE-13` Draft Room ·
`CE-15` Portfolio Trade Campaign · `CE-16` Trade Polls (optional) · `CE-17` Format/Utilization Lab ·
`CE-18` Trade Trees *surface* (the `C1-ACQ-03` engine is `V1-17`) · `CE-22`…`CE-27` ·
`C7-CE-01` aggregate · `C7-CMD-01` · `C9-TRUTH-01` · `C9-HIST-02` `PUBLIC_MAX_SEASONS` ·
`R-PICKENGINE` · `R-UPSIDE` · `R-POWERV3` (the consolidation itself is `V1-52`) · `R-STANCE` ·
`R-COMPARE` · `R-SYNC` · `R-NAV` · `R-MLGOV` · `MATH-CALIB` · `BS-COSTBASIS` · `BS-HANDCUFF` ·
`R-PICKQTY` · `PL-PRIVATIZE` · `PL-HISTGATE` · inv 2.5/2.6/2.9/2.10/2.11/2.12/2.14 ·
inv 3.3 Perfect Waivers · inv 3.4 · inv 6.3 franchise history · inv 6.6 Universal Player Profile ·
inv 8.3/8.4/8.5 news & signal hygiene · inv 10.2 adaptive weighting ·
MFB rows not otherwise cited (coverage rows for capabilities already classified above).

> **MFB coverage rows.** `MFB-1`…`MFB-104` are a ledger of approved scope in which most rows are
> coverage restatements of capabilities classified elsewhere in this document (e.g. `MFB-31`
> "league-wide roster strength rankings" is `V1-31`; `MFB-89` is `V1-12`). They are classified by
> the capability they restate, and are not separately counted — counting a capability twice
> would inflate the denominator as surely as dropping one deflates it.

---

## 5. RETIRED / SUPERSEDED

Must stay absent; `C10-U1` re-audits for reappearance.

| id | disposition |
|---|---|
| `X-01` / inv 0.1 | Schedule generator — **OWNER-REJECTED** |
| `X-02` / inv 6.5 | Money / dues / Constitution / League Media — **OWNER-REJECTED**, restated 2026-08-17 |
| `X-04` | Canonical Data Mode offline build — **SUPERSEDED**; the live contract is the source of truth |
| `X-05` | League-aware valuation overlay as canonical — **OWNER-REJECTED** on seven measured defects |
| `X-06` | `opportunity_stats.py` usage-signal engine — **SUPERSEDED** by `consensus_edge/opportunity.py` |
| `X-07` | General "link any Sleeper account" onboarding — **OWNER-REJECTED**, restated 2026-08-17 |
| `X-IDPGURU` | IDP Guru — out of scope by owner direction 2026-08-15. Not to be confused with The IDP Show / FantasyPros IDP / DraftSharks IDP, all still in scope |
| `ktc_crowd_faab.py` adapter | **RETIRED** 2026-08-18 — read a contract block present in 0 of 173 archives |
| market corridor clamp | **RETIRED** — anchor was a voter in the blend it corrected |
| `MFB-102` topology rules | players-only and exact-equal-player-count **WITHDRAWN**, superseded by `#841`/`#842` |
| `C2-GP-01` `/api/gameplan` | **DISCONNECTED** — measured zero frontend consumers. Outcome is binary: reachable or retired. Carried as an L2/L5 disposition, not V1 capability |

---

## 6. EXTERNALLY BLOCKED

Out of the denominator because no amount of engineering clears them.

| id | blocker |
|---|---|
| `F-EXT-01` | KTC data-use permission — owner-reported, **grant artifact not in the repository** (`OD-01`) |
| `F-EXT-02` | IDPTC authorization — **no record of any kind** (`OD-01`) |
| `F-EXT-03` | Credentialed / paywall-adjacent posture — 7 source keys behind credentials, zero recorded authorization (`OD-01`) |
| `F-U2` / `C4-U3` | Market Trade Ledger — blocked on the permission record above |
| `C6-U2` | Analyst ledger — credentials absent (`OD-03`) |
| `X-03` / `#801` | Establish The Run — **OWNER-PAUSED**; do not purchase or implement |
| `C7-GATE-01` / `CE-11` | Sleeper Action Gateway — gated on `OD-02` |
| `CE-28` | User feedback / polling — **not owner-approved**; needs `OD-06` |

---

## 7. Ambiguous items — seven resolved mechanically, three left for the owner

The owner's instruction was to flag the specific item rather than broaden the denominator, and
then (2026-08-18) to *"resolve any that follow mechanically from existing owner decisions"* and
batch only the genuinely irreducible product choices.

**Seven of the twelve follow mechanically and are now RULED**, each from a decision the owner has
already made, not from a fresh judgement of mine. Three of the remaining five collapse into a
single question, so §7.2 asks **three** questions rather than five.

**The rule that resolves five of them is one the owner already set**, and it is worth stating once
because it recurs: *a live defect in a required capability is V1; the post-V1 feature it sits
inside is not.* That is the same split used for `V1-127`, `V1-128` and `V1-129`, and the owner
restated it as *"new defects that violate an already-required V1 capability may be
added/tracked without redefining the product scope."*

### 7.1 Resolved

| # | item | ruling | the owner decision it follows from |
|---|---|---|---|
| A-1 | `C2-AGE-01` roster age-value portfolio | **→ V1, absorbed into `V1-33`** | **A correction of my own first ruling.** I initially recorded "POST-V1 confirmed — `#914` builds `V1-33` and does not build `C2-AGE-01`, resolved by measurement". That was false and I had not read the module: `#914` ships `src/roster_intel/age_portfolio.py`, whose first line is *"Roster Age-Value Portfolio / Young Core Index (row 1.6, #838)"*. The evidence points the other way and settles the original doubt — the index is a percentile of a value-weighted youth score over the meaningful core, i.e. it **is** the portfolio's composite, exactly as `#838` treats them. Absorbed into `V1-33`; no new row, denominator unchanged |
| A-2 | `C3-CON-01` recommendation-constraint owner | **→ V1 REQUIRED** (`V1-130`) | `V1-34` (untouchable control) is V1, and ONE CONCEPT, ONE CANONICAL OWNER is a ratified governance invariant. A required capability whose canonical owner is out of scope would have to be built without one, which the invariant forbids. The *owner* enters V1; the wider constraint feature set does not |
| A-3 | `C5-U1` ensemble vs current-season correctness | **As classified — confirmed** | The ratified boundary excludes "the full projection ensemble" and includes "required current season-model correctness" in the same sentence. That *is* the ruling: consolidate the two playoff engines and the two power-rankings engines onto what exists; do not build the ensemble |
| A-7 | `C6-EDGE-01` Consensus Edge | **Split — nav gating → V1** (`V1-131`); feature POST-V1 | "Truthful degraded states" is named in the boundary. Nav offering a page whose three endpoints 503 (`F-25`) is a live instance of it on a shipped surface |
| A-10 | `C3-REPLAY-01` historical trade replay | **Split — as classified, confirmed** | Same rule. `V1-97` is the wrong-semantics defect; the `MFB-101` three-lens feature stays out |
| A-11 | `C9-HIST-01` franchise continuity | **Split — as classified, confirmed** | Same rule. `V1-96` is the live defect (2024 declares ten teams, carries eight standings rows); Public League v3 stays out |
| A-12 | `C1-U7` owned-pick distributions | **POST-V1 — confirmed** | The owner ruled that lane continuation work does not enter the denominator, and specifically that becoming dependency-ready is not entry. `C1-U7` becoming reachable once `V1-31` lands is exactly that case |

**A-1 is the one to read twice.** It is the only ruling here I got wrong first time, and I got it
wrong in the most seductive way available: by writing "resolved by measurement" over an assumption
I had not measured. The lane's own module title refutes it in one line. The correction is recorded
in place rather than overwritten, because a register that quietly fixes its own errors is the
thing this contract exists to be an alternative to.


### 7.2 Still genuinely the owner's — three questions

| # | item | classified | the irreducible question |
|---|---|---|---|
| A-4 | `C5-PROJ-E` immutable projection archive | POST-V1 | It is **perishable**: an observation not made now cannot be made later, the same loss class as the `C1-RET-*` tranche which *is* V1. Deferring it is correct on scope and lossy on evidence. Recommend authorizing the archive alone, ahead of the ensemble |
| A-5 | `C7-DRAFT-02` pre-auction immutable snapshot | POST-V1 | Also perishable and **one-shot**: `--record-snapshot` must run before the 2026 rookie auction or Perfect Draft can never be backtested. Not a V1 capability, but it has a deadline V1 does not |
| A-6 | `C9-UR-02` Upside Report Preseason / Kickoff Edition | POST-V1 | Carries a hard publication date (Tuesday before Week 1). Deferred on scope; the date is not deferred |
| A-8 | inv 6.6 Universal Player Profile | POST-V1 | Named the second Premium migration reference route. If "high-use Premium UI" includes it, it moves into `V1-111` |
| A-9 | `C4-INS-01` Insider Trading | V1 (`V1-65`) | Included as Sharp core because it is live and cross-league ownership is the Sharp product. If "Sharp core" means only the cohort and roster-percentage boards, this is post-V1 |

**These five are three questions, and A-4/A-5/A-6 are one of them.**

**Q1 — perishable capture (A-4, A-5, A-6).** All three are POST-V1 *features* and that is not in
doubt. What is in doubt is whether to authorize the **capture** ahead of the feature, because each
loses evidence that cannot be recovered later: `C5-PROJ-E` archives projections that are
overwritten, `C7-DRAFT-02`'s `--record-snapshot` must run **before the 2026 rookie auction** or
Perfect Draft can never be backtested at all (`scripts/backtest_perfect_draft.py` already exits 2
and says so), and `C9-UR-02` carries a fixed publication date. The scope ruling is not the
question; the deadline is. *Recommend authorizing the capture only — no feature work, no
denominator change.*

**Q2 — is the Universal Player Profile "high-use"? (A-8).** This is a usage judgement about the
product, and I have no usage data. If it is high-use it joins `V1-111`; if not it stays post-V1.

**Q3 — does "Sharp core" include Insider Trading? (A-9).** Currently in, as `V1-65`. The case for
out: the boundary excludes **Manager Scout**, and cross-league ownership intelligence is nearer to
that than to "the cohort and the roster-percentage board". The case for in: it is live today and
cross-league ownership *is* the Sharp product. This one genuinely turns on what the owner meant by
"core", which no existing decision settles.

Nothing else in §7 is waiting on the owner. Everything in §7.1 is ruled and reflected in §3.

---

## 8. Preserved refutations

**Audit systems must themselves be auditable.** A conclusion that was wrong is corrected here in
place, with the original preserved. Erasing it would remove the evidence that the process works.

### 8.1 S-2 — the proposed design is REFUTED. Do not implement it more carefully.

**Original finding (stands):** failure attribution in the health UI compares two disjoint
vocabularies — scraper run names against registry keys — so a disposition does not round-trip and
a run-level "complete" does not decompose into which boards arrived (audit `F-12`, census `S-2`).
The underlying problem was real. It remained open at the time of this refutation and is now closed by the independently different replacement in `#1132`; the refutation below is preserved because it still governs what must not be reintroduced.

**The proposed fix — emitting `playerCount: null` to distinguish "reported zero" from "did not
report" — is refuted at high confidence, and this session verified both halves directly at HEAD
rather than relaying them:**

1. `server.py:2855` computes
   `site_count = len([s for s in result.get("sites", []) if s.get("playerCount", 0) > 0])`.
   A **present** key defeats `dict.get`'s default, so `None > 0` is evaluated and raises
   `TypeError` — confirmed by execution, not by reading.
2. That line sits above the promotion decision, inside a handler whose enclosing
   `except Exception as e:` calls `_mark_scrape_failure(e, elapsed)`. The proposal would therefore
   mark whole scrapes **FAILED** and break the two-hourly refresh.

**A correction to the refutation's own wording.** It has been stated as "`failedSources` is empty
in 176/176 inspected archives". Measured here across all 176 committed export archives: the key is
**absent from every one**, not present-and-empty. That is a stronger statement and a different
one — the field has never been populated, so a detector keyed on it would observe nothing. The
degradation historically capable of taking the board down was timeout-related, which the proposal
would not have observed either.

**Required posture (historical, satisfied 2026-08-25):** the refuted proposal stays dead. `#1132`
used actual failure data and adversarial tests to build a different replacement, preserved unknown/unmapped
failures explicitly, and met the row's L2 bar. `V1-76` is therefore closed without revising this refutation.

### 8.2 F-22 — reclassified P0 → P2, on measurement

**Original framing:** discarded `ktcSfTep` pick anchors were called a *live value defect*.

**Refuted by measurement:** the canonical contract reads the CSV independently and obtains all 36
vendor pick rows. There is therefore **no demonstrated canonical pick-value error**.

**Correct classification:** a **P2 reporting inconsistency** (`V1-85`), not a P0 value-corruption
incident. Both the original finding and this refutation are retained.

---

## 9. What production verification remains

Five merged units are `IMPLEMENTED_UNVERIFIED` because their checklists have never been executed.
**Owner decision, 2026-08-18: these block V1 completion. They are not carved out as externally
blocked**, and `CLOSED-PENDING-PROD` is not closure (`docs/EXECUTION_PLAN.md` §0.2).

| unit | checklist | contract items |
|---|---|---|
| `C0-R` | `C_SERIES_DIRECTIVE_RECONCILIATION_2026-08-17.md` §7.2 (7 checks) | `V1-120` |
| `C1-U5` | `C1_U5_CONFIDENCE_NAMING.md` §6 | `V1-11` |
| `C1-U8` | `C1_U8_ACQUISITION_LEDGER.md` §8 | `V1-15`, `V1-16`, `V1-17` |
| `C1-U9` | `C1_U9_*` §7 | `V1-19`, `V1-20` |
| `C2-U1` | `C2_U1_CANONICAL_LINEUP.md` §10 | `V1-27` |

Each requires an authenticated production session. The Integration Authority executes every check
reachable without owner-held credentials and records the remainder as `BLOCKED-EXTERNAL` — never
as a pass. **An unreachable check is not a green one.**

### 9a. Production-verification sweep — 2026-08-24

Run against `https://chaseupside.com` on `main` at `131abf9f9`. Standing tally recomputed from
the §3 row table before anything else: **88 / 136 = 64.7%** — the dispatched figure, confirmed
independently rather than accepted. **No row was promoted by this sweep**, and the tally is
unchanged. What follows is evidence, and in three places evidence that converts an assertion
into a measurement.

**The access question was re-tested from scratch, because a stale "blocked" is as untrustworthy
as a stale "green".** Result: unchanged, and now precisely bounded.

| probe | result |
|---|---|
| `~/.ssh` | empty — no keys; no prod host access |
| `.env`, deploy / Sleeper / admin credentials | absent |
| `/api/health`, `/api/status`, `/api/leagues`, `/api/scaffold/status`, `/api/rankings/sources`, `/api/public/league/*` | **200 — public**, and richer than previously used |
| `/api/data`, `/api/market/waivers`, `/api/market/trades`, `/api/version`, `/api/admin/guest-passes` | **401 `auth_required`** |
| `/admin` | **307** → `/login?next=%2Fadmin` |
| `POST /api/test/create-session` | **404** — correctly fail-closed; see below |

Production itself is healthy: `contract_ok: true`, `data_age_hours: 0.2`, last scrape succeeded
at 18:14:33Z in 278.3 s, 1,115 players, `sourceHealthOk: true` and `structuralErrors: []`.

**A verified negative worth recording (feeds `V1-103`).** The E2E session bootstrap is registered
in production but refuses: `GET` returns 405 (the path exists) and `POST` returns **404**, which
is its documented behaviour when `E2E_TEST_MODE` / `E2E_TEST_SECRET` / `E2E_TEST_USERNAME` are
unset — 404 rather than 401 so the endpoint is invisible rather than merely locked. There is no
session-minting exposure. This is also why no amount of cleverness produces an authenticated
session from the integration environment: the fail-closed design is working as intended, and
the correct response is to record `BLOCKED-EXTERNAL`, not to defeat it.

**Executed and PASSING, fresh (not carried forward):**

| row | check | result |
|---|---|---|
| `V1-19` | C1-U9 §7 items 1-2 at **L3** | 21 sources, **0** non-`DYNASTY`, **0** empty `gameTypeEvidence` |
| `V1-20` | game type proven per feed at **L3** | same measurement; the *fails-closed* clause stays **L2** — an all-`DYNASTY` population contains no negative case to observe |

**Measured, and the measurement is what blocks (`V1-87` / audit `F-24`).** The invisible gate
`RISKIT_FEATURE_LEDGER_RANK_CHANGE` was confirmed live and absent from the operator surface
(production reports exactly the 18 registered flags). Registering it needs a blast-radius
measurement; the read site predicted that measurement would be vacuous without a populated
ledger, and the prediction was **tested rather than trusted**: a real ledger was built here from
all 42 tracked `exports/archive` dates (**169,911 rows**), and the ON/OFF pair over the real
2026-08-24 board (992 rows, 740 ranked) returned `rankChange` non-null = **0 both ways** — a diff
of **0 rows**. Cause: the local ledger holds `scraper_blend` (89,638) and `source_value` (80,273)
and **no `canonical_board` rows at all, with zero non-null ranks in any lane**. That lane is
written only by production's live scrape-promotion site and by migrations of
`data/rank_history.jsonl` / `data/board_history.sqlite`, neither of which exists outside
production — which holds `rank_history.jsonl` at 36 snapshots. Rebuilding it from archives is
not available: exports carry the raw scraper payload, not the served board, and re-deriving an
old board with today's curve is forbidden by `src/history`'s own rule. **Blocked on the
production ledger, not on engineering.**

**Measured, and it re-classifies a row (`V1-49`).** `host_native_scoring` is
`{"enabled": false, "gateStatus": "LIVE"}` in production. Its promotion checklist is not merely
unexecuted — it is **unexecutable while the gate is off**.

#### Owner-decision / external-blocker packet

Collected here so these do not sit in the backlog looking like ordinary engineering. **25 of the
48 open rows require L3/L4 production verification**, and the sweep above establishes that none
of them is reachable without owner-held credentials.

**A — one credential grant unblocks the largest tranche.** `V1-11`, `V1-15`, `V1-16`, `V1-17`,
`V1-27`, `V1-56`, `V1-57`, `V1-58`, `V1-59`, `V1-61`, `V1-65`, `V1-83`, `V1-84`,
`V1-86`, `V1-101`, `V1-102`, `V1-109`, `V1-111`, `V1-124`, `V1-131`, `V1-87`, `V1-62`. *Decision:* provide
an authenticated production session (and, for `V1-15`/`V1-16`/`V1-17`, host access to
`data/retention/` + `data/intel/`), or accept that these rows cannot reach their own stated level
in V1. There is no third option that is truthful.

> **RESOLVED IN MECHANISM, 2026-08-25 (Integration), under the owner's general authority
> directive** (*"Proceed autonomously on all other already-authorized … production-verification
> … work"*; *"use existing authorized mechanisms"*; explicit allowance to build one generic
> verification primitive on existing secret storage). The session credential is now produced by
> existing authorized infrastructure rather than an owner grant: an on-box SSH step (the same
> `DEPLOY_*` mechanism the eleven dispatch workflows already use) mints a guest pass through the
> canonical `src/api/guest_passes.py::create()` at a non-default duration, the runner logs in
> through the real `/api/auth/login` guest-pass fall-through (no bypass, no test mode — the
> resulting session is a genuine time-bounded guest session that admin gates reject by
> construction), the verification suite runs with that session, and the pass is revoked with an
> audit trail. Host access for `V1-15`/`16`/`17` is likewise the existing SSH lane (read-only).
> The **admin-surface halves of `V1-101`/`V1-102` remain EXTERNAL_ACCESS_BLOCKED** — a guest
> session is not an admin session, no authorized infrastructure holds an admin credential, and
> none is being created.

**B — irrecoverable historical observation.** The pre-feature/pre-deploy snapshot that three
checks compare against was never captured, and no code recovers an observation nobody made.
- `V1-105` — the `/rankings` pre-feature performance baseline (PR #1006). *Decision:* **(A)**
  accept it as permanently unavailable, close on the baselines legitimately captured for
  still-unmodified surfaces, and preserve the rankings gap explicitly; or **(B)** accept the
  historical 08-04/05 single-run audit despite it not meeting the intended p95 standard.
- `V1-19` / `V1-27` item 4 — strict board value-inertness across the deploy. Same shape, same
  question, and it will recur for every unit whose proof is "the board did not move". *Decision:*
  accept the pre-merge on-box inertness measurement plus a healthy post-deploy scrape, or hold
  these rows on an observation that can no longer be made. **Recommend also authorising a
  pre-deploy snapshot step** so the class stops recurring.

> **PACKET B ANSWERED by owner, 2026-08-25.** `V1-105`: option (A) — the true pre-feature
> `/rankings` p95 baseline is accepted as **permanently irrecoverable**; no fabrication, no
> retroactive relabeling; close on the legitimately captured prospective baselines and preserve
> the gap explicitly (see the row for the closure route). `V1-19` / `V1-27` item 4: the missing
> pre-deployment snapshot is accepted as **permanently irrecoverable** where no already-authorized
> substitute exists; the substitute (pre-merge on-box inertness + healthy post-deploy scrape)
> exists for `V1-19` and its promotion record is in the row. The pre-deploy-snapshot process
> recommendation stands for future units.

**C — production configuration.** `V1-49`: `host_native_scoring` is OFF in production. *Decision:*
enable it (owner action, own blast radius) so the checklist can run, or classify the row
`STATE_BLOCKED` for V1.

> **PACKET C ANSWERED by owner, 2026-08-25 — controlled production activation AUTHORIZED**,
> conditions verbatim: exact-head CI green, blast radius measured first, rollback ready, any
> unexpected scoring behavior triggers rollback, no scoring-methodology change. See the `V1-49`
> row.

**D — state that must occur naturally.** `V1-52`: seven of eight production recipe sections
already pass; the final rank-agreement observation needs naturally existing scored weeks and
numeric ranks. Left `IMPLEMENTED_UNVERIFIED` / `STATE_BLOCKED`. No week was fabricated, no
production evidence simulated, and refusal behaviour was **not** read as numeric agreement.

> **PACKET D CONFIRMED by owner, 2026-08-25 — no acceptance-bar change.** Verbatim: *"Do not
> fabricate scored weeks or numeric ranks. Leave the final rank-agreement proof naturally
> state-blocked until real games make it measurable."* The row stays `IMPLEMENTED_UNVERIFIED`
> until the season produces the observation.

---

### §9b — Lane 4 batch harvest, 2026-08-25: access resolved once, zero rows promoted, one defect named

Ran as one pass, per the Lane 4 batch directive. **Nothing was promoted, and the tally is
unchanged at 89 / 136.** What this pass produces is (a) a definitive Phase-1 access answer,
(b) the exact operational defect behind `V1-59`, measured from real production journald,
and (c) two corrections. Recorded here so the next session re-tests rather than inherits.

#### Phase 1 — production access, re-tested from scratch

Both halves are **ABSENT**, not merely the session cookie:

| capability | probe | result |
|---|---|---|
| on-box / VPS | `which ssh scp` | **not installed** |
| on-box / VPS | `~/.ssh/` | empty — no keys, no `known_hosts`, no `config` |
| on-box / VPS | `DEPLOY_HOST` | a GitHub Actions secret; not reachable from a session |
| credentials | `.env`, `.env.local`, `.env.production` | absent |
| site auth | `RISKIT_SESSION_COOKIE` | unset |
| site auth | `GET /api/sharp/roster-percentage`, `/api/data`, `/api/terminal` | **401** each |
| site auth | `POST /api/test/create-session` | **404** — correct fail-closed behaviour, not defeated |

**The deployed automation has the same gap, and that is the more important finding.**
`data/ops/sharp-production-smoke.json` — written by `verify-sharp-production.yml` against real
production, committed at `a55d1eaf0` — reads `status: "unverifiable_unauthenticated"`,
`measured: false`, `cohort: {}`, `lastError: 401 from https://chaseupside.com/api/sharp/cohort`,
and **`lastMeasuredOn: null`**. That null is the load-bearing field: the Sharp production
verification workflow has **never once measured**. So `V1-58`/`V1-61` are not waiting on an
engineer with a terminal; the standing instrument itself has no session.

#### The instrument was run, in both modes, and behaved correctly

`scripts/verify_lane4_production.py` — not rebuilt, not weakened.

* `--mode remote --origin https://chaseupside.com --league dynasty_main` → **exit 3**.
  `R0` pass (all 5 routes registered, 108 discovered); `C1`/`C2`/`C3`
  `unverifiable_unauthenticated` (401); `C4`-`C9` `blocked`.
* `--mode onbox` in this sandbox → **exit 3**. `R0`/`R1`/`C4`/`C6`/`V65` pass, `C5`
  `unmeasurable`, and `C1`/`C2`/`C3`/`C7`/`C10`/`C8`/`C9`/`V57`/`V57b`/`V60`/`B58`/`B59`
  `blocked`.

Neither exit 0. The strict semantics (401 = `UNVERIFIABLE_UNAUTHENTICATED`, empty population =
`UNMEASURABLE`, absent store = `BLOCKED`) held without modification.

#### V1-65 — the local ledger is an ENVIRONMENT ARTIFACT, and is recorded as one

`data/intel/ledger.sqlite3` exists in this sandbox (458 KB). Read-only census: **29 tables, 0
rows in 28 of them**, `meta` = 4. Per the directive's own prohibition this is **not** read as
feature failure and **not** used as a population measurement. `V1-65` is `UNMEASURABLE` here.

The L1 half stands unchanged (`V65` check: single owner at `src/sharp/cohort.py:263`, every
surface re-exporting it). The L2 population measurement — Sharp-admitted league count,
Insider-admitted league count, explicit set difference — requires the **deployed** ledger and
was **not** attempted against this one. Sharp-admitted count is **UNKNOWN**, not 0.

#### V1-59 — the exact remaining operational defect, MEASURED

This is the one row whose blocker moves from "split evidence" to a named cause. Source: the
`Bootstrap Sharp Records` workflow (`sharp-records-bootstrap.yml`), which dumps real production
journald. Latest run **32813417583** on `a9e1c1de2`, job `97697004287`, step *"Install units and
populate the Sharp cohort"* — **failure**, 05:35:13 → 06:05:24 (**30 m 11 s**).

Of the directive's four hypotheses, **C and D are both present and D is upstream of C**:

1. **Crashloop on a start timeout.** `chaseupside-ffpc-sharp.service` repeatedly logs
   `start operation timed out. Terminating.` → `code=killed, status=15/TERM` →
   `Failed with result 'timeout'`, at a 30-minute `TimeoutStartSec`, restarting immediately
   (…06:33→07:03, 07:03:38→07:33:38, 07:35:23→08:05:23 in box-local time). Each cycle reports
   `Consumed 29 min 25-45 s CPU time` — **CPU, not wall-clock wait**, so it is spinning rather
   than blocked on a slow remote read.
2. **`sqlite3.OperationalError: database is locked`**, at
   `src/intel/platform_ledger.py:1219` in `register_asset_alias`, via
   `hydrate_sleeper_asset_catalog` (`:1820`), from `scripts/crawl_ffpc_sharp.py:111`.
3. **The error path dies on the same lock.** `could not persist FFPC failure report` —
   `record_ingestion_run` (`platform_ledger.py:1862`) raises `database is locked` while handling
   the exception above. The service therefore cannot record its own failure, which is why the
   ledger shows nothing rather than showing a failed run.

Run history is not a blip: of the 20 most recent `Bootstrap Sharp Records` runs, the
non-skipped ones are **failures** without exception, back to 2026-08-22.

`V1-59` stays **`IN PROGRESS`**. Not promoted, and explicitly not promoted on the grounds that
other Sharp surfaces look healthy — they are 401, so nothing about them is observed at all.

#### V1-60 — unavailable, NOT honestly empty

A consequence of the above that must not be mis-recorded. The directive permits a truthful
`no_cohort_managers_on_platform` to satisfy the empty-lane requirement **when measured on the
deployed real population**. It cannot be claimed here: the FFPC ingestion service never
completes a run, so the lane's state is **unavailable**, and coercing that to "honestly empty"
is the exact defect `#911` built `CollectResult.unavailable()` to prevent. `V1-60` remains
`IMPLEMENTED_UNVERIFIED`, `BLOCKED-EXTERNAL`.

#### Two corrections

* **`CLAUDE.md:1357` names a route that does not exist.** It states that
  ``/api/faab/recommend`` stamps `crowdMarket`. The only registered FAAB recommendation route is
  ``/api/waiver/faab-recommend`` (`server.py:5770`); there is no `/api/faab/*` route in
  `server.py` at all. Corrected in this pass. The Lane 4 directive independently flagged the same
  thing, and a verifier following the doc would have chased a 404 and recorded a false negative.
* **`V1-129` five checks are not startable.** 129a/129b need the real
  `data/faab/crowd_history_dynasty_main.json` (absent here — `C7`/`C10` `blocked`); 129c needs an
  authenticated `POST /api/waiver/faab-recommend` with a real rostered IDP player; 129d must read
  the real current ledger and was **not** simulated by artificially ageing anything; 129e needs the
  live `crowdMarket.targetFormatUnknown`. All five: **BLOCKED-EXTERNAL**.

#### V1-57

`systemctl` is unavailable in this sandbox (`System has not been booted with systemd as init
system`), so `V57`/`V57b` report `blocked`. Whether `dynasty-faab-history.timer` is installed and
has fired is **unobserved**, not failed. The templates exist in `deploy/systemd/`.

#### The single owner-facing action

> **Provide or establish an authenticated `chaseupside.com` admin session through the existing
> secure production-login / session mechanism, and on-box access to the deployed working
> directory.**

Nothing is to be pasted into GitHub or chat. Note that the same grant also un-blocks
`verify-sharp-production.yml`, which has never measured.

**The two outstanding grants collapse into ONE owner action (recorded 2026-08-25):** minting a
guest pass at `/admin` yields both halves at once — the authenticated-session evidence the
session-gated rows need (`V1-56`/`V1-61`/`V1-101`/`V1-102`/`V1-109`/`V1-111` et al.) AND the value
for the `PROD_VERIFY_GUEST_PASS` environment secret, which un-blocks the Lane 4 verifier's
`C8`/`C9` checks and its `--mode remote`. One action, two locks.

---

## 10. Changing this document

- **Status changes** (e.g. `IMPLEMENTED_UNVERIFIED` → `VERIFIED`) are recorded by the Integration
  Authority with the evidence that satisfied the stated level. No other lane edits §3.
- **Denominator changes** — adding or removing a V1 REQUIRED item — are **owner decisions**,
  recorded here with a date and a reason. A genuine omission from already-approved V1 scope is a
  legitimate addition; a new idea is not, and goes to the long-term roadmap.
- **Reclassification out of V1 REQUIRED because an item proved hard is not permitted.** That is
  the failure mode in §0, and it is the one this document exists to make visible.

### Denominator change log

| date | change | reason |
|---|---|---|
| 2026-08-20 | **0** — the first Premium Sports Intelligence production migration authorized, and deliberately NOT added | The owner authorized a bounded PSI reference migration — shared foundation, shell, Rankings, Player File (`EXECUTION_PLAN.md` §0, *PREMIUM SPORTS INTELLIGENCE — FIRST PRODUCTION MIGRATION AUTHORIZED*). An **execution-timing** decision, moving this number by **zero**. Recorded because a visible, owner-facing UI landing in production is the single most tempting thing to reclassify as V1 after the fact — it will look like the product. It is not: V1 REQUIRED is the §3 list, PSI stays POST-V1 (§4), and a reference route being *deployed* is not the same claim as its being *required*. Reclassification remains a separate owner scope decision recorded in this log. |
| 2026-08-20 | **0** — the POST-V1 C-Series mass-build campaign (Claude 9-13) authorized, and deliberately NOT added | The owner authorized five isolated lanes to begin implementing **POST-V1** C-Series capability now, before V1 is complete (`EXECUTION_PLAN.md` §0, *POST-V1 C-SERIES MASS-BUILD CAMPAIGN*). That is an **execution-timing** decision and it moves this number by **zero**. Recorded here rather than left to inference, because the tempting error is exactly the §0 failure mode in reverse: work starting early, or even shipping early, does not make it V1 REQUIRED, and a denominator that grows to match whatever is being built stops measuring V1 at all. POST-V1 stays POST-V1 (§4); Claude 9-13 may not mark a row `VERIFIED`; and reclassification remains a separate owner scope decision recorded in this log. |
| 2026-08-20 | **+4** — `V1-133`…`V1-136` added (owner decision: the multi-bridge / source-acquisition program is V1 and top priority) | An **owner scope decision**, which §10 makes the one legitimate way this number moves. Reconciled rather than taken at face value: the owner named **six** capabilities and two already have owners here — raw source preservation is `V1-19` (`C1-SRC-01` / `C1-U9`) continuing, and source-family / common-mode protection is `V1-10`, already `VERIFIED`, which the bridge must **hold** rather than re-earn. Admitting those twice would have inflated the denominator with work already counted, which is the mirror image of the §0 failure mode. The four that remain — the bridge mechanism, its fail-closed behaviour, ordinal-vs-cardinal preservation, and acquisition security — have no owner in this contract. Granularity is Integration's judgement and is flagged in §3.12 as the part most open to owner correction. **No production valuation change is authorized by this addition.** |
| 2026-08-18 | **0** — audit `F-35` filed and deliberately NOT added | A false GREEN, and by the same reasoning that admitted `V1-127` it would qualify — except that it fails the other half of the test. `idMappingCoverage.coverage_pct` reports **1.0 from 0 resolve attempts**, by explicit design (*"so a silent app doesn't look like a broken mapper"*), and will report it forever because `unified_mapper.resolve_player` has no production consumers after `C1-ID-01` and CLAUDE.md forbids adding any. So the surface is real and the defect is real, but the metric is **vestigial** and the blast radius is one admin panel — no canonical value, rank or contract field depends on it. Recording the decision rather than making it silently: the denominator is frozen, and "it is the same class as something already in" is not on its own a reason to enlarge it. If the panel is ever repointed at the live identity owner that is a unit with an owner decision in it, and it can be scoped then. |
| 2026-08-18 | **+1** — `V1-132` added (audit `F-34`, surfaced by lane 7 / `#916`) | A tracked **defect against an already-required capability** — canonical value and signal independence — not new product scope. Measured: the horizon pick year blends **one** vendor while every other pick year blends two, because the far-future injection clones from the RAW payload and `ktcSfTep`'s pick values arrive through the later CSV enrichment. `F-30` made the horizon GUARANTEE independent of which raw keys survive; the blended VALUE is still single-source. Filed so it does not disappear when `#916` closes. |
| 2026-08-18 | **+2** — `V1-130` (§7 A-2) and `V1-131` (§7 A-7) added | **Mechanical consequences of decisions the owner had already made**, resolved under the 2026-08-18 instruction to settle any ambiguity that follows from an existing ruling rather than leave the contract PROPOSED. `V1-130`: `V1-34` is required and `C3-CON-01` is its canonical owner — ONE CONCEPT, ONE CANONICAL OWNER makes excluding the owner incoherent. `V1-131`: "truthful degraded states" is named in the boundary and nav offering an all-503 page is a live instance; the Consensus Edge feature itself stays POST-V1. Neither adds product scope — both are the minimum needed to make an already-required item buildable or honest. |
| 2026-08-18 | **+1** — `V1-129` added (audit `F-33`, lane 4 / `#911`) | A tracked **defect against an already-required capability**, which the owner's 2026-08-18 direction explicitly permits: *"New defects that violate an already-required V1 capability may be added/tracked without redefining the product scope."* FAAB core is already V1 REQUIRED; this adds no product. The crowd pool feeds `rival_bid_cdf` at weight **0.6**, so admitting incomparable leagues, a stale ledger, or a position the retained population cannot price moves real recommended bids — a trustworthiness defect in a required capability, not a new one. |
| 2026-08-18 | **+1** — `V1-128` added (audit `F-28`) | Found by verifying the `#909` deploy against production: `data_age_hours` is **negative** because the naive `scrapeTimestamp` is the host's local time and `_board_age_hours` attaches UTC. A live measurement defect in the freshness signal the ops alerter reads — squarely "stability / false-green repairs" and "source-health correctness". Recorded the same day it was introduced, by the verification step that exists to catch exactly this. |
| 2026-08-18 | **+1** — `V1-127` added (audit `F-27`) | A genuine omission from already-approved V1 scope, not a new idea. `normalizationHealth.healthy` had been **false in production since C1-U6** because a stale private pick-name grammar flagged 18 deliberate canonical rows as malformed. That is a live false red on a shipped surface, which the owner's boundary names explicitly under "stability / false-green repairs" and "truthful degraded states". It was found after publication, during the same sweep, and is recorded here rather than folded in silently — the denominator moving is the kind of event this log exists for. |

