{
 "count": 692,
 "note": "Known missing-data coercions on decision paths, recorded so the gate can block NEW ones while the remediation batches burn these down. Each batch deletes its findings' entries. Do not add to this file to make a build pass.",
 "violations": [
  "frontend/lib/auction-power.js::const raw = names.map((n) => Number(totalsObj[n]) || 0);",
  "frontend/lib/bdvm.js::assetCount: _num(r?.assetCount) ?? 0,",
  "frontend/lib/bdvm.js::pickCount: _num(r?.pickCount) ?? 0,",
  "frontend/lib/bdvm.js::sourceCount: _num(projection.sourceCount) ?? 0,",
  "frontend/lib/draft-logic.js::(max, p) => Math.max(max, Number(p.rank) || 0),",
  "frontend/lib/draft-logic.js::(s, p) => s + (Number(p.amount) || 0),",
  "frontend/lib/draft-logic.js::(s, pk) => s + (Number(pk.amount) || 0),",
  "frontend/lib/draft-logic.js::(s, t) => s + (Number(t.initialBudget) || 0),",
  "frontend/lib/draft-logic.js::(s, v) => s + Math.max(0, Number(v) || 0),",
  "frontend/lib/draft-logic.js::(stats.budgetAdvantage || 0) >= SPEND_UP_ADVANTAGE_MIN",
  "frontend/lib/draft-logic.js::(stats.draftProgress || 0) * 100,",
  "frontend/lib/draft-logic.js::(stats.draftProgress || 0) >= SPEND_UP_PROGRESS_MIN &&",
  "frontend/lib/draft-logic.js::.reduce((s, p) => s + (Number(p.amount) || 0), 0);",
  "frontend/lib/draft-logic.js::.reduce((s, p) => s + (Number(p.preDraft) || 0), 0);",
  "frontend/lib/draft-logic.js::.sort((a, b) => (a.ts || 0) - (b.ts || 0))",
  "frontend/lib/draft-logic.js::: nominationPreDraftFallback.get(n.playerId) || 0;",
  "frontend/lib/draft-logic.js::: nominationPreDraftFallback.get(n?.playerId) || 0;",
  "frontend/lib/draft-logic.js::: playerPreDraftById.get(pk.playerId) || 0;",
  "frontend/lib/draft-logic.js::? (player.inflatedFair || 0) - (Number(pk.amount) || 0)",
  "frontend/lib/draft-logic.js::? { ...p, preDraft: Math.max(0, Number(newPreDraft) || 0) }",
  "frontend/lib/draft-logic.js::? { ...r, fairValue: Math.max(0, Number(newFairValue) || 0) }",
  "frontend/lib/draft-logic.js::Math.max(0, Number(p?.preDraft) || 0),",
  "frontend/lib/draft-logic.js::Number(n?.preDraftAtNomination) || 0,",
  "frontend/lib/draft-logic.js::amount: Math.max(0, Number(p?.amount) || 0),",
  "frontend/lib/draft-logic.js::auctionDollars: Math.max(0, Number(entry?.auctionDollars) || 0),",
  "frontend/lib/draft-logic.js::const amt = Math.max(0, Number(amount) || 0);",
  "frontend/lib/draft-logic.js::const at = tierOrder[a.player.tier] || 0;",
  "frontend/lib/draft-logic.js::const bid = Math.max(0, Number(currentBid) || 0);",
  "frontend/lib/draft-logic.js::const bt = tierOrder[b.player.tier] || 0;",
  "frontend/lib/draft-logic.js::const cur = tierInterestByTeam[idx][tierKey] ?? 1;",
  "frontend/lib/draft-logic.js::const fallback = preDraftById.get(pid) ?? 0;",
  "frontend/lib/draft-logic.js::const fv = Math.max(0, Number(fairValue) || 0);",
  "frontend/lib/draft-logic.js::const fv = Math.max(0, Number(row?.fairValue) || 0);",
  "frontend/lib/draft-logic.js::const have = counts[key] || 0;",
  "frontend/lib/draft-logic.js::const initial = Number(t.initialBudget) || 0;",
  "frontend/lib/draft-logic.js::const initialBudget = Math.max(0, Number(t?.initialBudget) || 0);",
  "frontend/lib/draft-logic.js::const interest = tierInterestByTeam[i]?.[tier] ?? 1;",
  "frontend/lib/draft-logic.js::const myRookiesBought = rookiesBoughtByIdx[myTeamIdx] || 0;",
  "frontend/lib/draft-logic.js::const myStarting = Number(myTeam.initialBudget) || 0;",
  "frontend/lib/draft-logic.js::const ourDollar = Math.max(0, p.preDraft || 0);",
  "frontend/lib/draft-logic.js::const paid = Number(pk.amount) || 0;",
  "frontend/lib/draft-logic.js::const phasePct = Math.max(0, Math.min(1, Number(stats.leagueSpentPct) || 0));",
  "frontend/lib/draft-logic.js::const preDraft = Math.max(0, Number(p.preDraft) || 0);",
  "frontend/lib/draft-logic.js::const preDraftAtNomination = Math.max(0, Number(player?.preDraft) || 0);",
  "frontend/lib/draft-logic.js::const preDraftAtPick = Math.max(0, Number(player?.preDraft) || 0);",
  "frontend/lib/draft-logic.js::const sorted = [...noms].sort((a, b) => (b.ts || 0) - (a.ts || 0));",
  "frontend/lib/draft-logic.js::const sorted = [...picks].sort((a, b) => (b.ts || 0) - (a.ts || 0));",
  "frontend/lib/draft-logic.js::const v = Math.max(0, Number(preDraft) || 0);",
  "frontend/lib/draft-logic.js::const vendorDollar = Math.max(0, Number(p[vendorKey]) || 0);",
  "frontend/lib/draft-logic.js::fair: player?.inflatedFair ?? 0,",
  "frontend/lib/draft-logic.js::fairValue: Math.max(0, Number(r?.fairValue) || 0),",
  "frontend/lib/draft-logic.js::myPicks: myRows.sort((a, b) => (a.ts || 0) - (b.ts || 0)),",
  "frontend/lib/draft-logic.js::needPositions.sort((a, b) => (shortages[b] || 0) - (shortages[a] || 0));",
  "frontend/lib/draft-logic.js::nominationsByTier[k] = (nominationsByTier[k] || 0) + 1;",
  "frontend/lib/draft-logic.js::paid = Math.max(0, Number(pick.amount) || 0);",
  "frontend/lib/draft-logic.js::paid: Number(pk.amount) || 0,",
  "frontend/lib/draft-logic.js::picks.sort((a, b) => (a.ts || 0) - (b.ts || 0));",
  "frontend/lib/draft-logic.js::players.map((p) => [p.id, Math.max(0, Number(p.preDraft) || 0)]),",
  "frontend/lib/draft-logic.js::players.map((p) => [p.id, Number(p.preDraft) || 0]),",
  "frontend/lib/draft-logic.js::preDraft: Math.max(0, Number(p.preDraft) || 0),",
  "frontend/lib/draft-logic.js::preDraft: Math.max(0, Number(p?.preDraft) || 0),",
  "frontend/lib/draft-logic.js::preDraft: player?.preDraft ?? pk.preDraftAtPick ?? 0,",
  "frontend/lib/draft-logic.js::return Math.max(0, (Number(t.initialBudget) || 0) - spent);",
  "frontend/lib/draft-logic.js::return Math.max(0, Number(remaining) || 0);",
  "frontend/lib/draft-logic.js::return rawValues.map((v) => Math.max(1, Math.round(Math.max(0, Number(v) || 0) * scale)));",
  "frontend/lib/draft-logic.js::stats.budgetAdvantage || 0",
  "frontend/lib/draft-logic.js::tbTotals.fairSum += p.inflatedFair || 0;",
  "frontend/lib/draft-logic.js::tbTotals.paidSum += p.pick?.amount || 0;",
  "frontend/lib/draft-logic.js::tbTotals.preDraftSum += p.preDraft || 0;",
  "frontend/lib/draft-logic.js::tbTotals.remainingFair += p.inflatedFair || 0;",
  "frontend/lib/draft-logic.js::tbTotals.remainingWinBid += p.myWinningBid || 0;",
  "frontend/lib/draft-logic.js::tbTotals.winBidSum += p.myWinningBid || 0;",
  "frontend/lib/draft-logic.js::ts: Number(row?.ts) || 0,",
  "frontend/lib/draft-logic.js::vendorDollar, Math.max(0, stats.topCompetitorMax || 0),",
  "frontend/lib/draft-logic.js::{ id, rank: nextRank, name, preDraft: Math.max(0, Number(preDraft) || 0) },",
  "frontend/lib/dynasty-data.js::) || 0;",
  "frontend/lib/dynasty-data.js::Number(player?.values?.finalAdjusted ?? player?.values?.overall ?? 0) || 0;",
  "frontend/lib/dynasty-data.js::const backendSourceCount = Number(player.sourceCount || 0);",
  "frontend/lib/dynasty-data.js::const displayVal = Number(player?.values?.displayValue ?? 0) || 0;",
  "frontend/lib/dynasty-data.js::const full = Number(player.rankDerivedValue ?? 0) || 0;",
  "frontend/lib/dynasty-data.js::const va = Number(a.rankDerivedValue) || 0;",
  "frontend/lib/dynasty-data.js::const vb = Number(b.rankDerivedValue) || 0;",
  "frontend/lib/dynasty-data.js::if (Number.isFinite(w) && w !== Number(src.weight ?? 1)) return true;",
  "frontend/lib/dynasty-data.js::player._rawComposite ?? player._rawMarketValue ?? player._composite ?? 0,",
  "frontend/lib/dynasty-data.js::raw: Number(player?.values?.rawComposite ?? 0) || 0,",
  "frontend/lib/dynasty-data.js::siteCount: Number(player.sourceCount || player._sites || 0),",
  "frontend/lib/dynasty-data.js::sourceCount: Number(player.sourceCount || 0),",
  "frontend/lib/edge-helpers.js::(r.sourceCount ?? 0) >= 2 &&",
  "frontend/lib/edge-helpers.js::(r.sourceRankSpread ?? 0) > 10 &&",
  "frontend/lib/edge-helpers.js::(r.sourceRankSpread ?? 0) > 15 &&",
  "frontend/lib/edge-helpers.js::(row.sourceCount || 0) >= 2 &&",
  "frontend/lib/edge-helpers.js::.filter((r) => r.confidenceBucket === \"high\" && (r.sourceCount ?? 0) >= 2 && !r.quarantined)",
  "frontend/lib/edge-helpers.js::detail: `spread ${r.sourceRankSpread ?? 0}`,",
  "frontend/lib/edge-helpers.js::filter: (row) => (row.rank ?? Infinity) <= LENS_INEFFICIENCY_RANK && (row.sourceRankSpread ?? 0) > LENS_INEFFICIENCY_SPR",
  "frontend/lib/edge-helpers.js::filter: (row) => (row.sourceRankSpread ?? 0) > LENS_DISAGREEMENT_SPREAD,",
  "frontend/lib/edge-helpers.js::filter: (row) => row.confidenceBucket === \"high\" && (row.sourceCount ?? 0) >= 2,",
  "frontend/lib/edge-helpers.js::sort: (a, b) => (b.sourceRankSpread ?? 0) - (a.sourceRankSpread ?? 0),",
  "frontend/lib/league-analysis.js::(best, s) => (Math.abs(s.pctGap) > Math.abs(best?.pctGap ?? 0) ? s : best),",
  "frontend/lib/league-analysis.js::const b = Number(m[3]) || 0;",
  "frontend/lib/league-analysis.js::const baseVal = row.values?.full || 0;",
  "frontend/lib/league-analysis.js::const g = Number(m[2]) || 0;",
  "frontend/lib/league-analysis.js::const r = Number(m[1]) || 0;",
  "frontend/lib/league-analysis.js::const sorted = teams.slice().sort((a, b) => (b.byGroup[g] || 0) - (a.byGroup[g] || 0));",
  "frontend/lib/league-analysis.js::const total = POS_GROUPS.reduce((s, g) => s + (byGroup[g] || 0), 0);",
  "frontend/lib/league-analysis.js::const val = row ? (row.values?.full || 0) : 0;",
  "frontend/lib/league-analysis.js::const vals = teams.map((t) => t.byGroup[g] || 0);",
  "frontend/lib/league-analysis.js::if ((row.values?.full || 0) < 500) continue;",
  "frontend/lib/league-analysis.js::ledger.set(it.name, (ledger.get(it.name) || 0) + 1);",
  "frontend/lib/league-analysis.js::ledger.set(it.name, (ledger.get(it.name) || 0) - 1);",
  "frontend/lib/league-analysis.js::meta: r.values?.full || 0,",
  "frontend/lib/league-analysis.js::normalizeTradeTimestampMs(a.trade?.timestamp) || Date.parse(a.date) || 0;",
  "frontend/lib/league-analysis.js::return row ? (row.values?.full || 0) : 0;",
  "frontend/lib/league-analysis.js::stats.posBias[pos] = (stats.posBias[pos] || 0) + 1;",
  "frontend/lib/league-analysis.js::value: row.values?.full || 0,",
  "frontend/lib/league-analysis.js::value: strippedRow.values?.full || 0,",
  "frontend/lib/market-movers.js::return (b.value || 0) - (a.value || 0);",
  "frontend/lib/market-movers.js::value: Number(r.rankDerivedValue || r.values?.full || 0),",
  "frontend/lib/movers.js::value: Number(r.rankDerivedValue || r.values?.full || 0),",
  "frontend/lib/news-service.js::const ta = Date.parse(a) || 0;",
  "frontend/lib/news-service.js::const tb = Date.parse(b) || 0;",
  "frontend/lib/perfect-draft.js::.map((p, i) => [i, Number(p.surplus) || 0])",
  "frontend/lib/perfect-draft.js::.reduce((s, r) => s + (Number(r?.effectiveCutCost) || 0), 0);",
  "frontend/lib/perfect-draft.js::budgetRemaining: budget - (bestPlan.spend || 0),",
  "frontend/lib/perfect-draft.js::const B = Math.max(0, Math.floor(Number(budget) || 0));",
  "frontend/lib/perfect-draft.js::const B = Math.max(0, Math.floor(Number(input?.budget) || 0));",
  "frontend/lib/perfect-draft.js::const bought = Math.max(0, Number(rookiesBought) || 0);",
  "frontend/lib/perfect-draft.js::const count = Math.max(0, Math.min(Math.floor(Number(n) || 0), ladder.length));",
  "frontend/lib/perfect-draft.js::const cuts = Math.max(0, Math.floor(Number(k) || 0) - Math.max(0, openRosterSpots || 0));",
  "frontend/lib/perfect-draft.js::const cuts = Math.max(0, k - Math.max(0, openRosterSpots || 0));",
  "frontend/lib/perfect-draft.js::const fair = Math.max(0, Math.round(Number(rookie?.price) || 0));",
  "frontend/lib/perfect-draft.js::const filled = Object.keys(needs).filter((pos) => (added[pos] || 0) > 0);",
  "frontend/lib/perfect-draft.js::const have = counts[pos] || 0;",
  "frontend/lib/perfect-draft.js::const made = Number(stats?.totalPicksMade) || 0;",
  "frontend/lib/perfect-draft.js::const n = Math.max(0, Math.floor(Number(k) || 0));",
  "frontend/lib/perfect-draft.js::const net = gross - (plan.replacement || 0) - (plan.displacement || 0);",
  "frontend/lib/perfect-draft.js::const open = Math.max(0, Math.floor(Number(openRosterSpots) || 0));",
  "frontend/lib/perfect-draft.js::const open = Math.max(0, Number(openRosterSpots) || 0);",
  "frontend/lib/perfect-draft.js::const p = Math.max(0, Number(price) || 0);",
  "frontend/lib/perfect-draft.js::const pool = Number(stats?.rookiePoolSize) || 0;",
  "frontend/lib/perfect-draft.js::const q = Math.max(0, Math.round(Number(bid) || 0));",
  "frontend/lib/perfect-draft.js::const remaining = short - (added[pos] || 0);",
  "frontend/lib/perfect-draft.js::const s = Math.max(0, Number(sigma) || 0);",
  "frontend/lib/perfect-draft.js::const spend = bought.reduce((s, p) => s + (Number(p?.pick?.amount) || 0), 0);",
  "frontend/lib/perfect-draft.js::const v = Number(n) || 0;",
  "frontend/lib/perfect-draft.js::counts[k] = Number(n) || 0;",
  "frontend/lib/perfect-draft.js::cumValue += Number(valuesDesc[k - 1]) || 0;",
  "frontend/lib/perfect-draft.js::eccPrefix.push(eccPrefix[i] + (Number(rungs[i]?.effectiveCutCost) || 0));",
  "frontend/lib/perfect-draft.js::for (let i = 0; i < cuts; i++) total += Number(ladder[i]?.effectiveCutCost) || 0;",
  "frontend/lib/perfect-draft.js::if (pos) added[pos] = (added[pos] || 0) + 1;",
  "frontend/lib/perfect-draft.js::infeasibleDraws: assessed.meta?.infeasibleDraws ?? 0,",
  "frontend/lib/perfect-draft.js::observedCount: assessed.meta?.observedCount ?? 0,",
  "frontend/lib/perfect-draft.js::priceShocks.set(p.id, (Number(p.price) || 0) * (s > 0 ? Math.exp(gauss(rand) * s) : 1));",
  "frontend/lib/perfect-draft.js::return s + (Number(p.price) || 0) * mult;",
  "frontend/lib/pick-stack.js::const T = Math.max(1, Math.floor(teamsPerRound) || 0);",
  "frontend/lib/pick-stack.js::stacks[team] = Number(t.auctionDollars) || 0; // upcoming draft $",
  "frontend/lib/portfolio-insights.js::(p.trend7 ?? 0) >= 3 &&",
  "frontend/lib/portfolio-insights.js::(selectedTeam.players?.length || 0) + (selectedTeam.picks?.length || 0);",
  "frontend/lib/portfolio-insights.js::.filter((p) => (p.trend7 ?? 0) <= -3)",
  "frontend/lib/portfolio-insights.js::.filter((p) => p.volLabel === \"high\" && (p.trend7 ?? 0) < 0)",
  "frontend/lib/portfolio-insights.js::.filter((r) => Number(r.rankDerivedValue || r.values?.full || 0) >= 3000);",
  "frontend/lib/portfolio-insights.js::.sort((a, b) => (a.trend7 ?? 0) - (b.trend7 ?? 0))[0];",
  "frontend/lib/portfolio-insights.js::.sort((a, b) => (b.trend7 ?? 0) - (a.trend7 ?? 0))[0];",
  "frontend/lib/portfolio-insights.js::const t30 = trend30 ?? 0;",
  "frontend/lib/portfolio-insights.js::const t7 = trend7 ?? 0;",
  "frontend/lib/portfolio-insights.js::const value = Number(row.rankDerivedValue || row.values?.full || 0);",
  "frontend/lib/portfolio-insights.js::else if ((p.trend7 ?? 0) <= -3) falling += 1;",
  "frontend/lib/portfolio-insights.js::for (const p of players) total += Number(p.value) || 0;",
  "frontend/lib/portfolio-insights.js::if ((p.trend7 ?? 0) >= 3) rising += 1;",
  "frontend/lib/portfolio-insights.js::value: Number(bestCand.rankDerivedValue || bestCand.values?.full || 0),",
  "frontend/lib/rankings-helpers.js::const v = Number(value) || 0;",
  "frontend/lib/ros-index.js::Number(r.confidence) || 0,",
  "frontend/lib/ros-index.js::Number(r.rosValue) || 0,",
  "frontend/lib/ros-index.js::Number(r.sourceCount) || 0,",
  "frontend/lib/roster-compare.js::const value = Number(r?.rankDerivedValue || r?.values?.full || 0);",
  "frontend/lib/roster-compare.js::sum += fam?.total || 0;",
  "frontend/lib/signal-engine.js::(c.trend7 ?? 0) <= -3 && c.trend30 != null && c.trend30 <= 0,",
  "frontend/lib/signal-engine.js::(c.trend7 ?? 0) <= -3,",
  "frontend/lib/signal-engine.js::(c.trend7 ?? 0) >= 3 && c.volatility?.label !== \"high\",",
  "frontend/lib/signal-engine.js::c.positiveImpactCount > 0 && (c.rankChange ?? 0) > 0,",
  "frontend/lib/signal-engine.js::c.volatility?.label === \"high\" && (c.trend7 ?? 0) <= -5,",
  "frontend/lib/signal-engine.js::return (b.context.value || 0) - (a.context.value || 0);",
  "frontend/lib/signal-engine.js::value: Number(row?.rankDerivedValue || row?.values?.full || 0),",
  "frontend/lib/starter-slots.js::return [REGISTRY_SLOT_ALIASES[upper] ?? upper, Number(n) || 0];",
  "frontend/lib/team-phase.js::const ageGap = (r.medianAge || 0) - (w.medianAge || 0);",
  "frontend/lib/team-phase.js::const score = Math.max(0, valueGap) * Math.max(0, ageGap || 1);",
  "frontend/lib/team-phase.js::value: Number(r.rankDerivedValue || r.values?.full || 0),",
  "frontend/lib/trade-logic.js::.map((r) => ({ name: r.name, pos: r.pos, value: Number(r.values?.[valueMode] || 0) }))",
  "frontend/lib/trade-logic.js::.map((v) => Number(v) || 0)",
  "frontend/lib/trade-logic.js::const adjustment = adjustments[i] || 0;",
  "frontend/lib/trade-logic.js::const d = Number(mv.dollars) || 0;",
  "frontend/lib/trade-logic.js::const premiumAfter = (after[team] || 0) - (post[team] || 0);",
  "frontend/lib/trade-logic.js::const premiumBefore = (before[team] || 0) - (leagueStacks[team] || 0);",
  "frontend/lib/trade-logic.js::const stackAdjustment = stack[i] || 0;",
  "frontend/lib/trade-logic.js::let hi = Math.max(t || 0, KTC_V_OVERALL_MAX);",
  "frontend/lib/trade-logic.js::result[i].net = result[i].received - result[i].given + (stack[i] || 0);",
  "frontend/lib/trade-logic.js::return Number(row.values?.[valueMode] || 0);",
  "frontend/lib/trade-logic.js::sumBoard += Number(mv.board) || 0;",
  "frontend/lib/trade-retro-value.js::const v = Number(it.val) || 0;",
  "frontend/lib/trade-share.js::const version = Number(parsed.v) || 1;",
  "frontend/lib/value-history.js::const rSpan = rMax - rMin || 1;",
  "frontend/lib/value-history.js::const tSpan = tMax - tMin || 1;",
  "frontend/lib/value-history.js::if ((nameVariantCount.get(lowered) || 0) > 1) return [];",
  "frontend/lib/value-history.js::nameVariantCount.set(cleanName, (nameVariantCount.get(cleanName) || 0) + 1);",
  "frontend/lib/waiver-logic.js::bestGain: bestMoves[0]?.netGain || 0,",
  "frontend/lib/waiver-logic.js::const g = Number(netGain) || 0;",
  "frontend/lib/waiver-logic.js::const minGain = Number(filters.minGain) || 0;",
  "frontend/lib/waiver-logic.js::const ra = UPGRADE_TIER_RANK[a.upgradeTier] || 0;",
  "frontend/lib/waiver-logic.js::const rb = UPGRADE_TIER_RANK[b.upgradeTier] || 0;",
  "frontend/lib/waiver-logic.js::if (minGain > 0 && (a.netGain || 0) < minGain) return false;",
  "server.py::\"data_stale\": (data_age_seconds or 0) > SCRAPE_INTERVAL_HOURS * 3 * 3600,",
  "server.py::\"player_count\": int((latest_contract_data or {}).get(\"playerCount\") or 0),",
  "server.py::\"valueNow\": int(row.get(\"rankDerivedValue\") or 0) or None,",
  "server.py::(latest_contract_data or {}).get(\"playerCount\") or 0",
  "server.py::_fpts_dec = int(_settings.get(\"fpts_decimal\", 0) or 0)",
  "server.py::_fpts_int = int(_settings.get(\"fpts\", 0) or 0)",
  "server.py::add_value = float(add_row.get(\"rankDerivedValue\") or 0)",
  "server.py::after_pick_no=int(afterPickNo or 0),",
  "server.py::count = int(row.get(\"playerCount\") or 0)",
  "server.py::denom_decay = sum(values[i] * weights[i] for i in range(n)) or 1.0",
  "server.py::denom_spine = sum(excess) or 1.0",
  "server.py::drop_value = float(drop_row.get(\"rankDerivedValue\") or 0) if drop_row else 0.0",
  "server.py::fetched_at = float(_public_league_cache.get(\"fetched_at\") or 0.0)",
  "server.py::fetched_at = float(cached.get(\"_cached_at\") or 0)",
  "server.py::float(rec.get(\"objective\", {}).get(\"pctOfOriginalBudget\") or 0.0)",
  "server.py::if cached and (now - float(cached.get(\"fetched_at\") or 0)) < _SLEEPER_NAME_TTL_SEC:",
  "server.py::int(v or 0) for k, v in _starters.items() if str(k).upper() != \"K\"",
  "server.py::key=lambda e: (-e[\"delta\"], -(e[\"valueNow\"] or 0), e[\"name\"], e[\"assetClass\"] or \"\")",
  "server.py::key=lambda e: (e[\"delta\"], -(e[\"valueNow\"] or 0), e[\"name\"], e[\"assetClass\"] or \"\")",
  "server.py::last_failure_at = float(_public_league_cache.get(\"last_failure_at\") or 0.0)",
  "server.py::limit = int(request.query_params.get(\"limit\") or 100)",
  "server.py::or 100",
  "server.py::or float(_public_league_cache.get(\"fetched_at\") or 0.0) == 0.0",
  "server.py::r = float(row.get(\"canonicalConsensusRank\") or row.get(\"rank\") or 0)",
  "server.py::roster_size = int(roster_settings.get(\"rosterSize\") or 0)",
  "server.py::sum(int(v or 0) for k, v in starters_map.items() if str(k).upper() != \"K\") or 20",
  "server.py::trending_for_player = {\"count\": int(trending_snapshot[\"counts\"].get(add_player_id) or 0)}",
  "src/api/bdvm_api.py::v = float(((asset.get(\"distribution\") or {}).get(s) or {}).get(\"ev\") or 0.0)",
  "src/api/bdvm_api.py::v = float((asset.get(\"tradeValue\") or {}).get(s) or 0.0)",
  "src/api/bdvm_signal_alerts.py::prev_notified_at = int(prev.get(\"notifiedAt\") or 0)",
  "src/api/chat.py::\"cache_creation_input_tokens\": usage.cache_creation_input_tokens or 0,",
  "src/api/chat.py::\"cache_read_input_tokens\": usage.cache_read_input_tokens or 0,",
  "src/api/data_contract.py::\"weight\": float(src.get(\"weight\") or 0.0),",
  "src/api/data_contract.py::(_to_int_or_none(canonical_sites.get(k)) or 0) > 0 for k in _IDP_SIGNAL_KEYS",
  "src/api/data_contract.py::(_to_int_or_none(canonical_sites.get(k)) or 0) > 0 for k in _OFFENSE_SIGNAL_KEYS",
  "src/api/data_contract.py::-int(r.get(\"rankDerivedValue\") or 0),",
  "src/api/data_contract.py::bonus = float(ctx.get(\"bonus_rec_te\") or 0.0)",
  "src/api/data_contract.py::bonus_rec_te = float(scoring.get(\"bonus_rec_te\") or 0.0)",
  "src/api/data_contract.py::current_value = float(row.get(\"rankDerivedValue\") or 0.0)",
  "src/api/data_contract.py::declared_weight = float(src_def.get(\"weight\") or 1.0)",
  "src/api/data_contract.py::default = float(src.get(\"weight\") or 0.0)",
  "src/api/data_contract.py::default_weight = float(src.get(\"weight\") or 0.0)",
  "src/api/data_contract.py::depth = source_pool_sizes.get(key) or meta.get(\"depth\") or 0",
  "src/api/data_contract.py::discount = float(row.get(\"pickYearDiscount\") or 1.0)",
  "src/api/data_contract.py::eff_r = int(float(row_csv.get(\"effectiveRank\") or 0))",
  "src/api/data_contract.py::fetched_at = float(_LEAGUE_CONTEXT_CACHE.get(\"fetched_at\") or 0.0)",
  "src/api/data_contract.py::has_any_source = any((_to_int_or_none(v) or 0) > 0 for v in canonical_sites.values())",
  "src/api/data_contract.py::has_idp_val = any((_to_int_or_none(canonical_sites.get(k)) or 0) > 0 for k in _IDP_SIGNAL_KEYS)",
  "src/api/data_contract.py::if float(py_val or 0) != float(js_val or 0):",
  "src/api/data_contract.py::int(p.get(\"rankDerivedValue\") or 0),",
  "src/api/data_contract.py::norm_v = int(float(row_csv.get(\"normalizedValue\") or 0))",
  "src/api/data_contract.py::orig_r = int(float(row_csv.get(\"originalRank\") or 0))",
  "src/api/data_contract.py::primary = int(row.get(\"rankDerivedValue\") or 0)",
  "src/api/data_contract.py::return float(r.get(\"rankDerivedValue\") or 0)",
  "src/api/data_contract.py::return int(row.get(\"_blendedValueUncapped\") or 0)",
  "src/api/data_contract.py::series = [-float(r.get(\"rankDerivedValue\") or 0) for r in tiered_rows]",
  "src/api/data_contract.py::size = int(data.get(\"total_rosters\") or 0)",
  "src/api/data_contract.py::sleeper_bonus = float(league_context.get(\"bonus_rec_te\") or 0.0)",
  "src/api/data_contract.py::str(s.get(\"key\") or \"\"): max(0.0, float(s.get(\"weight\") or 1.0)) for s in active_sources",
  "src/api/data_contract.py::value = float(row.get(\"rankDerivedValue\") or 0.0)",
  "src/api/data_contract.py::w = float(s.get(\"weight\") or 0.0)",
  "src/api/draft_capital_fallback.py::team_totals[p.current_owner] = team_totals.get(p.current_owner, 0) + (p.dollar_value or 0)",
  "src/api/faab_analytics.py::\"createdAt\": int(tx.get(\"status_updated\") or tx.get(\"created\") or 0),",
  "src/api/faab_analytics.py::bid = int(settings.get(\"waiver_bid\") or 0)",
  "src/api/faab_analytics.py::if r.get(\"roster_id\") == roster_id or r.get(\"roster_id\") == int(roster_id or 0):",
  "src/api/faab_analytics.py::waivers_recent_first = sorted(waivers, key=lambda tx: -int(tx.get(\"createdAt\") or 0))",
  "src/api/guest_passes.py::id=int(new_id or 0),",
  "src/api/guest_passes.py::return int(cur.rowcount or 0)",
  "src/api/ops_alerts.py::last_at = float(last.get(\"firedAt\") or 0.0)",
  "src/api/rank_history.py::val = int(round(float(row.get(\"rankDerivedValue\") or 0)))",
  "src/api/session_store.py::return cursor.rowcount or 0",
  "src/api/signal_alerts.py::prev_notified_at = int(prev.get(\"notifiedAt\") or 0)",
  "src/api/signal_state_migration.py::if int(legacy_entry.get(\"notifiedAt\") or 0) > int(new_entry.get(\"notifiedAt\") or 0):",
  "src/api/sleeper_overlay.py::\"createdAtMs\": ts_ms or 0,",
  "src/api/sleeper_overlay.py::\"timestamp\": ts_ms or 0,",
  "src/api/sleeper_overlay.py::age = now - float(cached.get(\"_cached_at\") or 0)",
  "src/api/sleeper_overlay.py::and (now - float(teams_cached.get(\"_cached_at\") or 0)) < _TEAMS_CACHE_TTL_SEC",
  "src/api/sleeper_overlay.py::if cached and (now - float(cached.get(\"_cached_at\") or 0)) < _DRAFT_ID_CACHE_TTL_SEC:",
  "src/api/sleeper_overlay.py::if cached and (now - float(cached.get(\"_cached_at\") or 0)) < _DRAFT_PICKS_CACHE_TTL_SEC:",
  "src/api/sleeper_overlay.py::if cached and (time.time() - float(cached.get(\"_cached_at\") or 0)) < _CACHE_TTL_SEC:",
  "src/api/sleeper_overlay.py::if full and (now - float(full.get(\"_cached_at\") or 0)) < _CACHE_TTL_SEC:",
  "src/api/sleeper_overlay.py::original = int(tp.get(\"roster_id\") or 0)",
  "src/api/sleeper_overlay.py::out.sort(key=lambda w: -int(w.get(\"createdAtMs\", 0) or 0))",
  "src/api/sleeper_overlay.py::owner = int(tp.get(\"owner_id\") or 0)",
  "src/api/sleeper_overlay.py::rnd = int(tp.get(\"round\") or 0)",
  "src/api/sleeper_overlay.py::start = int(d.get(\"start_time\") or 0)",
  "src/api/sleeper_overlay.py::trades.sort(key=lambda t: -int(t.get(\"timestamp\", 0) or 0))",
  "src/api/source_health_alerts.py::last_alerted = float(prev.get(\"lastAlertedAt\") or 0)",
  "src/api/team_assignment.py::return int(meta.get(\"years_exp\") or 0) == 0",
  "src/api/terminal.py::-(s.get(\"value\") or 0),",
  "src/api/terminal.py::[p for p in rosterValues if (p[\"trend7\"] or 0) <= -3],",
  "src/api/terminal.py::alert = ctx.get(\"alertCount\") or 0",
  "src/api/terminal.py::and 0 < (r.get(\"canonicalConsensusRank\") or 0) <= 150",
  "src/api/terminal.py::chip = max(chips, key=lambda p: p[\"trend7\"] or 0) if chips else None",
  "src/api/terminal.py::falling = sum(1 for p in rosterValues if (p[\"trend7\"] or 0) <= -3)",
  "src/api/terminal.py::if 3000 <= p[\"value\"] <= 7500 and (p[\"trend7\"] or 0) >= 3 and p[\"volLabel\"] != \"high\"",
  "src/api/terminal.py::key=lambda p: p[\"trend7\"] or 0,",
  "src/api/terminal.py::n for n in payload[\"news\"][\"items\"] if (n.get(\"relevance\") or 0) < 100",
  "src/api/terminal.py::neg = ctx.get(\"negativeImpactCount\") or 0",
  "src/api/terminal.py::pos = ctx.get(\"positiveImpactCount\") or 0",
  "src/api/terminal.py::rising = sum(1 for p in rosterValues if (p[\"trend7\"] or 0) >= 3)",
  "src/api/terminal.py::scored.sort(key=lambda n: (-(n.get(\"relevance\") or 0), -_ts_ms(n.get(\"ts\"))))",
  "src/api/terminal.py::tier1 = [p for p in rosterValues if p[\"volLabel\"] == \"high\" and (p[\"trend7\"] or 0) < 0]",
  "src/api/terminal.py::totalValue = sum(p[\"value\"] for p in rosterValues) or 0",
  "src/api/terminal.py::ts = int(tx.get(\"timestamp\") or 0)",
  "src/api/terminal.py::v = float(value or 0)",
  "src/api/terminal.py::value = ctx.get(\"value\") or 0",
  "src/api/trade_simulator.py::v = int(a.get(\"value\") or 0)",
  "src/bdvm/actuals.py::if int(raw.get(\"season\") or 0) != int(season):",
  "src/bdvm/actuals.py::week = int(raw.get(\"week\") or 0)",
  "src/bdvm/baseline.py::return float(v or 0)",
  "src/bdvm/context.py::return float(v or 0)",
  "src/bdvm/engine.py::s = sum(contrib) or 1.0",
  "src/bdvm/league_config.py::n = int(count or 0)",
  "src/bdvm/league_config.py::return float(self.scoring_settings.get(\"bonus_rec_te\", 0) or 0) > 0",
  "src/bdvm/league_config.py::roster_size = int(rs.get(\"rosterSize\") or 0)",
  "src/bdvm/league_config.py::taxi_size = int(league_settings.get(\"taxi_slots\") or 0)",
  "src/bdvm/league_config.py::taxi_size = int(rs.get(\"taxiSize\") or 0)",
  "src/bdvm/league_config.py::teams = int(league_settings.get(\"num_teams\") or 0)",
  "src/bdvm/league_config.py::teams = int(rs.get(\"teamCount\") or 0)",
  "src/bdvm/projections.py::games = float(row.get(\"games\") or 0)",
  "src/bdvm/projections.py::return abs(float(effective.get(key) or 0.0)) > 0.0",
  "src/bdvm/roster.py::repl_fpg = float((replacement.get(group) or {}).get(\"replacementFpg\") or 0.0)",
  "src/bdvm/schedule.py::week = int(float(row.get(\"week\") or 0))",
  "src/canonical/calibration.py::players.sort(key=lambda a: -(a.get(sort_key) or 0))",
  "src/canonical/confidence_intervals.py::canonical_value=float(p.get(\"rankDerivedValue\") or 0),",
  "src/canonical/confidence_intervals.py::cv = float(canonical_value or 0)",
  "src/canonical/idp_backbone.py::safe = max(min_synthetic_rank, int(round(max(1.0, float(position_rank or 1)))))",
  "src/canonical/idp_backbone.py::w = max(0.0, float(declared_weight or 0.0))",
  "src/intel/crawler.py::draft_rounds = int(lg_settings.get(\"draft_rounds\") or 0)",
  "src/intel/crawler.py::faab_bid = int(settings.get(\"waiver_bid\") or 0)",
  "src/intel/crawler.py::max_seen = int(fetch_state.get(\"maxCreatedSeen\") or 0)",
  "src/intel/crawler.py::rnd = int(tp.get(\"round\") or 0)",
  "src/intel/lead_service.py::buys=int(r[\"buys\"] or 0),",
  "src/intel/lead_service.py::sells=int(r[\"sells\"] or 0),",
  "src/intel/lead_service.py::uid: sum(int(r[\"tradeCount\"] or 0) for r in rows if str(r[\"userId\"]) == uid)",
  "src/intel/lead_service.py::unique_leagues=int(r[\"uniqueLeagues\"] or 0),",
  "src/intel/leads.py::fit_score = max(0.0, min(1.0, (fit_raw or 0.0) / reachable_max)) if fit_raw else 0.0",
  "src/intel/leads.py::magnitude = float(deficits.get(pos) or 0.0)",
  "src/intel/leads.py::magnitude = float(surpluses.get(pos) or 0.0)",
  "src/intel/league_filter.py::return int(settings.get(\"best_ball\") or 0) == 1",
  "src/intel/ledger.py::\"assetMovementCount\": int(row[\"movement_count\"] or 0),",
  "src/intel/ledger.py::\"buyCount\": int(row[\"buy_count\"] or 0),",
  "src/intel/ledger.py::\"buys\": int(r[\"buys\"] or 0),",
  "src/intel/ledger.py::\"lastTs\": int(r[\"last_ts\"] or 0) or None,",
  "src/intel/ledger.py::\"lastTs\": int(row[\"last_ts\"] or 0) or None,",
  "src/intel/ledger.py::\"leagueCount\": int(row[\"leagues\"] or 0),",
  "src/intel/ledger.py::\"managerCount\": int(row[\"managers\"] or 0),",
  "src/intel/ledger.py::\"movementCount\": int(row[\"movement_count\"] or 0),",
  "src/intel/ledger.py::\"movementCount\": int(row[\"movements\"] or 0),",
  "src/intel/ledger.py::\"net\": int(r[\"buys\"] or 0) - int(r[\"sells\"] or 0),",
  "src/intel/ledger.py::\"sellCount\": int(row[\"sell_count\"] or 0),",
  "src/intel/ledger.py::\"sells\": int(r[\"sells\"] or 0),",
  "src/intel/ledger.py::\"tradeCount\": int(r[\"trade_count\"] or 0),",
  "src/intel/ledger.py::\"tradeCount\": int(row[\"trade_count\"] or 0),",
  "src/intel/ledger.py::\"transactionCount\": int(row[\"transactions\"] or 0),",
  "src/intel/ledger.py::\"uniqueBuyers\": int(row[\"unique_buyers\"] or 0),",
  "src/intel/ledger.py::\"uniqueLeagueCount\": int(row[\"unique_leagues\"] or 0),",
  "src/intel/ledger.py::\"uniqueLeagues\": int(r[\"unique_leagues\"] or 0),",
  "src/intel/ledger.py::\"uniqueLeagues\": int(row[\"unique_leagues\"] or 0),",
  "src/intel/ledger.py::\"uniqueManagerCount\": int(row[\"unique_managers\"] or 0),",
  "src/intel/ledger.py::\"uniqueManagers\": int(row[\"unique_managers\"] or 0),",
  "src/intel/ledger.py::\"uniqueSellers\": int(row[\"unique_sellers\"] or 0),",
  "src/intel/ledger.py::\"volume\": int(r[\"buys\"] or 0) + int(r[\"sells\"] or 0),",
  "src/intel/ledger.py::buys = int(row[\"buys\"] or 0)",
  "src/intel/ledger.py::r[\"tx_type\"]: int(r[\"n\"] or 0)",
  "src/intel/ledger.py::removed = cur.rowcount or 0",
  "src/intel/ledger.py::sells = int(row[\"sells\"] or 0)",
  "src/intel/platform_ledger.py::\"movements\": int(row[\"movements\"] or 0),",
  "src/intel/roster_shape.py::if float(value_by_player.get(str(pid)) or 0.0) < starter_value_floor:",
  "src/intel/roster_shape.py::n = float(count or 0)",
  "src/intel/service.py::\"buys\": int(act.get(\"buys\") or 0),",
  "src/intel/service.py::\"net\": int(act.get(\"net\") or 0),",
  "src/intel/service.py::\"sells\": int(act.get(\"sells\") or 0),",
  "src/intel/service.py::\"tradedLeagueCount\": int(act.get(\"uniqueLeagues\") or 0),",
  "src/intel/service.py::\"volume\": int(act.get(\"volume\") or 0),",
  "src/intel/service.py::rows.sort(key=lambda r: (-r[\"volume\"], -(r[\"lastTs\"] or 0), r[\"assetId\"]))",
  "src/intel/signals.py::int(primary.get(\"uniqueLeagues\") or 0),",
  "src/intel/signals.py::int(primary.get(\"uniqueManagers\") or 0),",
  "src/intel/signals.py::int(primary.get(\"volume\") or 0),",
  "src/intel/signals.py::long_volume = int(long.get(\"volume\") or 0)",
  "src/intel/signals.py::net = int(win.get(\"net\") or 0)",
  "src/intel/signals.py::net=int(primary.get(\"net\") or 0),",
  "src/intel/signals.py::return (-(s.velocity or 0.0), -volume)",
  "src/intel/signals.py::return (-int(win.get(\"buys\") or 0), -volume)",
  "src/intel/signals.py::return (-int(win.get(\"sells\") or 0), -volume)",
  "src/intel/signals.py::short_rate = int(short.get(\"volume\") or 0) / float(short_span)",
  "src/intel/signals.py::unique_managers=int(primary.get(\"uniqueManagers\") or 0),",
  "src/intel/signals.py::volume = int(win.get(\"volume\") or 0)",
  "src/intel/signals.py::volume=int(primary.get(\"volume\") or 0),",
  "src/league_intel/adjustment.py::factor = float(measurement.get(\"factor\") or 1.0)",
  "src/league_intel/adjustment.py::return (self.league_adjusted_value or 0.0) / self.consensus_value",
  "src/league_intel/cross_market.py::c_total, o_total = counter.total or 0.0, offer.total or 0.0",
  "src/league_intel/cross_market.py::idp_total = sum(abs(a.normalized_market_value or 0.0) for a in assets if _is_idp(a.position))",
  "src/league_intel/reception_fit.py::flat = float(scoring.get(\"rec\") or 0.0)",
  "src/league_intel/reception_fit.py::return sum(float(shares.get(b, 0.0)) * (flat + float(scoring.get(b) or 0.0)) for b in BAND_KEYS)",
  "src/league_intel/replacement.py::confidence=float(p.get(\"confidence\") or 0.0),",
  "src/league_intel/replacement.py::per_pos.setdefault(base, []).append(float(row.get(\"rosValue\") or 0.0))",
  "src/league_intel/replacement.py::ros_value=float(p.get(\"rosValue\") or 0.0),",
  "src/league_intel/replacement.py::value = float(p.get(\"rosValue\") or 0.0)",
  "src/league_intel/replacement.py::value = float(value_by_key.get(key) or 0.0)",
  "src/league_intel/sim.py::priced = sum(1 for p in roster if float(p.get(\"rosValue\") or 0.0) > 0)",
  "src/league_intel/sim.py::ros = float(p.get(\"rosValue\") or 0.0)",
  "src/league_intel/sim_calibration.py::per_point = float(scale.get(\"rosValuePerPoint\") or 0.0)",
  "src/league_intel/sim_calibration.py::sample_size=int(blob.get(\"sampleSize\") or 0),",
  "src/league_intel/te_premium.py::best = max((_as_float(scoring.get(c)) or 0.0) for c in comparators)",
  "src/league_intel/te_premium.py::required = int(_as_float(starters.get(\"TE\")) or 0)",
  "src/league_intel/te_premium.py::te_val = _as_float(scoring.get(te_key)) or 0.0",
  "src/news/custom_alerts.py::min_delta = int(params.get(\"minDelta\") or 0)",
  "src/news/custom_alerts.py::min_delta = int(raw_params.get(\"minDelta\") or 0)",
  "src/news/custom_alerts.py::threshold = int(params.get(\"threshold\") or 0)",
  "src/news/providers/sleeper.py::count = int(row.get(\"count\") or 0)",
  "src/news/providers/sleeper.py::key=lambda it: (it.confidence or 0.0, it.severity == \"watch\"),",
  "src/news/providers/sleeper.py::top_count=int(top_add or 0),",
  "src/news/providers/sleeper.py::top_count=int(top_drop or 0),",
  "src/news/unified_signal_engine.py::abs(usage_signal_dict.get(\"carry_share_z\") or 0),",
  "src/news/unified_signal_engine.py::abs(usage_signal_dict.get(\"snap_pct_z\") or 0),",
  "src/news/unified_signal_engine.py::abs(usage_signal_dict.get(\"target_share_z\") or 0),",
  "src/public_league/activity.py::feed.sort(key=lambda t: -int(t.get(\"createdAt\") or 0))",
  "src/public_league/activity.py::out.append(float(valuation(asset) or 0.0))",
  "src/public_league/activity.py::wk = t.get(\"week\") or 0",
  "src/public_league/archives.py::rows.sort(key=lambda r: -(int(r.get(\"createdAt\") or 0)))",
  "src/public_league/awards.py::elif int(pk.get(\"previous_owner_id\") or 0) == rid:",
  "src/public_league/awards.py::games = r[\"gamesStarted\"] or 1",
  "src/public_league/awards.py::if (w.get(\"starterPoints\") or 0) <= 0:",
  "src/public_league/awards.py::if int(pk.get(\"owner_id\") or 0) == rid:",
  "src/public_league/awards.py::key=lambda r: -float(r.get(\"starterPoints\") or 0.0),",
  "src/public_league/awards.py::leg = tx.get(\"leg\") or tx.get(\"_leg\") or 0",
  "src/public_league/awards.py::out[str(pid)] = float(val or 0.0)",
  "src/public_league/awards.py::pts = float(raw or 0.0)",
  "src/public_league/awards.py::trade_gain = {r[\"ownerId\"]: float(r.get(\"pointsGained\") or 0.0) for r in trader_rows}",
  "src/public_league/awards.py::waiver_gain = {r[\"ownerId\"]: float(r.get(\"pointsGained\") or 0.0) for r in waiver_rows}",
  "src/public_league/draft.py::\"rounds\": int((draft.get(\"settings\") or {}).get(\"rounds\") or 0)",
  "src/public_league/draft.py::draft_rounds = int(settings.get(\"draft_rounds\") or 0)",
  "src/public_league/draft.py::drafts.sort(key=lambda d: (d[\"season\"], d.get(\"startTime\") or 0), reverse=True)",
  "src/public_league/draft.py::pick_no = int(pick.get(\"pick_no\") or 0)",
  "src/public_league/draft.py::round_ = int(pick.get(\"round\") or 0)",
  "src/public_league/franchise.py::-(r[\"championships\"] or 0),",
  "src/public_league/franchise.py::-(r[\"wins\"] or 0),",
  "src/public_league/matchup_narrative.py::\"cache_creation_input_tokens\": getattr(usage, \"cache_creation_input_tokens\", 0) or 0,",
  "src/public_league/matchup_narrative.py::\"cache_read_input_tokens\": getattr(usage, \"cache_read_input_tokens\", 0) or 0,",
  "src/public_league/matchup_narrative.py::\"wordCount\": int(article_obj.get(\"wordCount\") or 0),",
  "src/public_league/matchup_narrative.py::and away_bench[0][\"points\"] > (away_top.get(\"points\") or 0.0)",
  "src/public_league/matchup_narrative.py::and home_bench[0][\"points\"] > (home_top.get(\"points\") or 0.0)",
  "src/public_league/matchup_narrative.py::key=lambda r: (int(r.get(\"week\") or 0), mode_order.get(r.get(\"mode\"), -1)),",
  "src/public_league/matchup_narrative.py::key=lambda r: r.get(\"points\") or 0.0,",
  "src/public_league/matchup_narrative.py::pair_entries.sort(key=lambda e: int(e.get(\"roster_id\") or 0))",
  "src/public_league/matchup_narrative.py::points = float(pp.get(pid) or 0.0)",
  "src/public_league/matchup_narrative.py::points = float(pp.get(s_pid) or 0.0)",
  "src/public_league/matchup_narrative.py::round_num = int(entry.get(\"r\") or 0)",
  "src/public_league/matchup_narrative.py::seed_gap = abs((home_seed or 0) - (away_seed or 0))",
  "src/public_league/matchup_recap.py::pair_sorted = sorted(pair, key=lambda e: int(e.get(\"roster_id\") or 0))",
  "src/public_league/matchup_recap.py::points = float(pp.get(pid) or 0.0)",
  "src/public_league/metrics.py::entries.sort(key=lambda e: int(e.get(\"roster_id\") or 0))",
  "src/public_league/metrics.py::return float(a.get(\"points\") or 0.0) - float(b.get(\"points\") or 0.0)",
  "src/public_league/metrics.py::return float(entry.get(\"points\") or 0.0)",
  "src/public_league/metrics.py::return float(val or 0)",
  "src/public_league/player_journey.py::\"events\": sorted(all_events, key=lambda e: int(e.get(\"createdAt\") or 0)),",
  "src/public_league/player_journey.py::\"pickNo\": int(p.get(\"pick_no\") or 0) or None,",
  "src/public_league/player_journey.py::\"round\": int(p.get(\"round\") or 0) or None,",
  "src/public_league/player_journey.py::events.sort(key=lambda e: (int(e.get(\"createdAt\") or 0), e.get(\"kind\") or \"\"))",
  "src/public_league/player_journey.py::key=lambda e: (int(e.get(\"season\") or 0), e[\"week\"]),",
  "src/public_league/player_journey.py::pts = float(pp.get(pid) or 0.0)",
  "src/public_league/records.py::pts = float(raw or 0.0)",
  "src/public_league/snapshot.py::out.sort(key=lambda tx: int(tx.get(\"created\") or tx.get(\"status_updated\") or 0))",
  "src/public_league/snapshot.py::total_rosters = int(self.league.get(\"total_rosters\") or 0)",
  "src/public_league/streaks.py::return (yr, int(event.get(\"week\") or 0))",
  "src/public_league/weekly_recap.py::leg = int(tx.get(\"_leg\") or tx.get(\"leg\") or 0)",
  "src/ros/aggregate.py::row_weight = weight * (row.confidence or 1.0)",
  "src/ros/api.py::unmapped_total = sum(int(t.get(\"unmappedPlayerCount\") or 0) for t in team_strength)",
  "src/ros/direction.py::strength_pct = team_ros_strength_percentile or 0.0",
  "src/ros/lineup.py::base = max(0.0, float(player.ros_value or 0.0))",
  "src/ros/parse.py::base = float(src.get(\"base_weight\") or 0.0)",
  "src/ros/pick_projection.py::original = int(pd.get(\"original_roster_id\") or 0)",
  "src/ros/pick_projection.py::owner = int(pd.get(\"owner_roster_id\") or 0)",
  "src/ros/pick_projection.py::rid = int(row.get(\"rosterId\") or 0)",
  "src/ros/pick_projection.py::rid = int(t.get(\"roster_id\") or 0)",
  "src/ros/pick_projection.py::rnd = int(pd.get(\"round\") or 0)",
  "src/ros/pick_projection.py::season = int(str(pd.get(\"season\") or 0))",
  "src/ros/playoff_sim.py::\"rosValue\": float(p.get(\"rosValue\") or 0.0),",
  "src/ros/playoff_sim.py::a = float(a_row.get(metric) or 0.0)",
  "src/ros/playoff_sim.py::b = float(b_row.get(metric) or 0.0)",
  "src/ros/playoff_sim.py::bench = float(r.get(\"benchDepthScore\") or 0.0)",
  "src/ros/playoff_sim.py::ros = float(p.get(\"rosValue\") or 0.0)",
  "src/ros/playoff_sim.py::starter = float(r.get(\"startingLineupScore\") or 0.0)",
  "src/ros/playoff_sim.py::str(r.get(\"ownerId\") or \"\"): float(r.get(\"teamRosStrength\") or 0.0)",
  "src/ros/power_v2.py::games = s[\"games\"] or 1",
  "src/ros/power_v2.py::min(1.0, float(r.get(\"healthAvailabilityScore\") or 0.0) / 100.0),",
  "src/ros/power_v2.py::score = float(r.get(\"teamRosStrength\") or 0.0)",
  "src/ros/power_v2.py::weight_total = sum(active_weights.values()) or 1.0",
  "src/ros/scrape.py::base_weight=float(src_meta.get(\"base_weight\") or 0.0),",
  "src/ros/scrape.py::confidence=float(row.get(\"confidence\") or 1.0),",
  "src/ros/scrape.py::existing = float(row.get(\"confidence\") or 1.0)",
  "src/ros/scrape.py::rank=int(row.get(\"rank\") or 0),",
  "src/ros/sources/draftsharks_ros.py::rank = int(raw.get(\"rank\") or 0)",
  "src/ros/sources/fantasypros_ros_overall.py::rank = int(entry.get(\"rank_ecr\") or 0)",
  "src/ros/sources/fantasypros_ros_sf.py::rank = int(entry.get(\"rank_ecr\") or 0)",
  "src/ros/team_strength.py::confidence=float(agg.get(\"confidence\") or 0.0),",
  "src/ros/team_strength.py::out.sort(key=lambda t: -float(t.get(\"teamRosStrength\") or 0.0))",
  "src/ros/team_strength.py::ros_value=float(agg.get(\"rosValue\") or 0.0),",
  "src/ros/trade_deadline.py::rank = float(strength_row.get(\"rank\") or 0.0)",
  "src/ros/trade_deadline.py::total = max(1.0, float(len(strengths) or 1))",
  "src/roster_intel/engine.py::adjusted += float(row.get(\"leagueAdjustedDynastyValue\") or row.get(\"consensusValue\") or 0.0)",
  "src/roster_intel/engine.py::consensus += float(row.get(\"consensusValue\") or 0.0)",
  "src/roster_intel/engine.py::market += float(row.get(\"marketValue\") or 0.0)",
  "src/roster_intel/marginal.py::confidence=float(p.get(\"confidence\") or 0.0),",
  "src/roster_intel/marginal.py::ros_value=float(p.get(\"rosValue\") or 0.0),",
  "src/roster_intel/partner.py::delta = (activity or 0.0) + familiarity",
  "src/roster_intel/partner.py::entered = int(getattr(pp, \"entered_lineup\", 0) or 0)",
  "src/roster_intel/partner.py::gap = abs(float(ours.contend_probability or 0.0) - float(theirs.contend_probability or 0.0))",
  "src/roster_intel/partner.py::marg = float(getattr(pp, \"marginal_points\", 0.0) or 0.0)",
  "src/roster_intel/partner.py::received = float(shape.market_value_received or 0.0)",
  "src/roster_intel/partner.py::req = int(getattr(pp, \"required_slots\", 0) or 0)",
  "src/roster_intel/partner.py::sent = float(shape.market_value_sent or 0.0)",
  "src/roster_intel/partner.py::ts = float(getattr(pp, \"tradeable_surplus\", 0.0) or 0.0)",
  "src/roster_intel/profiles.py::byes = [int(meta.get(p.player_id, {}).get(\"byeWeek\") or 0) for p in players]",
  "src/roster_intel/profiles.py::floor = (starter_level or 0.0) * _SURPLUS_FLOOR_RATIO",
  "src/roster_intel/roster_source.py::\"confidence\": float(val_row.get(\"confidence\") or 0.0),",
  "src/roster_intel/roster_source.py::\"rosValue\": float(val_row.get(\"rosValue\") or 0.0),",
  "src/roster_intel/roster_source.py::if float(row.get(\"rosValue\") or 0.0) <= float(prev.get(\"rosValue\") or 0.0):",
  "src/roster_intel/targets.py::edge_mult = 1.0 + _clamp(edge or 0.0, -0.5, 0.5) * 0.4",
  "src/roster_intel/window.py::champ = {str(r[\"ownerId\"]): float(r.get(\"championshipOdds\") or 0.0) for r in rows}",
  "src/scoring/archetype_model.py::a[\"fit_sum\"] += float(row.get(\"raw_fit\", 1.0) or 1.0)",
  "src/scoring/backtest.py::baseline = float(fit.get(\"ppgTest\", 0.0) or 0.0)",
  "src/scoring/backtest.py::conf = float(fit.get(\"confidence\", 0.0) or 0.0)",
  "src/scoring/backtest.py::custom = float(fit.get(\"ppgCustom\", 0.0) or 0.0)",
  "src/scoring/backtest.py::mult = float(fit.get(\"productionMultiplier\", 1.0) or 1.0)",
  "src/scoring/backtest.py::ratio = float(fit.get(\"fitFinal\", 1.0) or 1.0)",
  "src/scoring/player_adjustment.py::p_base = max(0.0, float(baseline_ppg or 0.0))",
  "src/scoring/player_adjustment.py::p_league = max(0.0, float(league_ppg or 0.0))",
  "src/scoring/replacement_level.py::g = int(games or 0)",
  "src/scoring/replacement_level.py::p = float(points or 0)",
  "src/scoring/replacement_level.py::teams = max(1, int(num_teams or 0))",
  "src/scoring/scoring_delta.py::b = float(baseline.get(key, 0.0) or 0.0)",
  "src/scoring/scoring_delta.py::league_val = float(league.get(key, 0.0) or 0.0)",
  "src/scoring/scoring_delta.py::stat_value = float(stats_per_game.get(key, 0.0) or 0.0)",
  "src/scoring/scoring_delta.py::stat_value = float(stats_per_game.get(rule.key, 0.0) or 0.0)",
  "src/scoring/tiering.py::new = float(new_thresholds.get(pos, 0.0) or 0.0)",
  "src/scoring/tiering.py::old = float(old_thresholds.get(pos, 0.0) or 0.0)",
  "src/sharp/cohort.py::quality=max(0.0, min(1.0, float(item.score or 0.0) / 100.0)),",
  "src/sharp/cohort.py::quality=max(0.0, min(1.0, float(row.quality or 0.0))),",
  "src/sharp/consensus.py::float(person[\"quality\"] or 0.0),",
  "src/sharp/consensus.py::quality = float(person[\"quality\"] or 1.0)",
  "src/sharp/curated.py::\"totalActivityEventsImported\": int(summary[\"activity\"].get(\"activity_events\") or 0),",
  "src/sharp/curated.py::\"totalImportedAsCuratedSharps\": int(membership.get(\"curated_people\") or 0),",
  "src/sharp/curated.py::\"totalImportedAsResearchCandidates\": int(statuses.get(\"research_candidate\") or 0)",
  "src/sharp/curated.py::\"totalPositivelyVerifiedOnFFPC\": int(accounts.get(\"ffpc\") or 0),",
  "src/sharp/curated.py::\"totalPositivelyVerifiedOnSleeper\": int(accounts.get(\"sleeper\") or 0),",
  "src/sharp/curated.py::\"totalScreenedOutStored\": int(statuses.get(\"screened_out\") or 0),",
  "src/sharp/curated.py::\"totalSuperSharps\": int(membership.get(\"super_sharps\") or 0),",
  "src/sharp/curated.py::\"totalWithUsablePublicActivity\": int(summary[\"activity\"].get(\"people_with_activity\") or 0),",
  "src/sharp/curated.py::(float(item.score or 0.0) / 100.0 for item in qualified_scores or linked_scores),",
  "src/sharp/curated.py::(summary.get(\"latestImport\") or {}).get(\"duplicates_merged\") or 0",
  "src/sharp/curated.py::+ 0.10 * float(trackability or 0.0)",
  "src/sharp/curated.py::+ int(statuses.get(\"inactive_or_historical\") or 0)",
  "src/sharp/curated.py::+ int(statuses.get(\"insufficient_identity_information\") or 0),",
  "src/sharp/curated.py::0.60 * float(curated_weight or 0.0)",
  "src/sharp/curated.py::and int(existing[\"verified\"] or 0)",
  "src/sharp/curated.py::combined = 0.85 * curated_weight + 0.15 * float(trackability or 0.0)",
  "src/sharp/curated.py::else (float(row.get(\"curated_expertise_score\") or 0.0) / 100.0 or None),",
  "src/sharp/curated.py::else (float(row.get(\"trackability_score\") or 0.0) / 100.0 or None),",
  "src/sharp/curated.py::float(person[\"curated_expertise_score\"] or 0.0) / 100.0",
  "src/sharp/curated.py::float(person[\"trackability_score\"] or 0.0) / 100.0",
  "src/sharp/curated.py::float(row.get(\"confidence\") or 0.0),",
  "src/sharp/curated.py::float(row.get(\"verification_confidence\") or 0.0),",
  "src/sharp/curated.py::new_confidence = max(float(row[\"confidence\"] or 0.0), 0.35)",
  "src/sharp/curated.py::new_confidence = max(float(row[\"confidence\"] or 0.0), 0.72)",
  "src/sharp/curated.py::new_confidence = min(float(row[\"confidence\"] or 0.0), 0.35)",
  "src/sharp/curated.py::or 0",
  "src/sharp/discovery.py::n = int(league.get(\"total_rosters\") or 0)",
  "src/sharp/market.py::return (-(row.get(\"velocity\") or 0.0), -row[\"volume\"], row[\"assetId\"])",
  "src/sharp/market.py::timestamp = int(row.get(\"timestampMs\") or 0)",
  "src/sharp/platform_records.py::\"completedSeasonRows\": int(row[\"completed_rows\"] or 0),",
  "src/sharp/platform_records.py::\"leaguesWithRecords\": int(row[\"leagues\"] or 0),",
  "src/sharp/platform_records.py::\"managersWithRecords\": int(row[\"managers\"] or 0),",
  "src/sharp/platform_records.py::\"seasonRows\": int(row[\"season_rows\"] or 0),",
  "src/sharp/platform_records.py::str(r[\"manager_key\"]): int(r[\"n\"] or 0)",
  "src/sharp/records.py::\"completedSeasonRows\": int(row[\"complete_rows\"] or 0),",
  "src/sharp/records.py::\"leaguesWithRecords\": int(row[\"leagues\"] or 0),",
  "src/sharp/records.py::\"losses\": int(rec.get(\"losses\") or 0),",
  "src/sharp/records.py::\"managersWithRecords\": int(row[\"managers\"] or 0),",
  "src/sharp/records.py::\"points_against\": float(rec.get(\"points_against\") or 0.0),",
  "src/sharp/records.py::\"points_for\": float(rec.get(\"points_for\") or 0.0),",
  "src/sharp/records.py::\"seasonRows\": int(row[\"rows_total\"] or 0),",
  "src/sharp/records.py::\"ties\": int(rec.get(\"ties\") or 0),",
  "src/sharp/records.py::\"wins\": int(rec.get(\"wins\") or 0),",
  "src/sharp/records.py::losses = sum(int(s[\"losses\"] or 0) for s in seasons)",
  "src/sharp/records.py::pf = float(settings.get(\"fpts\") or 0) + float(settings.get(\"fpts_decimal\") or 0) / 100.0",
  "src/sharp/records.py::ties = int(settings.get(\"ties\") or 0)",
  "src/sharp/records.py::ties = sum(int(s[\"ties\"] or 0) for s in seasons)",
  "src/sharp/records.py::wins = int(settings.get(\"wins\") or 0)",
  "src/sharp/records.py::wins = sum(int(s[\"wins\"] or 0) for s in seasons)",
  "src/sharp/roster_collect.py::\"pagesFetched\": int(sleeper.get(\"callsUsed\") or 0),",
  "src/sharp/roster_collect.py::\"pagesParsed\": int(sleeper.get(\"leaguesExamined\") or 0)",
  "src/sharp/roster_collect.py::\"unmappedPlayers\": int(ffpc.get(\"unmappedAssets\") or 0),",
  "src/sharp/roster_collect.py::+ int(ffpc.get(\"leaguesExamined\") or 0),",
  "src/sharp/roster_collect.py::confidence = float(row[\"identity_confidence\"] or 0.0)",
  "src/sharp/roster_collect.py::losses = int(settings.get(\"losses\") or 0)",
  "src/sharp/roster_collect.py::te_bonus = float(scoring.get(\"bonus_rec_te\") or 0.0)",
  "src/sharp/roster_collect.py::ties = int(settings.get(\"ties\") or 0)",
  "src/sharp/roster_collect.py::wins = int(settings.get(\"wins\") or 0)",
  "src/sharp/roster_percentage.py::buys, sells = int(item.get(\"buys\") or 0), int(item.get(\"sells\") or 0)",
  "src/sharp/roster_percentage.py::if int(roster.get(\"observedMs\") or 0) < cutoff:",
  "src/sharp/roster_percentage.py::last_refreshed = max((int(r.get(\"observedMs\") or 0) for r in rosters), default=None)",
  "src/sharp/roster_percentage.py::pct = row.get(\"sharpRosterPct\") or 0.0",
  "src/sharp/roster_percentage.py::return (-pct, -(row.get(\"value\") or 0), row.get(\"displayName\") or \"\")",
  "src/sharp/roster_percentage.py::return (0 if rank is not None else 1, rank or 0, pct)",
  "src/sharp/roster_percentage.py::return (0 if value is not None else 1, (value or 0.0), -pct)",
  "src/sharp/roster_percentage.py::return (0 if value is not None else 1, -(value or 0.0), -pct)",
  "src/sharp/roster_store.py::\"assetCount\": int(row[\"asset_count\"] or 0),",
  "src/sharp/roster_store.py::\"eligibleRosters\": int(r[\"eligible\"] or 0),",
  "src/sharp/roster_store.py::\"eligibleRosters\": int(row[\"eligible\"] or 0),",
  "src/sharp/roster_store.py::\"firstSeenMs\": int(row[\"first_seen_ms\"] or 0),",
  "src/sharp/roster_store.py::\"leaguesWithRosters\": int(row[\"leagues\"] or 0),",
  "src/sharp/roster_store.py::\"managersWithRosters\": int(row[\"managers\"] or 0),",
  "src/sharp/roster_store.py::\"observedMs\": int(row[\"observed_ms\"] or 0),",
  "src/sharp/roster_store.py::\"playerCount\": int(row[\"player_count\"] or 0),",
  "src/sharp/roster_store.py::\"rosters\": int(r[\"n\"] or 0),",
  "src/sharp/roster_store.py::\"rosters\": int(row[\"rosters\"] or 0),",
  "src/sharp/score.py::qualified.sort(key=lambda e: -(e.score or 0.0))",
  "src/sharp/service.py::+ int((source_coverage.get(\"ffpc\") or {}).get(\"leagues\") or 0),",
  "src/sharp/service.py::ffpc_observable = int((source_coverage.get(\"ffpc\") or {}).get(\"managers\") or 0)",
  "src/sharp/service.py::int((value or {}).get(\"transactions\") or 0) for value in source_coverage.values()",
  "src/sharp/service.py::limit=int(query.get(\"limit\") or 100),",
  "src/sharp/service.py::sleeper_observable = int(graph.get(\"observedUsers\") or 0)",
  "src/sharp/transactions.py::\"lastFetchedMs\": int(row[\"last_fetched_ms\"] or 0),",
  "src/sharp/transactions.py::\"maxCreatedSeen\": int(row[\"max_created_ms\"] or 0),",
  "src/sharp/transactions.py::crawled = int(row[\"n\"] or 0) if row else 0",
  "src/sharp/transactions.py::int(state.get(\"lastFetchedMs\") or 0),",
  "src/sharp/transactions.py::int(state.get(\"maxCreatedSeen\") or 0),",
  "src/sharp/transactions.py::max_seen = int(state.get(\"maxCreatedSeen\") or 0)",
  "src/sharp/transactions.py::oldest = int(stale[\"oldest\"] or 0) if stale and stale[\"oldest\"] else None",
  "src/trade/angle.py::\"market_value\": int(round(float(asset.normalized_market_value or 0.0))),",
  "src/trade/angle.py::desired_market_total = float(desired_pkg.total or 0.0)",
  "src/trade/angle.py::market_sum = float(counter_pkg.total or 0.0)",
  "src/trade/angle.py::min_my_value_floor = max(0.0, float(min_player_my_value or 0.0))",
  "src/trade/angle.py::offer_market_sum = float(offer_pkg.total or 0.0)",
  "src/trade/angle.py::offer_market_total = float(offer_pkg.total or 0.0)",
  "src/trade/angle.py::return [float(a.normalized_market_value or 0.0) for a in pkg.assets]",
  "src/trade/faab_contention.py::winning_count = int(aggression_entry.get(\"winningCount\") or 0)",
  "src/trade/faab_engine.py::aggression = max(float(lo), min(float(hi), float(rival.aggression or 1.0)))",
  "src/trade/faab_engine.py::float(raw.get(\"weight\") or 0.0),",
  "src/trade/faab_engine.py::posture = float(posture_map.get(str(team.risk_posture or \"balanced\"), 1.0) or 1.0)",
  "src/trade/faab_engine.py::slots = float(starters.get(flex_key) or 0)",
  "src/trade/faab_engine.py::total = float(starters.get(family) or starters.get(pos) or 0)",
  "src/trade/faab_history.py::\"createdAt\": int(tx.get(\"status_updated\") or tx.get(\"created\") or 0),",
  "src/trade/faab_history.py::\"teamCount\": int(settings.get(\"num_teams\") or 0) or None,",
  "src/trade/faab_history.py::budget = int(settings.get(\"waiver_budget\") or 0) or 100",
  "src/trade/faab_history.py::by_week.setdefault(int(row.get(\"week\") or 0), []).append(pct)",
  "src/trade/faab_history.py::league_mean = priors.mean_pct or 1.0",
  "src/trade/faab_history.py::total_claims = sum(len(v) for v in buckets.values()) or 1",
  "src/trade/faab_recommender.py::count = int(entry.get(\"winningCount\") or 0)",
  "src/trade/faab_recommender.py::drop_value = float(drop_player_value or 0.0)",
  "src/trade/faab_recommender.py::open_roster_spots=int(open_roster_spots or 0),",
  "src/trade/faab_recommender.py::value=float(add_player_value or 0.0),",
  "src/trade/faab_recommender.py::weight = float(f.get(\"weight\") or 0.0)",
  "src/trade/faab_recommender.py::weight = float(getattr(f, \"weight\", 0.0) or 0.0)",
  "src/trade/finder.py::and (_int_or_none(pdata.get(\"_finalAdjusted\")) or 0) >= _MIN_ASSET_VALUE_COMPOSITE_SCALE",
  "src/trade/finder.py::group.sort(key=lambda a: -(a.market_value or 0))",
  "src/trade/finder.py::source_count = _int_or_none(pdata.get(\"_sites\")) or 0",
  "src/trade/finder_value_adjustment.py::(index, int(asset.market_value or 0))",
  "src/trade/monte_carlo.py::cv = float(row.get(\"rankDerivedValue\") or row.get(\"values\", {}).get(\"full\") or 0)",
  "src/trade/monte_carlo.py::p10=_shifted(band.get(\"p10\") or 0),",
  "src/trade/monte_carlo.py::p50=_shifted(band.get(\"p50\") or 0),",
  "src/trade/monte_carlo.py::p90=_shifted(band.get(\"p90\") or 0),",
  "src/trade/monte_carlo.py::total = sum(p50s) or 1.0",
  "src/trade/suggestions.py::n = int(count or 0)",
  "src/trade/suggestions.py::seller = max(s.give, key=lambda p: p.dispersion_cv or 0)",
  "src/trade/suggestions.py::target = max(s.receive, key=lambda p: p.dispersion_cv or 0)",
  "src/trade/symmetrize.py::total = sum(p.p50 for p in side) or 1.0",
  "src/trade/symmetrize.py::va_val = float(va_adj.get(\"effectiveValue\") or va_adj.get(\"value\") or 0)",
  "src/trade/team_impact.py::by_value = sorted(win_now, key=lambda a: int(a.get(\"value\") or 0), reverse=True)",
  "src/trade/team_impact.py::combined = [int(a.get(\"value\") or 0) for a in before[\"starters\"][p]] + [",
  "src/trade/team_impact.py::int(a.get(\"value\") or 0) for a in after[\"starters\"][p]",
  "src/trade/team_impact.py::int(a.get(\"value\") or 0) for a in before_assets if not _is_pick(a) and _is_young(a)",
  "src/trade/team_impact.py::pick_value = sum(int(a.get(\"value\") or 0) for a in before_assets if _is_pick(a))",
  "src/trade/team_impact.py::pool.sort(key=lambda a: int(a.get(\"value\") or 0), reverse=True)",
  "src/trade/team_impact.py::starter_value = {p: sum(int(a.get(\"value\") or 0) for a in starters[p]) for p in _BASE_POSITIONS}",
  "src/trade/team_impact.py::top10_value = sum(int(a.get(\"value\") or 0) for a in by_value[:10])",
  "src/trade/team_impact.py::total_value = sum(int(a.get(\"value\") or 0) for a in before_assets) or 1",
  "src/trade/waiver.py::budget = int(league_budget or 0)",
  "src/trade/waiver.py::items.sort(key=lambda c: -int(c.get(\"adjustedValue\") or 0))",
  "src/trade/waiver.py::source_count = int(row.get(\"sourceCount\") or 0)"
 ]
}
