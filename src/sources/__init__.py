"""Per-vendor source primitives that are OWNERS, not adapters.

A module lands here when more than one caller needs the same vendor-shaped
parse and the alternative is a second private copy of it.
"""
