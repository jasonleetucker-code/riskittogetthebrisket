# Repository Agent Entry

Read `AI_INSTRUCTIONS.md` first. It routes every model to the core
`docs/AGENT_OPERATING_SYSTEM.md`, current authorization, coordination and
task-relevant technical guidance. This adapter adds no repository policy.

Active working copy: `C:\Users\jason\code\riskittogetthebrisket`.
Use a task branch or isolated worktree; preserve existing changes. Do not edit
OneDrive copies unless the owner explicitly requests it. Working-copy mechanics
are canonical in `ASSISTANT_COORDINATION.md`.

For material local work, run `bash scripts/agent_session_start.sh`.
Load specialist guidance only when its trigger matches the task.
